# Журнал реализации `docpipe`

Ведётся по ходу работы. Одна запись на задачу из
[`parser-implementation-plan.md`](parser-implementation-plan.md).
Фиксируются: что сделано, что проверено, где план разошёлся с реальностью.

**Окружение:** Python 3.14.6 (система) / venv через `uv` на 3.12+, `uv 0.11.32`.
`.NET SDK отсутствует` — по проекту и не требуется.

---

## Сводка прогресса

| Задача | Статус | Отклонения от плана |
|---|---|---|
| T00 Каркас проекта | ✅ готово | 3 (см. ниже) |
| T01 Фикстура .NET | ✅ готово | 1 (тест строже плана) |
| T01b Фикстура «из дикой природы» | ✅ готово | новая задача; расширена после ABP до 13 файлов |
| T02 hashing.py | ✅ готово | 0 |
| T03 model.py | ✅ готово | 2 (типизированные псевдонимы, extra=forbid) |
| T04 config/discovery | ✅ готово | 3 (os.walk вместо rglob, фикс `**`, поиск `.slnx`) |
| T05 csproj/sln | ✅ готово | 6 (TFM из props, `.slnx`, id и ссылки по путям, `$(…)`, резолв по имени) |
| T05b связанные исходники | ⬜ отложена | новая задача по итогам стресс-теста |
| T06 parser: объявления | ✅ готово | 1 (цепочка containing_type) |
| T07 parser: члены | ✅ готово | 4 (event_field, declarator-ы, список видов, вырезание тривии) |
| T08 parser: usings | ✅ готово | 2 (запрос вместо детей корня, `global::`) |
| T09 cache.py | ✅ готово | 3 (битая БД, батч-коммит, уровень zlib) |
| T10 resolve: FQN | ✅ готово | 3 (ключ с модулем и арностью, порядок резолва, global using) |
| T11 resolve: замыкание | ✅ готово | 2 (переход по арности, параллельные списки) |
| T12 endpoints.py | ✅ готово | 3 (`[Route]` на методе, регистр токенов, сортировка) |
| T13 di.py | ✅ готово | 3 (typeof, пропуск безымянных, узлы снаружи) |
| T14 classify.py | ✅ готово | 3 (matches_glob, валидация набора, is_excluded) |
| T15 tree.py | ✅ готово | 3 (парная скобка, петля DI, тривия в сигнатуре — правка T07) |
| T16 emit.py + scan | ✅ готово | 2 (проверка даты, parse_error_files в сидкаре) |
| T17 детерминизм | ✅ готово | 2 (os.walk вместо rglob, сравнение без поля parser) |
| T18 скоуп-режим | ✅ готово | 3 (отбор по скоупу, prune, модули из previous) |
| T19 diff.py | ✅ готово | 0 (спецификация подошла как есть) |
| T20 stats/dry-run/validate | ✅ готово | 3 (отдельный модуль, ScanResult, таблица) |
| T21 финализация | ✅ готово | 1 (docpipe.example.yaml вместо docpipe.yaml) |

**Шаг `web` (фронтенд на Angular), F01–F18 — закончен.**

| Задача | Статус | Отклонения от плана |
|---|---|---|
| F01 Разведка боевого репозитория | ✅ готово | — |
| F02 Обход `.ts`/`.html` и фикстура | ✅ готово | 5 (умолчания в emit.py, `nx.json` в отборе, вложенная nx-форма, алиасы в `tsconfig.base.json`, состав фикстуры) |
| F03 Нормализация маршрута | ✅ готово | 2 (направление шага 3, `?` внутри подстановки) |
| F04 Парсер TypeScript | ✅ готово | 3 (JSDoc-якорь, три клаузы наследования, импорты отложены в F05) |
| F05 Резолв: импорты, алиасы, бочки | ✅ готово | 3 (JSONC, замещение `paths`, `docpipe/symbols.py`) |
| F06 Модуль фронта | ✅ готово | 3 (ключ — каталог-граница, `sourceRoot` nx, правила по месту) |
| F07 HTTP-вызовы и реестр | ✅ готово | 3 (разбор и конфигурация врозь, поля по классу, порядок конкатенации) |
| F08 Дерево роутов | ✅ готово | 3 (опознание корня, две стрелки, `:id` дописан в `route.py`) |
| F09 Сторона .NET | ✅ готово | 0 (правки `endpoints.py` не потребовалось — как и предсказано) |
| F10 Схема 2.0 | ✅ готово | 2 (`type_kind` только синтаксический, `lang` обязателен) |
| F11 `docpipe web scan` | ✅ готово | 4 (второй сдвиг золотого файла, свой сборщик узлов, отдельный кэш, минимальные правила) |
| F12 Правила классификации | ✅ готово | 2 (`page` и `api-service` — повышения, `unless` у отсева констант) |
| F13 `docpipe web link` | ✅ готово | 2 (различитель вне сопоставления, маршруты в фикстуре .NET) |
| F14 Шаг 2 для web | ✅ готово | 3 (шаблон в `sources`, скелеты плоско, экшены стора не сделаны) |
| F15 Якорь `page` | ✅ готово | 2 (`route` в фактах, зависимости на фронте) |
| F16 Владение на web-узлах | ✅ готово | 0 (кода править не пришлось) |
| F17 Детерминизм и объём | ✅ готово | 1 (`--jobs` не заведён: 1,4 с на 1301 файле) |
| F18 Документация | ✅ готово | 0 |
| F19 `web pages`: страницы и обоснование | ✅ готово | 2 (поля источника в `RouteEntry`, нормализация невосстановленного пути) |

---

## T00 — Каркас проекта ✅

**Создано:** `pyproject.toml`, `docpipe/__init__.py`, `docpipe/cli.py`,
`tests/__init__.py`, `.gitignore`

**Зафиксированные версии** (`uv sync` разрешил так):

```
tree-sitter==0.26.0        tree-sitter-c-sharp==0.23.5
pydantic==2.13.4           typer==0.27.0
pyyaml==6.0.3              pytest==9.1.1
ruff==0.16.0               mypy==2.3.0
```

**Проверено:**

```
uv sync                     → без ошибок
uv run docpipe version      → 0.1.0
uv run ruff check .         → All checks passed
uv run ruff format --check  → 4 files already formatted
uv run mypy docpipe         → Success: no issues found in 2 source files
uv run pytest -q            → no tests ran (ожидаемо, тестов ещё нет)
```

### Трудности и отклонения от плана

**1. Typer схлопывает единственную команду в корневую.**
`uv run docpipe version` падал с `Got unexpected extra argument(s) (version)`.
Причина: при одной зарегистрированной команде typer делает её корневой, и слово
`version` разбирается как лишний позиционный аргумент.
Решение: пустой `@app.callback()` переводит приложение в многокомандный режим.
План обновлён — этот пункт добавлен в T00, иначе агент-реализатор упрётся в то же самое.

**2. `ruff format --check` форматирует Python-блоки внутри markdown.**
Проверка падала на `docs/parser-implementation-plan.md`: ruff переписывал компактные
псевдо-сигнатуры вида `def f(a,\n b) -> X: ...` в свой стиль. Это документация, а не код.
Решение: `extend-exclude = ["docs", "examples"]` в `[tool.ruff]`.
План обновлён.

**3. hatchling не находил пакет автоматически.**
Добавлен явный `[tool.hatch.build.targets.wheel] packages = ["docpipe"]`.
План обновлён.

**4. `.gitignore` уже существовал** со строкой `examples/` (туда склонированы `abp` и
`eshoponweb` для финальной проверки). Строка сохранена, остальные правила дописаны.

### Замечание на будущее

`uv run docpipe ...` пересобирает и переустанавливает пакет на каждом вызове
(`Built docpipe @ file:///...`). Это ~0.5 с накладных расходов на команду. На тестах
не сказывается (они импортируют модуль напрямую), но при ручной отладке CLI заметно.

---

## T01 — Фикстура: тестовое .NET-решение ✅

**Создано:** 15 файлов в `tests/fixtures/SampleSolution/` (1 `.sln`, 2 `.csproj`, 12 `.cs`),
плюс `tests/conftest.py` с фикстурой `sample_solution` и `tests/test_fixture.py`.

**Проверено:**

```
uv run pytest tests/test_fixture.py -q   → 6 passed
```

Дополнительно, разовой проверкой вне тестов: **все 12 файлов `.cs` парсятся
tree-sitter с `err=0`**, `root_node.has_error == False`. Это важно было проверить
до T06: если бы фикстура не разбиралась, вся дальнейшая работа строилась бы на песке.

### Отклонение от плана

**Тест сделан строже, чем требовал план.** План просил проверить только количество
(`.cs` ровно 12, `.csproj` 2, `.sln` 1). Вместо этого зафиксирован точный отсортированный
список путей и добавлены три содержательных теста:

- `test_generated_file_is_present_but_will_be_excluded` — файл под `obj/` существует;
- `test_partial_class_lives_in_two_files` — обе половинки содержат `partial class`;
- `test_cross_module_inheritance_chain` — цепочка `PricingController` →
  `BaseApiController` (другой модуль) → `ControllerBase` на месте.

Причина: проверка на количество не ловит подмену состава. Если кто-то удалит
`PricingService.Calculations.cs` и добавит любой другой `.cs`, счётчик сойдётся,
а тесты T10 и T18 начнут проверять не то, что задумано.

### Трудность: `.gitignore` мог тихо убить фикстуру (T01)

Файл `obj/Debug/net8.0/Sample.Generated.g.cs` нужен, чтобы проверять правила исключения.
Любой стандартный .NET-шаблон `.gitignore` содержит `obj/` и `bin/` — добавь его кто-нибудь
позже, и файл исчезнет из репозитория, а тесты T04/T14 продолжат проходить, ничего не
проверяя. Проверено `git check-ignore` (сейчас не игнорируется), в `.gitignore` добавлен
явный запрет-предупреждение.

---

## T02 — `hashing.py`: примитивы детерминизма ✅

**Создано:** `docpipe/hashing.py`, `tests/test_hashing.py`

**Проверено:**

```
uv run pytest tests/test_hashing.py -q   → 16 passed
uv run pytest -q                         → 22 passed (весь набор)
ruff check / format --check              → чисто
mypy docpipe                             → чисто (3 файла)
```

Все 7 пар из таблицы плана проходят без правок алгоритма. Сверх плана добавлены тесты:
идемпотентность `slugify` (повторное применение к результату ничего не меняет),
невозможность пустого slug, независимость `stable_hash` от порядка ключей,
чувствительность `stable_hash` к порядку элементов списка.

### Отклонений от плана нет

Алгоритм реализован ровно как в спецификации. Регекс границ CamelCase
`(?<=[a-z0-9])(?=[A-Z])|(?<=[A-Z])(?=[A-Z][a-z])` корректно обрабатывает аббревиатуры.

### Кириллица в именах типов — вопрос закрыт

**Итог: действий не требуется.** Владелец проекта подтвердил, что в сигнатурах АС CF
кириллицы нет (в комментариях — есть, но они не затрагиваются).

`slugify` применяется ровно в одном месте — имя типа → имя файла в `doc_path`.
Проверено на парсере, что во всех остальных местах кириллица доходит до манифеста
без изменений:

| Что | Кириллица | Куда попадает |
|---|---|---|
| XML-doc `///` | сохраняется | `symbol.xml_doc` |
| Обычные комментарии | не извлекаются | никуда |
| Namespace, FQN, `node.id` | сохраняется | идентификаторы узлов |
| Аргументы атрибутов | сохраняется | `endpoints[].route` |
| Имя модуля в пути | сохраняется | `docs/modules/Расчёты/…` |
| Имя типа → имя файла | **схлопывается в `unnamed`** | только имя `.md`-файла |

Имя модуля идёт в путь сырым, а имя типа — через `slugify`, и это обосновано:
имя модуля берётся из имени файла `*.csproj`, то есть валидно для ФС по построению;
имя типа берётся из идентификатора, который валидным быть не обязан.

Разбор ниже сохранён на случай, если кириллические имена типов всё же обнаружатся
в каком-нибудь модуле.

### Разбор (справочно)

Проверка на реалистичных именах (вне тестов):

```
PricingController    -> 'pricing-controller'      OAuth2Handler   -> 'o-auth2-handler'
FXRateProvider       -> 'fx-rate-provider'        HTTPSProxy      -> 'https-proxy'
IRepository`1        -> 'i-repository-1'          GridGainService -> 'grid-gain-service'
DTOMapper            -> 'dto-mapper'              ApiV2Controller -> 'api-v2-controller'
ЦенаСервис           -> 'unnamed'   ← проблема
Сервис               -> 'unnamed'   ← проблема, коллизия с предыдущим
```

**Любое имя без ASCII-букв схлопывается в `unnamed`.** Корректность при этом не страдает:
разрешение коллизий в T15 добавит хэш-суффикс, и пути останутся уникальными. Но
читаемость теряется полностью — вместо осмысленного имени получится
`unnamed-a1b2c3d4.md`.

В C# кириллические идентификаторы легальны. Насколько это актуально для АС CF — вопрос
к владельцу проекта. Варианты, если понадобится:

1. **Оставить как есть** — коллизии разрешаются хэшем, имена нечитаемы. Ноль работы.
2. **Транслитерация** (`Сервис` → `servis`) — детерминированно, читаемо, нужна таблица
   соответствий на ~66 символов.
3. **Разрешить Unicode в именах файлов** — читаемо, но рискованно для кросс-платформенных
   путей, git на macOS (нормализация NFD/NFC) и внешних инструментов документации.

Реализован вариант 1, как и предписывал план. К нему возвращаться не нужно, пока
`docpipe stats` на реальном репозитории не покажет узлы со slug `unnamed`.

---

## T03 — `model.py`: модели данных и JSON Schema ✅

**Создано:** `docpipe/model.py` (16 моделей), `tests/test_model.py`,
`schema/doc-tree.schema.json` (580 строк, генерируется);
изменён `docpipe/cli.py` (команда `schema`).

**Проверено:**

```
uv run pytest tests/test_model.py -q   → 12 passed
uv run pytest -q                       → 34 passed (весь набор)
docpipe schema дважды подряд           → diff пуст, байт-в-байт
ruff check / format --check            → чисто
mypy docpipe                           → чисто (4 файла)
git check-ignore schema/…              → не игнорируется, попадёт в репозиторий
```

### Отклонения от плана

**1. Типизированные псевдонимы вместо голых `str`.** План описывал
`Symbol.type_kind: str`. Вместо этого заведены псевдонимы `TypeKind`, `MemberKind`,
`Lifetime`, `Confidence`, `DependencyVia`, `RelationKind` и используются на всех
уровнях единообразно. Причина: `type_kind` участвует в правилах классификации (T14),
и опечатка вида `"klass"` иначе всплыла бы только на проде в виде пустой выборки.
Есть тест `test_literal_fields_reject_typos`.

**2. Добавлен `extra="forbid"`.** Плана не нарушает, но не был им предписан.
Ловит опечатки при загрузке чужого манифеста (`--from-manifest` в T18) вместо
молчаливого игнорирования поля. Совместимость вперёд при этом не теряется:
`schema_version` объявлен как `Literal["1.0"]`, то есть несовместимость версий
и так фиксируется явно.

Обе модели `frozen=True` — как и предписывал план.

### Трудность: `Final` для константы со значением `Literal`

`SCHEMA_VERSION = "1.0"` выводился mypy как `str` и не подходил полю
`Literal["1.0"]`:

```
model.py:252: error: Incompatible types in assignment
  (expression has type "str", variable has type "Literal['1.0']")
```

Лечится явной аннотацией `SCHEMA_VERSION: Final[Literal["1.0"]] = "1.0"`.
Мелочь, но mypy strict её не пропускает, а агент-реализатор потратит на это время.

### Замечание: состав `$defs` в схеме

В сгенерированную схему попали 11 моделей:
`Attribute, Dependency, DocNode, Endpoint, Member, Module, ParserVersions,
PartialInfo, Relation, SourceSpan, Symbol`.

**`RawDeclaration`, `FileParseResult`, `DiRegistration`, `RunMeta` отсутствуют — и это
правильно.** Схема описывает `Manifest`, а перечисленные модели из него недостижимы:
первые три относятся к уровню парсера и живут только в кэше (T09), `RunMeta` — это
сидкар-файл. Если понадобится валидировать содержимое кэша, схему для
`FileParseResult` нужно будет генерировать отдельной командой.

---

## T04 — `config.py` и `discovery.py` ✅

**Создано:** `docpipe/config.py`, `docpipe/discovery.py`, `tests/test_discovery.py`

**Проверено:**

```
uv run pytest tests/test_discovery.py -q   → 27 passed
uv run pytest -q                           → 61 passed (весь набор)
ruff check / format --check                → чисто
mypy docpipe                               → чисто (6 файлов)
```

### Найденный баг в спецификации: `fnmatch` не понимает `**`

Самая содержательная находка на текущий момент. План предписывал
«использовать `fnmatch` по POSIX-пути» и приводил один пример. Проверка показала:

```
fnmatch("src/A/obj/x/y.cs", "**/obj/**")  -> True
fnmatch("obj/y.cs",         "**/obj/**")  -> False   ← дыра
fnmatch("src/A/Sample.g.cs", "**/*.g.cs") -> True
fnmatch("Sample.g.cs",       "**/*.g.cs") -> False   ← дыра
```

`fnmatch` транслирует `*` в `.*` без учёта разделителей, поэтому шаблон `**/obj/**`
требует наличия `/` перед `obj`. Следствие: **файлы в корневых `obj/`, `bin/` и
сгенерированные файлы в корне репозитория молча просачивались бы в документацию.**
Наивная реализация по плану прошла бы все приведённые в нём критерии приёмки
и содержала бы эту дыру.

Исправлено функцией `matches_glob`: при неудаче и префиксе `**/` пробуется второй
прогон с отброшенным префиксом. Покрыто параметризованным тестом на 9 случаев.
План обновлён — добавлены и описание ловушки, и требование использовать
`matches_glob` в T14 (`exclude.path_glob` там сравнивается так же).

### Отклонения от плана

**1. `os.walk` вместо `Path.rglob`.** План предписывал `rglob`. Заменено, чтобы
получить отсечение каталогов: из каждого glob с суффиксом `/**` выводится glob
каталога (`**/obj/**` → `**/obj`), и в совпавшие каталоги обход не заходит вовсе.
На реальном репозитории это разница между полным обходом `.git`/`node_modules`
и мгновенным пропуском — а заявленные в архитектуре «1–2 секунды на десятках тысяч
файлов» без этого недостижимы.

Отсечение — чистая оптимизация и на результат влиять не может: если каталог совпал
с dir-глобом, любой файл под ним совпал бы и с исходным шаблоном. Это зафиксировано
тестом `test_pruning_does_not_change_result`, который сравнивает обход с отсечением
и без.

Побочная выгода: `os.walk(followlinks=False)` не разыменовывает симлинки из коробки.
Симлинк на дерево исходников иначе породил бы дубли символов с одинаковыми FQN.
Есть тест.

**2. Контрольный тест «без исключений».** Добавлен
`test_discover_without_excludes_sees_generated_file`: с пустым списком исключений
находится ровно 12 файлов. Без него тест на исключения проходил бы и в случае,
если бы файла просто не было на диске, — то есть проверял бы пустоту.

### Замечание: enrollment в этой задаче не реализован

`DocpipeConfig.enrolled` объявлен, но пока никем не читается — он применяется в T15
при сборке дерева, а не при обходе. Это соответствует замыслу: неenrolled модули
**должны** парситься, их символы нужны для графа наследования. Обход их не фильтрует.

### Отложено до T21

Замер скорости обхода на `examples/abp` не делался. Прогон на `examples/eshoponweb`
выполнен досрочно по запросу владельца — результаты в
[`findings-eshoponweb.md`](findings-eshoponweb.md).

---

## Внеплановый прогон на реальном репозитории (после T04)

По запросу владельца запущено то, что готово, на `examples/eshoponweb` (254 файла `.cs`,
10 проектов, 2 решения). Полный отчёт — [`findings-eshoponweb.md`](findings-eshoponweb.md),
здесь только выводы для реализации.

**Подтвердилось главное:** tree-sitter разобрал **244 файла из 244 без единой ошибки**
за 0.13 с (1948 файл/с). Отказ от Roslyn обоснован. Обход ФС — 17 мс.

**Три бага в спецификации, все внесены в план:**

1. **Многострочные `base_list`** (T06). Реальный случай:
   `class AuthenticateEndpoint : EndpointBaseAsync\n .WithRequest<…>\n .WithActionResult<…>`.
   План предписывал брать текст «как есть» — тогда `signature_hash` начинает зависеть
   от форматирования файла, и переформатирование заставляет агента на шаге 3
   перегенерировать документ впустую. Плюс правила по `base_type` перестают совпадать.
   Исправление: `re.sub(r"\s+", " ", text).strip()`.

2. **DI-регистрации в top-level statements** (T13). Все три `Program.cs` в eShopOnWeb
   написаны без класса и метода. Реализация, написанная под фикстуру (где `Program` —
   обычный статический класс) и ищущая вызовы внутри `method_declaration`,
   **не нашла бы ни одной регистрации в реальном проекте**. Обход обязан идти
   по всему дереву файла.

3. **`unclassified` смешивает разное** (T20). 9 интерфейсов `*Service` отсекаются
   предикатом `type_kind: [class, record]` — это правильно по замыслу, но в статистике
   они попадают в кучу с типами, про которые правила ничего не знают. Введена
   отдельная категория `interface_covered`.

**Наблюдения, не требующие правок кода:**

- Правила из плана дали 18 страниц на 244 файла (81% неклассифицировано). Это ожидаемо:
  дефолтный ruleset намеренно консервативен, а набирать правила нужно по `docpipe stats`
  на конкретном проекте. В eShopOnWeb HTTP-поверхность построена на паттерне REPR
  (10 типов `*Endpoint`), контроллеров всего 4.
- 22 имени типов дублируются, но почти все разнесены по модулям — модульная раскладка
  дерева разводит их без хэш-суффиксов. Подтверждает выбор структуры.
- Только **16 типов из 247 имеют XML-doc** (6.5%). Для шага 3 это значит, что описания
  придётся выводить из структуры кода, а не из комментариев. Стоит замерить ту же
  цифру на АС CF до планирования шага 3.
- 239 файлов с file-scoped namespace против 1 с блочным — редкая форма встречается
  ровно один раз, и именно на ней всё сломается, если её не поддержать.
- 45 типов (18%) лежат в тестовых проектах. Отсекать через `enrolled` в `docpipe.yaml`,
  а не через `exclude`: тестовые проекты должны парситься ради графа наследования,
  но узлов документации не порождать.

---

## T01b — Фикстура `WildSolution` ✅ (задача добавлена по итогам прогона)

**Создано:** `tests/fixtures/WildSolution/` (11 файлов `.cs`, 2 `.csproj`, 1 `.sln`),
`tests/test_fixture_wild.py`; в `tests/conftest.py` добавлена фикстура `wild_solution`.

**Проверено:**

```
uv run pytest tests/test_fixture_wild.py -q   → 20 passed
uv run pytest -q                              → 81 passed (весь набор)
ruff check / format --check                   → чисто
mypy docpipe                                  → чисто
```

Каждый из 11 файлов разбирается с `parse_errors == 0`.

### Зачем отдельное решение, а не расширение SampleSolution

Критерии приёмки T04–T20 завязаны на точные количества в `SampleSolution`
(11 файлов, 10 символов, 6 узлов). Расширение той фикстуры потребовало бы
перенумеровать их все — а числа там уже выверены и служат защитой от регрессий.

### Покрытые случаи

Все восемь найдены прогоном на eShopOnWeb, все ломают реализацию, написанную
только под `SampleSolution`:

| Случай | Что ломается без обработки |
|---|---|
| Многострочный `base_list` | `signature_hash`, правила `base_type` |
| Top-level statements в `Program.cs` | теряются **все** DI-регистрации |
| Тип без namespace | FQN с ведущей точкой |
| Блочный `namespace N { }` | namespace не определяется |
| `global using` / `using static` / алиас | резолв FQN |
| Вложенные типы с общим именем | два узла схлопываются в один |
| Два `Constants` в одном модуле | коллизия `doc_path` |
| Тестовый проект | тесты попадают в документацию |

Тесты фикстуры проверяют **наличие конструкций**, а не только файлов. Иначе
случайное «упрощение» фикстуры оставило бы тесты T06/T13 зелёными и бессмысленными.

### Уточнение контракта, найденное при написании фикстуры

Схлопывания пробелов (`\s+` → ` `), которое я внёс в план после прогона на eShopOnWeb,
**оказалось недостаточно**. На реальной строке получалось:

```
'EndpointBaseAsync .WithRequest<AuthenticateRequest> .WithActionResult<…>'
```

Пробел перед точкой остаётся, и это не канонический C#. Нужен второй шаг —
`re.sub(r"\s*\.\s*", ".", text)`. Итог совпадает с тем, как база была бы записана
в одну строку. План обновлён.

### Побочно: контракт сопоставления имён базовых типов (T14)

Фикстура вскрыла, что правило «последний сегмент после точки» неверно.
Для `Sample.Common.Web.BaseApiController` значим последний сегмент,
для `EndpointBaseAsync.WithRequest<A>.WithActionResult<B>` — **первый**
(последний даёт бессмысленное `WithRequest`).

Различаются по признаку «точка после первого `<`». Функция `base_type_candidates`
с таблицей проверенных случаев внесена в T14. Условие с `<` существенно: без него
`Sample.Common.Web.BaseApiController` порождал бы кандидата `Sample`, и правило
`base_type: ["Sample"]` совпадало бы со всем подряд.

Это тот случай, когда фикстура окупилась до написания кода, который она проверяет.

---

## T05 — `dotnet/csproj.py` и `dotnet/sln.py` ✅

**Создано:** `docpipe/dotnet/__init__.py`, `docpipe/dotnet/csproj.py`,
`docpipe/dotnet/sln.py`, `tests/test_project_graph.py`;
в `WildSolution` добавлен `Directory.Build.props`, `Wild.Api.csproj` пересохранён с BOM,
из `Wild.Tests.csproj` убран собственный `TargetFramework`.

**Проверено:**

```
uv run pytest tests/test_project_graph.py -q   → 16 passed
uv run pytest -q                               → 97 passed (весь набор)
ruff check / format --check                    → чисто
mypy docpipe                                   → чисто (9 файлов)
```

### Три ловушки, найденные осмотром реальных `.csproj` до написания кода

Осмотр 10 файлов проектов eShopOnWeb окупился третий раз подряд.

**1. BOM в каждом файле.** Все 10 сохранены в UTF-8 с BOM (`ef bb bf`). Чтение через
`read_text(encoding="utf-8")` оставляет BOM первым символом, и `ET.fromstring` падает
с `not well-formed`. Читаю байтами — `ElementTree` разбирается сам.

**2. `TargetFramework` не объявлен ни в одном проекте.** 0 из 10. Значение приходит
из `Directory.Packages.props` уровня решения (нетипично — обычно это
`Directory.Build.props`, но MSBuild импортирует и его). Реализация строго по плану
дала бы `target_frameworks: []` **у каждого модуля реального репозитория**.

**3. XML-namespace в legacy-проектах.** В eShopOnWeb таких нет, но АС CF — большой
старый проект, и там они почти наверняка есть. Сравнение по полному тегу молча
не находило бы ничего. Сравниваю по локальному имени.

Плюс в `.sln`: из 14 записей `Project(...)` в `eShopOnWeb.sln` только 10 — проекты C#.
Остальные — папки решения (кладут в поле пути своё имя, а не файл) и `docker-compose.dcproj`.
Фильтрую по расширению.

### Отклонение от плана: наследование TFM из props-файлов

План не предусматривал обход вверх по дереву за `Directory.Build.props` /
`Directory.Packages.props`. Добавлено (~25 строк), потому что без этого поле
`target_frameworks` бесполезно на любом современном репозитории.

Реализация намеренно поверхностная: условия `Condition`, подстановки `$(…)`
и цепочки `Import` не разворачиваются. Воспроизводить вычисление свойств MSBuild —
отдельный проект; для структурных фактов достаточно буквального чтения
`PropertyGroup`. Ограничение зафиксировано в docstring модуля.

Правило приоритета как в MSBuild: ближайший props-файл побеждает дальний,
собственное значение проекта побеждает унаследованное. Оба случая покрыты тестами.

### Тесты самодостаточны

`examples/` в репозиторий не входит (gitignore), поэтому все реальные конструкции
воспроизведены либо в `WildSolution`, либо во временных каталогах: legacy-`csproj`
с `xmlns`, `.sln` с папкой решения и `.dcproj`, две ступени props-файлов, битый XML.

Отдельно проверено, что битый проект падает с `ET.ParseError`, а не отдаёт
пустой `Module`: молчаливая деградация здесь означала бы модуль без зависимостей
в манифесте, что заметить почти невозможно.

---

## T06 — `dotnet/parser.py`: объявления типов ✅

**Создано:** `docpipe/dotnet/queries/declarations.scm`, `docpipe/dotnet/parser.py`,
`tests/test_parser_declarations.py`

**Проверено:**

```
uv run pytest tests/test_parser_declarations.py -q  → 37 passed
uv run pytest -q                                    → 134 passed (весь набор)
ruff check / format --check                         → чисто
mypy docpipe                                        → чисто (10 файлов)
```

**Прогон на eShopOnWeb уже настоящим парсером:**

```
файлов 244, объявлений 247, ошибок разбора 0
время 0.10s (2539 файл/с)
виды типов: class 229, interface 16, record_struct 1, enum 1
вложенных 5, без namespace 1, с xml_doc 16
объявлений с пустым именем: 0
```

Все три случая, ради которых заводился `WildSolution`, отработали на реальном коде:
многострочная база нормализовалась в каноническую однострочную форму, блочный
namespace определился, тип в глобальном namespace получил пустую строку.

### Проверка подозрительной цифры

Единственный `record` в eShopOnWeb определился как `record_struct`. Ложное
срабатывание детекции испортило бы `type_kind` у всех записей, поэтому проверил
исходник: `public readonly record struct CatalogItemDetails` — детекция верна.

### Отклонение от плана: `containing_type` — цепочка, а не ближайший предок

План предписывал «имя ближайшего предка-типа». Для `Outer.Middle.Inner` это дало бы
`containing_type == "Middle"`, и FQN в T10 собрался бы как `N.Middle.Inner` вместо
`N.Outer.Middle.Inner`. Реализована полная цепочка; есть тест на тройную вложенность.

В eShopOnWeb вложенных типов 5, все с одним уровнем — на нём ошибка не проявилась бы.
Нашлось только потому, что тест писался специально на двойную вложенность.

### Решения, которые стоит знать

**Запрос `.scm` только находит узлы объявлений.** Имя, модификаторы, базы и атрибуты
берутся обходом потомков, а namespace и `containing_type` — обходом предков.
Запросом второе выразить нельзя: tree-sitter не умеет подниматься вверх по дереву.
Файл `declarations.scm` остаётся местом, где перечислены виды объявлений, — добавить
новый вид можно, не трогая код.

**`span` начинается с атрибутов**, а не с `public`: `attribute_list` — потомок
объявления. Это желаемое поведение, ссылка в документации ведёт на объявление целиком.
Зафиксировано тестом, чтобы случайная «починка» не сдвинула ссылки.

**Квалифицированные имена атрибутов приводятся к простому**
(`System.ComponentModel.Description` → `Description`): правила классификации пишутся
по простому имени.

**`verbatim_string_literal` — единый лист** без потомка `string_literal_content`,
в отличие от обычной строки. `@` и кавычки снимаются вручную, `""` разворачивается в `"`.
Внесено в план.

### Замечание: покрытие XML-doc

16 типов из 247 (6.5%) — цифра совпала с черновой разведкой. Подтверждает вывод
из [`findings-eshoponweb.md`](findings-eshoponweb.md): на шаге 3 описания придётся
выводить из структуры кода, а не из комментариев.

---

## Внеплановый прогон на ABP (после T06)

По запросу владельца запущено то, что готово, на `examples/abp` — фреймворк
`abpframework/abp`: **671 проект, 7869 файлов `.cs`, 3,9 ГБ на диске**, в 32 раза больше
eShopOnWeb. Полный отчёт — [`findings-abp.md`](findings-abp.md), здесь выводы
для реализации.

**Производительность подтвердилась второй раз:** обход ФС 0,40 с, разбор 5,0 с
(1570 файлов/с, 17,5 МБ исходников), **0 падений**. Roslyn потребовал бы `restore`
671 проекта.

### Четыре бага в уже написанном коде — исправлены здесь же

1. **`.slnx` не искался вовсе** (T04, T05). ABP мигрировал на новый XML-формат решений
   целиком: **30 файлов `.slnx` и ноль `.sln`**. `discovery` знал только про `.sln`,
   то есть нашёл бы там ноль решений. Теперь `sln_files` собирает оба расширения,
   а `parse_sln` выбирает разбор по суффиксу. Проверка: 651 проект из 30 решений,
   все существуют на диске.

2. **`module:{name}` — не уникальный id** (T05). Имена проектов повторяются **39 раз**,
   включая три разных `MyCompanyName.MyProjectName.csproj`. Разные модули склеились бы
   в один, и часть документации исчезла бы молча. Теперь `id = "module:" + путь к csproj`.
   Выбран путь, а не суффикс-разрешитель коллизий: путь уникален по построению и
   считается локально из одного файла, тогда как суффикс потребовал бы глобального
   прохода и менял бы id существующих модулей при добавлении нового.

3. **`ProjectReference` сопоставлялись по `.stem`** (T05) — та же проблема: ребро графа
   уходило бы в произвольный из одноимённых модулей. Теперь `Include` разрешается
   в репо-относительный путь. На ABP разрешаются **2383 ссылки из 2383**,
   на eShopOnWeb — 18 из 18.

4. **`$(TargetFrameworks)` попадал в манифест** (T05) как строка, неотличимая
   от настоящей платформы. Значения с `$(` отбрасываются.

### Три правки плана — на задачи, которые ещё не написаны

5. **`#if` прячет члены типа от прямого обхода** (T07 — следующая задача). Члены под
   `#if` не являются прямыми детьми `declaration_list`, они уходят под `preproc_if`
   и `preproc_else`. Реализация «перебрать детей тела» потеряла бы их молча.
   `#region`, в отличие от `#if`, контейнером не является — асимметрию легко не заметить.
   В спецификацию T07 добавлено требование спускаться вглубь с остановкой на вложенных
   типах и тест с ожиданием четырёх членов там, где наивная реализация вернёт два.

6. **`#if` внутри аргументов атрибута уничтожает объявление** (T20). Директива рвёт
   выражение, а атрибут — потомок объявления, поэтому ломается и оно: остаток файла
   переразбирается как top-level statements, и `class_declaration` пропадает.
   Так теряется `CmsKitWebUnifiedModule` в ABP. Обойти в рамках tree-sitter нельзя,
   поэтому `validate` обязан ловить комбинацию «`parse_errors > 0` и ноль объявлений».
   Воспроизведено один в один в фикстуре `WildSolution/…/Modules/ConditionalModule.cs`
   (8 ошибок, 0 объявлений — те же числа, что на настоящем файле).

7. **FQN не является уникальным ключом символа** (T15). План задавал
   `id = "type:" + fqn` и разрешение коллизий `doc_path` суффиксом `-{sha256(fqn)[:8]}`.
   На 9075 объявлениях ABP: по `fqn` — **255 коллизий**, по `module + fqn` — 108,
   по `module + fqn + арность` — 3. Причин две: перегрузка по арности дженерика
   (112 групп, рекорд — шесть арностей у одного имени) и одинаковые FQN в разных
   сборках. Хэш от FQN не разводит ничего там, где FQN совпадает: внутри одного модуля
   195 групп дают одинаковый slug, и в 105 из них FQN тоже одинаков. Ключ и суффикс
   переопределены в T15; фикстура с тремя арностями одного имени — в
   `WildSolution/…/Services/CrudAppService.cs`.

### Трудности прогона

**Минимальное воспроизведение потери объявления не удалось получить сразу.** Синтетический
пример с `#if` в аргументах атрибута парсился с ошибками, но объявление **выживало** —
терялось только на настоящем файле. Бисекция показала, что восстановление грамматики
зависит от того, что идёт за атрибутом: на упрощённом теле класса она справляется,
на реальном (с ещё одним `#if` внутри метода) — нет. Точный порог — внутреннее свойство
tree-sitter, гоняться за ним смысла нет. Вывод для плана сформулирован так, чтобы
не зависеть от порога: признаком считается пара «ошибки есть и объявлений нет», а фикстура
скопирована с реального файла, а не сочинена.

**Тест на фикстуру пришлось писать по тексту файла, а не по дереву.** Разбор сломан,
узла `attribute_list` в дереве нет, поэтому наличие конструкции проверяется по исходнику,
а последствие — по дереву. Обе половины нужны: без первой тест сломается, если файл
«починят», без второй — если грамматика научится восстанавливаться.

### Наблюдения, не требующие правок

- **BOM в 72 % файлов `.cs`** (5699 из 7869). Чтение байтами, заложенное ради `.csproj`,
  спасает и здесь. Файлов не в UTF-8 нет, кириллицы в идентификаторах нет.
- **Покрытие XML-doc 3,8 %** (348 из 9075) — ещё ниже, чем 6,5 % в eShopOnWeb.
  Второе подтверждение: на шаге 3 описания придётся выводить из кода.
- **DI через маркерные интерфейсы**: самые частые базовые типы — `ITransientDependency`
  (696) и `ISingletonDependency` (167). T13, ищущий только вызовы `services.Add*`,
  на такой кодовой базе нашёл бы почти ничего. АС CF на ABP не построен, но приём
  распространённый — держать как точку расширения правил, а не зашивать в код.
- **Вложенность типов мелкая**: 8291 объявление на верхнем уровне, 685 / 97 / 2 глубже.
  Полная цепочка `containing_type` из T06 нужна ровно ради этих 99.
- **37 файлов из 7869 (0,5 %) разбираются с ошибками**, все — `#if` посередине выражения.
  Объявления выживают везде, кроме одного случая (пункт 6).

### Проверка

```
uv run pytest -q                    147 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        22 files already formatted
uv run mypy docpipe                 Success: no issues found in 10 source files
```

Повторный прогон на ABP после правок: 30 решений найдено, 0 дублей `id`,
0 значений с `$(`, 2383 ссылки из 2383 разрешены, 651 проект из решений существует.
Регрессия на eShopOnWeb не обнаружена: 18 ссылок из 18, 0 ошибок разбора.

---

## T07 — `dotnet/parser.py`: члены типов ✅

**Создано:** `docpipe/dotnet/queries/members.scm`, `tests/test_parser_members.py`;
в `parser.py` добавлены `_owning_type`, `_signature`, `_member_names`, `_build_members`.

**Проверка**

```
uv run pytest -q                    166 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        23 files already formatted
uv run mypy docpipe                 Success: no issues found in 10 source files
```

Все критерии приёмки на `PricingController` совпали дословно, включая сигнатуру
`"public async Task<ActionResult<decimal>> GetAsync(Guid id, CancellationToken ct)"`.
`RiskComputeService` — ровно 3 метода, `PriceDto` — 0 членов без падения.

На реальном коде: **37 262 члена** на ABP (18 828 методов, 10 780 свойств, 4148 полей,
3484 конструктора, 22 события) и 958 на eShopOnWeb. Ни одного члена без имени,
ни одной пустой сигнатуры. Разбор замедлился с 5,0 до 5,6 с (1406 файлов/с) —
плата за второй запрос по дереву.

### Отклонения от плана

**1. `event_field_declaration` — отдельный узел, план его не упоминал.** В грамматике
события объявляются двумя разными узлами: `event_declaration` — только форма
с `add`/`remove`, а обычное `public event EventHandler Changed;` — это
`event_field_declaration`. Реализация строго по плану потеряла бы обычную форму,
которой в ABP вдвое больше. План исправлен, узлов в `members.scm` шесть.

**2. Поле с несколькими объявителями даёт несколько членов.** План предписывал брать
имя первого `variable_declarator`; тогда в `private int _a = 1, _b = 2;` поле `_b`
пропало бы молча. На ABP таких полей 0 из 1652 — то есть правило плана ничего бы
не сломало на практике, — но корректная обработка стоит трёх строк, и молчаливая
потеря данных того не стоит. План исправлен.

**3. Список видов членов сужен явно.** `indexer_declaration`, `operator_declaration`,
`conversion_operator_declaration`, `destructor_declaration`, `delegate_declaration`
не извлекаются: в `MemberKind` таких значений нет. На 3000 файлов ABP их суммарно 20
против 14 000 обычных членов. Раньше это просто не оговаривалось, теперь записано
в план вместе с инструкцией, что менять при необходимости.

### Решения, которые стоит знать

**Владелец члена ищется обходом вверх, а не перебором детей тела.** Это следствие
находки ABP: члены под `#if` живут под `preproc_if`/`preproc_else` и прямыми детьми
`declaration_list` не являются. Запрос находит их на любой глубине, а ближайший
предок-тип сам разводит вложенные типы — отдельной логики «остановиться на вложенном
типе» не понадобилось. Тест на пять членов (`One`, `Two`, `TwoOld`, `Region`, `Three`)
ловит наивную реализацию: она вернёт три.

**Сигнатура режется по узлам, а не поиском `{` в тексте.** Терминаторы —
`block`, `arrow_expression_clause`, `accessor_list`, `;`. Текстовый поиск сломался бы
на `void M(string s = "{")` и на инициализаторах коллекций. Атрибуты отбрасываются,
хотя и являются потомками узла: для правил есть отдельное поле `attributes`,
а в сигнатуре они шумят. Ограничения `where T : class` и `: base(x)` стоят до тела
и в сигнатуру входят — это желаемое поведение.

**Срез текста берётся относительно самого узла** (`member.text[start - member.start_byte : …]`),
поэтому в функцию не нужно тащить исходник файла целиком.

### Замечание: тяжёлый хвост у сигнатур полей

`{` инициализатора коллекции лежит **внутри** `variable_declaration`, а не отдельным
потомком, поэтому обрезка по узлам его не отсекает и инициализатор входит в сигнатуру.
Обычно это полезно — значение `const` часть контракта, и его изменение должно менять
`signature_hash`. Но хвост тяжёлый: медиана сигнатуры поля 52 символа, p99 — 187,
максимум **1283** (поле с ASCII-графикой в инициализаторе). Всего сигнатур длиннее
200 символов — 460 из 37 262 (1,2 %).

Решено оставить как есть и записать в план требование к шагу 3 усекать сигнатуру
при выводе. Альтернатива — резать поле по `=` — теряет значения констант ради
1,2 % случаев.

### Замечание: покрытие XML-doc у членов

1703 члена из 37 262 (**4,6 %**) имеют `<summary>`. У типов было 3,8 %. Третье
подтверждение подряд: описания на шаге 3 придётся выводить из кода.

---

## T08 — `dotnet/parser.py`: usings ✅

**Создано:** `docpipe/dotnet/queries/usings.scm`, `tests/test_parser_usings.py`;
в `parser.py` добавлены `_using_name` и `_usings`.

**Проверка**

```
uv run pytest -q                    180 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        24 files already formatted
uv run mypy docpipe                 Success: no issues found in 10 source files
```

Оба критерия приёмки совпали: `PricingController.cs` даёт три `usings` и пустые
`global_usings`, `GlobalUsings.cs` — два `global_usings` и пустые `usings`.

На реальном коде: **33 368 директив** на ABP (1223 уникальных пространства имён)
и 884 на eShopOnWeb. Ни одного подозрительного имени. Разбор 5,6 → 6,2 с
(1278 файлов/с) — цена третьего прохода запросом по дереву.

### Отклонения от плана

**1. Понадобился запрос по всему дереву, а не перебор детей `compilation_unit`.**
Директива `using` может лежать **внутри блочного namespace** — в ABP таких 8.
Перебор детей корня их не увидел бы. Заодно проверено, что ложных срабатываний
внутри методов нет: `using var s = …` — это `local_declaration_statement`,
а `using (…) { }` — `using_statement`; узлом `using_directive` не является ни то,
ни другое. План исправлен, добавлен `usings.scm`.

**2. Узел имени бывает третьего типа — `alias_qualified_name`.** План называл только
`qualified_name` и `identifier`. Форма `using global::AutoMapper;` (в ABP две штуки)
даёт `alias_qualified_name` с текстом `global::AutoMapper`. Префикс снимается:
это квалификатор глобального пространства имён, а не часть имени, и без снятия
резолв на T10 такой namespace не нашёл бы. Прочие extern-алиасы
(`using MyLib::Some.Namespace;`) оставлены как есть — без разрешения алиасов сборок
они всё равно не резолвятся, а врать в манифесте хуже.

### Решения, которые стоит знать

**Имя берётся как последний именованный потомок.** У алиаса перед именем стоит ещё
`identifier` самого алиаса, поэтому «первый `identifier` или `qualified_name`»
дал бы имя алиаса. Алиасы всё равно отбрасываются, но правило «последний» корректно
для всех форм разом и не зависит от порядка проверок.

**`global` и `static` — анонимные узлы**, в `named_children` они не попадают,
поэтому различение идёт по `{child.type for child in directive.children}`.

**Ловушка с `using global::X;`.** Реализация, ищущая подстроку `global` в тексте
директивы, отнесла бы её в `global_usings` — хотя прямым потомком `global` здесь
не является. Зафиксировано отдельным тестом.

### Замечание: global usings в реальных репозиториях

В ABP и eShopOnWeb — **ноль** `global using` (формы: 33 374 обычных, 58 алиасов,
10 `using static`). Фича из C# 10, и обе кодовые базы её не используют: ABP целится
в том числе в netstandard2.0. Проверить эту форму можно только на фикстуре — ещё один
довод в пользу того, что `WildSolution` собирается вручную, а не копированием
из реального репозитория.

### Ограничение, которое стоит помнить на T10

Алиасы отбрасываются целиком, поэтому тип, на который ссылаются через алиас,
на T10 не разрешится. В ABP таких директив 58 на 33 368 (0,17 %). Лечится только
полноценной таблицей алиасов на файл; пока не окупается.

---

## T09 — `cache.py`: sqlite-кэш разобранных файлов ✅

**Создано:** `docpipe/cache.py`, `tests/test_cache.py`.

**Проверка**

```
uv run pytest -q                    197 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        26 files already formatted
uv run mypy docpipe                 Success: no issues found in 11 source files
```

### Замер на ABP — ради этого задача и делалась

| | время | попаданий | промахов |
|---|---|---|---|
| холодный прогон | 8,19 с | 0 | 7869 |
| тёплый прогон | **0,79 с** | 7869 | 0 |

Ускорение **10,3×**, результаты обоих прогонов побитово идентичны (сравнение всех 7869
`FileParseResult`). Размер БД — 8,0 МБ на 17,5 МБ исходников.

Оставшиеся 0,79 с — это чтение и хэширование всех файлов, то есть неустранимый пол
инкрементального режима. Совпадает с оценкой «~1–2 с на десятках тысяч файлов»
из §2.3 архитектуры.

### Отклонения от плана

**1. Битая БД пересоздаётся, а не роняет прогон.** В плане этого не было. Кэш —
расходный материал: оборванная запись, обрезанный файл или посторонний файл с тем же
именем не должны стоить отказа команды с советом «удалите каталог». Открытие проверяется
запросом `SELECT count(*) FROM sqlite_master`, на `sqlite3.DatabaseError` файл удаляется
и создаётся заново. Отдельно проверено, что пустой файл (0 байт) — валидная пустая БД
и пересоздавать его не нужно.

**2. `put` не коммитит.** План API коммит не описывал. Коммит на каждую запись — это
десятки тысяч fsync на большом репозитории. Коммит перенесён в `close()`; записи видны
через `get` в том же соединении, поэтому внутри прогона поведение не меняется, а на
аварийном завершении теряется кэш, но не результат.

**3. Уровень сжатия `zlib` задан явно.** Значение по умолчанию менялось между версиями
Python, а payload хочется иметь воспроизводимым побайтово.

Все три внесены в план.

### Решения, которые стоит знать

**Попадание определяется хэшем содержимого, `mtime` не используется вовсе.** Время
модификации меняется при checkout и при переносе репозитория, не меняя ни байта в файле:
кэш на `mtime` то промахивался бы там, где не должен, то попадал бы там, где нельзя.
Это то же требование, что и к манифесту, — результат обязан зависеть только от содержимого.

**`prune` считает разницу в Python, а не через `NOT IN (…)`.** Список путей измеряется
тысячами, а число параметров в запросе sqlite ограничено.

**Payload — `model_dump_json` от pydantic, а не `stable_json_dumps`.** Побайтовая
стабильность payload на корректность не влияет (сверяется `content_hash`, не payload),
а родная сериализация модели гарантирует точный round-trip. `stable_json_dumps`
используется только для строки версий в `meta`, где нужно сравнение на равенство.

### Ошибка, найденная своим же тестом

Первая версия `test_round_trip_keeps_full_structure` клала БД в
`sample_solution.parent / "tmp-cache"` — то есть **внутрь `tests/fixtures/`**. Тесты
проходили, но каталог фикстур засорялся. Замечено по `git status` сразу после прогона.
Исправлено на `tmp_path`. Вывод на будущее: любой тест, который что-то пишет, обязан
писать в `tmp_path`, даже если путь строится от фикстуры.

---

## T10 — `dotnet/resolve.py`: FQN и слияние partial ✅

**Создано:** `docpipe/dotnet/resolve.py`, `tests/test_resolve_fqn.py`.

**Проверка**

```
uv run pytest -q                    226 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        28 files already formatted
uv run mypy docpipe                 Success: no issues found in 12 source files
```

Все шесть критериев приёмки на `SampleSolution` совпали: ровно 10 символов с ожидаемыми
FQN, `PricingService` слит из двух файлов с `Discount` в членах, `BaseApiController`
резолвнут через using, `ControllerBase` остался сырым, generic-аргумент отброшен,
неоднозначностей нет.

### На реальном коде

| | ABP | eShopOnWeb |
|---|---|---|
| объявлений → символов | 9075 → 8966 | 257 → 253 |
| время сборки индекса | 0,26 с | 0,02 с |
| базовых типов резолвнуто в индекс | 6515 из 7400 (88 %) | 54 из 140 (39 %) |
| `ambiguous` | **1** | 0 |
| символов с 2+ источниками без `partial` | **3** | 0 |
| FQN, за которыми стоит больше одного символа | **261** | 0 |

Три не-partial символа с двумя источниками — ровно те, что предсказаны в
[findings-abp.md](findings-abp.md): `Platforms/iOS/` против `Platforms/MacCatalyst/`
и `EntityFrameworkCore/UnifiedDbContext.cs` против `MongoDB/UnifiedDbContext.cs`.
Механизм отработал, аномалия видна и передана в T20 как предупреждение.

Единственная неоднозначность на ABP настоящая: `IFourthDbContext` виден через два
разных using сразу. Ложных срабатываний нет — это подтверждает выбор порядка резолва
(см. ниже).

Разница 88 % против 39 % объясняется составом: в eShopOnWeb большинство баз — внешние
(`ControllerBase`, `PageModel`, `DbContext`), в ABP много внутренних (`AbpModule`,
`ITransientDependency`).

### Отклонения от плана

**1. Ключ индекса — не FQN.** План задавал `dict[str, Symbol]` с ключом-FQN. На ABP это
255 коллизий: 112 групп перегрузки по арности дженерика и 155 пар одинакового FQN
в разных сборках. Ключом стал `symbol_key(модуль, FQN, арность)` — тот же формат, что
у id узла на T15. Добавлена функция `index_by_fqn`: она возвращает **список** ключей,
потому что один FQN законно принадлежит нескольким типам (на ABP таких FQN 261).

**2. Namespace и usings проверяются последовательно, а не скопом.** План предписывал
собрать совпадения из namespace и usings вместе и при количестве >1 ставить `ambiguous`.
Это пометило бы неоднозначным обычный случай, когда тип виден и по namespace, и по using,
хотя в C# ближнее имя просто перекрывает дальнее. Реализовано как в C#: сначала
полностью квалифицированное имя, затем namespace и его префиксы от длинного к короткому
(побеждает первое совпадение), и только при неудаче — usings, где несколько совпадений
действительно означают неоднозначность. Результат — 1 неоднозначность на 8966 символов
вместо шума.

**3. `strip_generics` удаляет сбалансированные группы, а не всё от первого `<`.**
`A.B<C<D>>.E` — это вложенный тип `E` внутри generic-типа `B`, его FQN равен `A.B.E`.
Обрезка по первому `<` дала бы `A` и потеряла бы тип.

Все три внесены в план.

### Ошибка, которую нашёл собственный тест

Первая версия брала для резолва usings **только своего файла**. Тест
`test_global_using_participates_in_resolution` был написан под это поведение — и в момент
написания утверждения стало видно, что оно неверное: `global using` действует на весь
проект, а не на файл, где объявлен, и обычно все они лежат в одном `GlobalUsings.cs`.
То есть реализация не резолвила бы **ни один** тип, видимый исключительно через них.

Исправлено: набор видимых usings файла = собственные плюс все `global_usings` его модуля.
Границу модуля пересекать нельзя — проекты изолированы, на это добавлен отдельный тест.

Показательно, что на реальных репозиториях эта ошибка не проявилась бы никак: в ABP
и eShopOnWeb `global using` нет вовсе. Нашлась она только потому, что тест писался
формулировкой ожидания, а не подгонкой под вывод.

### Решения, которые стоит знать

**Базовые типы резолвятся до слияния, каждое объявление со своими usings.** Половинки
`partial class` лежат в разных файлах, и usings у них разные; резолв после слияния
применил бы к одной половине контекст другой.

**`base_types` хранит FQN, а не ключи символов.** По ним матчатся правила классификации
(`base_type: ["EndpointBaseAsync"]`), и ключ с модулем и арностью там был бы нечитаем.
Обратный переход даёт `index_by_fqn`.

**Атрибуты дедуплицируются вручную.** Поля `args` и `named_args` — список и словарь,
поэтому модель нехэшируема и `set` не работает.

**Пустой префикс namespace обязателен** при переборе: глобальное пространство имён видно
из любого места, и тип без namespace должен находиться.

---

## T11 — `dotnet/resolve.py`: замыкание наследования ✅

**Создано:** `tests/test_resolve_closure.py`; в `resolve.py` добавлены `base_type_arity`,
`_base_symbol_key`, `compute_closures`. Помощники `index_of` и `by_fqn` вынесены
в `tests/conftest.py`.

**Проверка**

```
uv run pytest -q                    241 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        29 files already formatted
uv run mypy docpipe                 Success: no issues found in 12 source files
```

Ключевой критерий выполнен:
`PricingController.base_type_closure == ["ControllerBase", "Sample.Common.Web.BaseApiController"]` —
транзитивно через границу модуля и границу индекса. Ради этого шага индекс символов
и существует.

### На реальном коде

| | ABP | eShopOnWeb |
|---|---|---|
| время замыкания | 0,17 с | 0,02 с |
| символов с непустым замыканием | 5714 из 8966 | 131 из 253 |
| замыкание шире прямых баз | **3666** | 16 |
| самое глубокое | 25 (`Volo.Abp.Identity.IdentityUser`) | 5 (`EfRepository`) |

3666 символов на ABP получили в замыкании то, чего нет в их прямых базах, — то есть
транзитивность работает не в теории, а на большинстве иерархий.

### Отклонения от плана

**1. Переход «базовый тип -> символ» идёт по паре (FQN, арность).** План описывал обход
по `base_types`, то есть по одним FQN. Замер: на ABP **813 рёбер наследования из 7400**
ведут к группе символов с одинаковым FQN и разной арностью — переход по FQN раскрыл бы
произвольный из них.

Крайний случай нашёлся в самом ABP: `Volo.Abp.AI.IChatClientAccessor : IChatClientAccessor<T>`,
где база имеет **тот же FQN, что и наследник**. Переход по FQN даёт петлю на себя;
защита от циклов её погасит, но замыкание получится неверным. Из-за этого же собственный
FQN символа исключается из его замыкания: иначе тип попадал бы в своё же замыкание,
и правило по базовому типу начало бы матчить сам тип.

Правило выбора: сначала символ своего модуля, иначе лексикографически меньший ключ.
Замер на ABP: из 846 рёбер, ведущих к нескольким символам, 1967 разрешаются своим
модулем, 4438 — единственным кандидатом, и остаются **8** действительно неоднозначных.

**2. `base_types` и `base_types_raw` сделаны параллельными** (правка T10). Раньше это были
два независимо отсортированных множества, и соответствие raw ↔ resolved терялось —
а без него не восстановить арность базового типа. Дедупликация теперь по сырому имени;
если половинки `partial` резолвят одно имя по-разному (usings у файлов разные),
побеждает первая по порядку файлов.

Оба отклонения внесены в план, включая тест, который проваливает реализация без арности:
`IService<T> : IOneArg`, `IService<T,K> : ITwoArgs`, `User : IService<int,string>` →
в замыкании `User` должен быть `ITwoArgs`.

### Замечание про организацию тестов

Первая версия импортировала помощники `_index_of` и `_by_fqn` из `test_resolve_fqn.py`.
Это связывает тестовые модули между собой и ломается при переименовании; `ruff` заодно
ругнулся на порядок импортов. Помощники переехали в `tests/conftest.py` под именами
`index_of`, `by_fqn` и `module_of` — они понадобятся всем задачам от T12 и дальше.

---

## T12 — `dotnet/endpoints.py` ✅

**Создано:** `docpipe/dotnet/endpoints.py`, `tests/test_endpoints.py`.

**Проверка**

```
uv run pytest -q                    261 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        31 files already formatted
uv run mypy docpipe                 Success: no issues found in 13 source files
```

### Главное отклонение: `[Route]` на методе

Разведка перед реализацией (по установившейся привычке смотреть на реальный код раньше,
чем писать) сразу дала цифру: в ABP **245 из 356** HTTP-атрибутов идут вместе
с отдельным `[Route]` на том же методе:

```csharp
[Route("api/abp/multi-tenancy")]
public class AbpTenantController
{
    [HttpGet]
    [Route("tenants/by-name/{name}")]
    public Task<TenantDto> FindTenantByNameAsync(string name) { }
```

План предписывал брать шаблон метода только из аргумента `Http*`. По этому плану **всем**
методам `AbpTenantController` достался бы один и тот же маршрут — маршрут типа. Таблица
эндпоинтов выглядела бы правдоподобно и была бы бесполезной, а на фикстуре все критерии
приёмки прошли бы: в `SampleSolution` шаблон записан прямо в `[HttpGet("{id:guid}")]`.

Исправлено: шаблон метода = аргумент `Http*`, при его отсутствии — аргумент `[Route]`
на том же члене. Случая, когда аргументы есть у обоих, ни в ABP, ни в eShopOnWeb нет
ни разу, поэтому приоритета достаточно простого.

### Прочие отклонения

**Токены сравниваются без учёта регистра.** План называл ровно `[controller]` и `[action]`;
ASP.NET их регистр не различает. Значение подставляется с исходным регистром имени.

**Сортировка дополнена третьим ключом** — `member`. По `(route, http_method)` два метода
с одинаковым маршрутом и глаголом (в ABP есть: `[HttpPost]` + `[MapToApiVersion]` на двух
перегрузках) остались бы в порядке обхода.

Заодно исправлен пример в критериях приёмки: он показывал GET первым, хотя по собственному
правилу сортировки `api/v1/Pricing` — префикс `api/v1/Pricing/{id:guid}`, и первым идёт POST.

### На реальном коде

| | ABP | eShopOnWeb |
|---|---|---|
| типов с эндпоинтами | 97 | 4 |
| эндпоинтов | 356 | 26 |
| с непустым маршрутом | 346 | 26 |
| глаголы | GET 226, POST 68, PUT 34, DELETE 26, PATCH 2 | GET 14, POST 12 |

Подстановка токенов проверена на eShopOnWeb: `[Route("[controller]/[action]")]` даёт
`Manage/ChangePassword`.

### Пустой маршрут — это не ошибка

10 эндпоинтов ABP получили пустой `route`: у контроллера нет `[Route]`, у метода нет
шаблона, то есть путь задаётся **конвенциональной** маршрутизацией в конфигурации
приложения. Атрибутов для него не существует, вывести путь неоткуда.

Эндпоинт всё равно выдаётся: то, что метод обрабатывает GET, — факт, и он полезен шагу 3.
Записано в план как явно не поддерживаемый случай, чтобы пустой маршрут не приняли за баг.

### Что решено не разбирать

- **конвенциональная маршрутизация** — задаётся не на типе;
- **minimal API** (`app.MapGet`) — не привязан к типу;
- **`[Route]` без `Http*`** (11 членов в ABP) — маршрут без ограничения по глаголу,
  а `Endpoint.http_method` обязателен.

---

## T13 — `dotnet/di.py` ✅

**Создано:** `docpipe/dotnet/queries/di.scm`, `docpipe/dotnet/di.py`, `tests/test_di.py`;
`parser.py` заполняет `FileParseResult.di_registrations`.

**Проверка**

```
uv run pytest -q                    282 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        33 files already formatted
uv run mypy docpipe                 Success: no issues found in 14 source files
```

Критерии приёмки на `SampleSolution/Program.cs` совпали: три регистрации
(`scoped`, `singleton`, `transient`) с `confidence = "high"`. Ловушка с top-level
statements закрыта на `WildSolution/Program.cs`: четыре регистрации в файле,
где нет ни одного объявления типа.

### На реальном коде

| | ABP | eShopOnWeb |
|---|---|---|
| регистраций | 182 | 31 |
| `high` / `medium` | 175 / 7 | 31 / 0 |
| lifetime | transient 84, singleton 73, scoped 19, hosted 6 | scoped 28, singleton 2, transient 1 |

Разбор замедлился с 6,2 до 6,7 с — цена четвёртого прохода запросом по дереву.

### Отклонения от плана

**1. `typeof(...)` — не запасной вариант, а обычная форма записи.** План её не упоминал
вовсе. Разведка показала: в ABP и eShopOnWeb так записаны **47 регистраций из 267** —
больше, чем двумя типами-аргументами (34+13):

```csharp
context.Services.TryAddTransient(typeof(IKernelAccessor<>), typeof(KernelAccessor<>));
```

Имена типов здесь названы так же явно, как в `<…>`, поэтому `confidence` остаётся `high`.
Без этой ветки 47 полностью определённых регистраций были бы потеряны.

**2. Регистрация без единого имени типа пропускается.** План предписывал брать
«`service_type` из текста». Текстом там оказывается тело лямбды
(`sp => sp.GetRequiredService<X>()`), переменная (`AddSingleton(builder)`) или приведение
типа — в поле `service_type` манифеста попал бы мусор. Такую регистрацию нельзя связать
с символом, то есть документации она ничего не даёт. Таких вызовов 50 из 267.

**3. Узлы вызовов приходят в `di.py` снаружи.** План говорил «создать `di.py`, изменить
`parser.py`», не уточняя границу. Прямое обращение `di.py` к грамматике потребовало бы
импортировать `_LANGUAGE` из `parser.py`, который сам импортирует `di.py`, — цикл.
Запрос выполняет `parser.py` (ему и принадлежит вся работа с tree-sitter), а `di.py`
получает список узлов и занимается только смыслом.

Все три внесены в план вместе с тестами.

### Решения, которые стоит знать

**Запрос сужен до `function: (member_access_expression)`.** Это отсекает вызовы без
получателя (`Foo()`), которых в файле подавляющее большинство, и заодно даёт бесплатную
проверку: `AddScoped<IFoo, Foo>()` без `services.` регистрацией не считается.

**Имя метода лежит в поле `name` у `member_access_expression`** и бывает двух видов:
`identifier` или `generic_name`. `type_argument_list` принадлежит `generic_name`,
а **не** `invocation_expression` — на этом легко ошибиться.

**Открытые дженерики сохраняются как есть** (`IKernelAccessor<>`). Резолвом занимается
T15, и в план ему добавлено требование снимать generic-часть при сопоставлении с символом:
`KernelAccessor<>` не совпадёт с именем символа `KernelAccessor` буквально.

### Замечание: маркерные интерфейсы

Основной способ регистрации в ABP — не вызов, а наследование от `ITransientDependency`
(696 типов) или `ISingletonDependency` (167). Вызовов `Add*` там всего 182 на 8966 типов.
Синтаксически это не регистрация, и в `di.py` ему делать нечего — ловится правилом
классификации по базовому типу на T14. Записано в findings-abp.md ещё при прогоне;
здесь подтверждено цифрами.

---

## Стресс-тест на четырёх репозиториях (после T13)

По запросу владельца прогон всего написанного (T04–T13) на двух новых репозиториях —
`opentelemetry-dotnet` (767 файлов, 70 проектов) и `semantic-kernel` (2960 файлов,
190 проектов, плюс 1244 файла Python и каталоги TypeScript и Java). Полный отчёт —
[`findings-stress.md`](findings-stress.md).

**Итог по всем четырём: 11 850 файлов `.cs`, 941 проект, ноль падений парсера.**
Детерминизм проверялся четырьмя способами на каждом репозитории (повторный обход,
разбор, сборка индекса, замыкание) — везде побитовое совпадение. Кэш даёт 8–18×
и возвращает результат, равный полному разбору.

### Один настоящий баг — исправлен

**Безымянные члены.** Три члена на двух репозиториях получили пустое имя: при
восстановлении после ошибки грамматика отдаёт `field_declaration` без имени — обломок
выражения из-под `#if`. Один случай оказался иным: `extension(char) { … }` — **члены-расширения
C# 14** из .NET 10, которых грамматика 0.23 ещё не знает.

Безымянный член превратился бы в безымянный раздел документации. Исправлено фильтрацией
в `_member_names`; после правки безымянных членов на всех четырёх репозиториях ноль.

### Одна ошибка в разборе проектов — исправлена

**`ProjectReference` через подстановки MSBuild.** В OpenTelemetry разрешались
**2 ссылки из 109**: все остальные записаны как `$(RepoRoot)\src\X\X.csproj`.
Хуже того, старый код склеивал такое значение с каталогом проекта и получал
правдоподобный, но выдуманный путь `test/X/$(RepoRoot)/src/…`.

Значение `$(RepoRoot)` вычисляется функцией MSBuild — воспроизводить это здесь нечем.
Но имя файла в ссылке записано буквально. Добавлен проход `csproj.resolve_references`:
дорезолвливает по имени файла и **только при единственном совпадении** (имена проектов
уникальны не везде — в ABP 39 повторов).

| | до | после |
|---|---|---|
| OpenTelemetry | 2 / 109 | **108 / 109** |
| остальные три | без изменений | без изменений |

Оставшаяся одна — `@(TrimmerRootAssembly->'%(Path)')`, преобразование item-группы,
а не путь. Правильно осталась неразрешённой.

### Две находки осознанно не исправлены

**`TargetFramework` через цепочку props-файлов: 70 модулей из 70 без TFM.** Все проекты
OpenTelemetry пишут `<TargetFrameworks>$(TargetFrameworksForLibraries)</TargetFrameworks>`,
а переменная определена в `build/Common.props`, который не импортируется ни из
`Directory.Build.props`, ни из `.csproj` — цепочка идёт через ещё два файла, и значение
содержит ещё одну подстановку. Достать это можно только полным вычислением свойств MSBuild.

План с самого начала объявляет это границей подхода, и граница подтвердилась. Вывод:
`target_frameworks` — метаданные, а не опора; ни одно правило на них не завязано.
Требование к T20: сообщать о модулях без TFM, чтобы пустое поле не приняли за свойство кода.

**Связанные исходники: 5 % типов вне модулей.** OpenTelemetry и semantic-kernel подключают
общий код через `<Compile Include="$(RepoRoot)\src\Shared\Guard.cs" Link="…" />`. Такой файл
лежит вне каталога любого проекта, и правило «ближайший `.csproj` вверх» не находит модуля:
39 файлов и 50 типов (5,0 %) в OpenTelemetry, 131 файл и 205 типов (4,8 %) в semantic-kernel.

Исправление ломает предположение «файл принадлежит одному модулю» — связанный файл
компилируется в несколько сборок сразу, — то есть меняет сигнатуру `build_symbol_index`
и порождает вопрос, в каком модуле показывать документ. Вместо тихой переделки сданных
задач заведена **T05b** с замерами и с проверкой, которую нужно сделать до реализации:
есть ли этот приём в целевом репозитории вообще.

### Известное ограничение подтвердилось в новых местах

Все 170 файлов с ошибками разбора на четырёх репозиториях — одна и та же причина,
найденная ещё на ABP: директива препроцессора внутри выражения. Стресс-тест добавил три
новых места: **модификатор объявления** (`public` / `#else` / `internal`), **базовый список**
и **шаблон в pattern matching**.

Доля пострадавших файлов зависит от мультиплатформенности: 0 % в eShopOnWeb (один TFM),
0,5 % в ABP, 2,2 % в semantic-kernel и **8,7 %** в OpenTelemetry, который таргетит
от `netstandard2.0` до `net10.0`.

Проверено, что причина именно в директивах: удаление из файла всех строк, начинающихся
с `#`, даёт разбор без единой ошибки и все объявления на месте.

### Что подтвердилось из прежних решений

- **`.slnx` — не экзотика.** Три репозитория из четырёх его содержат, semantic-kernel
  держит оба формата сразу. Правка после ABP окупилась дважды.
- **Полиязычный репозиторий не мешает.** 165 МБ semantic-kernel с 1244 файлами Python
  обходятся за 0,09 с; посторонние языки просто не попадают в выборку.
- **Slug-коллизии внутри модуля** — 2, 195, 13 и 165 по репозиториям. Хэш-суффикс
  в `doc_path` (T15) нужен не для экзотики, а для обычного случая.
- **`ambiguous`** — одна неоднозначность на 14 221 символ, и та настоящая.

---

## T14 — `classify.py` и `rules/dotnet.yaml` ✅

**Создано:** `docpipe/classify.py`, `rules/dotnet.yaml`, `tests/test_classify.py`.

**Проверка**

```
uv run pytest -q                    321 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        35 files already formatted
uv run mypy docpipe                 Success: no issues found in 15 source files
```

Все девять строк таблицы критериев приёмки совпали, включая главный тест приоритетов:
`RiskComputeService` даёт `ignite_service` при `matched_rules == ["ignite.service", "service"]`.

### Отклонения от плана

**1. `path_glob` работает через `discovery.matches_glob`, а не через `fnmatch`.** План
называл `fnmatch`, но у него известная дыра: `**` не означает «ноль или больше сегментов»,
и `**/obj/**` не поймал бы файл в `obj/` в корне репозитория. Это ровно та ловушка,
из-за которой `matches_glob` был написан на T04. Добавлен тест с файлом в `Handlers/`
и правилом `**/Handlers/**`.

**2. `load_ruleset` проверяет структуру целиком.** План этого не требовал. Опечатка
в имени предиката (`attribut` вместо `attribute`) дала бы правило, которое просто никогда
не срабатывает — набор молча теряет вид сущности, и заметить это можно только по счётчику
`unclassified`, то есть никак. Проверяются: известность предиката, тип значения,
компилируемость регулярных выражений, обязательные поля правила, уникальность `id`.
Четыре теста на падение.

**3. `is_excluded` вынесен в публичный API.** План предлагал расширить возврат `classify`
до `Classification | Literal["excluded"] | None` либо завести `classify_with_reason`.
Отдельный предикат проще и не усложняет сигнатуру `classify`, а T20 всё равно нужно
различать «исключён» и «не подошёл»: это разные счётчики.

Плюс косметика: правила в YAML переставлены по убыванию приоритета (в плане `repository`
шёл перед `provider` с меньшим приоритетом). На результат это не влияет — есть отдельный
тест `test_rule_order_in_file_does_not_matter`, который прогоняет один и тот же набор
в прямом и обратном порядке.

### Замер покрытия на четырёх репозиториях

| | символов | классифицировано | исключено | `unclassified` |
|---|---|---|---|---|
| eShopOnWeb | 253 | 18 (7 %) | 36 | 199 |
| ABP | 8966 | 828 (9 %) | 996 | 7142 |
| OpenTelemetry | 945 | 10 (1 %) | 535 | 400 |
| semantic-kernel | 4057 | 69 (2 %) | 1492 | 2496 |

Низкое покрытие — исходное состояние, а не дефект: словарь «контроллер / сервис /
провайдер / репозиторий» описывает прикладной код, а не SDK. OpenTelemetry
и semantic-kernel — библиотеки, и в них этих сущностей действительно почти нет.
Набор правил набирается по `docpipe scan --stats` на конкретном проекте.

Отдельно разобрано, **почему** символы исключаются, — это понадобится при настройке:

| | главная причина исключения |
|---|---|
| eShopOnWeb | `name_regex` (28 из 36) |
| ABP | `name_regex` (508 из 996) |
| OpenTelemetry | **`require_public`** (463 из 535) |
| semantic-kernel | **`require_public`** (1203 из 1492) |

То есть в библиотечных репозиториях главный рычаг — `require_public`: там много
`internal`-реализаций. В прикладных на первое место выходит отсев Dto/Request/Response.
Обе цифры внесены в план, чтобы настраивающий не гадал.

### Решения, которые стоит знать

**Порядок правил в файле не влияет на результат.** При равном приоритете побеждает
лексикографически меньший `id`. Иначе перестановка строк в YAML меняла бы манифест —
прямое нарушение детерминизма.

**`base_type_candidates` проверяет и `base_types`, и `base_types_raw`.** Первое даёт
резолвнутый FQN, второе — то, как написано в коде; правило может быть записано и так,
и так. Условие «точка после первого `<`» проверено на настоящей fluent-базе из фикстуры
`WildSolution`, а тест `test_qualified_name_does_not_give_first_segment` фиксирует
обратное: `Sample.Common.Web.BaseApiController` не должен давать кандидата `Sample`,
иначе правило `base_type: ["Sample"]` совпало бы со всем подряд.

---

## T15 — `tree.py`: сборка дерева ✅

**Создано:** `docpipe/tree.py`, `tests/test_tree.py`; в `tests/conftest.py` добавлен
помощник `build_tree` — полный проход по решению, который понадобится всем задачам
от T16 и дальше.

**Проверка**

```
uv run pytest -q                    357 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        37 files already formatted
uv run mypy docpipe                 Success: no issues found in 16 source files
```

Все семь критериев приёмки выполнены, включая два, ради которых правился план после ABP:
три `ICrudAppService` с арностями 1, 2, 3 дают **три** узла с разными id и разными
`doc_path`, и суффикс считается от id, а не от FQN (у этих трёх FQN одинаков).

### На реальных репозиториях

| | модулей | узлов | дублей `doc_path` | с хэш-суффиксом | рёбер |
|---|---|---|---|---|---|
| eShopOnWeb | 10 | 18 | 0 | 1 | 37 |
| ABP | 671 | 828 | 0 | 315 | 1012 |
| OpenTelemetry | 70 | 10 | 0 | 5 | 8 |
| semantic-kernel | 190 | 69 | 0 | 18 | 191 |

Ни одного повтора `doc_path` и ни одного повтора id. **315 узлов из 828 на ABP получили
хэш-суффикс** — разрешение коллизий работает не в углу, а на трети дерева.

### Два бага, найденных проверкой на реальном коде

Проверка была простая: прогнать регулярку «похоже ли это на имя типа» по всем
`target` рёбер-конструкторов. Она сразу дала 219 подозрительных значений.

**1. Инициализатор конструктора ломал разбор параметров.** Сигнатура конструктора
намеренно включает `: base(...)` (это решение T07), а первая версия искала закрывающую
скобку через `rfind(")")` — то есть скобку инициализатора. Из
`public C(IOptions o) : base(o)` получался «параметр» `IOptions o) : base(o`.

На ABP так ломался **каждый пятый конструктор**: 217 рёбер из 1016. Исправлено поиском
**парной** скобки счётчиком глубины. После правки доля `low`-confidence на ABP упала
с 456 до 259 — сломанные имена перестали быть «нерезолвимыми».

**2. Тривия внутри сигнатуры (правка T07).** Оставшиеся два случая оказались другой
природы:

```csharp
[Obsolete("use another")]
#pragma warning disable CS0809 // Obsolete member overrides non-obsolete member
public override PropagationContext Extract<T>(…)
```

`#pragma` между атрибутом и модификатором становится **потомком** объявления, а не
соседом, и сигнатура начиналась с него. Комментарий на той же строке после схлопывания
пробелов сливался в общую строку. Второй вид — комментарий внутри списка параметров:
там `//` съедал бы остаток сигнатуры, потому что после схлопывания границы строки уже нет.

Замер до правки: **252 испорченные сигнатуры** на четырёх репозиториях (0,37 % от 68 828).
Мало по доле, но каждая портит и `signature_hash`, то есть заставляет шаг 3
перегенерировать документ впустую.

Исправлено вырезанием байтовых диапазонов узлов `comment` и `preproc_*` — именно
диапазонов, а не поиском `//` в тексте: `//` встречается внутри строковых литералов
(`"otpauth://totp/…"`, реальный случай из eShopOnWeb). После правки — **ноль**
на всех четырёх.

### Отклонения от плана

**Регистрация типа на самого себя ребра не даёт.** План говорил «DI-регистрации, где
`impl_type` соответствует символу», не оговаривая случай `AddTransient<ValuationWorkflow>()`,
где сервис и реализация — один тип. Петля в графе зависимостей — артефакт формы записи,
а не факт о коде, и в документе она читалась бы как «зависит от самого себя».

Побочно вскрылся пробел: **время жизни (`scoped`/`singleton`/`transient`) до манифеста
не доходит** — `Dependency` его не хранит, а `DocNode` не хранит `DiRegistration`.
Для самозарегистрированного типа это единственное, что регистрация сообщала. Вопрос
к шаблонам шага 2, отмечен здесь, чтобы не потерялся.

**`target` DI-ребра резолвится в FQN** тем же способом, что и параметры конструктора.
План не уточнял, что класть в `target`; сервис, записанный простым именем, в манифесте
выглядел бы иначе, чем тот же тип в рёбрах конструктора.

### Решения, которые стоит знать

**Домены проверяются в лексикографическом порядке ключей.** Словарь пришёл из YAML,
и порядок записи в файле источником решения быть не может — перестановка строк меняла бы
манифест. То же правило, что и для приоритетов правил на T14.

**`build_tree` вынесен в `conftest.py`.** Начиная с T15 почти каждой проверке нужен
готовый манифест целиком, а не его половина; повторять сборку в каждом тестовом модуле
означало бы четыре расходящиеся копии пайплайна.

---

## T16 — `emit.py` и команда `scan` ✅

**Создано:** `docpipe/emit.py`, `tests/test_scan_e2e.py`, `tests/golden/doc-tree.json`;
в `cli.py` добавлена команда `scan`, в `RunMeta` — поле `parse_error_files`.

**Проверка**

```
uv run pytest -q                    373 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        39 files already formatted
uv run mypy docpipe                 Success: no issues found in 17 source files
```

Первый работающий сквозной прогон. На фикстуре статистика сошлась с таблицей, заранее
записанной в план для T20: 6 классифицировано, 3 `unclassified`, 1 исключён, 10 символов.

### Первый прогон на реальных репозиториях

| | модулей | узлов | время | манифест | RSS |
|---|---|---|---|---|---|
| eShopOnWeb | 10 | 18 | 0,4 с | 107 КБ | 50 МБ |
| ABP | 671 | 828 | 9,7 с | **5,1 МБ** | 212 МБ |
| OpenTelemetry | 70 | 10 | 1,8 с | 85 КБ | 80 МБ |
| semantic-kernel | 190 | 69 | 6,3 с | 552 КБ | 140 МБ |

Предупреждение о сломанных файлах сработало на трёх репозиториях из четырёх
(1, 7 и 9 файлов) — ровно те, что нашёл стресс-тест.

**Параллелизм и кэш на ABP:**

| | время |
|---|---|
| `--jobs 1` | 9,6 с |
| `--jobs 4` | 4,9 с |
| `--jobs 8` | 4,2 с |
| с тёплым кэшем | 2,8 с |

Все варианты дают **побайтово равные** манифесты — это проверено тестом, а не на глаз.

Отдача от `--jobs` упирается в 2,3×: разбор параллелится, а резолв, классификация
и сборка дерева — нет. С тёплым кэшем видно то же самое с другой стороны: 10,2 → 2,8 с
вместо ожидаемых 8× по чистому разбору. **Разбор перестал быть узким местом**; если
понадобится ускорять дальше, оптимизировать нужно последующие стадии, а не парсер.

### Отклонения от плана

**1. Критерий «в манифесте нет текущего года» невыполним.** План предлагал грубую проверку
на отсутствие даты, но `ruleset_version` сам выглядит как дата (`2026-07-26.1`) — и это
значение из того же плана. Проверка падала бы на нём, ничего не проверив.

Заменено на точное: значения `generated_at`, `host` и `duration_seconds` из сидкара
не должны встречаться в тексте манифеста. Плюс отдельный тест на побайтовое равенство
двух прогонов.

Попутно выяснилось, что сравнивать **сидкары** двух прогонов между собой бессмысленно:
на быстрой фикстуре оба укладываются в одну секунду и дают одинаковый `generated_at`.
Первая версия теста на этом упала.

**2. `RunMeta.parse_error_files` добавлен сейчас, а не на T20.** План требует, чтобы
`validate` падал по списку файлов с ошибками разбора и без объявлений, а `stats` —
это `dict[str, int]`, куда список путей не положить. Поле заведено сразу: сидкар пишется
здесь, и возвращаться к `emit.py` на T20 ради одного поля незачем.

### Решения, которые стоит знать

**Оркестрация живёт в `emit.py`, а не в `cli.py`.** План говорил «создать `emit.py`,
изменить `cli.py`», не уточняя границу. Шестьдесят строк пайплайна в обработчике команды
означали бы, что сквозной прогон нельзя вызвать из теста без `CliRunner`.

**`_parse_one` — функция уровня модуля.** Иначе `ProcessPoolExecutor` не сможет её
сериализовать. Результаты после сбора **обязательно** пересортировываются по пути:
при `--jobs > 1` порядок завершения задач случаен, а от порядка `results` зависит
слияние `partial`-объявлений на T10.

**Золотой файл содержит версии грамматики**, поэтому апгрейд `tree-sitter` его ломает.
Это желаемое поведение: изменение вывода парсера должно быть видно в диффе, а не
проскакивать незамеченным.

---

## T17 — Тесты детерминизма ✅

**Создано:** `tests/test_determinism.py` — девять тестов.

**Проверка**

```
uv run pytest -q                    382 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        40 files already formatted
uv run mypy docpipe                 Success: no issues found in 17 source files
```

Требование из `purpose.md` стало проверяемым свойством. Сравнение везде побайтовое:
манифест не содержит недетерминированных полей, поэтому не нужно ни одного «сравнить,
игнорируя такие-то поля» — ради этого метаданные прогона и вынесены в сидкар.

### Отклонения от плана

**1. Монкипатчить нужно `discovery.os.walk`, а не `Path.rglob`.** `rglob` в `discovery`
нет: обход идёт через `os.walk`, потому что только так можно отсекать каталоги правкой
`dirnames` на месте (решение T04). Разворачивать при этом нужно **оба** списка —
и `dirnames`, и `filenames`: порядок каталогов влияет на порядок файлов.

**2. При подмене версии грамматики манифесты нельзя сравнивать целиком.** Версии лежат
в самом манифесте (поле `parser`), поэтому оно законно изменится. Сравниваются `modules`
и `nodes`; именно они и не должны поменяться от того, что кэш очистился.

### Что добавлено сверх плана

**Перенос репозитория** (две проверки). Две копии фикстуры в разных каталогах дают
побайтово равные манифесты, а кэш, наполненный из одной копии, годится для другой.
Это прямая проверка правила «пути только репо-относительные»: абсолютный путь ломал бы
документацию при любом различии в расположении рабочих копий у разработчика и в CI —
причём незаметно, потому что у автора-то всё сходится.

**Удаление файла.** Инкрементальный прогон после удаления обязан совпасть с полным:
кэш не должен воскрешать удалённый файл. За это отвечает `prune`, и до этого теста
он не был покрыт на уровне сквозного прогона.

**Все списки в манифесте отсортированы** — отдельный тест обходит модули и узлы
и проверяет каждое поле-список. Явная сортировка здесь не стиль, а условие
воспроизводимости, и проверять её точечно в каждой задаче ненадёжно.

**Обратимость JSON.** Манифест — контракт с шагом 2; запись и чтение обязаны быть
обратимы, иначе расхождение всплывёт у потребителя, а не здесь.

---

## Раздел о настройке в README (после T17)

По запросу владельца README дополнен разделом «Как этим пользоваться» и пошаговой
инструкцией «Настройка под конкретный проект». Каждая команда оттуда прогнана руками.

### Два бага, найденных при написании инструкции

**1. Аргументы primary constructor попадали в базовые типы.** Рецепт «посмотреть частые
базовые типы среди неклассифицированных» на semantic-kernel выдал `(output)` — 266 раз
и `(fixture)` — 159. Причина:

```csharp
public class MyTests(ITestOutputHelper output) : BaseTest(output), IDisposable
```

Тип с primary constructor передаёт аргументы базовому классу прямо в списке баз, и
`(output)` лежит там отдельным узлом `argument_list`. `_base_types` брал все
`named_children` и записывал его как базовый тип. Мусор расползался дальше по замыканию
наследования и попадал в `signature_hash`.

На четырёх репозиториях таких записей было **425**. Исправлено фильтром по типу узла;
после правки — ноль на всех четырёх.

Показательно, что баг нашёлся не тестом и не прогоном, а попыткой **воспользоваться**
собственным результатом: в таблице «частые базовые типы» строка `(output)` бросается
в глаза сразу, а в манифесте на 5 МБ её никто бы не заметил.

**2. Ошибки пользователя печатались traceback'ом.** Опечатка в `--rules` или несуществующий
`--root` роняли команду с трассировкой на полстраницы — ровно в тот момент, когда человек
первый раз пробует запустить всё руками. Заменено на сообщение и код возврата 2,
с тремя тестами.

### `tools/unclassified.py` — временный инструмент

Написан потому, что инструкция «смотрите на `unclassified` и добавляйте правила»
невыполнима: в манифест попадают только **классифицированные** типы, а для настройки
нужно ровно обратное. Счётчик в сидкаре говорит «7142», но не говорит, какие правила писать.

Скрипт печатает пять срезов по непокрытым типам: модули, окончания имён, базовые типы
с учётом замыкания, атрибуты, namespace. На ABP он сразу показывает
`ITransientDependency` (997 типов — больше, чем покрывает весь набор по умолчанию),
на semantic-kernel — что половина непокрытого это тесты и примеры, то есть вопрос
к `enrolled`, а не к правилам.

Требование внесено в план T20: `--stats` должен печатать эти же срезы, а скрипт при
реализации — удалить, а не оставлять рядом.

### Замечание про кэш

При проверке инструкции обнаружилось, что прогон на фикстуре создаёт
`tests/fixtures/SampleSolution/.docpipe/cache/`. Это ожидаемое поведение (`cache_dir`
относителен корню сканирования), и `.gitignore` его ловит, но для чужого дерева
поведение неочевидно. Описано в README вместе с `--no-cache`.

---

## Исключение тестов из дерева (по запросу владельца)

Требование: типы, чьё имя оканчивается на `Test`, в дерево документации попадать
не должны.

### Замер показал, что одного правила по имени мало

Прямолинейное `exclude.name_regex: ["^.*Tests?$"]` отсеивает **ноль** узлов на всех
четырёх проверочных репозиториях. Причина простая, но неочевидная: класс
`PricingServiceTests` не оканчивается ни на `Service`, ни на `Controller`, поэтому
и так не проходит ни одно правило классификации и узлом не становится.

Реальный шум идёт из другого места — вспомогательные типы **внутри** тестовых проектов
названы как продуктовые и классифицируются на общих основаниях:

```
AuditTestController                            -> controller
AuthorizationTestPermissionDefinitionProvider  -> provider
```

На ABP таких **104 узла из 828**. Отсекает их только `path_glob` по каталогам тестов.

### Что сделано

В `exclude` добавлено и то, и другое:

```yaml
path_glob: ["**/test/**", "**/tests/**", "**/*Test/**", "**/*Tests/**"]
name_regex: ["^.*Tests?$"]
```

Правило по имени оставлено, хотя сегодня оно ничего не ловит: оно фиксирует намерение
и сработает, когда в набор добавят более широкое правило — например, по атрибуту `[Fact]`
или по `type_kind`.

Эффект: ABP 828 → 694 узла, OpenTelemetry 10 → 7, semantic-kernel 69 → 67,
eShopOnWeb без изменений (его тестовые типы и так не классифицировались).

### Что проверено отдельно

**Форма `Tests` важнее формы `Test`**: в ABP 777 против 54, в semantic-kernel 829 против 17.
Правило `name_suffix: ["Test"]` не поймало бы `PricingServiceTests` вовсе —
`"PricingServiceTests".endswith("Test")` ложно. Поэтому регулярное выражение с `s?`.

**Ложных срабатываний по пути нет.** `**/*Tests/**` не цепляет `Contest/` и `Testing/` —
проверено и тестом, и на глобах напрямую.

**Три типа остались осознанно**: `AbpTestBaseWithServiceProvider`,
`BloggingTestAppBrandingProvider`, `ProcessTestController`. Все три лежат в `src/`
(это отгружаемая библиотека для тестов, демо-приложение и хост интеграционных тестов),
и ни один не оканчивается на `Test`. Поведение соответствует правилу.

### Замечание, которое стоит держать в голове для АС CF

В предметных областях, где «тест» — бизнес-понятие, правило по имени опасно.
Для финансового моделирования это не гипотеза: `StressTest` и `BackTest` — настоящие
сущности предметной области, а не юнит-тесты. Предупреждение вписано и в сам
`rules/dotnet.yaml`, и в README, и в план.

---

## T18 — Скоуп-режим ✅

**Создано:** `docpipe/merge.py`, `tests/test_scoped.py`; в `emit.scan` добавлены
параметры `scope` и `previous`, в CLI — флаги `--scope` и `--from-manifest`.

**Проверка**

```
uv run pytest -q                    406 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        44 files already formatted
uv run mypy docpipe tools           Success: no issues found in 19 source files
```

### Главный инвариант выполняется, в том числе на ABP

На фикстуре `scoped.nodes == full.nodes` для скоупа по каждому из двух модулей
и для обоих сразу. `PricingController` при скоупе по `Sample.Pricing.Api` сохраняет
`kind == "controller"` и полное замыкание `["ControllerBase", "Sample.Common.Web.BaseApiController"]`,
хотя модуль `Sample.Common` в этом прогоне не парсился вовсе.

Замер на ABP (671 проект, 7869 файлов):

| | |
|---|---|
| полный прогон | 10,2 с |
| скоуп по одному модулю | **2,1 с** |
| разобрано файлов | 301 |
| восстановлено из кэша | 7568 |
| узлов совпало с полным прогоном | 694 из 694 |

### Баг, найденный собственным тестом: удалённый тип воскресал

Первая реализация брала из кэша всё, «чего не нашли обходом». Файл, удалённый **внутри**
скоупа, обходом уже не находится, но в кэше ещё лежит — и восстанавливался как «файл вне
скоупа». Тип оставался в дереве после удаления.

Исправлено отбором по **принадлежности пути скоупу**, а не по списку найденных файлов.
Разница видна только на удалении, поэтому тест на него обязателен и внесён в план.

### Открытие, которое сделал второй упавший тест

Ожидалось, что при холодном кэше скоуп-прогон «потеряет часть данных»: у контроллера
оборвётся замыкание наследования. Реальность жёстче — **узел исчезает целиком**.

Правило `controller.aspnet` смотрит на `ControllerBase`, до которого два шага через
`Sample.Common`. Без кэша цепочка обрывается на `BaseApiController`, ни одно правило
не совпадает, новый узел не порождается, а старый выброшен при слиянии как «в скоупе».
Тип просто пропадает из документации.

Это меняет требование к команде: предупреждение о ненайденных в кэше файлах — не
удобство, а необходимое условие. Добавлено в stderr и в статистику сидкара
(`restored_from_cache`, `missing_from_cache`).

### Отклонения от плана

**1. Отбор файлов вне скоупа — по пути, а не по «остальным».** См. выше; формулировка
плана «для всех остальных файлов из кэша» допускала оба прочтения, и неверное давало
воскрешение удалённых типов. Формулировка исправлена.

**2. `cache.prune` в скоуп-режиме не вызывается.** План этого не оговаривал. `prune`
удаляет из кэша всё, чего нет в списке найденных файлов, — то есть в скоуп-режиме снёс бы
весь кэш вне скоупа, ровно то, ради чего он здесь и нужен. Следующий скоуп-прогон
работал бы уже вслепую.

**3. Модули вне скоупа берутся из `previous`.** План не описывал, как файл вне скоупа
привязывается к проекту: обход туда не заходит, `.csproj` не найден. Список путей
к проектам собирается объединением найденного и `previous.modules[*].csproj`.

### Решение, которое стоит знать

**Узел «в скоупе», если в скоупе хотя бы один его источник.** Половинки `partial class`
могут лежать по разные стороны границы; правило «все источники в скоупе» оставило бы
такой тип неперестроенным, а правило «любой» перестраивает его целиком. Проверено
на `PricingService`, у которого два файла.

---

## T19 — `diff.py` и команда `diff` ✅

**Создано:** `docpipe/diff.py`, `tests/test_diff.py`; в `cli.py` добавлена команда `diff`.

**Проверка**

```
uv run pytest -q                    421 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        46 files already formatted
uv run mypy docpipe tools           Success: no issues found in 20 source files
```

Все четыре критерия приёмки выполнены. Отклонений от спецификации нет — редкий случай,
когда план подошёл как есть.

### Проверка на реальном сценарии

Дифф проверялся не на искусственном изменении кода, а на том, ради чего он нужен:
**правке правил**. Настроенный набор для ABP (правило по `IAbpModule` и по
`ITransientDependency`) против набора по умолчанию:

```
Всего изменений: 1790 (added: 1324, reclassified: 142, removed: 324)
```

828 узлов превратились в 1694. Ровно это и происходит при настройке под проект,
и по диффу видно, что именно изменилось: 1324 типа стали документироваться,
142 сменили вид, 324 ушли под новые правила исключения.

### Решения, которые стоит знать

**На узел приходится одно изменение, самое существенное.** Смена вида почти всегда
влечёт и смену `doc_path` — вид входит в путь. Сообщать «переехал» там, где тип сменил
природу, значит скрыть главное: шагу 3 нужно переписать документ, а не перенести файл.
Порядок проверок — `reclassified` → `moved` → `signature_changed`, есть тест
на узел, у которого изменилось всё сразу.

**Переименование — это `added` + `removed`, а не одно изменение.** Идентификатор узла
построен от FQN, и связать два имени между собой на этом уровне нечем. Если шагу 3
понадобится распознавать переименования, у него есть `signature_hash`: у переименованного
типа он тот же, что был у исчезнувшего.

**Код возврата всегда 0.** Команда предназначена для конвейера, где по её выводу решают,
что перегенерировать, а не для гейта в CI. Наличие изменений — не ошибка.

**Пустой результат печатается строкой «Изменений нет».** Пустой вывод неотличим от
«команда не отработала», и на глаз это не проверить.

---

## T20 — `stats`, `--dry-run`, `validate` ✅

**Создано:** `docpipe/stats.py`, `tests/test_stats.py`; в `cli.py` добавлены флаги
`--stats`, `--dry-run` и команды `validate`, `stats`; `emit.run` возвращает `ScanResult`.
Удалён временный `tools/unclassified.py` — его работу делает `scan --stats`.

**Проверка**

```
uv run pytest -q                    444 passed
uv run ruff check .                 All checks passed
uv run ruff format --check .        47 files already formatted
uv run mypy docpipe                 Success: no issues found in 20 source files
```

### Противоречие в плане: закреплённая таблица против собственного требования

План одновременно требовал ввести категорию `interface_covered` **и** проверять таблицу
с `unclassified 3` на фикстуре. Это несовместимо: два интерфейса фикстуры
(`IPricingProvider`, `IPricingService`) имеют документируемые реализации, то есть
как раз попадают в новую категорию, и `unclassified` становится 1.

Таблица в плане исправлена. Добавлено требование, которого не было: **сумма всех
счётчиков обязана равняться `total`** — иначе цифрам нельзя верить, а они здесь
рабочий инструмент, а не отчётность. На это есть отдельный тест.

Ширину первой колонки пришлось сделать по самой длинной метке: `interface_covered` —
17 символов, при фиксированных 16 метка съезжает в колонку чисел.

### Эффект `interface_covered` на реальных данных

| | было в `unclassified` | стало | ушло в `interface_covered` |
|---|---|---|---|
| ABP | 7142 | 5013 | 254 |
| semantic-kernel | 2496 | 1257 | 11 |

На ABP разница больше, чем кажется по одной колонке: `unclassified` упал на 2129,
потому что вместе с `interface_covered` заработало и исключение тестов из прошлой
задачи. Смысл категории от этого не меняется: интерфейс с задокументированной
реализацией — не пробел в правилах, и держать его в главном сигнале настройки нельзя.

### Отклонения от плана

**1. Счётчики живут в `docpipe/stats.py`, а не в `tree.py`.** План называл `tree.py`,
но там сборка узлов, и сто строк статистики её бы заслонили. Заодно `stats.py`
понадобился обеим командам — `scan --stats` и `stats MANIFEST`.

**2. `emit.run` возвращает `ScanResult`.** `--stats` нужен индекс символов, которого
в `(Manifest, RunMeta)` нет. Вместо расширения кортежа до тройки — что переписало бы
все вызовы в тестах — добавлен `run()`, возвращающий манифест, метаданные и статистику,
а `scan()` остался обёрткой над ним.

**3. Отдельная функция `classify_with_reason` не понадобилась.** План предлагал расширить
возврат `classify` до `Classification | Literal["excluded"] | None` либо завести вторую
функцию. Публичный `is_excluded`, сделанный ещё на T14, закрывает это без изменения
сигнатур.

### Решения, которые стоит знать

**`--stats` и `--dry-run` ничего не пишут.** Обе команды зовут в цикле настройки правил,
где перезаписывать манифест на каждой итерации незачем, а `--dry-run` вдобавок
сравнивает с тем самым файлом, который перезаписал бы.

**`stats MANIFEST` не может показать `unclassified`.** В манифест попадают только узлы,
то есть уже классифицированное. Посчитать непокрытое можно лишь имея индекс символов,
поэтому для настройки нужен именно `scan --stats`. В docstring команды это сказано прямо,
чтобы отсутствие колонки не приняли за баг.

**`validate` читает сидкар.** Список файлов со сломанным разбором лежит в
`<manifest>.run.json`: он про прогон, а не про результат, и в манифесте ему не место.
Если сидкара нет, проверка просто пропускает этот пункт.

**Предупреждение и ошибка разведены.** Тип, объявленный в нескольких файлах без `partial`
(на ABP таких три), — предупреждение: слияние в один документ там допустимо. Повтор
`id` или `doc_path` — ошибка: это молчаливая потеря документа.

---

## T21 — Финализация ✅

**Создано:** `docpipe.example.yaml`, `tests/test_smoke_cli.py`. README дополнялся
по ходу работы и на этой задаче только сверялся с фактами.

**Проверка** — полная, как в критериях приёмки:

```
uv run ruff check .                 All checks passed
uv run ruff format --check .        48 files already formatted
uv run mypy docpipe                 Success: no issues found in 20 source files
uv run pytest -q                    458 passed
```

Ручная проверка тоже пройдена: два прогона подряд дают побайтово одинаковый файл
(`diff` молчит), `--stats` печатает ожидаемую таблицу.

### Баг, найденный собственным примером конфигурации

`docpipe.example.yaml` не загрузился с первого раза:

```
ValidationError: domains — Input should be a valid dictionary, input_value=None
```

Причина: секция, у которой **все записи закомментированы**, разбирается YAML как `None`,
а модель ждёт словарь. При настройке закомментировать записи — самое обычное действие,
и падать на нём нельзя. Исправлено в `load_config`: ключи со значением `None`
отбрасываются, и поле получает значение по умолчанию.

Показательно, что нашлось это не тестом, а попыткой воспользоваться собственным
артефактом — третий раз за проект (первые два: `(output)` в базовых типах и traceback
на опечатке в `--rules`).

### Отклонение от плана

**Файл назван `docpipe.example.yaml`, а не `docpipe.yaml`.** Конфигурация не подхватывается
автоматически — путь всегда задаётся флагом `--config`. Файл с именем `docpipe.yaml`
в корне репозитория выглядел бы действующим, и настройки в нём молча не применялись бы:
худший вид ошибки, тот, который выглядит как успех. Суффикс `.example.` снимает
двусмысленность бесплатно.

Автоподхват не добавлялся: он в плане не предусмотрен и порождает вопрос «почему прогон
изменился, я же ничего не менял».

### Что проверяет smoke-тест

Не дублирует тесты отдельных команд, а закрывает то, что между ними: все шесть команд
живы одновременно, `--help` каждой отвечает, коды возврата означают обещанное
(0 — успех, 1 — манифест не прошёл проверку, 2 — ошибка вызова), сквозной сценарий
«прогон → validate → stats → скоуп → diff» отрабатывает целиком, и **пример
конфигурации из репозитория действительно работает** — проверяется сам файл,
а не его копия. Пример, который не запускается, хуже отсутствующего: он выглядит рабочим.

---

## Итог по шагу 1

23 задачи из 24. Отложена одна — **T05b** (связанные исходники через `<Compile Include>`):
на двух репозиториях из четырёх она стоит 5 % типов, но требует отказаться
от предположения «файл принадлежит одному модулю». Решение — за владельцем, и в
[`CASHFLOW.md`](../CASHFLOW.md) записана команда, которая покажет, актуальна ли она
для АС CF вообще.

Итоговые цифры: 20 модулей, 3714 строк кода, 458 тестов. Проверено на четырёх реальных
репозиториях — 11 850 файлов C#, 941 проект, ноль падений парсера.

Того, что в план не входило, в коде нет: ни создания `.md`-файлов, ни шаблонов,
ни генерации представлений, ни парсеров других языков, ни интеграции с LLM.

---

## Подбор конфигурации для semantic-kernel (после T21)

По запросу владельца — подобрать конфигурацию, дающую максимум распознанных сущностей.
Результат в `examples/semantic-kernel/docspipe/` (каталог в репозиторий не входит).

**67 узлов на наборе по умолчанию → 503 на настроенном**, 29 видов сущностей вместо 4.
Пять итераций по срезам `--stats`: enrollment → базовые типы → конвенции имён →
отсев носителей данных → контракты.

### Найден дефект в самом инструменте настройки

`--stats` считал по **всему** индексу, игнорируя `enrolled`. На semantic-kernel это
означало, что счётчик `unclassified` (1257) состоял из примеров и тестов **целиком** —
то есть главный сигнал настройки показывал ровно то, что настраивать не нужно.

Исправлено: добавлена категория `not_enrolled`, срезы «чего не хватает правилам»
её не включают. После правки на том же репозитории: 1597 `not_enrolled`,
708 действительно непокрытых. Требование внесено в план T20.

Дефект нашёлся не тестом, а первым же применением команды по назначению — четвёртый
раз за проект, когда баг вскрывается попыткой воспользоваться собственным артефактом.

### Находка, меняющая планирование шага 3

Покрытие XML-doc **не является свойством .NET-кода вообще**, как можно было заключить
из первых двух прогонов:

| | покрытие |
|---|---|
| ABP, все типы | 3,8 % |
| eShopOnWeb, все типы | 6,5 % |
| **semantic-kernel, все типы** | **52,5 %** |
| **semantic-kernel, узлы дерева** | **93,4 %** |

Разброс между проектами — четырнадцатикратный. Вывод «на комментарии опираться нельзя»
верен для ABP и неверен для semantic-kernel.

Вторая цифра интереснее первой: у типов, **попавших в дерево**, покрытие 93 % против
52 % по репозиторию. Это не совпадение — правила отбирают публичную поверхность API,
а её документируют лучше остального. То есть классификация попутно работает фильтром
качества входных данных для шага 3.

Обе цифры внесены в `CASHFLOW.md` с указанием мерить **оба** числа на АС CF:
по всем типам и по узлам готового манифеста.

### Что показала подобранная конфигурация о самом инструменте

- **Правила по базовому типу сильнее правил по имени** — и это должно быть отражено
  в приоритетах. `AzureOpenAIChatCompletionService` обязан быть `ai_service`
  (реализует `IAIService`), а не `service` по суффиксу; иначе весь SDK схлопывается
  в один вид.
- **Публичные интерфейсы для библиотеки — главный артефакт.** 57 интерфейсов, включая
  `IChatCompletionService` и `IAgent`, изначально не классифицировались вовсе. Правило
  `abstraction` с самым низким приоритетом добавляет их, не отбирая у более конкретных
  правил.
- **`interface_covered` исчезает**, если интерфейсы классифицируются напрямую. Категория
  осмысленна для прикладного кода (документируем реализацию) и избыточна для библиотеки
  (документируем и контракт, и реализацию).
- Конфигурация воспроизводима: два прогона подряд дают побайтово одинаковый манифест.

---

## T22 — Поставка в репозиторий АС CF (после T21)

По запросу владельца: инструмент должен лежать **внутри** боевого репозитория,
в `docs/ml/docspipe`, без среды разработки. Сделано `deploy/`: установщик, отдельный
манифест зависимостей с отдельным локом и каталог `cashflow-docspipe/` с настройкой
под проект.

Окружение поставки — **17 пакетов против 40** в разработке. Не `uv sync --no-dev`
поверх общего лока, а свой `pyproject.toml` без dev-группы и свой `uv.lock`: иначе
состав поставки не виден по файлам и проверяется только флагом при запуске.
Плата за это — второй манифест, который может разъехаться с первым; сверка вынесена
в `tests/test_deploy_bundle.py`.

### Каталог инструмента внутри сканируемого дерева: нужен третий вид исключений

Инструмент, положенный в `docs/ml/docspipe`, — это чужой код внутри репозитория,
который docpipe обязан не читать. Ни `enrolled`, ни `exclude` из `rules/dotnet.yaml`
для этого не годятся: первое управляет составом документации, второе — классификацией,
и оба оставляют файл прочитанным. Обход же зашивался константой `DEFAULT_EXCLUDE`
в `emit.py`, и настроить его было нечем.

Добавлено поле `exclude` в `DocpipeConfig` и `exclude_globs(config)` в `emit.py`.
Два решения, каждое против конкретной молчаливой ошибки:

- **шаблоны складываются со встроенными, а не замещают их.** При замещении строка
  `exclude: ["docs/**"]` вернула бы `obj/` и `bin/` в документацию — и никак бы себя
  не проявила, кроме раздутого манифеста;
- **тест фиксирует ловушку `"docs"` без `/**`.** Такой шаблон совпадает с самим
  каталогом, но не с файлами под ним. Команда отрабатывает, манифест строится,
  каталог как сканировался, так и сканируется.

Понятий стало три, и в README, `docpipe.example.yaml` и плане T04 они теперь
разведены таблицей: enrolled — «что документируем», `exclude` в правилах — «что
не документируем, но читаем», `exclude` в конфигурации — «куда не заходим»
(символ исчезает из индекса, наследование через него рвётся).

### Дефект, вскрывшийся при первой же установке

**Поле `out` в `docpipe.yaml` не читалось никогда.** У флага `--out` было значение
по умолчанию `artifacts/doc-tree.json`, поэтому `settings.out` не использовался ни
разу за весь проект, а `docpipe.example.yaml` описывал его как рабочее.

В разработке это незаметно: конфигурация лежит рядом с местом запуска, и оба пути
совпадают. На боевом репозитории конфигурация лежит в `docs/ml/docspipe/cashflow-docspipe/`,
запуск идёт из корня, и манифест уехал бы в `artifacts/` рядом с корнем — то есть
в рабочее дерево продукта.

Исправлено: тип параметра `Path | None`, значение по умолчанию убрано,
`destination = out or Path(settings.out)`. Требование внесено в план T16.

Пятый раз за проект дефект найден не тестом, а попыткой воспользоваться собственным
артефактом по назначению. Закономерность устойчивая: тесты проверяют то, что автор
предполагал, а применение — то, что он предположить не догадался.

### Проверено на симуляции боевой раскладки

Копия `SampleSolution` плюс посторонний `.csproj` с `.cs` под `docs/legacy/`.
Установка, `run.sh stats`, `run.sh scan`, `run.sh validate`, повторный прогон,
повторная установка поверх правленого конфига, ручной вызов из корня репозитория
и запасной путь `python -m docpipe` без установки проекта:

- 10 символов и 6 узлов — ровно как на фикстуре без постороннего каталога;
- посторонний тип в манифесте отсутствует, модулей 2, а не 3;
- два прогона подряд дали побайтово одинаковый файл;
- правка в `docpipe.yaml` пережила переустановку, новая версия легла как `.new`.

### Разбор диагностики по АС CF

Ответы на восемь команд §1 `CASHFLOW.md` получены и разобраны в новом §1а. Четыре
вывода, меняющие настройку:

- **17 988 файлов при 491 проекте** — крупнее всего, на чём инструмент проверялся
  (ABP: 7869 файлов при 671 проекте). Проекты вдвое-втрое крупнее, поэтому цена
  ошибки в `enrolled` выше: лишний глоб втягивает сотню типов, а не десяток;
- **правило `^.*Tests?$` решено оставить**, вопреки заготовке в §2 плана. Выражение
  заякорено с обеих сторон, поэтому `StressTestRun` под него не подпадает — «Test»
  в середине имени безопасен. Опасность оказалась в другом месте: шаблоны путей
  `**/*Test/**` и `**/*Tests/**` отсекают любой каталог с таким окончанием, включая
  предметный `StressTest/`. Сужены до `**/*.Test(s)/**` — точка перед Test оставляет
  только тестовые проекты по конвенции .NET;
- **маркерных интерфейсов DI нет** (одно вхождение `IDependency` на репозиторий) —
  правило `cf.injected-service` оставлено выключенным;
- **две команды из §1 отвечают не на тот вопрос, который кажется.** `grep -rl "^#if"`
  не видит директив с отступом, а искать надо не их, а `#if` внутри выражения —
  единственный ответ даёт `parse_error_files` после прогона. В команде про типы
  `*Test` стоял `head -30`, то есть ответ «около тридцати» мог быть усечением.
  Обе команды в плане поправлены.

---

## T22 — доработка поставки под закрытый контур

Первая попытка установки в АС CF провалилась: `uv sync` упирался в
`invalid peer certificate` и тянул пакеты не оттуда. Разбор показал три
независимые причины — исправлены все.

### Лок из поставки за пределами PyPI бесполезен

`uv.lock` ссылается на файлы `files.pythonhosted.org` **поимённо и с хэшами**:
162 URL и 16 записей `registry = "https://pypi.org/simple"`. Настройка индекса
готовый лок не перенаправляет — с `--frozen` uv идёт ровно по записанным адресам.
То есть флаг `--index-url` в такой конфигурации не помогает вообще, и это
неочевидно: выглядит как «зеркало настроено, но не используется».

Лечится только пересборкой: `uv lock` против настроенного индекса. Проверено, что
uv в этом случае **пере**решает зависимости, а не падает на несовпадении источника,
поэтому удалять лок перед пересборкой не нужно — записанные версии останутся, если
зеркало их отдаёт.

Установщик получил `--index URL`, который и записывает адрес, и влечёт пересборку.

### Настройки uv — файлом рядом с пакетом, а не переменными окружения

Проверено экспериментально: `uv.toml` из каталога, указанного в `--project`,
подхватывается и `lock`, и `sync`, и `run`, из любого текущего каталога. Это лучше
переменных окружения — настройка переживает смену терминала и не требует помнить
её при каждом вызове.

В `uv.toml` поставки три строки, каждая против конкретного отказа:

- `[[index]] default = true` — **вместо** PyPI, а не в дополнение. С `default = false`
  uv всё равно ходил бы на pypi.org за тем, чего нет в зеркале;
- `native-tls = true` — системное хранилище сертификатов. Причина
  `invalid peer certificate` именно в этом: прокси подписывает соединения
  сертификатом внутреннего УЦ, которого нет в наборе, вшитом в uv;
- `python-downloads = "never"` — иначе uv, не найдя интерпретатор, уходит за
  сборкой Python на github.com и упирается в тот же прокси, но сообщение будет уже
  про загрузку Python. С этой строкой ошибка честная.

Две ловушки, обе проверены на месте:

- **порядок ключей в TOML.** Ключи верхнего уровня обязаны стоять до первой таблицы
  `[[index]]`, иначе TOML отнесёт их к ней. Это не синтаксическая ошибка — настройка
  просто перестаёт действовать. Закреплено тестом;
- **`native-tls` объявлен устаревшим** в пользу `system-certs`, и свежий uv на него
  ругается. Переименовать нельзя: неизвестный ключ uv.toml не игнорирует, а падает
  с ошибкой разбора, то есть `system-certs` сломает установку на старом uv, а
  `native-tls` перестанет работать, только когда его выпилят совсем. Оставлено
  старое имя с объяснением в комментарии.

### Сборка пакета оказалась необязательной

`uv sync` собирает `docpipe` через `hatchling` — ещё один пакет, который зеркало
может не отдавать. Режим `--no-install-project` ставит только пять зависимостей:
15 пакетов вместо 17, ни сборочного бэкенда, ни шага сборки.

Расплата — нет консольной команды `docpipe`. Поэтому `run.sh` переведён на
`python -m docpipe` с `PYTHONPATH` на каталог поставки: это работает в **обоих**
режимах, и выбор режима перестал влиять на то, как инструмент вызывают. Проверено
сквозным прогоном в обеих установках — манифест одинаковый.

### Запуск не должен ходить в сеть

`run.sh` переведён на `uv run --no-sync`. Без этого каждый запуск сверялся бы
с индексом и падал бы там, где индекс недоступен, — на команде, которая
с зависимостями ничего не делает. Окружение поднимает установщик, и это его работа.

### Дефект, который был бы обнаружен только при обновлении

Установщик копировал `deploy/uv.lock` поверх целевого безусловно. На машине, где
лок уже пересобран против внутреннего зеркала, обновление инструмента вернуло бы
ссылки на pypi.org — и рабочая установка сломалась бы **после** успешного
обновления, то есть в момент, никак с обновлением не связанный по виду.

Теперь установщик отличает свой лок по строке `registry = "https://pypi.org/simple"`:
чужой сохраняется, а вместо копирования запускается пересборка против того же
зеркала. Обе строки — в локе и в скрипте — закреплены тестом, иначе смена формата
записи в uv.lock тихо вернёт прежнее поведение.

### Диагностика вместо чужого стектрейса

При неудаче установщик печатает три причины в порядке частоты, каждую с готовой
командой и подставленным путём репозитория, и сообщает, что скопированные файлы
на месте. Ошибка uv остаётся выше — она нужна, — но человеку больше не приходится
по ней догадываться, что настраивать.

---

## T22 — сборка окружения внутри контура и переход на `$WORK`

Продолжение разбора установки на боевой машине. Выяснилось два обстоятельства,
каждое из которых меняет процедуру.

### Зеркало может отдавать не всё

Настройки индекса недостаточно: часть пакетов внутреннее зеркало не проксирует
вовсе. Значит, набор зависимостей нужно **подбирать** внутри контура, а не
переносить готовым.

Написан `deploy/OFFLINE.md` — процедура из семи шагов: клонировать, разложить
поставку без сети (`--skip-sync`), настроить `uv.toml`, итеративно подобрать
`uv lock`, проверить, зафиксировать результат обратно в `deploy/` клона.

Три вещи, которые в такой процедуре неочевидны и потому вынесены явно:

- **подбор идёт в установленной поставке, а не в `deploy/`.** `uv lock` собирает
  метаданные пакета, а `deploy/pyproject.toml` объявляет `packages = ["docpipe"]` —
  рядом с ним пакета нет, и команда падает раньше, чем доходит до зависимостей.
  В `$BUNDLE` лежит и то и другое, и это ровно тот каталог, где инструмент потом
  работает: подбираем там, где проверяем;
- **свобода по пакетам разная.** `tree-sitter` ниже 0.25 не заработает — там
  сменился API запросов, это не вкусовщина. Версия `tree-sitter-c-sharp` попадает
  в манифест и в ключ кэша, то есть её смена законна, но обнуляет кэш и может
  изменить вывод. Для остальных ограничения почти безразличны. Сведено в таблицу,
  иначе подбор превращается в угадывание, какую строку можно трогать;
- **проверка зеркала — до подбора и отдельным пустым проектом.** Иначе
  `invalid peer certificate` неотличим от «пакета нет в зеркале», и человек
  правит `pyproject.toml` там, где надо править `SSL_CERT_FILE`.

Для случая, когда пакета нет вовсе: `uv export` → `pip download` на машине
с доступом → `find-links` плюс `offline = true` в `uv.toml`. Обе настройки
проверены на этом uv; `offline = true` заодно работает проверкой полноты набора —
недостача видна сразу и по имени пакета, а не по таймауту.

Установщик научился брать `deploy/uv.toml`, если он есть: после одного подбора
`--index` больше не нужен ни на одной машине периметра. Сам файл добавлен
в `.gitignore` — адрес внутреннего зеркала за периметр не нужен, а `git pull`
его не тронет.

**Сверка манифестов ослаблена до имён пакетов.** Тест сравнивал `dependencies`
целиком, но на закрытом контуре ограничения версий расходятся законно — их
подстраивают под то, что отдаёт зеркало, и возвращают в `deploy/`. Сравнение
имён по-прежнему ловит то, ради чего тест писался: забытую при добавлении
зависимость.

### Обновление не должно требовать сети

Установщик пересобирал лок при каждом обновлении поверх установки с внутренним
зеркалом. Это лишнее: если состав зависимостей не менялся, пересобирать нечего,
а требование связи с зеркалом на ровном месте — это отказ там, где можно было
обойтись. Теперь `pyproject.toml` поставки сравнивается с установленным, и
пересборка идёт только при расхождении.

### Пути через `$WORK`

В целевой системе задана переменная `$WORK` = `/home/work/<логин>`. Все пути
в `CASHFLOW.md`, `deploy/README.md`, `install.sh` и `docpipe.yaml` переведены
на неё: `/home/work/$USER` неверен там, где логин и имя каталога различаются,
и молча уводит установку не туда. На сами скрипты это не влияет — `install.sh`
берёт репозиторий аргументом, `run.sh` вычисляет пути от своего расположения.

---

## B01 — декларативный читатель реестров ✅

Первая задача бизнес-слоя. Разведка репозитория АС CF (см.
`docs/findings-cashflow-registries.md`) показала, что точки входа объявлены не в
коде: джобы, workflow и обработчики событий лежат строками в XML и исполняются
универсальными раннерами, получающими имя типа из данных. Читатель этих файлов —
вход всего бизнес-слоя, и он не зависит ни от шага 2, ни от .NET вообще.

Создано: `docpipe/registry/{__init__,model,config,reader}.py`,
`tests/test_registry_reader.py` (36 тестов), `tests/fixtures/registries/**`.

### Что решено по ходу

**Формат `json` на верхнем уровне не реализован.** План перечислял `xml`, `json`,
`inline`, но способ найти записи в JSON-файле нигде не задан: для XML это
`item_xpath`, для JSON аналога не описано. Придумывать его — значит завести формат,
которым никто не пользуется. JSON читается только как цель `follow`, где структура
известна (`Steps` у workflow-core). План поправлен.

**Номера строк у записей не хранятся.** ElementTree позиций не отдаёт, а способы их
достать (подмена `TreeBuilder`, работа с expat напрямую) хрупки и завязаны на версию.
Записи ссылаются на файл; этого хватает для отчётов, а B03 при необходимости найдёт
строку поиском по якорю. План B03 поправлен: `explain` печатает файл, не строку.

**Записи верхнего уровня сортируются, вложенные — нет.** Разница принципиальная и
её легко стереть «для единообразия». Записи верхнего уровня приходят из нескольких
файлов, и их порядок — следствие обхода ФС. Порядок шагов workflow — **данные**:
это цепочка `NextStepId`, и сортировка по `ref` её порвёт. Правило записано как
комментарий в `read_registry` и закреплено тестом
`test_children_keep_declaration_order`, который проверяет, что порядок шагов
фикстуры **не** совпадает с алфавитным.

### Ловушки, подтверждённые на фикстурах

- **`.//add` ловит интерцепторы.** У `<interceptors><add type="…"/></interceptors>`
  нет `@name`, поэтому обход по `.//add` дал бы либо падение, либо фантомный сервис.
  Только прямые дети.
- **`.//Item` ловит справочники.** В `Items_Basic.xml` рядом с `JOBS` лежат
  справочники БД; якорь на `List[@InnerName='JOBS']` обязателен.
- **ElementTree не выбирает атрибуты.** `find("./File[…]/@Path")` возвращает `None`.
  Суффикс `/@Attr` читатель отрезает сам. Без этого описание реестра выглядит
  рабочим и молча отдаёт пустые значения — худший вид отказа.
- **База для `follow` задаётся явно.** Путь в реестре отсчитывается от корня
  развёртывания, а не от каталога файла реестра. Тест
  `test_follow_base_is_not_the_registry_directory` проверяет именно удвоенный путь
  `deployment/Data/Items/Data/Items/Workflows/…`, который получается при наивной
  склейке и выглядит настоящим.
- **BOM.** Фикстура `Items-Workflows.xml` сохранена с BOM намеренно, и это закреплено
  отдельным тестом `test_fixture_still_has_bom`: «чистка» фикстуры иначе оставит
  проверку чтения байтами бессмысленной.

### Что добавилось сверх плана

**Отчёт о пустом реестре.** Реестр, не давший ни одной записи, внешне неотличим от
«таких точек входа в системе нет», и это самая дорогая из возможных ошибок настройки.
Сообщение выдаётся, только если файлы прочитаны, но записей нет **во всех** —
ноль записей в отдельном файле нормально: глоб `Items*.xml` для реестра джобов
законно захватывает `Items-Workflows.xml`, где списка `JOBS` нет. К сообщению
добавляется подсказка про пространство имён: при объявленном `xmlns` поиск по голому
имени тега не находит ничего и об этом не сообщает.

### Проверка

```bash
uv run pytest tests/test_registry_reader.py -q     # 36 passed
uv run ruff check . && uv run ruff format --check . && uv run mypy docpipe && uv run pytest -q
                                                   # 534 passed
```

---

## B02 — четыре реестра АС CF ✅

Реестры описаны данными: `registries.example.yaml` в корне, зеркало по фикстурам —
`tests/fixtures/registries/registries.yaml`. Кода добавилось мало: `parse.py` на два
разбора и одно выражение в читателе.

Создано: `registries.example.yaml`, `docpipe/registry/parse.py`,
`tests/fixtures/registries/registries.yaml`, `tests/test_registry_cashflow.py`
(26 тестов).

### Пример и фикстура не должны разъехаться

Описание реестров поставляется как пример с боевыми путями, а проверяется на фикстурах
с другими путями. Это готовое место для расхождения: поправят одно, забудут другое,
и обнаружит это настройщик на боевом репозитории — там, где отладка дороже всего.

Поэтому `test_example_matches_fixture` сравнивает два описания целиком, **исключая
ровно три поля**: `path`, `paths` и `follow.base`. Всё остальное — `item_xpath`, состав
полей, вложенные записи — обязано совпадать.

### `#tag`: вид поля лежит в имени элемента

Обнаружено при реализации. Вид поля списка (`FieldText`, `FieldLookup`, `FieldDateTime`)
записан **тегом**, а не атрибутом, и при этом входит в состав бизнес-сущности —
он нужен в `business_hash` (B05). Читатель такого не умел.

Добавлено четвёртое выражение поля — `#tag`. Три строки в `_extract_xml`, план B01
поправлен.

Заодно выяснилось, что формулировка плана «читать по префиксу `Field*`» **невыразима**:
подмножество XPath в ElementTree допускает `*` только как компонент целиком, `Field*`
в позиции тега не поддерживается. Получилось `./Fields/*` — все дети `<Fields>`. Это
не слабее белого списка, а строже: перечень видов заведомо неполон, и белый список
молча потерял бы поле нового вида. Вид при этом не теряется — приходит из `#tag`.
План поправлен.

### `parse_schedule`: экранирование делает не тот, на кого думаешь

`JOBSCHEDULE` записан как `&lt;JobSchedule Interval=&quot;60&quot; /&gt;`, и «разэкранировать»
звучит как отдельная работа. На деле разэкранирование выполняет **первый** разбор XML:
в поле приходит уже `<JobSchedule …/>`. Остаётся второй `fromstring`, и именно его
легко не заметить, приняв значение за обычную строку.

`Interval` — в секундах (подтверждено на АС CF). Отсюда вывод, который записан в docstring
и понадобится B08: `Interval=60` у отчётного джоба — это опрашивающий цикл, а не
«отчёты формируются раз в минуту». Различать опрос и расписание — дело сборщика
документа; разбор отдаёт число.

Мусор не роняет инвентаризацию: пустая строка, число, обрезанный тег дают `None`.

### `split_type_name`: запятая вне скобок

`StepType` и `DataType` записаны как `"FQN, Сборка"`. Наивный `split(",")[0]` на закрытом
дженерике `MapStep`2[[System.String, mscorlib],[System.Int32, mscorlib]], Sbt.Sample`
даёт `MapStep`2[[System.String` — обрубок, который не найдётся ни в одном индексе
символов и будет выглядеть как отсутствующий тип. Резать надо по первой запятой **вне
квадратных скобок**; фикстура `Sample.v2.json` такой шаг содержит намеренно, и есть
отдельный тест, показывающий, что наивная резка даёт другой результат.

Из остатка берётся только первый сегмент: полное отображаемое имя сборки содержит
`Version=`, `Culture=`, `PublicKeyToken=`, а сопоставляется всё это с именем проекта.

### Что добавилось в описание сверх плана

- **`override`** у джобов и workflow. `Override="false"` означает, что при развёртывании
  запись не перезаписывает существующую строку в БД, то есть расписание и признак
  активности для неё — «объявлено при установке», а не факт. Без этого поля документ
  уверенно сообщал бы неверную периодичность;
- **`error_behavior`** у workflow: `DefaultErrorBehavior` описывает, что происходит при
  сбое шага, и это бизнес-значимо.

### Проверка

```bash
uv run pytest tests/test_registry_cashflow.py -q   # 26 passed
uv run ruff check . && uv run ruff format --check . && uv run mypy docpipe && uv run pytest -q
                                                   # 560 passed
```

---

## B03 — `docpipe anchors`: инвентаризация точек входа ✅

Первый артефакт бизнес-слоя, полезный сам по себе. Отвечает на вопрос «сколько
в системе точек входа и что их реализует» — таблицы, которой в АС CF, по-видимому,
не существует, а без неё объём работы по каталогу процессов остаётся догадкой.

Создано: `docpipe/registry/anchors.py`, `tests/test_anchors_cli.py` (24 теста).
Изменено: `docpipe/cli.py` (под-typer `anchors` с `list` и `explain`),
`docpipe/registry/parse.py` (`strip_generic_arity`).

### Резолв по соглашению об именах, а не по видам реестра

Соблазн был написать `if kind == "job": …`. Вместо этого поле, содержащее имя типа,
называется одним из четырёх имён — `impl_fqn`, `contract_fqn`, `impl_type`, `data_type`, —
и резолв идёт по имени поля. Новый реестр достаточно описать, назвав поле так же.

Порядок один для всех: прямое совпадение по `symbol.fqn`, затем реализации интерфейса.
Отдельная ветка «для джобов идти через интерфейс» не понадобилась — она получается сама,
а `via` в результате говорит, что именно сработало. Индекс реализаций строится перебором
`related` у каждого узла: обратного ребра `implemented_by` в манифесте нет,
`tree._relations` его намеренно не создаёт (интерфейс мог не стать узлом).

### Обработчик события — вход, список — нет

Структурное решение, которое стоило продумать. Записи реестра структуры — это списки,
а точками входа являются объявленные на них обработчики. Поэтому дети, чей вид входит
в `ENTRY_KINDS`, поднимаются на верхний уровень отдельными якорями со ссылкой на
родителя в `scope` (`UserTasks/ItemAdded`), а поля списка и шаги workflow остаются
вложенными: они описывают устройство, а не вход.

Правило сформулировано через множество видов, а не через `if kind == "list"`. Побочная
выгода: на боевом `Cashflow.Structure.xml` поля списков (их тысячи) не заливают вывод.

### «Не найден среди узлов» — это НЕ «мёртвая запись»

План B03 говорил «мёртвая запись реестра выводится в отчёт». По манифесту такой диагноз
поставить **нельзя**: узлами становятся только enrolled и классифицированные типы,
поэтому неразрешённая ссылка одинаково выглядит и когда типа нет, и когда он просто
не документируется. Различает их только индекс символов, а он строится при `scan`
и никуда не сохраняется.

Формулировка исправлена везде: B03 сообщает «не найден среди узлов документации» и
печатает почему; диагноз «мёртвая запись» ставит B06, где индекс доступен. План поправлен.

### Дженерики

`StepType` закрытого дженерика приходит как `MapStep`2[[System.String, mscorlib],…], Asm`,
а `symbol.fqn` в манифесте хранится без арности (она вынесена в `type_parameters`
и приписывается только к `id` узла). Без нормализации ни один дженерик-шаг не сопоставился
бы. Добавлен `strip_generic_arity` — резать по первой обратной кавычке достаточно:
в сегментах namespace она встречаться не может.

### Что сказано прямо, а не обойдено

**Команда известна не у всех якорей.** `@team` объявлен только в `services.config`;
у джобов, workflow и обработчиков событий владелец должен браться из `ownership.yaml`
(M06), которого ещё нет. `--team` их поэтому не отбирает. Это записано в план, чтобы
пустой результат не приняли за отсутствие.

### Флаги состояния убраны из описания реестров (решение владельца)

`Override`, `IsActive`, `JOBDISABLED` больше не читаются. Три довода, каждого хватило бы:
инструмент видит репозиторий и боевой БД не знает никогда, поэтому «джоб выключен»
сказать не может; прочитанный флаг рано или поздно кто-нибудь прочтёт как факт; политика
поставки меняется независимо от кода, и привязка к ней устаревала бы от правки процедуры
развёртывания.

Оговорка «объявлено в репозитории, а не наблюдается в бою» верна для любого значения
из реестра безусловно — она и заменяет флаги. Закреплено тестом
`test_state_flags_are_deliberately_not_read`; атрибуты в фикстурах при этом оставлены,
они описывают реальность файлов.

### Проверка

```bash
uv run pytest tests/test_anchors_cli.py -q          # 24 passed
uv run ruff check . && uv run ruff format --check . && uv run mypy docpipe && uv run pytest -q
                                                    # 587 passed
```

---

## M03 — обратимый разбор документа ✅

Сделан вне очереди шага 2: B04 разбирает бизнес-документы этим же кодом, и
дублировать разбор нельзя — форматы зон разъехались бы, а обнаружилось бы это
на первом документе, который правили руками.

Создано: `docpipe/materialize/{__init__,model,document}.py`,
`tests/test_materialize_document.py` (45 тестов).

### Инвариант

`assemble(parse_document(t)) == t` байт в байт — проверено на 14 документах:
с front matter и без, CRLF, без завершающего перевода строки и с тремя, с BOM,
с юникодом, с чужими ключами, с маркерами в непривычных пробелах.

Держится он на двух решениях, каждое из которых легко нарушить «упрощением»:
сегмент хранит **и** тело, **и** полный исходный текст с маркерами (иначе сборка
нормализует маркер к каноническому виду); строки бьются своим выражением, а не
`splitlines(keepends=True)` — последний режет ещё и по `\v`, `\f`, ` `,
и строка документа могла бы разъехаться там, где переводом строки не пахнет.

### Маркер внутри блока кода: ограничение подтвердилось, тест был неверен

Первая версия теста утверждала, что маркер внутри ```-блока не считается маркером.
Это неверно: разбор построчный, о блоках кода не знает, и такой маркер **делает
документ структурно испорченным**. План это и описывал — обходной путь один, отступ
в четыре пробела. Тест переписан на честное поведение: голый маркер в блоке кода даёт
`DocumentError`, маркер с отступом — обычный текст. План дополнен явной формулировкой.

### Валидация состояния — не здесь

План требовал отказа, если `docpipe_state` «не проходит валидацию». Типизированно
это здесь невозможно: у шага 2 в принятом состоянии два хэша и список членов,
у бизнес-слоя — `business_hash`, а разбор общий. Проверяется общее: `docpipe`
и `docpipe_state`, если они есть, — словари. Схему валидирует модель своего слоя.
План поправлен.

---

## B04 — каталог бизнес-документов ✅

Создано: `docpipe/business/{__init__,model,catalog}.py`,
`tests/test_business_catalog.py` (32 теста), `tests/fixtures/business/**`.

### Поправка владельца: сущности пишутся, а не генерируются

Я предположил, что 289 списков `Cashflow.Structure.xml` — готовый каталог
бизнес-сущностей. Владелец проверил на боевых данных: это таблицы и их структура,
полезен там **только** `DisplayName`, и сгенерировать 289 малозначимых сущностей —
выстрел в ногу. Их никто не прочтёт, а отчёт о непокрытом станет вечно красным.

Решение записано в план: документ `be.*` заводится поштучно и только когда сущность
нужна процессу; реестр структуры даёт **справку**, а не заготовку. Отчёт о непокрытом
считается только по `ENTRY_KINDS`, и `list` в него не входит никогда.

Ценность реестра структуры при этом подтвердилась в другом: `list_event` — отличная
точка входа, на боевых данных их 176.

### Каталог — это множество файлов

Отдельного манифеста бизнес-слоя нет. Аналитик работает в IDE, и один файл на процесс
с `git add` дешевле промежуточного артефакта, который пришлось бы пересобирать перед
каждой правкой.

Документом каталога считается `.md`, у которого `docpipe.schema` начинается
с `business/`. **Именно по схеме, а не по наличию ключа `docpipe`:** формат зон общий
с шагом 2, и отбор по ключу перепутал бы слои в обе стороны. В фикстурах на это стоит
отдельный документ с `schema: materialize/1`, лежащий внутри дерева каталога.

### Что решено по ходу

**`doc_path_for(doc_id, business_root)` — без параметра `kind`.** План передавал вид
документа отдельно, но он однозначно задан префиксом идентификатора. Два источника
истины про вид разошлись бы, и документ уехал бы не в тот каталог. Соответствие
префикса и объявленного `kind` проверяется отдельной проверкой. План поправлен.

**`scope` вместо `list:` и `workflow:`.** В примере плана область якоря называлась
по виду родителя. У `ResolvedAnchor` из B03 это `scope`; одна вещь — одно имя, иначе
сверка каталога с инвентаризацией превращается в перевод. План поправлен.

**Не всякая ошибка выбрасывает документ.** Документ с нерезолвящейся `capability`
структурно исправен — не разрешается только ссылка, и выбросить его значило бы
спрятать целый процесс из всех отчётов из-за опечатки в одном поле. Выбрасываются
только документы, чью **идентичность** установить нельзя: неверный формат
идентификатора, расхождение с путём, несоответствие вида префиксу.

**Ошибки собираются, а не бросаются на первой.** Аналитик правит документы пачкой,
и увидеть все проблемы сразу дешевле, чем по одной за прогон.

### Проверка

```bash
uv run pytest tests/test_materialize_document.py tests/test_business_catalog.py -q  # 77 passed
uv run ruff check . && uv run ruff format --check . && uv run mypy docpipe && uv run pytest -q
                                                                                    # 664 passed
```

---

## M02 — `impl_hash` в шаге 1 ✅

Возврат на ветку M после B01–B04. Порядок внутри яруса выбран не случайно: M02
меняет `SCHEMA_VERSION` и пересчитывает золотой манифест, поэтому сделанный после
M05/M08 он заставил бы перебазировать эталонное дерево документов второй раз —
а оно сравнивается байт в байт.

Изменено: `docpipe/model.py`, `docpipe/dotnet/parser.py`, `docpipe/dotnet/resolve.py`,
`docpipe/tree.py`, `docpipe/cache.py`, `tests/golden/doc-tree.json`,
`schema/doc-tree.schema.json`, `docs/manifest.md`, `README.md`.
Добавлено 11 тестов в `test_resolve_fqn.py`, `test_cache.py`, `test_scan_e2e.py`.

`SCHEMA_VERSION` 1.0 → 1.1, `CACHE_VERSION` 1 → 2.

### Граница нормализации оказалась уже, чем звучал критерий

Критерий приёмки говорил: «переформатирование файла (лишние пробелы, переносы строк)
не меняет `impl_hash`». Первая версия теста проверяла это на
`Compute( int x )` против `Compute(int x)` — и упала.

Причина в самой спецификации: `re.sub(r"\s+", " ", …)` схлопывает
**последовательности** пробельных символов, но не удаляет одиночный пробел,
вставленный там, где его не было. Отступы, переносы строк и пустые строки покрыты;
смена правил расстановки пробелов вокруг скобок — нет.

Усиливать нормализацию не стал: правила пришлось бы выдумывать, а агрессивная
нормализация схлопывает и значимые различия. Вместо этого граница зафиксирована
явно — отдельным тестом `test_normalisation_boundary_is_whitespace_runs`, записью
в `docs/manifest.md` и в план. Практическое следствие: смена настроек форматтера
даст **разовую** волну «реализация изменилась» по всему дереву; событие однократное
и снимается приёмкой.

Это тот случай, когда тест был сильнее спецификации, и правильный выход — не
подгонять реализацию под тест и не ослаблять тест молча, а записать границу.

### Вторая граница: XML-doc

`SourceSpan` не включает XML-doc — комментарий является предшествующим соседом
объявления, а не его потомком. Поэтому правка XML-doc `impl_hash` не меняет.
Записано тестом и в справочнике: менять устройство span ради этого не нужно,
но знать об этом шагу 3 придётся.

### Проверено парами

Смысл двух хэшей теряется, если проверять их порознь, поэтому критерии
сформулированы парами:

- изменение тела метода — `impl_hash` меняется, `signature_hash` **нет**;
- изменение типа возврата — меняются **оба**;
- перестановка файлов `partial`-типа — не меняется ничего;
- правка любой половины `partial` — `impl_hash` меняется.

### Проверка

```bash
uv run pytest tests/test_resolve_fqn.py tests/test_tree.py tests/test_cache.py \
              tests/test_scan_e2e.py tests/test_determinism.py -q     # 122 passed
uv run ruff check . && uv run ruff format --check . && uv run mypy docpipe && uv run pytest -q
                                                                        # 675 passed
```

---

## M01 — `ruleset.py`: общий движок правил ✅

Создано: `docpipe/ruleset.py`, `tests/test_ruleset_engine.py` (30 тестов).
Изменено: `docpipe/classify.py`.

### Критерий «поведенчески пусто» выполнен буквально

```
git diff --stat tests/test_classify.py     # пусто
git diff --stat tests/golden/doc-tree.json # пусто
```

Ни один тест классификации не потребовалось тронуть, золотой манифест совпал
байт в байт. Это и был смысл задачи: рефакторинг, который что-то «заодно
улучшил» в сообщениях, невозможно отличить от рефакторинга, который что-то
сломал.

Сообщения об ошибках перенесены **дословно** — они и были главной причиной
выноса. `tests/test_classify.py` проверяет четыре формулировки по подстрокам
(`неизвестный предикат`, `повтор id`, `регулярное выражение`, `без полей`),
и любая правка текста уронила бы их.

### Что осталось у классификации, а что ушло в движок

В движок: `validate_condition`, `evaluate`, `pick_winner`, `load_rule_items`,
`COMBINATORS`, `PredicateTable`.

У классификации: сами предикаты и модели `Rule`/`Ruleset`/`Classification`.
Предикаты знают про `Symbol`, то есть про предметную область, и их вынос сделал
бы движок зависимым от моделей .NET — ровно того, чего бизнес-слой и владение
избегают.

`regex_keys` в таблице вместо захардкоженного списка: у владения будет свой
набор regex-предикатов (`namespace_regex`), и знать о нём движку неоткуда.

### Тест движка — на искусственной таблице

Соблазн проверять движок на `rules/dotnet.yaml` велик и неверен: такой тест
проверял бы заодно классификацию, то есть не проверял бы движок. Взята таблица
из трёх предикатов над игрушечным предметом.

Отдельно проверено то, ради чего вынос и делался: путь до узла в сообщении
(`r.when.all[1].any[0]`) — без него опечатку в глубоком условии искать негде.

### Проверка

```bash
uv run pytest tests/test_ruleset_engine.py tests/test_classify.py tests/test_scan_e2e.py -q
                                                                       # 92 passed
uv run ruff check . && uv run ruff format --check . && uv run mypy docpipe && uv run pytest -q
                                                                       # 705 passed
```

---

## M04 — шаблоны и образцы ✅

Создано: `templates/README.md`, семь скелетов, четыре образца,
`docpipe/materialize/template.py`, `tests/test_materialize_template.py` (42 теста).

### Комплект скелетов сверяется с набором правил

Тест читает `rules/dotnet.yaml` и сравнивает множество значений `template`
с множеством имён файлов. Добавить вид сущности теперь значит дописать правило
**и** завести скелет: иначе прогон упадёт с явной ошибкой вместо молчаливой
потери документов.

### Что проверяется при загрузке и почему

- **генерируемый блок есть и пуст** — скелет объявляет его место, а не
  содержимое; положенный туда текст затрётся при первом прогоне, и человек
  решит, что инструмент сломан;
- **каждая секция пуста по `is_section_empty`** — самая дорогая проверка
  во всём шаге 2: подсказка обычным текстом сделала бы каждый новый документ
  «уже написанным», и агент обошёл бы дерево впустую;
- **есть секция `notes`** — при реклассификации должна остаться хотя бы одна
  секция-приёмник;
- **нет front matter** — он собирается целиком по манифесту;
- **ключи подстановки из белого списка** — `{{ tema }}` иначе осталась бы
  текстом в каждом документе дерева.

### Маркер с неверным именем ловится счётом, а не разбором

Неочевидное следствие устройства M03: маркер, имя которого не по образцу
`[a-z][a-z0-9_]*`, разбором **не распознаётся** и молча становится обычным
текстом. Ошибки при этом нет — документ структурно корректен, просто секции
в нём нет.

Поймать это можно только сравнением: число вхождений `docpipe:section:(start|end)`
в тексте против удвоенного числа разобранных секций. Проверка добавлена.

Граница при этом такая: если испорчен **только один** из пары маркеров, ошибку
даёт уже разбор документа, и его сообщение точнее — оно указывает на строку.
Счёт ловит случай, когда испорчены оба (`Notes`, `my-notes`), — то есть когда
человек последовательно назвал секцию не по образцу. Оба случая закреплены
тестами.

### Образцов четыре, а не семь, и это решение

Постановка требует шаблоны сервиса, провайдера, воркфлоу и контроллера — они
и заполнены, на реальных типах `SampleSolution`, включая эндпоинты и связи
из золотого манифеста.

Для `ignite-service` и `ignite-compute` образцов **нет**: специфика кластера
на боевом репозитории не проверена (`CASHFLOW.md` §3.2), и придуманный образец
врал бы агенту про то, чего никто не видел. Отсутствие закреплено тестом, чтобы
образец не появился «за компанию».

Отдельный тест сверяет состав секций образца с составом секций его скелета:
иначе образец учил бы структуре, которой скелет не создаёт.

### Проверка

```bash
uv run pytest tests/test_materialize_template.py -q    # 42 passed
uv run ruff check . && uv run ruff format --check . && uv run mypy docpipe && uv run pytest -q
                                                       # 747 passed
```

---

## M05 — `build.py`: front matter, генерируемый блок, кросс-ссылки ✅

Создано: `docpipe/materialize/build.py`, `tests/test_materialize_build.py`,
`tests/test_materialize_links.py` (28 тестов).

Единственный модуль шага 2, знающий про устройство `DocNode`. Остальные —
разбор документа, шаблоны, план, запись — работают с зонами и файлами; на этом
и стоит бизнес-слой, у которого свой `build`.

### Front matter сверяется байт в байт

Эталон из раздела «Формат документа» воспроизведён точно, и в плане заменены
многоточия на настоящие хэши: эталон с `sha256:77af12c0…` невозможно сверить,
а значит, он и не эталон.

Порядок ключей задаётся порядком полей модели плюс `sort_keys=False`. Чужие
ключи при этом **сортируются**: их порядок — тот, что человек мог поменять
руками, и без сортировки перестановка двух строк вызывала бы перезапись
документа при каждом прогоне.

Проверено на реальных значениях: `node_id` с `#` и обратной кавычкой дампер
выводит без кавычек и читает обратно верно; кириллица в `domain` не
экранируется; значение с пробелами длиннее 80 символов не переносится
(`width=10**9`).

### `example_ref` бывает `null`, и это пришлось записать в план

План описывал поле как всегда заполненное. Но образцов четыре, а шаблонов семь:
для `ignite-service` и `ignite-compute` образцов нет намеренно. Подставлять путь
к несуществующему файлу нельзя — агент пошёл бы его читать. Поле стало
`str | None`, обоснование дописано в план.

### Кросс-ссылки: неоднозначность показывается, а не разрешается

Сужение идёт по **идентификаторам** модулей, а не по именам: имена проектов
не уникальны (в ABP 39 повторов), и сужение по имени выбирало бы произвольный
из одноимённых. `project_references` в манифесте — пути к `.csproj`, поэтому
строится отображение «путь → id модуля».

Если после сужения кандидатов всё ещё несколько, рендерятся **все**,
отсортированные по `doc_path` и подписанные модулем. Выбор одного — это ложь
в документации, которая обнаружится нескоро.

Шаг «реализации интерфейса» проверен на золотом манифесте: `PricingController`
зависит от `IPricingService`, интерфейсы по умолчанию не документируются,
и без этого шага таблица зависимостей у каждого сервиса состояла бы из строк
«вне дерева».

### Экранирование таблиц: инвариант вместо точечной проверки

Первая версия теста считала трубы в строке и сравнивала с числом колонок —
арифметика оказалась неверной (у строки из N колонок N+1 труб). Переписано
на настоящий инвариант: **внутри одной таблицы у всех строк одинаковое число
неэкранированных `|`**. Значение с трубой иначе добавляет колонку, и таблица
рассыпается.

`xml_doc` идёт цитатой и в таблицу не попадает никогда: перевод строки внутри
ячейки ломает её так же.

### Проверка

```bash
uv run pytest tests/test_materialize_build.py tests/test_materialize_links.py -q  # 28 passed
uv run ruff check . && uv run ruff format --check . && uv run mypy docpipe && uv run pytest -q
                                                                                 # 775 passed
```

---

## M06 — `ownership.py`: владение и его диагностика ✅

Создано: `docpipe/materialize/ownership.py`, `ownership.example.yaml`,
`tests/test_ownership.py` (24 теста).

Первый потребитель общего движка правил из M01 — и вынос себя оправдал: код
владения не содержит ни валидации условий, ни вычисления, ни выбора победителя.
Своё в нём только восемь предикатов над `DocNode` и диагностика.

### Проверено то, ради чего владение заводится

Искусственный манифест воспроизводит шэренный `Cf.Shared`, внутри которого лежит
код трёх команд:

| Тип | Владелец | Почему |
|---|---|---|
| `src/Cf.Shared/Pricing/CurveStore.cs` | `pricing` | каталог, приоритет 50 |
| `src/Cf.Shared/Risk/Exposure.cs` | `risk` | каталог, приоритет 50 |
| `src/Cf.Shared/Common/Clock.cs` | `platform` | проект, приоритет 10 |
| `Cf.Shared.Pricing.Serialization.CurveWriter` | `platform` | **namespace 80 бьёт каталог 50** |
| `Cf.Pricing.Engine.StressTestRunner` | `risk` | **точечно 100 бьёт проект 10** |

Отдельным тестом закреплено, что перестановка правил в YAML не меняет ни одного
владельца: порядок строк в файле источником решения быть не может.

### `matched` хранит все совпавшие правила, а не только победившее

Без этого настройка границ команд превращается в гадание — не видно, что ещё
сработало и почему выиграло не оно. `explain` печатает их по убыванию приоритета
с пометкой победителя; на `CurveWriter` это сразу показывает, что каталог
ценообразования проигрывает namespace платформы.

### Ничьи — предупреждение, а не ошибка

Два правила с равным максимальным приоритетом дают формально детерминированный
результат (побеждает лексикографически меньший `id`), но почти всегда означают
забытый `priority`. Человек узнаёт об этом, только когда документ уезжает
не в ту команду, — поэтому `lint` про них сообщает.

`lint` возвращает пару `(находки, предупреждения)`: находка — почти наверняка
дефект настройки (мёртвое правило, узлы без владельца, команда без узлов),
предупреждение — формально корректное, но обычно недосмотр (ничьи,
типы с файлами в разных каталогах).

### Срезы, а не счётчик

«Узлов без владельца: 137» не говорит, что дописать. Поэтому к счётчику идут два
среза — по модулям и по каталогам внутри модуля, — по образцу `stats`. Именно они
показывают, где не хватает правила.

### Проверка

```bash
uv run pytest tests/test_ownership.py -q     # 24 passed
uv run ruff check . && uv run ruff format --check . && uv run mypy docpipe && uv run pytest -q
                                             # 799 passed
```

---

## M07 — `plan.py`: план, статусы, решения ✅

Создано: `docpipe/materialize/plan.py`, `tests/test_materialize_plan.py` (23 теста).
Изменено: `docpipe/materialize/build.py`.

Самая насыщенная задача шага 2. Ни одной записи на диск: план считается целиком,
проверяется и только потом применяется — иначе ошибка на середине оставляет дерево
наполовину обновлённым, и об этом никто не знает.

### Два «действия» названы по-разному

План предупреждал, что статус и действие путают. Оказалось, что путают и два
**действия**: «что сделать с файлом» и «что делать агенту». В коде они
`file_action` (create/update/unchanged/refuse/relocate) и `agent_action`
(write/review/skip). Одно имя на оба — и через месяц никто не разберёт, почему
`current` получает `update`.

### Три ошибки, найденные тестами

**Пустая строка после front matter прирастала каждый прогон.** `dump_front_matter`
возвращал `---\n\n`, а при слиянии эта строка складывалась с той, что уже есть
в сохранённом теле. Второй прогон давал `update` вместо `unchanged`, третий — ещё
одну строку. Функция теперь возвращает `---\n`; разделитель добавляет сборка нового
документа. Эталон M05 поправлен, ловушка записана в план.

**Кандидаты на перенос искались не по тому признаку.** Я взял «`node_id` нет
в манифесте» — и переклассификация не находилась вовсе: там узел остаётся тем же,
меняется только путь. Правильный признак — «файл не стоит на `doc_path` ни одного
узла». Сиротой (статусом) такой файл при этом не становится: сирота — это отсутствие
узла, а не отсутствие места.

**Осиротевшие секции считались по собранному тексту.** Но строка про них идёт
**внутрь** генерируемого блока, то есть знать о них надо до сборки. Считаются
по файлу.

### Два теста были неправы, а код прав

`relocated` не выигрывает у `undeclared`: по таблице плана «приёмки не было, а текст
есть» стоит выше. Перенесённый, но не принятый документ — это `undeclared`, и это
верно: сверять его с кодом всё равно придётся. Тест поправлен, чтобы принимать
документ до переноса.

Второй тест делал сироту, подменяя `node_id` у файла, лежащего на пути живого узла.
С исправленным определением такой файл — не сирота, а кандидат на усыновление,
и по пересечению источников он к своему узлу и вернулся. Настоящая сирота лежит
там, где узла нет.

### Проверка

```bash
uv run pytest tests/test_materialize_plan.py -q   # 23 passed
uv run ruff check . && uv run ruff format --check . && uv run mypy docpipe && uv run pytest -q
                                                  # 822 passed
```

---

## M08 — `apply.py` и команда `docpipe materialize` ✅

Создано: `docpipe/materialize/apply.py`, `tests/test_materialize_cli.py` (17 тестов),
`tests/test_relocation.py` (8 тестов), `tests/golden/docs/**` (6 документов).
Изменено: `docpipe/cli.py`, `docpipe/config.py`, `docpipe.example.yaml`,
`docpipe/materialize/plan.py`.

Первая задача шага 2, которая пишет на диск. Инструмент стал прогоняемым:

```
uv run docpipe materialize tests/golden/doc-tree.json --root /tmp/x
Сделано:
  создано:      6
  без изменений:  0
```

### Дыра, найденная при написании `apply`

Испорченный документ исключался из `by_path`, поэтому узел на его пути оказывался
«без файла», получал `create` и **затирал документ, прочитать который не удалось**.
То есть ровно то, что план запрещает: единственный экземпляр принятого состояния
и авторского текста живёт в этом файле.

Теперь испорченный файл на пути узла обрабатывается вместе с узлом: `refuse`,
статус `broken`, файл не тронут. Собранный из шаблона текст при этом кладётся
в план — он нужен `--force`, который сохраняет исходник в `*.md.broken`
и пересоздаёт документ. Ловушка записана в план.

### Отметка о переносе ставится планом, а не записью

Соблазн был выставить `docpipe_state.review = {reason: relocated}` в `apply` —
там же, где происходит `Path.replace`. Но тогда содержимое документа считалось бы
в двух местах, и `apply` перестал бы быть тупым исполнителем. Отметка ставится
в `build_plan`, где собирается весь текст.

### Неоднозначный перенос — не сбой

Тест сначала ожидал код 1, но это неверно: ничего не сломалось, документ просто
остался на месте, о чём сказано в отчёте. Дальше он виден как сирота и переносится
командой `docs adopt`. Код 1 дают только блокирующие ошибки, отказы по битым
документам и ошибки записи.

### Тест на read-only оказался бессмысленным в первой редакции

Файл снимался с записи, но прогон и не собирался его писать — содержимое
не менялось, действие было `unchanged`, и код возврата был нулевым «правильно
по неправильной причине». Переписан: манифест меняется так, что обновления
требуют все шесть документов, каталог одного снимается с записи, и проверяется,
что **остальные пять обновлены**, а прогон дал код 1.

### Эталонное дерево

`tests/golden/docs/**` — шесть документов, сравниваются байт в байт. Они же живая
документация формата: видно и front matter, и генерируемый блок с таблицами
эндпоинтов и кросс-ссылками, и пустые секции с подсказками в комментариях.

### Проверка

```bash
uv run pytest tests/test_materialize_cli.py tests/test_relocation.py -q   # 25 passed
uv run ruff check . && uv run ruff format --check . && uv run mypy docpipe && uv run pytest -q
                                                                          # 847 passed
```

---

## M09 — `docpipe docs status` ✅

Создано: `docpipe/materialize/status.py`, `tests/test_docs_status.py` (18 тестов).
Изменено: `docpipe/cli.py`, `docpipe/materialize/plan.py`, `docpipe/materialize/build.py`.

Команда, ради которой строился весь шаг 2: агент шага 3 спрашивает «что делать
с этим документом» и получает ответ с причиной.

```
Состояние документов:
  empty            6
  всего            6

write   empty       docs/modules/Sample.Pricing.Api/controllers/pricing-controller.md
        документ не наполнен: purpose, api, behaviour, collaboration, notes
```

### Проверка ссылок вынесена из плана

`build_plan` не трогает файловую систему — это его главное свойство, на нём стоит
`--dry-run`. Проверка ссылок по определению требует ФС, поэтому встроить её внутрь
значило бы это свойство потерять.

Сделано двумя функциями: `check_links(existing, root)` смотрит на диск,
`with_links(plan, broken)` приписывает результат готовому плану. План остаётся
чистым. Ловушка записана.

Проверяются **только авторские секции**: ссылки в генерируемых блоках чинятся сами,
блоки пересобираются в том же прогоне. Внешние адреса и якоря не проверяются —
они не про файловую систему.

### `--fail-on` с опечаткой — код 2

Отдельная проверка на входе, до всякой работы. `--fail-on statle` иначе дал бы
пустой фильтр и вечно зелёную проверку в CI — то есть худший из возможных
результатов: команда стоит, выглядит работающей и ничего не ловит. То же
для `--action`.

### `current` только в счётчике

На АС CF подробный список по всем документам — тысячи строк, и в них теряется
то немногое, ради чего команду звали. В подробностях остаются лишь документы,
требующие действия.

### Поля отчёта, которых не было в плане

`orphan_sections` добавлен в JSON рядом с `empty_sections`: агенту полезно знать,
что в документе осталась секция не из текущего шаблона, — иначе он не поймёт,
почему в файле есть текст, которого он не писал.

`template_refs` вынесен общей функцией в `build.py`: путь к скелету и образцу
считается и во front matter, и в отчёте, а два места — это два разных ответа
через полгода.

### Проверка

```bash
uv run pytest tests/test_docs_status.py -q     # 18 passed
uv run ruff check . && uv run ruff format --check . && uv run mypy docpipe && uv run pytest -q
                                               # 865 passed
```

---

## M10 — `docs accept`, `docs adopt`, `docs owners` ✅

Создано: `tests/test_docs_accept.py` (22 теста).
Изменено: `docpipe/cli.py`, `docpipe/materialize/plan.py`.

Цикл замкнут. Принятое состояние выглядит ровно так, как обещал план:

```yaml
docpipe_state:
  accepted:
    signature_hash: sha256:0e29455f…
    impl_hash: sha256:74ba5471…
    kind: controller
    members:
    - GetAsync
    - PricingController
    - RecalculateAsync
  review: null
```

### Приёмка проходит через тот же план и ту же запись

Соблазн был сделать `accept` отдельным путём: открыть файл, подменить
`docpipe_state`, закрыть. Это дало бы вторую реализацию слияния зон, и она
разошлась бы с первой на первом же документе с чужими ключами front matter.

Сделано как `PlanOptions.accept` — множество путей, для которых план подставляет
новое состояние. Дальше всё как обычно: то же слияние, та же атомарная запись,
та же идемпотентность. `accept` дважды подряд не меняет файл ни на байт, потому
что времени в состоянии нет.

### Публичные члены считаются одной функцией

`accept` фиксирует список, `decide` с ним сравнивает. В первой версии они
считались по-разному: приёмка брала публичные, сравнение — все. Такой документ
навсегда остался бы `stale`, а причина выглядела бы как «контракт изменился»
без единого изменения в коде. Введена `public_members(node)` — один источник
для обоих.

### `accept` требует отбора

Без `PATH`, `--node`, `--team` или `--all` команда отказывается работать.
Опечатка здесь стоит слишком дорого: приёмка объявляет документы
соответствующими коду, и снять её можно только новой приёмкой.

### `adopt` не ставит отметку о пересмотре

Автоперенос ставит `review: {reason: relocated}`, потому что переезд случился
сам и человек о нём не знает. Ручной перенос человек делает осознанно, а документ
всё равно писался для другого типа — и обычная логика статусов помечает его сама:
принятое состояние не совпадёт с новым узлом, и получится `stale` или `undeclared`.
Дублировать это отметкой незачем.

### Тесты `adopt` были неправы

Сценарий копировал документ контроллера — то есть создавал второй файл с тем же
`node_id`, — и `adopt` его законно отклонял. Проверка сработала верно, а тест
описывал не тот случай: усыновляют документ **исчезнувшего** узла, а не копию
живого. Переписано; отдельным тестом закреплён и отказ по дублю.

### Проверка

```bash
uv run pytest tests/test_docs_accept.py -q    # 22 passed
uv run ruff check . && uv run ruff format --check . && uv run mypy docpipe && uv run pytest -q
                                              # 887 passed
```

---

## M11 — документация ✅

Создано: `docs/materialize.md`.
Изменено: `README.md`, `CLAUDE.md`, `CASHFLOW.md`, `docs/manifest.md`,
`docs/parser-architecture.md`, `docs/parser-implementation-plan.md`.

### Обещание, от которого отказались, записано как решение

`docs/_views/**` — генерируемые индексы «для людей» — были обещаны в трёх местах:
в архитектуре шага 1 (§2.5 и «Точки расширения»), в справочнике по манифесту
и в разделе «что НЕ входит» плана шага 1.

Соблазн был просто вычистить упоминания. Вместо этого в §2.5 оставлена запись
о том, что решение изменилось при проектировании шага 2 и почему: документация
просто лежит в репозитории набором `.md`, навигацию даёт файловое дерево и поиск.
Там же сказано, что манифест по-прежнему содержит всё нужное такому слою
(`domain`, `endpoints`, `dependencies`), и сделать его снаружи можно в любой момент.

Молча удалённое обещание через полгода выглядит как забытое, и кто-нибудь начнёт
его реализовывать.

### `docs/materialize.md` — справочник, а не пересказ плана

По образцу `docs/manifest.md`: что лежит в файле, какие зоны, какие статусы,
какие команды, как выглядит цикл. Обоснования остались в плане — дублировать их
значило бы завести два места, которые разойдутся.

Все команды из справочника прогнаны на тестовом решении: `scan` → `materialize` →
`docs status` → `docs accept` → `docs owners --lint` → `anchors list`.

### CLAUDE.md: семь правил шага 2

Реализовывать дальше будет более слабый агент, поэтому в раздел жёстких правил
добавлено то, что ломается **молча**: зоны документа и инвариант обратимости;
различение слоёв по `docpipe.schema`; `is_section_empty`; сравнение по
нормализованному тексту; отказ вместо самолечения; `accept` берёт хэши
из манифеста; приёмка идёт через тот же план и ту же запись.

### CASHFLOW: §9 про шаг 2

Три вещи, специфичные для боевого репозитория: `docs_root` и `docs_scan_exclude`
с каталогом самого инструмента; обязательный первый прогон с `--dry-run` как
первая оценка объёма; таблица «что смотреть в первом отчёте `docs status`».
Три пункта добавлены в чек-лист.

---

## Итог по шагу 2

Одиннадцать задач, M01–M11. 887 тестов, из них 427 добавлено шагом 2 и бизнес-слоем.

Что получилось сквозным прогоном:

```
docpipe scan        →  doc-tree.json          что документировать
docpipe materialize →  docs/**/*.md           документы с зонами
docpipe docs status →  json для агента        что делать с каждым
docpipe docs accept →  зафиксированное состояние
```

Свойства, которые проверяются, а не декларируются:

- **дерево из золотого манифеста совпадает байт в байт** с `tests/golden/docs/**`;
- **повторный прогон не меняет ни байта и не трогает `mtime`**;
- **авторский текст переживает** пересборку блока, смену владельца, домена, обоих
  хэшей, шаблона и переезд документа;
- **ни один сценарий не удаляет файл**;
- **испорченный документ не изменяется ни на байт**, включая случай, когда узел
  на его пути выглядит «без файла»;
- **`docpipe.materialize` не импортирует `docpipe.dotnet`** — проверяется обходом AST.

Что осталось за пределами шага 2 намеренно: индексы и карты «для людей», интеграция
с LLM, поставка шага 2 в репозиторий АС CF, парсеры Python и TypeScript.

---

## T23 — решения вместо неклассифицированного ✅

Задача пришла не из плана, а из работы на боевом репозитории: `./run.sh stats` дал
около 4100 неклассифицированных, и по отчёту было нельзя понять, что из этого требует
правила, а что вообще не должно документироваться. Замечание было сформулировано так:
«их надо либо классифицировать, либо отметить, что классификация не нужна,
соответственно и отчёт должен это отражать».

### Почему отчёт не мог сказать «готово»

Причина оказалась механической, а не стилистической.

Состояние «решено не документировать» в модели было — счётчик `excluded`, — но
безымянное: ни по какому решению, ни зачем. Разбивка по причинам, которая есть
в README («на ABP `name_regex` — 508 из 996»), была добыта ручными замерами, инструмент
её не показывал.

Хуже второе. Секция `exclude` умела ровно четыре условия, и решение «типы с атрибутом
`[GenerateCode]` не документируем» выразить было **нечем**. На АС CF это 1478 типов,
которые оставались в `unclassified` навсегда, сколько бы раз человек их ни просмотрел.
То есть счётчик смешивал «ещё не смотрели» с «посмотрели и решили, что не надо»,
потому что второе физически некуда было положить.

### Что сделано

Три исхода, и все три — решения: `<kind>` (документируем), `not_documented`
(не документируем, с причиной), `undecided` (решения нет). Плюс два состояния, которые
решением человека не являются и потому стоят отдельно: `not_enrolled` — решено уровнем
выше, в `docpipe.yaml`; `interface_covered` — решил инструмент.

`exclude` стал списком правил с обязательным `reason` и условием из тех же десяти
предикатов. Нового кода это почти не потребовало: движок в `ruleset.py` (M01) generic,
а таблица предикатов в `classify.py` уже содержала `attribute`, `namespace_regex`
и `inherits`. Ровно та дырка, из-за которой 1478 типов были неотсеиваемы, закрылась
переиспользованием того, что уже было.

`require_public` оставлен полем. Отрицания в движке нет намеренно, и «не public» через
предикаты не выражается; но довод не только технический — это глобальный переключатель
«документируем ли внутреннюю кухню», а не решение о группе типов. В отчёте он участвует
псевдоправилом `exclude.require_public` с приоритетом ниже всех остальных.

### Что оказалось важнее, чем выглядело

**Появился признак завершения, а с ним и смысл в CI.** `--fail-on-undecided` даёт код 1,
пока есть типы без решения. Пока «не решено» было счётчиком «неклассифицировано»,
он был большим всегда, и новый тип в репозитории в нём не выделялся: +1 к 7142 не видно.
Это и есть настоящая причина, по которой замечание было верным.

**Совместимость оказалась обязательной, а не любезной.** Копия набора правил уже лежит
на боевом репозитории АС CF, и правка, требующая одновременной миграции, означала бы,
что настройку там надо переделывать. Поэтому краткая форма из четырёх полей
разворачивается в правила с зарезервированными id `exclude.<поле>` и общими причинами,
а формы складываются: `rules:` можно дописать рядом с полями, ничего в них не меняя.
Развёрнутый набор АС CF так и оставлен в краткой форме — переведён только эталонный
`rules/dotnet.yaml`, а в набор АС CF добавлены закомментированные заготовки по трём
крупным группам из его отчёта.

### Ловушки

**Атрибуция при нескольких совпавших решениях.** Побеждает больший `priority`, при равном —
меньший `id` (`pick_winner`), и никогда порядок в файле: иначе перестановка двух правил
в YAML меняла бы цифры, которые идут в журнал и в CI. Правила краткой формы получили
отрицательные приоритеты (`path_glob` −1, `name_regex` −2, `type_kind_deny` −3,
`require_public` −4), чтобы решение человека забирало атрибуцию себе, а порядок между
собой повторял порядок проверок прежней реализации — иначе замеры «по какой причине
отсеяно» из findings перестали бы сходиться.

**Правило-заглушка.** `when: {}` отвергается при загрузке само — условие обязано быть
словарём с одним ключом, это проверка из M01. А вот `path_glob: ["**"]` с причиной
«разберусь потом» синтаксически законно, и запретить его нельзя: законные широкие
решения существуют. Видно только в таблице причин, по неправдоподобно большому
счётчику у одного решения. Поэтому таблица сортируется по убыванию счётчика,
а не по id.

**Отчёт для человека, а не для парсера.** Пришлось написать `plural`: «1 правил»
в отчёте выглядит дефектом инструмента. Строка «решение не принято» остаётся на месте
и при нуле, с подписью «решены все символы»: исчезнув, она заставила бы выяснять,
всё решено или отчёт сломался.

**Таблица видов перестала печатать `total`.** Сумма видов меньше числа символов, потому
что служебные категории ушли в блок решений, — и итог в этой таблице читался бы как
ошибка. По манифесту (`docpipe stats`) итог печатается: там он равен сумме.

### Проверка

Главный критерий — **манифест по фикстуре отличается от эталонного ровно одной строкой,
`ruleset_version`**. Перевод эталонного набора в форму с причинами не изменил ничего
по существу, и это проверено побайтовым сравнением, а не рассуждением. Отдельный тест
`test_reference_set_migration_changed_nothing` держит то же самое иначе: состав отсева
на `SampleSolution` и `WildSolution` у краткой формы и у формы с причинами совпадает.

Ключи сидкара переименованы: `unclassified` → `undecided`, `excluded` →
`not_documented`, добавлен агрегат `documented`. Ничего, кроме документации, эти ключи
не читало — проверено grep по `.sh`, `.yaml`, `.json`; README при этом показывал ключ
`classified`, которого инструмент никогда не выдавал, и теперь пример стал правдой.

909 тестов (+22).

---

## T24 — `docpipe symbols` ✅

Задача пришла с того же боевого репозитория, из следующего шага цикла настройки.
После T23 отчёт по одному проекту выглядел так:

```
Вне области            24170
РЕШЕНИЕ НЕ ПРИНЯТО         6
  модули:  6  Sbt.Cashflow.Grid.Services.AutoConclusionService
  окончания имён:  4 (прочее), 1 Factory, 1 Converter
```

И дальше двигаться нечем: известно, что осталось шесть символов и что четыре
не опознаются по имени, но **что это за символы — не известно**. Формулировка была
такая: «отладка каждого проекта будет занимать время, и надо иметь инструмент,
который позволит удобно это смотреть».

### Признак, который уже был

До T20 роль такого инструмента играл разовый скрипт `tools/unclassified.py`; его
удалили, когда появился `scan --stats`. Здесь повторилось то же самое: для работы
по инструкции понадобился скрипт поверх внутренних функций `docpipe`, которого
в инструменте нет. Это и есть признак нехватки команды, а не нехватки скрипта.

### Что сделано

`docpipe symbols` печатает сами символы с фильтрами по состоянию решения, модулю,
namespace, правилу и виду. На символ — семь строк: вид и модификаторы, путь со
строкой объявления, прямые базы, замыкание наследования, атрибуты, публичные члены
и принятое решение с причиной.

Показываются не все поля, а решающие — те, по которым пишется предикат. Полный дамп
заставляет листать вывод вместо того, чтобы принять решение; для остального есть
`--format json`.

Публичные члены попали в вывод не для полноты: имена методов grid-сервиса — это
контракт (их вызывают по имени через прокси), и на АС CF вид сущности опознаётся
по ним там, где имя типа не говорит ничего. Ровно тот случай, что в отчёте выглядит
как «4 (прочее)».

### Что оказалось важным

**Одна реализация решения на отчёт и на выборку.** Две разошлись бы на первой новой
категории, и тогда `--stats` говорит «шесть без решения», а `symbols` показывает
пять — доверять нельзя ни одному. Решение вынесено в `stats.decide`, `collect_stats`
переписан на неё; счётчики от этого не изменились (проверено существующими тестами
до правки строк), а согласованность закреплена отдельным тестом.

**`--rule` работает в обе секции сразу.** Спрашивают его, чтобы проверить только что
написанное правило, и помнить, в какой оно секции, человек не должен. Побочно вышло
полезное: на классифицированных он показывает правила, которые совпали, но не
победили, — `--rule service` находит `RiskComputeService`, ставший `ignite_service`.
До этого такой аудит делался через `jq` по `matched_rules` в манифесте.

**Индекс символов пришлось вернуть из прогона.** `ScanResult` получил поле `index`:
спрашивать «что осталось без решения» у манифеста бессмысленно, туда по определению
попадает только решённое.

### Побочно исправлено

Обрезка срезов переехала из подсчёта в вывод, и появился `--top`. На репозитории
с сотнями проектов топ-15 модулей выглядел полным списком — то есть половина работы
была не видна; теперь обрезанный срез помечается остатком («и ещё 476»).

Заодно `_top` перестал пользоваться `most_common(15)`: тот при равных счётчиках
сохраняет порядок вставки, то есть порядок обхода индекса. Формально это нарушало
жёсткое правило проекта про явный `sorted()` с явным ключом, и на срезе с длинным
хвостом единичных счётчиков могло проявиться. Теперь сортировка явная: по убыванию,
при равенстве по имени.

932 теста (+23).

---

## T25 — вызов через CLI вместо скрипта-обёртки ✅

Замечание с боевой машины: «мне не нравится, что есть какой-то скрипт типа `run.sh`,
который по сути дублирует функционал CLI… если нужно объявить какие-то переменные
окружения, то лучше это будет просто списком переменных, а не скриптом, который надо
разбирать».

### Чем скрипт мешал

Он завёл свой набор режимов — `stats`, `scan`, `dry-run`, `validate`, `diff`,
`symbols`, `--` — поверх тех же команд `docpipe`. Два следствия, оба выяснились
на практике:

- **инструкция перестала совпадать с тем, что человек набирает.** Читая
  «`./run.sh stats`», он не находит там ни одного своего флага, а чтобы узнать,
  какие пути подставляются, обязан прочитать bash;
- **каждая новая команда требовала строки в `case`.** В T24 `symbols` пришлось
  добавлять в двух местах. Обёртка, перечисляющая команды, разъезжается с CLI
  по умолчанию — это вопрос времени, а не аккуратности.

### Что в нём было по существу

Три вещи, каждая из которых иначе даёт отказ именно на закрытом контуре:
`uv run --no-sync` (без него каждый вызов сверяется с индексом и падает там, где
индекса нет), `python -m docpipe` с `PYTHONPATH` (при установке
с `--no-install-project` консольной команды нет вовсе) и `cd $CF_ROOT` перед вызовом
(`out` в конфигурации задан от текущего каталога, а не от `--root`).

Всё переехало в инструкцию: пять строк `export` и одна строка `alias`. После этого
команды в документации выглядят так, как их набирают:

```bash
docpipe scan --root . --config $BUNDLE/docpipe.yaml --stats
docpipe symbols --root . --config $BUNDLE/docpipe.yaml --module <проект>
```

`--rules` из инструкций убран заодно: путь к `rules.yaml` прописан в `docpipe.yaml`,
и дублировать его флагом значило бы иметь два источника истины.

### Ловушка, которую пришлось закрыть

Требование `--no-sync` осталось без исполнителя. Пока был скрипт, флаг лежал
в коде и проверялся тестом по этому коду; теперь единственный носитель — текст
инструкции. Поэтому тест проверяет её: `--no-sync` обязан присутствовать
в `deploy/README.md`, `cashflow-docspipe/README.md` и в шапке `docpipe.yaml`.
Без этого флаг тихо выпадет при следующей правке документации, а обнаружится
на машине без доступа к индексу — то есть в худшем возможном месте.

Третьим носителем сделана финальная подсказка `install.sh`: она печатает тот же
список переменных и алиас, поэтому рабочий вызов человек получает сразу после
установки, не открывая README.

### Что осталось на боевой машине

`install.sh` файлы копирует, но не удаляет, поэтому уже установленный `run.sh`
там останется до ручного удаления. Работать он продолжит — команды CLI, которые
он зовёт, не менялись.

934 теста (+2: инструкция проверяется вместо скрипта).

---

## T26 — один набор правил в поставке ✅

Замечание с боевой машины: «настроечный файл при обновлении не затирается,
рядом кладётся `rules.yaml.new` — тогда зачем тот, что уровнем выше? Можно же
оставить только те, что в `cashflow-docspipe`».

Замечание верное, и оно лучше того, что предлагал я. За полчаса до этого я
разбирал ту же копию и предлагал её **переименовать** — в
`rules/dotnet.reference.yaml`, чтобы её нельзя было подхватить по умолчанию.
Это лечило симптом; вопрос «а зачем она вообще» я не задал, хотя механизм
`keep_configured` с `.new` описан в этом же журнале двумя разделами выше.

### Почему копия не нужна

Она отвечала на вопрос «что настроено под АС CF». Но на него уже отвечают две
вещи: пометки «АС CF» прямо в наборе и `rules.yaml.new`, который приносит новую
версию при обновлении. Третий ответ был лишним с самого начала.

После T23 он к тому же испортился: эталон переехал в форму с причинами, набор
АС CF остался в краткой, и `diff` между ними распух до 68 строк даже без
комментариев — показывал форму записи, а не настройку.

### Почему она была вредна

Значение `rules` по умолчанию — `rules/dotnet.yaml` **относительно текущего
каталога**. Прогон из каталога инструмента без `--config` брал эталонный набор
и завершался успешно: манифест построен не теми правилами, ни предупреждения,
ни намёка. Воспроизвёл на копии раскладки, чтобы не рассуждать.

Из `$CF_ROOT`, как написано в инструкции, ловушка не срабатывает — там `rules/`
нет и прогон падает с «файл не найден». То есть она ждала ровно того, кто зайдёт
в каталог инструмента, а туда заходить логично: там лежит настройка.

### Про остаток на уже установленных машинах

Установщик по правилу не удаляет ничего, поэтому оставшийся `rules/dotnet.yaml`
там останется — вместе с ловушкой. Промолчать было нельзя, поэтому добавлено
предупреждение в stderr с объяснением и командой `rm -r`. Тест проверяет обе
половины: что сказали и что **не** удалили.

Тесты установщика впервые прогоняют его по-настоящему (`--skip-sync`, сеть не
нужна) и смотрят на получившееся дерево, а не на текст скрипта. Проверка «ровно
два `.yaml` в дереве» переживёт любую перестановку строк в `install.sh` —
в отличие от grep по команде `cp`, который я сначала и собирался написать.

936 тестов (+2).

---

## Исправление: шаблоны не входили в поставку ✅

Найдено при попытке запустить шаг 2 на боевом репозитории: `materialize` падал
с «Каталог шаблонов не найден», и по сообщению не было видно, что виновата
не команда.

### Два дефекта, а не один

**`install.sh` каталог `templates/` не копировал вообще.** Слова «templates»
в `deploy/` не встречалось ни разу. То есть никакой путь в конфигурации не помог
бы — файлов на целевой машине не существовало.

**`templates` отсчитывается от текущего каталога.** Та же ловушка, что у `out`
и `rules`: `templates_path = templates_dir or Path(settings.templates)`, без
разрешения относительно `--root` или самого `docpipe.yaml`. Значение по умолчанию
(`templates`) при запуске из `$CF_ROOT` указывает на `<корень>/templates`, которого
в репозитории продукта нет и не будет. Про `out` это было написано в трёх местах,
про `templates` — ни в одном.

Второй дефект пережил бы починку первого: положи шаблоны куда угодно, без явного
пути в конфигурации прогон всё равно ищет не там.

### Решение

Шаблоны кладутся в `cashflow-docspipe/templates/`, рядом с `rules.yaml`, а не
уровнем выше к пакету. Довод: скелет документа настраивается под проект ровно
в той же мере, что и правила классификации, и правят его те же люди. Отсюда же
`keep_configured` пофайлово вместо `cp -r` — обновление инструмента не имеет
права затирать переписанный шаблон, и правка сохраняется так же, как в `rules.yaml`.

В конфигурацию поставки заодно добавлены `docs_root` и `docs_scan_exclude`:
они висели в чек-листе `CASHFLOW.md` как «дописать руками», а без второго обход
дерева документации пошёл бы по `.venv` инструмента и по самим шаблонам поставки.
Ключ `ownership` оставлен закомментированным — файла ещё нет.

### Проверено

Прогоном установщика на копии раскладки, а не чтением скрипта: `scan` →
`materialize --dry-run` → `materialize` → `docs status` с конфигурацией поставки
из корня фиктивного репозитория. Шесть документов создались, каталог инструмента
сиротами не стал — `docs_scan_exclude` отработал.

Три новых теста в `test_deploy_bundle.py` смотрят на получившееся дерево:
что все семь `template` из настроенного набора правил загружаются из
установленного каталога, что путь из `docpipe.yaml` ведёт именно туда, куда
установщик положил, и что правленый шаблон переживает повторную установку.
Второй из них ловит главное — расхождение двух записей об одном и том же месте:
разъедутся они молча, конфигурация останется валидной, а отказ вылезет на чужой
машине.

Документация: `CASHFLOW.md` §0 и §9 (раскладка, ловушка про текущий каталог,
чек-лист), `deploy/README.md`, `deploy/cashflow-docspipe/README.md` (появился
раздел про шаг 2 — его там не было вовсе), `docs/materialize.md` (откуда берётся
каталог шаблонов).

941 тест (+5).

---

## Поставка: `ownership.yaml`, раскладка по каталогам в отчёте, задача M12 ✅

Три правки одним заходом, все выросли из первого запуска шага 2 на боевом репозитории.

### `ownership.yaml` в поставке

Тот же класс дефекта, что и с шаблонами: `docpipe.yaml` ссылался на файл, которого
в поставке нет. Образец лежал только в клоне разработки (`ownership.example.yaml`).

Приезжает **заготовкой** — `teams: []`, `rules: []` — а не копией образца, и это
решение, а не лень. Выдуманные команды хуже отсутствующих: правило с несуществующим
`module_glob` молча не срабатывает, и «ничьих узлов 4820» уже не отличить от «правило
написано с опечаткой». Пустой файл говорит правду: владения нет, вот команда, которая
покажет, с чего начинать. Порядок работы и все восемь предикатов — в комментариях
самого файла, чтобы на закрытом контуре не требовался клон разработки.

Ключ `ownership` в конфигурации поставки при этом **включён**: файл приезжает вместе
с путём на себя, иначе ссылка снова вела бы в никуда.

Попутно поймано в собственной документации: у `docs owners` нет `--root`, а во всех
трёх местах, где я его дописал, он стоял. Проверено запуском, а не чтением справки.

### Где именно окажутся документы

`--dry-run` печатал «создано: 6» и молчал о том, где. На боевом репозитории это первый
же вопрос, и ответа на него не было ни в отчёте, ни в манифесте — надо было писать jq.

Добавлена раскладка по каталогам созданных документов. Порядок — по убыванию
количества, при равенстве по имени: список идёт в отчёт, а отчёты сравнивают между
прогонами. Показываются двадцать самых крупных каталогов, остаток сворачивается
в строку с двумя числами. Обрезка, теряющая документы, врала бы об объёме — а ради
оценки объёма всё и печатается, поэтому остаток остаётся посчитанным.

Тест сверяет каталоги из `--dry-run` с деревом настоящего прогона, а не с константой:
константу пришлось бы править при каждой правке правил классификации, и она проверяла
бы саму себя.

### M12 — базовый шаблон

Задача записана в план, не реализована. Сейчас неизвестный `template` попадает
в `_blocking_errors` и отменяет прогон целиком: ни одного документа. Для настройщика
на чужом репозитории это худший из возможных ответов — первое же своё правило
классификации приносит новый `template`, и вместо дерева получается отказ.

В задаче зафиксированы четыре ловушки, каждая из которых даёт зелёные критерии
приёмки и сломанный инструмент: подстановка обязана быть видимой числом (иначе
опечатка `contoller` перестаёт ловиться — а ловил её именно этот отказ); `docs status`
обязан разрешать шаблон так же, как `materialize` (иначе документ вечно `stale`
и агент шага 3 переписывает его каждый прогон); `template_ref` обязан указывать
на применённый шаблон, а не на несуществующий; `test_skeletons_match_rule_templates`
надо исключить `default` явно, а не ослаблять до «подмножества».

Из «Что НЕ входит в этот план» убрана строка про поставку шага 2 отдельным заходом —
она сделана.

947 тестов (+6).

---

## M12 — базовый шаблон: подстановка вместо отказа ✅

Создано: `templates/default.md`, `template.resolve_template`.
Изменено: `plan.py`, `build.py`, `apply.py`, `templates/README.md`, `docs/materialize.md`.

### Что было

Неизвестный `template` попадал в `_blocking_errors`, а те отменяют прогон целиком:
не записывается ни одного документа. Для настройщика на чужом репозитории это худший
из возможных ответов — первое же своё правило классификации приносит новый `template`.

### Разрешение шаблона живёт в одной функции

`resolve_template(name, templates)` возвращает три исхода: запрошенный скелет, `default`,
`None`. Вызывают её и план, и `build.template_refs`. Это не про экономию строк:
разойдись разрешение в двух местах — документ, созданный по базовому скелету, сверялся
бы с другим и оставался бы `stale` навсегда, а агент шага 3 переписывал бы его на каждом
прогоне. Тест проверяет это прогоном (`materialize` → `materialize` → `docs status`),
а не чтением кода.

`None` — отказ, а не подстановка «ничего»: без `default.md` поведение прежнее,
блокирующая ошибка, не записано ничего. Сообщение теперь называет причину и лечение.

### Подстановка обязана быть видимой числом

Отдельный раздел отчёта с перечнем: какой `template` не нашёлся, сколько узлов его
получили. Довод тот же, что у классификации: подстановка — решение по умолчанию,
а не покрытие. Невидимая, она превращает `template: contoller` с опечаткой из отказа
прогона в молча документированный вид сущности — а ловил опечатку именно этот отказ.

Считается по тому, что прогон действительно делает: при `--team` чужие узлы не пишутся,
и их подстановки в отчёте были бы шумом. Порядок — по убыванию количества, при равенстве
по имени, явным ключом.

### Мелочи, каждая из которых предсказана в плане

`template_ref` указывает на применённый скелет (`templates/default.md`), а не
на запрошенный: иначе агент пошёл бы читать несуществующий файл. Поле `template`
при этом остаётся запрошенным — это факт классификации, и подменять его нельзя.

`test_skeletons_match_rule_templates` исключает `default` явно (`- {DEFAULT_TEMPLATE}`),
а не ослаблением до подмножества: ослабление перестало бы ловить забытый скелет,
ради чего тест и написан. Добавлен тест, что в базовом скелете нет слов «контроллер»,
«сервис», «workflow» и прочих — он применяется к тому, о чём инструмент ничего не знает,
и упомяни он вид сущности, документ утверждал бы непроверенное.

Золотое дерево не изменилось ни на байт: у всех шести узлов шаблоны есть.
В поставку `default.md` попадает сам — `install.sh` копирует `templates/*.md` глобом.

955 тестов (+8).

---

## B05 — разрешение якорей и `business_hash` ✅

Создано: `docpipe/business/{resolve,fingerprint}.py`, `tests/business_support.py`,
`tests/test_business_resolve.py`, `tests/test_business_hash.py`.
Изменено: `docpipe/business/__init__.py`, `docs/business-implementation-plan.md`.

### Три расхождения плана с реальностью, все исправлены в плане

**1. `JOBDISABLED` в хэше противоречил ловушке 8.** Таблица состава хэша требовала
включить его, а решение «флаги состояния из реестров не читаются» — не читать вовсе.
`registries.example.yaml` его и не читает, то есть противоречие было внутри плана.
Победило решение: флаг — политика поставки и состояние боевой БД, которой инструмент
не видит. Читать его только ради хэша значило бы завести второй, негласный источник
истины про то же самое.

**2. Форма типа в манифесте отсутствует, и вычислить её нельзя.** Строка `type`
требовала «отсортированные (имя на проводе, нормализованный тип, обязательность)».
Проверено на парсере: `record Money(decimal Amount, string Currency)` даёт **ноль**
членов — форма лежит в списке параметров, а не в объявлениях свойств. Реализация
«по свойствам» дала бы пустую форму у каждой записи, одинаковый хэш у всех сразу
и изменение контракта, прошедшее незамеченным. Это ровно ловушка 14, только с той
стороны, с которой её не ждали. `type` теперь разрешается ступенью 3 и вносит
в хэш одну свою идентичность; строка вернётся вместе с отложенным `shape_hash`.

**3. Словари видов расходятся, и моста в плане не было.** Аналитик пишет `table`
и `kafka`, реестр объявляет `list` и `kafka_topic`. Без явной таблицы такой якорь
просто никогда не разрешался бы, и выглядело бы это как «инструмент не нашёл»,
а не как «слова разные». Мост — `resolve.REGISTRY_KIND`, и на него есть тест:
`table` разрешается, `list` — нет.

### Неразрешённый якорь входит в хэш

Не было в критериях, но следует из состава: если пропажу записи из реестра из хэша
выкинуть, исчезновение точки входа даст `skip`. Самое важное изменение оказалось бы
единственным незаметным. Поэтому в хэш идёт идентичность якоря плюс факты, и пустые
факты — тоже значение. Обратная сторона: если реализация grid-сервиса перестанет
документироваться вовсе (исключена, а не переклассифицирована), список методов станет
пустым и хэш изменится. Это верный сигнал, а не ложный: контракт перестал быть виден.

### Ступень 3 видит меньше, чем называется

«Индекс символов» в манифесте не сохраняется — он строится при `scan`. Поэтому
ступень видит только задокументированные типы, и «не разрешён» здесь означает
«не найден среди узлов документации». Та же ловушка, что в B03, и сообщение
сформулировано так же: диагноз «типа нет» ставить нечем.

### Литеральная ступень пропускается, а не даёт пустой результат

Для `workflow`, `job`, `grid_service`, `list_event`, `workflow_step` и `table`
ступень 2 не запускается вовсе: `tried` для них равен `["реестр"]`. Разница видна
в диагностике линта — «пробовали и не нашли» и «не пробовали, потому что искать
нечего» это разные сообщения, и второе не должно выглядеть как неудача.

### Тесты правят копию дерева реестров

Каждая пара «должно и не должно» вносит правку в `shutil.copytree` фикстуры
и перечитывает реестры целиком: проверяется цепочка «файл → читатель → якорь →
факты → хэш», а не одна функция. Помощник `edit` работает байтами (у
`Items-Workflows.xml` есть BOM, снимать его нельзя) и заменяет **только первое**
вхождение: `<Fields>` встречается у каждого списка, и замена всех вхождений правила бы
заодно соседний список — тест продолжал бы проходить, проверяя не то, что написано
в его названии.

Помощники вынесены в `tests/business_support.py`, а не в `conftest.py`: тот весь
про .NET, и складывать туда бизнес-слой значило бы связать его с разбором C# ровно
там, где план требует обратного.

983 теста (+28).

---

## B06 — `docpipe business lint` ✅

Создано: `docpipe/business/lint.py`, `tests/test_business_lint.py`.
Изменено: `docpipe/cli.py`, `docpipe/config.py`, `docpipe/business/catalog.py`,
`tests/business_support.py`, `docs/business-implementation-plan.md`.

### Главная правка плана: «код 1 при находках» делал линт бесполезным

План требовал ронять прогон на любой находке и одновременно печатать отчёт
о непокрытых точках входа. Вместе это означает ненулевой код на любом репозитории,
где каталог не дописан, — то есть всегда, и в первый день особенно. Красный
с первого дня линт выключают на второй, и вместе с ним пропадают шесть проверок,
которые работают.

Правило, по которому проведена граница: **находка роняет прогон, только если её может
починить автор документа.** Якорь не разрешается, процесс без входа, неоднозначная
версия, необъявленная возможность, расхождение команд, битый идентификатор — код 1.
Сколько точек входа ещё не описано и какие записи реестров не нашли своего узла —
состояние работы, а не дефект: печатаются всегда, код возврата не меняют, пока
не названы в `--fail-on`. На фикстуре это видно прямо: находок восемь, код 0.

### Диагноз «мёртвая запись реестра» поставить нечем

Проверка 2 требовала резолва `impl_fqn` «в индексе символов». Индекса нет: он
строится при `scan` и не сохраняется — ровно то, что уже отмечено ловушкой в B03,
только там из этого сделали вывод «диагноз поставит B06», а B06 нечем.

Отличить «класса нет» от «класс не документируется» по манифесту нельзя. Поэтому
проверка называется `registry-unlinked` и говорит ровно то, что знает: «не найден
среди узлов документации». На фикстуре она срабатывает на двух записях, и обе
легальные: `BlockFilesService` и реализация `ILoadLimitsJob` намеренно оставлены
без документации. Назови их линт мёртвыми — первое же, что сделал бы настройщик,
это удалил бы рабочий сервис из `services.config`.

Настоящий диагноз появится, когда индекс FQN начнёт сохраняться. Это правка шага 1:
новое поле манифеста и версия схемы. Делать её внутри B06 значило бы протащить
изменение формата под видом линта, поэтому она записана в план как отдельная работа.

### Одна причина — одна находка

Неоднозначный якорь (`SampleWorkflow` без версии) не разрешается, и наивная
реализация выдала бы про него две строки: «не разрешён» и «неоднозначная версия».
Вторая строка чинить не помогает, а количество находок перестаёт быть количеством
проблем. `Resolution.candidates` отличает одно от другого, и проверка 1 такие якоря
пропускает.

### Текст про необъявленную возможность собирается одной функцией

`catalog.capability_error` вызывают и загрузка каталога, и линт. Иначе два текста
про одно и то же разошлись бы на первой правке, и линт показывал бы находку,
которой нет в списке ошибок каталога. Заодно это единственный надёжный способ
отделить проверку 4 от проверки 6, не разбирая строки регуляркой.

### Команда записи реестра известна не всегда, и это сказано вслух

`@team` объявлен только в `services.config`. Для джобов, workflow и обработчиков
событий владелец берётся из `ownership.yaml` по реализации, а без правил владения
его нет вовсе — в срезе такие записи идут строкой `(не задан)`, а не пропадают.
Пустой срез выглядел бы как «ничьих нет».

### Мелочи

Ключи `registries` и `business_root` добавлены в `DocpipeConfig`: команду зовут
часто, и повторять четыре пути руками никто не станет. `business_root` — параметр,
а не константа, по причине из плана: на АС CF каталог заведут внутри `docs/ml/docspipe`
и потом вынесут.

В тестах появился `combined_tree`: фикстуры реестров и каталога лежат в разных
каталогах, а команде `--root` один на всё, и проверять её надо в такой же раскладке,
а не в удобной тестам.

1003 теста (+20).

---

## B07 и B08 — скелеты, `business new`, `business build`, генерируемый блок ✅

Создано: `docpipe/business/build.py`, `templates/business/{process,entity,capability}.md`,
`templates/business/README.md`, `templates/business/examples/process.md`,
`tests/test_business_materialize.py`, `tests/test_business_build.py`.
Изменено: `docpipe/cli.py`.

Сделаны одним заходом: `build.py` и есть то, что собирает документ, и писать
для B07 временный генерируемый блок значило бы выбросить его через задачу.
Критерии приёмки обеих задач проверяются своими файлами тестов.

### Настоящая ошибка: зеркало `business_hash` ломало чтение документа

Первая версия писала текущий хэш в `docpipe:` рядом с остальной проекцией —
по образцу шага 2, где так лежат `signature_hash` и `impl_hash`. Прогон прошёл,
файлы записались, и следующий же `build` показал `1 validation error for
BusinessDoc: business_hash`. `BusinessDoc` объявлен с `extra="forbid"`, и загрузка
падала на собственном выводе инструмента: каталог из трёх документов стал каталогом
из нуля, а три строки ошибок ушли в stderr.

Поле можно было бы добавить в модель, но правильный ответ — не писать его вовсе.
Единственный хранимый хэш — **принятый**, и он лежит в `docpipe_state`. Зеркало
текущего завело бы второй источник, который обязан отстать: документ правят руками
чаще, чем пересобирают. `accept`, взявший значение оттуда, зафиксировал бы устаревший
хэш, и документ навсегда остался бы `current`, будучи `drifted`, — ровно та ловушка,
которая в плане описана для B10. Пересчитать хэш дешевле, чем сверять два.

### `build` не создаёт документов

Скелет в `compose` не передаётся вовсе: функция пересобирает документ поверх его
текущего текста. Создание — отдельное действие (`business new`).

Это то же решение, из-за которого у бизнес-слоя нет статуса `orphan`:
бизнес-документы создаются людьми, а не выводятся из кода. Совмести создание
со сборкой — и каталог начал бы расти сам, а `build` перестал бы быть безопасной
операцией, которую можно звать в CI.

### Один сборщик входных данных на все команды

`_business_context` читает манифест, реестры, каталог и владение один раз и отдаёт
`BusinessInputs`. Первая версия `lint` собирала это сама, и при добавлении `build`
получилось бы две сборки: разойдись они в наборе реестров — `build` собирал бы
документ по одному набору, а `status` сравнивал бы с другим, и документ навсегда
остался бы «изменившимся». Ошибка при этом выглядела бы как дефект хэша.

### Мелочи генерируемого блока

Разделы идут фиксированным порядком, раздел без данных печатается со словом «Нет.»:
пропущенный раздел читается как «инструмент не умеет», а «Нет.» — как факт.

Агрегат состояния workflow выводится из реестра, поэтому объявленный `contracts`
с тем же `ref` не повторяется — одна и та же строка дважды выглядит как две
разные вещи. Направления печатаются по-русски: `state` в тексте документа для
аналитика не значит ничего.

Без правил владения шаги не пропадают из раздела «Участники», а идут строкой
`(не задан)`. Пустой раздел выглядел бы как «шагов нет», то есть как факт о процессе,
хотя это факт об отсутствии настройки.

### Что осталось незакрытым

`templates/business/**` в поставку АС CF **не входит**: `install.sh` копирует
`templates/*.md` и `templates/examples/*.md`, подкаталога `business` в глобе нет.
На работу шага 2 это не влияет (обход каталога скелетов не рекурсивный, и
`load_templates` подкаталог не видит), но `business new` на боевом репозитории
не найдёт скелета. Чинится в B11 вместе с остальной поставкой.

1041 тест (+38).

---

## B09 — обратные ссылки в технических документах ✅

Изменено: `docpipe/materialize/build.py`, `docpipe/cli.py`,
`tests/test_materialize_build.py`, `tests/test_materialize_cli.py`,
`tests/test_business_build.py`, `docs/business-implementation-plan.md`.

### Граница пакета держится тем же способом, что и с .NET

`docpipe/materialize/**` не импортирует `docpipe/business/**`. Обратный индекс
«узел → бизнес-документы» строится в `cli._with_business_links` и кладётся
в `BuildContext` как данные. Проверяется обходом AST — тем же, что запрещает
импорт `docpipe.dotnet`; общая часть двух проверок вынесена в `_imports_of`.

Импорт проверил бы только то, что модуль загружается, а не то, что зависимости нет.

### Отдельное поле `business_root` вместо «непустого индекса»

Раздел «Бизнес-контекст» появляется, только когда каталог задан. Отличать это
по непустому индексу нельзя: тогда раздел возникал бы у всех узлов сразу при
первом же бизнес-документе в репозитории — то есть структура технического
документа менялась бы от появления чужого файла. Поэтому в контексте два поля:
`business_root` отвечает на «каталог задан», `business_links` — на «этот узел
упомянут». Узел без упоминаний получает «Нет.», а не отсутствие раздела.

### Неготовый бизнес-слой не роняет шаг 2

Реестры могут быть описаны раньше, чем появится первый бизнес-документ, а каталог
может быть временно битым. Отказ материализовать техническую документацию из-за
этого был бы наказанием не за то: шаг 2 самостоятелен, бизнес-слой к нему
пристёгивается. Ошибка идёт строкой в stderr, раздел не собирается, остальное
пишется.

Золотое дерево не изменилось: `business_root` по умолчанию `None`.

1047 тестов (+6).

---

## B10 — `business status` и `business accept` ✅

Создано: `docpipe/business/status.py`, `tests/test_business_accept.py`.
Изменено: `docpipe/cli.py`, `docpipe/business/build.py`,
`docs/business-implementation-plan.md`.

### Статуса `missing` у бизнес-слоя нет

План называет его среди статусов, но взяться ему неоткуда: документ здесь **и есть
файл**. Каталог собирается обходом `.md`, поэтому «файла нет» означает «документа
нет», а не «документ потерян». Чтобы сообщить о ненаписанном, понадобился бы второй
источник истины о том, какие документы должны существовать, — ровно то, от чего
отказались решением «каталог — это множество файлов».

Отвечает на этот вопрос отчёт линта о непокрытых точках входа: он считается
по реестрам, то есть по единственному источнику, который действительно знает,
чего не хватает.

### `broken` означает неразрешённый якорь, а не нечитаемый документ

Документ, который не разбирается, в каталог не попадает вовсе и виден проверкой
`catalog` в линте. Поэтому `broken` здесь занят другим смыслом — «точка входа
не найдена», — и это не совмещение, а разделение: «не нашли вход» и «состав
изменился» требуют разных действий. Смешай их в одном статусе, и по отчёту нельзя
будет понять, чинить реестр или переписывать текст.

### Приёмка фиксирует факты, а не хэш

`accepted.entry` — снимок разрешённых фактов, а не копия объявленных якорей. Из него
`status` считает разницу словами: «добавлен шаг NotifyStep», «шаг A: переход был 'B',
стал 'C'». Хранить один хэш было бы дешевле, но тогда отчёт сообщал бы «состав
изменился» и два шестидесятизначных числа, между которыми читателю предлагается
догадываться. По разнице фактов сразу видно, надо ли вообще править текст.

### Найдено тестом: отсутствие поля не равно пустому списку

Первая версия разницы сравнивала `before` и `after` через `isinstance(..., list)`
у обоих. Категория фактов, которой раньше не было (у списка появились поля, у события
— сборки), падала в ветку «было None, стало [...]» и печаталась дампом питоновского
списка — ровно в тот момент, когда сообщение нужнее всего. Отсутствие поля теперь
равно пустому списку.

### Формы слов согласованы таблицей, а не склейкой окончаний

«добавлен поле» в отчёте выглядит как дефект инструмента, а не как описание
изменения, и на такой строке перестают доверять всему отчёту. Пары «добавлен шаг /
убран шаг», «добавлена сборка», «добавлено поле» заданы явно.

### Отказ принимать ненаполненный документ

Приёмка означает «текст сверен с реализацией», а в пустом документе сверять нечего.
`--force` оставлен для документов, описанных целиком в чужой системе: у них заполнен
`external_ref`, и собственного текста в них не будет никогда.

1065 тестов (+18).

---

## B11 — документация и поставка бизнес-слоя ✅

Создано: `docs/business-layer.md`, `business.example/**`,
`deploy/cashflow-docspipe/registries.yaml`.
Изменено: `README.md`, `CLAUDE.md`, `CASHFLOW.md`, `docs/materialize.md`,
`docs/materialize-implementation-plan.md`, `docs/business-implementation-plan.md`,
`deploy/install.sh`, `deploy/cashflow-docspipe/docpipe.yaml`, `docpipe/cli.py`,
`tests/test_deploy_bundle.py`, `tests/test_business_materialize.py`.

### Три дыры в поставке, каждая из которых давала отказ на пустом месте

Проверялось прогоном установки, а не чтением скрипта.

1. **`registries.yaml` в поставку не входил** — `anchors` и `business` отказывались
   работать с «Реестры не заданы», и настройщику пришлось бы писать описание четырёх
   реестров с нуля, хотя оно готово и лежит в `registries.example.yaml`.
2. **Скелеты `templates/business/**` не копировались.** Причина неочевидная и та же,
   ради которой каталог скелетов шага 2 обходится не рекурсивно: глоб
   `templates/*.md` подкаталог не берёт. `business new` отказывал со «Скелет
   не найден», и по сообщению виден путь, но не видно, что виновата поставка.
3. **Ключей `registries` и `business_root` не было в конфигурации поставки** —
   команды пришлось бы звать с путями руками.

Тест поставки проверяет получившееся дерево: `registries.yaml` на месте, скелеты
бизнес-слоя на месте, **и набор скелетов шага 2 при этом не изменился** —
подкаталог в него попасть не должен.

### Найдено прогоном: `business new` искал скелеты не там

Каталог был задан константой `templates/business` относительно текущего каталога.
В репозитории разработки это работает, потому что команды и так зовутся из корня;
в установке инструмент лежит не в корне сканируемого репозитория — и по умолчанию
искалось бы несуществующее. Ровно та ловушка, из-за которой путь к шаблонам шага 2
обязателен в конфигурации.

Теперь путь выводится из ключа `templates` плюс `business`, а сообщение об отказе
называет и ключ, и флаг. Проверяется двумя тестами: скелет находится через
конфигурацию с нестандартным `templates`, и сообщение называет причину.

### Заодно закрыто то, с чего начался заход

Команда в `docs/materialize.md` — `docs owners MANIFEST --ownership ownership.yaml` —
не могла сработать никогда: файла `ownership.yaml` в репозитории нет и не будет,
есть `ownership.example.yaml`. Сообщения об отказе при этом молчали о главном:
что владение **необязательно** и что файл заводится копией примера.

Исправлено и то и другое. В разделе про владение теперь сказано прямо, на что оно
влияет (поле `team`, строка в блоке, колонка в отчёте, фильтр `--team`) и на что
не влияет (состав документов, пути, статусы), и показан порядок настройки через
`--explain` и `--lint`. Три сообщения об отказе называют файл, причину и лечение;
чтение манифеста отделено от чтения правил, поэтому битый манифест больше
не выглядит проблемой владения.

### Образец каталога

`business.example/` — два документа и дерево возможностей. Нужен по той же причине,
что и `templates/examples/`: формат, описанный в справочнике, и формат, который
видно глазами, — разные вещи по цене освоения. Каталог называется `.example`,
потому что автоматически не подхватывается: путь всегда задаётся `business_root`.

1069 тестов (+4).

---

## Раскладка документов: `doc_layout`, по умолчанию `kind-first` ✅

**Что просили.** Собрать документы одного вида в общий каталог:
`docs/modules/gridservices/Sbt.Cashflow.Grid.Services.AutoConclusion/auto-conclusion.md`
вместо `docs/modules/Sbt.Cashflow.Grid.Services.AutoConclusion/gridservices/…`.
С оговоркой: «теоретически может возникнуть дублирование, придумать правило».

**Правило не понадобилось, и это главный результат захода.** Обе раскладки —
перестановка одной и той же тройки (модуль, вид, slug). Путь в каждой из них
однозначно определяется тройкой, поэтому множество узлов, спорящих за один файл,
у них **общее**: два узла конфликтуют в одной ровно тогда, когда конфликтуют
и в другой. Разводит их один и тот же хэш-суффикс от `id`, написанный в T15.

Замерено, а не выведено: на ABP (694 узла, 671 проект) обе раскладки дают 666
различных путей, 19 конфликтующих путей, 47 узлов в конфликте — цифры совпадают
до узла. На eShopOnWeb и OpenTelemetry конфликтов нет ни в одной.

Отдельно проверен опасный на вид случай — модуль, названный как вид (`Services`).
Он ни во что не упирается: уровни в пути не смешиваются, имя модуля всегда стоит
на своём уровне, вид — на своём.

**Ошибка в старых записях, из-за которой вопрос вообще возник.** В
`docs/findings-eshoponweb.md` стояло, что модульная раскладка «разводит дубли
автоматически» и это «подтверждает выбор модульного дерева», а раскладка по видам
дала бы массовые коллизии. Первая половина верна, вторая — нет. Дубли разводит то,
что имя модуля **входит в путь**, а не то, каким сегментом оно стоит. Массовые
коллизии дала бы раскладка без модуля (`docs/{kind}/{slug}.md`), которую никто
не предлагал. Запись исправлена, потому что иначе следующий заход снова упрётся
в несуществующий риск.

**Что сделано.** `doc_layout` — параметр конфигурации со значениями `kind-first`
(по умолчанию) и `module-first`; расчёт пути вынесен в `tree.doc_path_for`.
Параметром, а не константой, по той же причине, что и `business_root`: выбор
между раскладками — вопрос того, как команда читает дерево, а не корректности,
и менять его правкой кода дорого.

**Плата ровно одна, и её нельзя обойти.** Модуль перестал быть префиксом пути,
поэтому `docs status МАНИФЕСТ docs/modules/Sample.Common` больше не выбирает
проект целиком: его документы разложены по каталогам видов. Внутри вида префикс
работает (`docs/modules/controllers/Sample.Common`). Зафиксировано тестом как
поведение, а не обойдено. Владения это не касается: оно считается по путям
исходников, а не по `doc_path`.

**Второе следствие, менее очевидное.** Относительные ссылки между документами
разных видов стали на уровень длиннее (`../services/x.md` →
`../../services/Модуль/x.md`), между документами одного вида — короче.
Генерируемые блоки пересобираются сами, поэтому в глаза это не бросается,
но золотое дерево `tests/golden/docs/` пришлось перегенерировать целиком.

**Миграция уже написанного дерева.** Смена значения меняет `doc_path` у всех
узлов сразу — единственный случай, когда переезжают все документы. Механизм M08
справляется без единой правки: `node_id` лежит во front matter каждого документа,
поэтому сопоставление точное (`exact`), ни один документ не пересоздаётся заново.
Проверено сквозным прогоном на `SampleSolution`: 6 переносов, авторский текст цел
во всех пяти секциях, повторный прогон — «без изменений: 6».

Два места, на которых легко обжечься, поэтому они в `docs/materialize.md`:

- принятый документ после переезда встаёт в `relocated` и просит пересмотра.
  Для переезда из-за смены кода это правильно, для смены раскладки — шум. Снимать
  пометку нужно списком переехавших, **не** `docs accept --all`: приёмка отклоняет
  ненаполненные документы целиком, поэтому на дереве, где хоть один документ ещё
  не написан, `--all` не примет ничего, включая те, что только переехали;
- опустевшие каталоги перенос не удаляет — `find docs -type d -empty -delete`.

**Трудность.** 54 упавших теста после смены умолчания — почти все от захардкоженных
путей вокруг золотого манифеста. Разбирать их пришлось по одному, потому что часть
путей в тестах намеренно выдуманная (симуляция переклассификации), и механическая
замена сделала бы такие тесты бессмысленными, оставив зелёными.

1077 тестов (+8): раскладка обоих видов, инвариантность коллизий на наборе,
где конфликты есть на самом деле (на фикстурах их не случается ни одного, и тест
на них проходил бы вхолостую), и три теста миграции дерева.

---

## Фикстура: два обработчика на одной паре «список + `EventType`» ✅

Пришло вопросом с настройки на боевом репозитории: у команды один свой
`EventReceiver`, но на списке их много, — и было непонятно, как якорь
`UserTasks/ItemAdded` вообще может сойтись на нужном.

Ответ по механике оказался верным (ключ — пара, а не список, обработчики
с другим `EventType` не мешают), но при проверке выяснилось, что случай
**двух подписчиков на одной паре** в фикстурах не воспроизведён вовсе,
хотя в АС CF он встречается. В `tests/fixtures/registries/Structure.xml`
на `UserTasks + ItemAdded` теперь два обработчика из **разных** сборок.

### Зачем именно так

Первым в файле лежит обработчик из сборки, которая при сортировке окажется
второй. Иначе утверждение «порядок записей в XML на `business_hash` не влияет»
проверялось бы на уже упорядоченных данных и не проверяло бы ничего.

### Что оказалось зелёным и бессмысленным

`test_list_event_collects_assemblies_of_all_handlers` — название обещало
«всех обработчиков», а фикстура давала одного, и тест утверждал список
из одной сборки. Теперь их две, и сортировка проверяется.

`test_event_receiver_gives_list_event_class_and_assembly` и
`test_event_receiver_resolves` брали запись через `next()`. С одной записью
это работало; с двумя `next()` берёт произвольную, и утверждение про сборку
проверяло бы порядок записей в XML, а не чтение реестра. Оба переписаны
на проверку обеих записей — второй тест теперь ещё и фиксирует, что один
подписчик резолвится в узел, а второй нет, и это нормальное состояние.

### Что изменилось в числах

Инвентаризация считает **записи реестра**, а не различимые пары: точек входа
стало 11 вместо 10, `list_event` — 4 вместо 3, покрытых одним документом — 5
вместо 4. Это не дефект счёта, но и не то, что человек назовёт «одиннадцать
точек входа»: пара `UserTasks + ItemAdded` для платформы одна, подписчиков
у неё двое. Решено оставить как есть и назвать вслух — менять семантику
счётчика ради красивого числа значило бы разойтись с отчётом о покрытии,
который считается по тем же записям.

Генерируемый блок при этом различает одно и другое сам: в «Точках входа»
пара — одна строка, в «Реализации» — по строке на подписчика. Раньше это
нигде не проверялось; теперь проверяется
(`test_all_handlers_of_one_event_are_printed`).

### Добавлено

`test_repeated_event_type_gives_two_records` — читатель не схлопывает записи
по `ref`; `test_two_handlers_on_one_pair_are_not_ambiguity` — исключение
из правила «несколько записей на якорь = неоднозначность» работает именно
для обработчиков события; `test_handler_leaving_the_pair_changes_hash` —
ушедший подписчик меняет `business_hash`, при этом якорь продолжает
разрешаться реестром, а не «пропадает».

1081 тест (+4 добавлено, 7 переписано).

---

## `anchors which`: обратный поиск от типа к якорю ✅

Пришло тем же вопросом с настройки, что и фикстура с двумя обработчиками, только
сформулированным иначе: «я знаю, что это мой EventReceiver, — как это должно
сойтись с `entry`». Оказалось, что механика была объяснена верно, а инструмент
на этот вопрос не отвечает вовсе.

### Чего не хватало

`anchors list` идёт реестр → код. `anchors explain` ищет совпадение только
по `ref` и по строке показа: запрос по FQN давал «Якорь не найден», код 1.
Пути «класс → якорь» не было ни одного, хотя данные для него уже посчитаны —
`AnchorTarget` несёт `fqn`, `node_id` и `doc_path`. На боевом репозитории это
означает просмотр сотен записей глазами.

### Три решения, которые неочевидны

**Ищутся вложенные записи.** Шаг workflow на верхний уровень не поднимается
(`workflow_step` не входит в `ENTRY_KINDS`), а команда чаще владеет шагом, чем
процессом целиком. Искать только по верхнему уровню значило бы не отвечать
на самый частый вопрос. Соседи при этом считаются в том же наборе, откуда
пришёл кандидат: у поднятого обработчика события это верхний уровень, у шага —
дети своего workflow. Единый перебор не нашёл бы вторых вовсе.

**Печатается готовый кусок `entry`, а не описание.** Строка показа собрана для
человека и обратно не разбирается — требовать от аналитика собрать её в поля
значит воспроизвести ровно ту ошибку, ради которой строку и не разбирают.
Вид в сниппете переводится в словарь каталога через `ANCHOR_KIND_BY_REGISTRY`,
построенный обращением `REGISTRY_KIND`: два словаря руками разошлись бы, и
команда предлагала бы вид, которого каталог не знает. Порядок ключей — как
в модели `Anchor`, иначе первый же `business build` переставил бы строки и
человек решил бы, что вставил не то.

**«Не найдено» — код 0.** Точка входа есть не у всякого типа; код 1 объявлял бы
нормальное состояние почти всего дерева дефектом.

### Что нашлось при проверке

Сниппет для шага workflow, объявленного в нескольких версиях, заведомо
не работает: без `version` линт даёт `ambiguous-version`, а `version` у записи
шага нет (известная дыра, `registry/anchors.py:_anchor`). Печатать его молча
значило бы выдать нерабочий кусок за готовый к вставке — ровно та ошибка,
которую команда должна убирать. Такой случай теперь называется вслух
предупреждением с указанием проверки линта.

Тест на сниппет сделан кругом: он разбирается как YAML, превращается в `Anchor`
и разрешается реестром. Проверка «строка совпадает с ожидаемой» подтвердила бы
формат, но не работоспособность.

### Убрано

Поле якоря `member` удалено из модели и из справочника: оно было объявлено
и задокументировано, но не использовалось нигде — ни в разрешении, ни в хэше,
ни в выводе. Задокументированное поле, которое молча ничего не делает, рано
или поздно заполнят и будут ждать эффекта. Модель `extra="forbid"`, поэтому
оставшийся в чьём-то документе ключ станет явной ошибкой каталога, а не
тихо проигнорируется.

1093 теста (+12).

---

## Селектор `only`: своя часть общей точки входа ✅

Продолжение разговора про двух подписчиков на одной паре. Механику мы описали
верно — якорь адресует контракт, а не класс, — но у неё три последствия,
которые для команды, владеющей одним обработчиком из двух, выглядят дефектами:
в её «Реализации» стоит чужой класс, чужая подписка меняет `business_hash`
и гонит документ в `drifted`, а раздел «Участники» для событий не считается
вовсе.

### Что выбрано и почему не FQN

Обсуждались пять вариантов селектора. Отвергнут FQN в якоре — он ломается
от переименования класса, то есть ровно от того, ради защиты от чего слой
и строился. Взяты два: `assembly` (единица поставки, переживает переименование,
работает без настройки) и `team` (переживает ещё и переезд между сборками,
но требует `ownership.yaml`). Рекомендация в руководстве — `assembly` сейчас,
`team` после заполнения правил владения.

Ключ ровно один из двух: два селектора — два источника истины о том, какая
часть общей точки входа наша, и разойдутся они на первом же переезде.

### Решения, которые легко сделать неправильно

**Сужение идёт до проверки на неоднозначность.** Иначе чужой подписчик
объявляется неоднозначностью в нашем документе, хотя наша запись одна.

**Промах селектора — отдельное состояние, а не `unresolved`.** Якорь на месте,
сузили в пустоту; чинится одной строкой. Находка `selector-empty` перечисляет,
кто сейчас на якоре, — без этого правку пришлось бы угадывать. Пришлось заодно
исключить промах из `ambiguous-version`: там `candidates` означают версии
workflow, а здесь — держателей якоря, и слитые вместе они послали бы автора
искать `version` там, где его не бывает.

**`only.team` без правил владения не сужает ничего и говорит об этом.** Молча
пустой набор неотличим от «наших записей на якоре нет».

**На покрытие сужение не влияет.** Пара остаётся покрытой целиком: иначе отчёт
потребует документ на чужой обработчик, а линт, красный с первого дня,
выключат на второй.

**`assembly` сравнивается точным равенством.** Префикс `Sbt.Cashflow.ML`
поймал бы и `Sbt.Cashflow.MLOps`, и заметили бы это не сразу.

### Трудность

`only.team` считается по реализации через `ownership.yaml`, а `ResolveContext`
правил владения не имел — они жили только на шаге 2 и передавались прямо
в `generated_block`. Пришлось протащить их в контекст разрешения, иначе
селектор влиял бы на показ, но не на `business_hash`, а без влияния на хэш
он бесполезен: смысл ровно в том, чтобы чужая правка не создавала нам работу.

На шаге 2 правила читаются молча (`_load_ownership_quietly`): нечитаемый файл
не должен ронять материализацию технической документации из-за селектора
бизнес-слоя. Скажет об этом `business lint`, где это и есть предмет разговора.

### Документация

Написаны два руководства, которых не хватало:

- `docs/entry-guide.md` — как заполнить `entry` от своего класса до рабочей
  ссылки: `anchors which`, что писать по видам, сужение, пять типовых ошибок,
  известное ограничение с версиями workflow;
- `docs/ownership.md` — справочник по `ownership.yaml`: восемь предикатов,
  слои приоритетов, порядок разметки снизу вверх, диагностика `--lint`.

Оба внесены в индексы `README.md` и `CLAUDE.md`.

1109 тестов (+16): сужение по сборке и по команде, промах с перечнем
держателей, отличие промаха от исчезнувшего якоря, порядок «сужение раньше
неоднозначности», отказы каталога на пустом и двойном селекторе и на `only`
при `verify: false`, отметка о сужении в собранном документе и главное —
что сужённый хэш не совпадает с общим.

---

## Диагностируемость: почему «Реализация: Нет.» стоила часов ✅

Разбор реального случая с боевого репозитория. Аналитик описал точку входа,
сузил её `only: {team: ml}`, а в `ownership.yaml` команда объявлена как `ML`.
Инструмент повёл себя формально правильно и практически бесполезно: селектор
не отобрал ничего, «Реализация» напечатала `Нет.`, и это выглядело как
оборванная связь бизнес-документа с технической документацией — то есть как
отказ главного требования всего слоя.

### Что было сделано не так

**Асимметрия проверок.** `load_ownership` отвергает правило с необъявленной
командой и перечисляет объявленные. Селектор такой проверки не имел вовсе.

**Сообщение печатало не то поле.** `selector-empty` перечислял держателей
якоря как FQN и сборку. Разницу `ml`/`ML` в таком перечне увидеть нельзя —
там команд просто нет.

**Сборка молчала.** `business build` знает, что ссылки не будет, в момент
сборки — и не говорил ничего.

**Отчёт линта тонул в инвентаре.** Находка была, но выше сотен строк про
непокрытые точки входа, до которых аналитику сейчас нет дела.

### Шесть правок

| Что | Зачем |
|---|---|
| проверка `unknown-team` | симметрия с загрузкой правил владения; перечисляет объявленные идентификаторы |
| команда держателя в `selector-empty` | ради разницы регистров, которую иначе не видно |
| предупреждения `business build` | три причины пустой «Реализации» различает инструмент, а не человек |
| `business lint --scope catalog` | отчёт только по своим документам |
| близкие имена в `anchors which` | опечатка в букву давала «не найдено» вместо «нашлось похожее» |
| селектор в `anchors explain` | `only` пишется копированием, а не наугад по перечню полей |

Регистр намеренно не приводится: `ml` и `ML` — два идентификатора, и склеив
их, мы завели бы два написания одного имени, из-за чего правило, написанное
в другом регистре, начало бы работать «иногда».

### Трудность

Тест на команду держателя не проходил, и это вскрыло настоящую конфигурацию:
правила владения нужны линту в **двух** местах — параметром (для
`team-mismatch`) и внутри `ResolveContext` (для `only.team`). Помощник тестов
передавал их только параметром, то есть проверял раскладку, которой в проде
нет. Исправлен помощник; заодно это единственное место, где две ссылки на один
объект могут разойтись, и оно теперь закрыто тестом.

### Проверено воспроизведением

Собран каталог с `only: {team: ml}` при объявленной `ML`. До правок — пустая
«Реализация» и молчание. После:

```
Внимание: …/quote.md: якорь list_event UserTasks/ItemAdded разрешён, но `only`
(команда ml) не совпал ни с одной записью — ссылки на технический документ
не будет. Сейчас на якоре: …PricingService (сборка …, команда ML)

unknown-team: 1
  …: `only.team` называет команду 'ml', которой нет в `ownership.yaml`;
  объявлены: ML
```

1123 теста (+14).

---

## `unless` в правилах отсева: вырез внутри решения ✅

Пришло вопросом с настройки: «в каталоге множество сервисов, документировать
надо только наследников `GridService`; поставил `inherits` в `rules`, поднял
приоритет, исключил каталог — исключается всё».

### Что оказалось непонятым, и это дефект объяснения, а не настройки

Приоритет не сравнивает исключающее правило с включающим. `classify` сначала
спрашивает `is_excluded` и при истине выходит, не дойдя до `rules`; `priority`
ранжирует **внутри** стадии — в отсеве выбирает, чья причина попадёт в отчёт,
в правилах выбирает вид. Ожидание «более высокий приоритет include перебьёт
exclude» не выполнимо в принципе, а из документации это не следовало.

Второе, менее очевидное: **чтобы тип не документировался, исключать его
не нужно вовсе.** Узел создаётся только при совпадении с правилом из `rules`
(`tree.py:366`). `exclude` существует ради отчёта — отличить «посмотрели
и решили» от «решения нет». Настройщик, пришедший из мира «запретить всё,
разрешить нужное», делает лишнюю работу и ломает себе документацию.

Обе вещи теперь написаны прямо: ловушкой в плане и разделом в README.

### Что добавлено

Поле `unless` у правила отсева: правило совпадает, когда `when` истинно
и `unless` ложно.

```yaml
    - id: grid.support
      reason: "Вспомогательные типы каталога сервисов"
      when:
        path_glob: ["src/Grid/**"]
      unless:
        inherits: ["GridService"]
```

Это единственная форма отрицания в движке. Общий `not` внутри дерева условий
не заводился и не заведён: довод про «систему уравнений, которую нельзя
прочитать построчно» остаётся в силе. `unless` от него отличается тем, что
привязан к одному правилу и описывает вырез из его группы — строка читается
сверху вниз целиком.

### Решения

**`unless` вычисляется только при истинном `when`.** Правило описывает группу,
исключение — вырез внутри неё; обратный порядок сделал бы `unless` вторым
условием отсева, а не выключателем.

**Причина остаётся одна на группу.** Вырез не заводит второго решения, поэтому
таблица «не документируем — по какому решению» от него не дробится.

**Пустой `unless` — отказ при загрузке.** «Исключений нет» и «правило
не дописано» выглядели бы одинаково, а вело бы себя оно как обычный отсев,
то есть тихо забрало бы типы, которые настройщик собирался сохранить.

### Проверено воспроизведением

Каталог из шести типов: два наследника `GridService`, сам базовый тип, опции,
расширения и хелпер.

```
  документируем            2  1 вид
  не документируем         4  1 решение
  решение не принято       0  решены все символы
```

Ровно то, чего добивался настройщик, и без регулярок с lookahead, которыми
это приходилось обходить.

1130 тестов (+7).

## Предикаты по контракту типа в правилах владения ✅

### Чего не хватало

Правила классификации умеют `attribute`, `base_type` и `inherits`, правила
владения — нет. Команда, которая опознаёт свои типы правилом «наследник
`MlApiControllerBase` — наш контроллер», обязана была выразить ту же границу
вторым способом, через `path_glob`. Два описания одной границы расходятся на
первом же файле, переехавшем в другой каталог, и расхождение ничем не
проявляется: документ просто уезжает не в ту команду.

Данные для предикатов лежали в манифесте с самого начала — `Symbol.attributes`
и `Symbol.base_type_closure` внутри `DocNode.symbol`. Отсутствие было решением
(«восемь предикатов, намеренно мало»), а не ограничением, и записанного довода
именно против этих трёх не существовало.

### Что сделано

`_matches_any_type` в `classify.py` стала публичной `matches_any_type`, и на неё
опираются оба набора правил. Своя копия в `ownership.py` означала бы, что одна
и та же строка условия значит в двух файлах разное: `base_type_candidates`
разбирает fluent-базы и квалифицированные имена нетривиально, и вторая
реализация разошлась бы с первой незаметно. Теперь условие переносится из
`rules/dotnet.yaml` в `ownership.yaml` буквально — на это есть тест, который
берёт условие правила `controller.aspnet` без единой правки.

Набор вырос до одиннадцати. `name_suffix`, `name_regex`, `type_kind`, `modifier`
и `has_member_with_attribute` не заведены: они описывают форму типа, а не
границу ответственности, а владение по имени уже выражается `fqn_prefix`.

### Ловушка, найденная проверкой, — не та, что ожидалась

Ожидалось: через исключённый в `docpipe.yaml` модуль наследование рвётся, и
правило по базовому типу оттуда не срабатывает. Прогон на `SampleSolution`
с исключённым `Sample.Common` показал точнее:

```
Sample.Pricing.Api.Controllers.PricingController
  прямые базы     BaseApiController
  замыкание       BaseApiController
  решение         решение не принято
```

Рвётся **транзитивная** часть замыкания. Прямая база уцелела — нерезолвнутая,
коротким именем, — а `ControllerBase`, приходивший из её объявления, потерян.
Поэтому правило по прямой базе продолжает работать, а правило по базе через
уровень молча перестаёт.

И проявляется это обычно раньше владения: та же база участвует в правиле
классификации, тип уходит в `undecided` и узлом не становится вовсе. В
`docs owners` его нет — владеть нечем, а выглядит как «предикат не работает».
Отсюда порядок проверки, записанный в справочник: сначала
`docpipe symbols --state undecided`, потом `docs owners --lint`.

Первая формулировка в `docs/ownership.md` и в плане была написана до прогона
и утверждала более грубое «правило не сработает»; исправлена по факту.

### Проверено воспроизведением

`SampleSolution`, владение по `inherits: ["BaseApiController"]` при правиле
на весь репозиторий с меньшим приоритетом:

```
rest      5
web       1
      80  web.controllers              → web ← победитель
      10  rest.all                     → rest
```

Граница прошла по базовому типу через границу проекта: `PricingController`
лежит в `Sample.Pricing.Api`, база — в `Sample.Common`. Ни `path_glob`,
ни `module_glob` так не умеют.

1137 тестов (+7).

---

## M13 — `docpipe worklist`: файл-очередь для внешнего исполнителя шага 3 ✅

### Зачем

Обход документов на шаге 3 живёт в другом проекте, и ему нужен файл, а не поток.
Содержимое такого файла уже отдавал `docs status --format json`, но как артефакт
для чужого процесса он не годился по двум причинам, и обе — его контракт, а не
дефект: `--action` сужал `counts` и `total` вместе со списком, поэтому сводка
по дереву и очередь к работе в один его вывод не помещались, и у вывода не было
конверта — внешний модуль не мог проверить ни версию формата, ни что читает файл
от того самого манифеста.

Дописать конверт туда — сломать её потребителей и запретить впредь менять
человекочитаемый вывод. Поэтому `docs status` осталась информационной командой,
а `worklist` стал версионированным артефактом.

### Что сделано

`docpipe/materialize/worklist.py`: `Worklist`, `WorklistEntry`, `WorklistTotals`
плюс `select_documents` и `build_worklist`. Команда `docpipe worklist` пишет файл
через `write_atomic`, схема — `docpipe schema --model worklist`.

Записи документов собирает **та же** функция, что и `docs status`: приватная
`_document_json` стала публичной `document_json`, `WorklistEntry` валидируется
из её результата. На это есть тест, сравнивающий обе выдачи запись в запись.
`extra="forbid"` держит связь: новое поле в `document_json` уронит валидацию
здесь, и о расхождении станет известно на тестах, а не у потребителя.

### Решения, о которых стоило бы спорить, если бы не довод

**Времени в файле нет.** С `generated_at` файл менялся бы на каждом прогоне: его
нельзя ни сравнить между прогонами, ни закоммитить без шума, а тест на детерминизм
превратился бы в сравнение с исключением полей. Свежесть даёт `manifest_sha256`,
время файла — файловая система. Тот же довод, по которому недетерминированное
шага 1 живёт в `doc-tree.run.json`.

**Пустая очередь — записанный файл с нулём записей.** Отсутствие файла означает
«прогон не состоялся». Внешнему модулю эти два состояния путать нельзя, а других
признаков у него нет.

**При блокирующих ошибках плана файл не переписывается.** Прежняя очередь
достовернее полупустой новой, а о коде возврата чужой процесс может и не узнать.

**Порядок — по приоритету статуса, затем по `doc_path`.** Отличается от
`docs status`, где сортировка только по пути. Без приоритета `--limit` резал бы
очередь по алфавиту и отдавал исполнителю `Api/…` вместо документа, которого
вообще нет.

### Ловушка, которую нашла реализация `--team`

Сводка обязана описывать дерево целиком, а `PlanOptions.teams` сужает **план**.
Передав `--team` внутрь `build_plan`, команда получила бы дерево без чужих узлов
и сводку по нему же — тихо и правдоподобно. Поэтому план строится без `teams`,
а команда отбирается уже среди готовых записей. Записано ловушкой в M13.

### `docs/modules` перестал быть литералом

Заодно вскрылось расхождение: `docs_root` в конфигурации был, но `doc_path_for`
в `tree.py` собирал путь из литерала `docs/modules`. Установив `docs_root: documentation`,
настройщик получил бы манифест с путями `docs/modules/**` и поиск существующих
документов в `documentation/**` — каждый документ навсегда `missing`, каждый
прогон переписывает их заново, и всё это без единого сообщения.

Префикс теперь собирается из пары ключей: `docs_root` и нового `modules_dir`.
Пара, а не один путь целиком, именно поэтому: при паре инвариант «пишем туда,
где ищем» держится структурно, одним значением его можно было бы развести.

Значения уходят в `doc_path` каждого узла, поэтому абсолютный путь, `..` и `\`
отвергаются при загрузке конфигурации, а не в `validate`: там уже поздно —
манифест записан и роздан.

### Проверено

`tests/test_worklist.py`, 20 тестов: конверт и версия схемы, `counts` по всему
дереву при суженной очереди, порядок и `--limit`, байт-в-байт на повторе,
отсутствие времени в файле, отказ не переписывает прежний файл, совпадение
записи с `docs status`, обе настройки путей и отказ на путях за корнем.

1157 тестов (+20).

## Ревизия конфигурации: мёртвый ключ, три сборки входа, немые расхождения

Разбор «какой ключ `docpipe.yaml` кем читается» начался с жалобы: `worklist`
пишет «в очереди документы, которых нет на диске», хотя `materialize` отработал
и документы лежат. Сам симптом объяснился не конфигурацией (см. ниже), но
разбор вскрыл семь мест, где ключ либо не доезжал до кода, либо доезжал дважды.

### Что нашлось

**`roots` не читался ничем.** Объявлен, разобран, покрыт тестом на разбор —
и ни одного потребителя. `parser-architecture.md` при этом писал «обход от
`roots`». Настройщик, сузивший обход этим ключом, не получил бы ничего и не
узнал бы об этом. Ключ доведён до `discover` и складывается с `--scope`:
сужения не заменяют друг друга, иначе флаг на один прогон отменял бы
настройку репозитория.

**Раскладка замерзает в манифесте, конфигурация читается заново.** `doc_path`
собирается на шаге 1 из `docs_root` + `modules_dir` + `doc_layout`. Правка
любого из трёх без повторного `scan` не ломала ничего видимо: документы
оставались на прежних путях и выглядели целыми, `docs status` показывал
нормальные статусы, а `worklist` клал в очередь `modules_root` из конфигурации
и сообщал внешнему исполнителю префикс, которого в очереди нет. Проверено на
раскладке боевого репозитория: `modules_dir` изменён на заведомо чужое
значение — `counts: {empty: 6}`, ни ошибки, ни предупреждения. Теперь это
блокирующая ошибка с обоими вариантами починки в тексте сообщения.

**Вход шага 2 собирался тремя копиями одного блока** — в `materialize`,
в `docs status` и в `_prepare`. Ключ, добавленный в одну, до остальных не
доезжал; писателем документов при этом был `materialize`, то есть та копия,
где отставание заметили бы последней. Осталась одна `_prepare`, возвращающая
`Step2Inputs` целиком.

**Цепочка «реестры → якоря» жила в трёх экземплярах** с тремя политиками
отказа. Политики различаются намеренно — шаг 2 не обязан падать из-за
неготового бизнес-слоя, — но сборка обязана быть одна: `read_anchors`.
Политику задаёт вызывающий.

**Команды `anchors` ключ `registries` не читали вовсе**, у `list` не было даже
`--config`. Один и тот же ключ работал в `business *` и молчал здесь.

**Проверку `--team` делал только `materialize`.** `docs status` и `worklist`
на опечатку молча сужали выборку до пустой и отвечали «документов нет» —
неотличимо от честного «у этой команды документов нет».

**`cache_dir` — единственный «путь прогона», склеиваемый с `--root`**, и
единственный без валидатора: `Path("/repo") / "/tmp/x"` даёт `/tmp/x`, то есть
абсолютное значение уносило кэш за пределы репозитория молча.

### Чего разбор НЕ объяснил

Исходный симптом. При запуске из корня репозитория с `--root`, равным корню,
обе базы отсчёта совпадают, и ни один путь разъехаться не может. Проверено
отдельно: `docs_root: docs/ml` вложен в дефолтный `docs`, а обход возвращает
пути относительно `--root`, поэтому даже забытый `--config` даёт обход шире,
а не уже, и документы всё равно находятся. Остаются пофайловые фильтры
`scan_docs` — их и называет `tools/why_missing.py`.

### Проверено

`tests/test_discovery.py` (+7): `roots` сужает обход и доходит до манифеста,
значение по умолчанию не сужает ничего, `roots` и `--scope` складываются,
валидаторы `cache_dir` и `roots`. `tests/test_materialize_plan.py` (+4):
расхождение раскладки роняет план и называет оба префикса. `tests/test_anchors_cli.py`
(+4): `registries` из конфигурации в обеих командах, приоритет флага, отказ
без обоих. `tests/test_docs_status.py` (+2): неизвестная команда в `--team`
отвергается в `docs status` и в `worklist`, очередь при этом не пишется.

1174 теста (+17). Семнадцать падений `test_deploy_bundle` и
`test_materialize_template` — предсуществующие и к ревизии отношения не имеют:
они требуют `templates/examples/`, а `examples/` стоит первой строкой
в `.gitignore`, так что в свежем клоне каталога нет.

---

## Фронтенд: аналитика перед планом (Angular) 🔎

Задача — включить Angular-часть АС CF в пайплайн: своя документация, явная связь
с контроллерами .NET, маппинг бизнес-документов на страницы, управление теми же
`docpipe.yaml` / `rules` / `registries` / `ownership`. Плана ещё нет; разбор —
в [`docs/frontend-analysis.md`](frontend-analysis.md).

Решения, зафиксированные сразу, чтобы их не пришлось принимать заново:

**Связь фронт↔бэк — якорь, а не ссылка на узел.** Ключ — нормализованная пара
`(HTTP-метод, маршрут)`. Идентификатор бэкенд-узла содержит путь `.csproj` и FQN,
то есть переименование контроллера рвало бы связь при неизменном HTTP-контракте.
Это тот же довод, по которому бизнес-слой не знает `node_id`.

**Приведение маршрута к нижнему регистру обязательно.** Маршрутизация ASP.NET Core
нечувствительна к регистру, и в примере владельца фронт пишет `getforupdate`, тогда
как метод контроллера почти наверняка `GetForUpdate` — при подстановке `[action]`
бэкенд объявит другой регистр. Без нормализации не найдётся **ни один** такой вызов.

**Преобразование URL интерцептором задаётся конфигурацией.** `FixUrlInterceptor` стоит
между сервисом и сетью; выводить его логику статически — значит писать интерпретатор
TypeScript и ошибаться ровно там, ради чего он писался.

### Проверено на грамматике

`tree-sitter-typescript` 0.23.2 с `tree-sitter` 0.26.0, файл с NGXS-стором, сервисом
на `HttpClient`, таблицей роутов и компонентом.

**Декоратор — сосед объявления, а не потомок, и это инверсия правила C#.** У
экспортируемого класса `decorator` лежит в `export_statement` рядом с
`class_declaration`; у неэкспортируемого — внутри `class_declaration`. В Angular
экспортируется всё, что имеет декоратор, поэтому реализация, перенесённая с .NET
буквально, не классифицирует **ни одного** компонента и сервиса — при нулевом числе
ошибок разбора. Собирать декораторы надо с обоих уровней.

**Списки захватов `QueryCursor.captures()` не выровнены между собой.** На одном запросе
`@method` пришёл в порядке `[innerDebtsSnapshot, get, post, …]`, а `@call` — в другом;
`zip` даёт пары, которых в коде нет. Существующий разбор C# этим не болен — он берёт
один ключ и спускается от узла, — но соблюдается это неявно, а новый парсер естественно
начинается с запроса на несколько захватов сразу.

**Шаблонная строка разбирается на `string_fragment` и `template_substitution`,** поэтому
«подстановка в начале строки — это база URL, а не параметр пути» — проверяемое правило,
а не эвристика.

Разбивка на F01–F15 и перечень того, что нельзя решить без разведки боевого
репозитория (в первую очередь `FixUrlInterceptor` и природа страниц — код или
`<Forms>` реестра), — в конце документа аналитики.

### Поправка владельца: маршрут в контроллере записан литералом

Первая версия §2.3 утверждала, что бэкенд объявит `api/ml/structure/GetForUpdate`
через подстановку `[action]`. Фактически контроллер пишет
`[HttpGet("getforupdate/{modelVersionId}")]` — литералом, и регистр совпадает с фронтом
сам собой. Правило приведения к нижнему регистру осталось, но обоснование другое:
оно нужно для той части набора, где стоят токены `[controller]`/`[action]`,
подставляющие PascalCase имени класса и метода. Долю того и другого меряет
разведка, §6.9 — пункт добавлен по этой же поправке.

Оттуда же вторая находка: вызов `'api/ml/structure/getforupdate'` идёт без параметра,
а маршрут объявлен с `{modelVersionId}`. Строгое сравнение ключей развело бы такую пару
по двум разным корзинам — «эндпоинт без вызывающего» и «вызов без эндпоинта», — и почти
найденная связь выглядела бы как две независимые дырки. Поэтому заведена отдельная
категория «почти совпало»: ключи, различающиеся только числом `{}`.

### `tools/recon-frontend.sh`

Разведка §6 собрана в один читающий скрипт: владелец системы знает TypeScript хуже,
чем .NET, и отвечать на вопросы аналитики глазами по чужому коду дороже, чем прогнать
скрипт и прислать вывод. Около 200 строк вывода, десять разделов по номерам подразделов
§6, в конце — четыре места, которые всё-таки надо прочитать человеку.

Проверен на синтетическом репозитории, воспроизводящем описанный шаблон (angular.json
с двумя проектами, интерцептор, сервис с четырьмя формами записи URL, NGXS-стор,
таблица роутов с `loadChildren`, контроллер с литеральным маршрутом и второй
с токенами). Код возврата 0, все десять разделов дают непустой вывод.

**Ловушка, на которой скрипт попался сам.** `git ls-files -- '*.ts'
':(exclude)**/node_modules/**'` **не исключает** `node_modules/` в корне: в pathspec
без магии `:(glob)` звёздочка перекрывает `/`, поэтому ведущее `**/` требует хотя бы
одного каталога перед именем. Та же ловушка `**`, что записана в `CLAUDE.md` про
`fnmatch`, — третий инструмент подряд, где она повторяется. Взяты две формы,
`node_modules/**` и `*/node_modules/**`: `:(glob,exclude)` решает то же одной строкой,
но требует более свежего git, а скрипт запускают в закрытом контуре, где неизвестная
магия pathspec означает падение вместо отчёта.

---

## Разведка фронтенда: что показал боевой репозиторий 🔎

Скрипт прогнан на АС CF (коммит `ea1124e74b6`, 45142 файла). Первый прогон был
**без области** и не ответил ни на один из четырёх своих главных вопросов — разбор
причин ниже. Второй, по модулю команды, ответил на все, кроме одного. Отчёт по фактам —
[`docs/findings-cashflow-frontend.md`](findings-cashflow-frontend.md), выводы разнесены
по §2.3, §2.4, §2.6, §3.3, §6 и §9 аналитики.

### Блокер снят: интерцептор трогает базу, а не путь

`FixUrlInterceptor` был назван в аналитике «самой опасной неизвестной во всей задаче».
Он делегирует конвейеру `cf-api/url-decorator`: тот сверяет URL с `EXCLUDE_URLS`
и прогоняет через список декораторов, из которых по умолчанию зарегистрирован один —
`SetApiUrl`, подставляющий базу `CF_API_URL`. Вся ветка не выполняется, если
`environment.apiUrl` пуст. Плюс `proxy.conf.js` модуля объявляет `context: ['/**']`
без `pathRewrite`.

Значит для ML **литерал в сервисе и есть маршрут контроллера**. Решение «преобразование
задаётся конфигурацией, а не выводится из кода интерцептора» не отменено, а подтверждено
конструкцией: конфигурация у конвейера уже есть — два DI-токена, и они расширяемы.

### Три находки, меняющие модель, а не детали

**`api/items/query` — один маршрут на много смыслов.** Четырнадцать вызовов в ML идут
в один эндпоинт платформы и различаются полем тела `listInnerName` (`users`, `models`,
`OkkChain`, `MODELVERSIONPRODUCTTYPES`, `MVLOADREASONS`). Ключ «метод + маршрут» склеил
бы их в одну точку. Якорь здесь — пара «маршрут + имя списка», то есть **уже
существующий якорь бизнес-слоя** (вид `list`): фронт цепляется к реестру напрямую,
минуя C#. Отсюда требование к извлечению вызовов — разбирать тело запроса для
маршрутов, названных в конфигурации, и никогда не перечислять такие маршруты в коде.

**`[Route]` на бэке стоит и на методах.** Токенов `[controller]`/`[action]` — ноль
на 433 `[Route]`, это ожидалось. Не ожидалось другое: гистограмма префиксов показала
среди маршрутов `state/byclient`, `innerDebts/insert`, `guarantee/delete` — это методные
атрибуты. Значит 556 пустых `[HttpGet]`/`[HttpPost]` дают только глагол, а путь метода
приходит отдельным `[Route]`. Реализация, считающая пустой `[HttpGet]` эквивалентом
маршрута класса, склеит в один ключ десятки действий одного контроллера.

**Дерево роутов собирается спредом, а не `loadChildren`.** В ML `loadChildren` — ноль,
`RouterModule` — ноль, а `app.routes.ts` подключает ветки как `children: [...modelsPath]`
из `routesPath/*.ts`. Разбор одного файла даёт два пути из двадцати шести, и молча:
ошибок нет, узлы просто не появляются. §3.3 писалась под ленивый `import()`; на реальном
коде первой встретилась другая форма, и уметь надо обе.

### Покрытие: две трети сразу, остальное — две формы констант

79 HTTP-вызовов по получателю (не 1219, как показывал прогон без области): 26 литералов,
27 шаблонных строк, 21 «переменная». Третья корзина обманчива — почти вся она это
`export const auditUrl = '/integration/log/AuditJ'` (10 вызовов в одном сервисе) и
`private readonly baseUrl = '/api/ml/debtsconsgroup'` (ещё шесть). Разрешение константы
модуля и `readonly`-поля переведено в F07 как обязательная часть, а не «потом»: без него
отчёт назовёт необъяснёнными 27 % вызовов там, где настоящая дыра — единицы.

### Почему первый прогон соврал, и что из этого следует для скрипта

Фронтов в АС CF семь. Срезы «примеры» и «содержимое» ограничены `head -N` и режут
список по алфавиту, а `Sbt.CMS.Cashflow.ML` идёт седьмым — он выпал целиком вместе
с `fix-url.interceptor.ts`, ради которого раздел и писался. В выводе это неотличимо
от «в модуле такого нет»: разведка выглядела отработавшей.

Скрипт получил область первым аргументом (второй — область `.cs`). Заодно разобраны
находки, каждая из которых однажды дала неверный ответ:

- **сужение нельзя задать префиксом в pathspec.** На git 2.55 положительный шаблон
  с литеральным каталогом в паре с любым `:(exclude)` обнуляет выборку целиком, даже
  если исключению нечему совпадать. Отчёт при этом не пустой, а с нулями в счётчиках.
  Область задаётся через `git -C`, шаблоны остаются прежними;
- **счётчик `.get/.post/…` без получателя меряет `Map.get` и `form.get`** — из
  двенадцати примеров «литерального URL» настоящим вызовом был один. Всё считается
  от `this.http`/`httpClient`, прежнее число оставлено с пометкой «не показатель»;
- фильтр интерцепторов по содержимому был регистрозависим и стоял **после** обрезки
  списка — нужный файл не печатался вовсе;
- `*nx.json` в pathspec ловит любой файл, чьё имя кончается на `nx.json`: первой
  строкой раздела стоял `DuplicatedRecordsBugEvanx.json` из тестовых данных .NET;
- список зависимостей обрезался ровно между `@ngrx/store` и `@ngxs/store`, унося
  с собой версии TypeScript и rxjs;
- пустой срез теперь называется вслух — то же правило, что уже было у счётчиков.

Проверено на синтетическом репозитории с двумя фронтами, где целевой модуль сортируется
последним: с областью, без области и с опечаткой в области (код 2 и список каталогов).

### Область работ: ML первый, но не единственный

Владелец обозначил рамку явно: документируется в первую очередь
`Sbt.CMS.Cashflow.ML`, но остальные шесть фронтов со счетов не сбрасываются. Разведка
показала, насколько это существенно: Angular от 10 до 19, `@ngrx` рядом с `@ngxs`,
nx-монорепа рядом с одиночными workspace — и главное, у каждого фронта свой префикс
прокси (`/pm`, `/cf`, `/tm`, `/realty`, а у админки `^/api/` вообще **дописывается**).
У ML таблица префиксов пустая, поэтому на нём одном ошибка нормализации не видна.
Плюс `Sbt.CMS.Cashflow.Front` лежит в том же субрепозитории, что и бэкенд ML: один
`Sbt.CMS.Cashflow.NetCore` обслуживает минимум два фронта. Якорь-маршрут это переживает
сам собой, ссылка на узел не пережила бы, — довод, ранее принятый умозрительно,
теперь проверен на данных.

---

## Три последних факта по фронту и план шага `web` 🔎

### Конвейер URL выключен, и это сильнее, чем «работает предсказуемо»

Владелец проверил окружения: файлов `environment*.ts` много, и **ни в одном нет поля
`apiUrl`** — есть `apiRoot`. Условие интерцептора читает `environment?.apiUrl`, то есть
отсутствующее поле, поэтому ветка подмены не выполняется ни в одной сборке. Список
`EXCLUDE_URLS` пуст. Значит URL уходит в сеть ровно таким, каким записан в сервисе, —
уже не предположение, а факт.

Из этого следует требование к инструменту, а не к разбору: факт «конвейер выключен»
обязан быть **записан в конфигурации**, а не выведен. Пустой `url_rewrite` — законное
значение, и ставит его человек, прочитавший интерцептор. Иначе первая же правка
`environment` тихо расстроит все ключи, и виноват будет инструмент, который «раньше
работал». Осталось непроверенным, где используется `apiRoot`: поле есть, значит база
куда-то подставляется, но не здесь.

### У обращения к реестру две формы, а не одна

`checkUrl(type)` внутри `getItems()` строит `api/items?listInnerName=models` или
`…=dictionaries` — тот же общий эндпоинт списков, но различитель в **query-строке**
и собран выражением. Вместе с уже известным `api/items/query`, где то же имя списка
лежит в **теле**, это ломает три наивных реализации подряд: вызов попадает
в «невосстановимые» до разрешения константы, нормализация отбрасывает query и склеивает
все справочники в один ключ, а одного правила на оба случая не хватает. Поэтому
`registry_calls` в конфигурации называет и маршрут, и место различителя (`in: body`
либо `in: query`).

### Отчёт по разведке дописан под будущий поиск аналогов

В `findings-cashflow-frontend.md` добавлен раздел «профиль системы»: девять признаков,
по которым искать похожие open-source проекты, в порядке убывания ценности, и по каждому
— чем искать. Самый редкий и самый ценный признак не Angular, а платформа, управляемая
метаданными: универсальный эндпоинт списков с именем списка в параметре. Аналог нужен
не для тестов (они самодостаточны), а для разведки граничных случаев — так уже работало
на .NET, где ABP, eShopOnWeb и OpenTelemetry дали половину ловушек `CLAUDE.md`.

### План шага `web`

`docs/frontend-implementation-plan.md`, задачи F01–F18 с критериями приёмки и командами
проверки. Отличия от прикидки §10 аналитики, каждое — следствие разведки:

- **два манифеста, одна схема.** `materialize`, `docs status` и бизнес-слой работают
  от `Manifest` и про язык не знают, поэтому отдельный `doc-tree.web.json` не меняет
  ни байта .NET-манифеста и не удваивает время `scan`;
- **F09 оказалась проверкой, а не переписыванием.** Чтение `dotnet/endpoints.py`
  показало, что склейка «маршрут класса + `[Route]` метода при пустом `[HttpGet]`»
  уже реализована — это преобладающая форма и в ABP, и в АС CF. Задача сузилась
  до фикстуры на четыре формы и до отдельной категории для конвенциональной
  маршрутизации в отчёте связи;
- **F10 — схема 2.0 с переименованием `csproj` → `project_file`.** Разовый сдвиг байтов
  золотого манифеста отдельным коммитом; альтернатива — имя поля, которое врёт в каждом
  отчёте по фронту навсегда;
- **F03 (нормализация) стои́т без зависимостей и раньше F07.** Чистая функция со списком
  случаев пишется и проверяется до того, как появится хоть один разобранный файл, и она
  же самое вероятное место будущих ошибок. Порядок шести шагов зафиксирован: приведение
  к нижнему регистру идёт последним, иначе сравнение префикса `/PM` с настройкой `/pm`
  перестанет работать;
- **в F07 разрешение констант — обязательная часть, а не «потом».** Без него отчёт
  назовёт невосстановленными 27 % вызовов там, где настоящая дыра единицы, и доверие
  к числу пропадёт раньше, чем его починят.

Состав фикстуры `WebWorkspace` расписан пофайлово: каждый файл воспроизводит факт
из отчёта разведки. Отдельно записаны два инварианта, повторяющие ошибку, уже сделанную
на .NET: не добавлять `node_modules/` в `.gitignore` (иначе тест исключений станет
бессмысленным) и проверять в фикстуре **наличие конструкций**, а не файлов.

---

## F02 — обход `.ts`/`.html` и фикстура `WebWorkspace` ✅

**Изменено:** `docpipe/discovery.py`, `docpipe/emit.py`, `tests/conftest.py`,
`tests/test_discovery.py`, `.gitignore`
**Создано:** `tests/fixtures/WebWorkspace/**` (39 файлов), `tests/test_fixture_web.py`

Обход находит исходники фронта: `ts_files`, `html_files` и `web_project_files`
в том же `Discovered`, тем же одним проходом по дереву. Два прохода пришлось бы
держать согласованными по `exclude`, `scope` и `roots` — расходились бы они молча.

Фикстура — 27 файлов `.ts` вне `node_modules` и 2 под ним, 2 `.html`, 4 файла
объявления модуля. Числа зафиксированы константой `EXPECTED_TS`: на них будут
стоять критерии приёмки F04–F17, как T04–T20 стоят на `SampleSolution`.

### Отклонения от плана

**1. Умолчания исключений живут в `emit.py`, а не в `config.py`.** План называл
`config.py` в списке правок; на деле `DEFAULT_EXCLUDE` объявлен в `emit.py`,
и `config.exclude` с ним складывается там же. Правка `config.py` не потребовалась
вовсе — план поправлен.

**2. `nx.json` добавлен в список имён, которых план не называл.** План перечислял
`angular.json`, `project.json`, `package.json`. Без `nx.json` правило 2 из F06
(«`nx.json` рядом с `apps/*/project.json`») применить не к чему: файл просто
не доедет до `modules.py`. Добавлен четвёртым именем.

**3. Nx-форма в фикстуре вложена, а не лежит одним файлом.** План писал
`nx-app/project.json`; чтобы правило F06 сработало по своей же формулировке,
нужен `nx-app/nx.json` рядом с `nx-app/apps/widget/project.json`. Сделано так.

**4. Алиасы вынесены в `tsconfig.base.json`.** План клал `paths` в `tsconfig.json`.
Тогда цепочка `extends`, которую обязан пройти резолв F05, в фикстуре не
воспроизводилась бы вовсе. Существенно то, что в TypeScript `paths` дочернего
конфига **замещает** родительский целиком, а не дополняет его: объявить алиасы
в обоих файлах — значит написать фикстуру, которая врёт про поведение компилятора.
Поэтому `paths` только в базе, а `tsconfig.json` их не переобъявляет.

**5. В фикстуру добавлены файлы, которых план не перечислял**, и каждый — под
критерий приёмки следующих задач: цели роутов (`shell`, `detail`, `quiz`,
`forecast`), функциональный интерцептор (F04), бочка и цепочка наследования
длиной два (F05), приманки `Map.get`/`FormGroup.get` (F07), `src/index.html`
(`.html` бывает не только шаблоном компонента), `global.d.ts` и `list.component.spec.ts`
(обходом не исключаются), `list.component.scss` (F14: правка стилей документ
не устаревает), второй модуль с литералом `/pm/api/…` (F13: модуль, не названный
в `url_rewrite`).

### Ловушка, которую нашла реализация

**Комментарий внутри фикстуры ломает проверку по подстроке.** Тест «в фикстуре
не встречается `@State(` без дженерика» падал на комментарии, объясняющем эту
самую ловушку; тест «в `proxy.conf.js` нет `pathRewrite`» — на комментарии
«`pathRewrite` нет». Форма, отсутствие которой утверждается, не должна встречаться
и в пояснении. Дописано в план: следующий реализатор напишет такой же комментарий.

### Решение, которое стоило обдумать

**Тест целостности фикстуры ищет конструкции по всему корпусу `.ts`, а не в
названном файле.** Проверка вида «в `debt.state.ts` есть `@State<`» переживёт
переименование файла и исчезновение конструкции вместе с ним: тест позеленеет
на пустом месте. Поиск по корпусу разрешает перенести конструкцию куда угодно
и запрещает её удалить — это ровно то свойство, ради которого инвариант
и заведён (`CLAUDE.md`, «Инварианты фикстур»).

### Проверено

```
uv run pytest tests/test_discovery.py tests/test_fixture_web.py -q  → 77 passed
uv run pytest -q                                                    → 1211 passed
```

---

## F03 — `docpipe/route.py`: нормализация маршрута ✅

**Создано:** `docpipe/route.py`, `tests/test_route.py`

Шесть шагов в фиксированном порядке, `RouteKey` из трёх полей, `almost_equal`.
Модуль лежит не под `web/`: им пользуются обе стороны, а две копии нормализации
разойдутся на первом же частном случае и разойдутся молча.

### Ловушка, найденная реализацией: шаг 2 режет шаблон ASP.NET

`[HttpGet("{id?}")]` — законная и частая форма. Шаг 2 отбрасывает query по первому
`?` в строке и обрезает маршрут до `api/x/{id`; шаг 4 такую подстановку уже
не распознаёт. Получается ключ `api/x/{id` — выглядит правдоподобно и не совпадает
ни с чем. Починка: скобки считаются, разделитель берётся на нулевой глубине.
Тест написан раньше починки и падал именно на этой строке таблицы.

### Отклонение: направление шага 3

План говорил «снять `add_prefix`, затем `strip_prefix`» — то есть снять оба.
Реализовано наоборот: `strip_prefix` **снимается**, `add_prefix` **дописывается**,
и общий ключ живёт в пространстве имён бэкенда.

Довод не в удобстве, а в том, что обратное направление неосуществимо. Правило
заведено **на модуль фронта** (`web.url_rewrite[].module`). У маршрута контроллера
модуля фронта нет, и выбрать для него правило не из чего: чтобы «снять `add_prefix`»
с маршрута `admin/api/x`, надо сначала знать, что его зовёт именно админка, — а это
и есть тот ответ, ради которого связь строится. Приведение фронта к тому, что видит
бэкенд, такой петли не образует.

Порядок внутри шага — сначала снять, потом дописать: у правила, где заданы оба
поля, это замена головы пути. В обратном порядке дописанная голова закрывает собой
ту, которую надо снять, и снятие молча не срабатывает. На АС CF ни у одного модуля
оба поля не заданы, то есть проверить это на боевом коде было бы нечем.

### Три решения, каждое против правдоподобного неверного ключа

**Префикс сравнивается по сегментам.** `pathRewrite` — регулярное выражение,
и `^/pm` срезает `/pmloadrequests/x` до `/loadrequests/x`. У PM.Front в прокси
объявлены оба контекста, `/pm/api` и `/pm/pmloadrequests`, то есть обе формы
в репозитории существуют, а различает их только граница сегмента.

**Регистр складывается с обеих сторон.** Тот, кто написал в конфигурации `/PM`,
обязан получить тот же результат. Приведение к нижнему регистру стои́т последним
шагом именно поэтому: сравнение идёт до него и складывает регистр само.

**`add_prefix` не дублируется.** Литерал, уже написанный с префиксом, дал бы
`/admin/admin/api/x` — сразу две находки, «вызов без эндпоинта» и «эндпоинт без
вызывающего», обе с видом дефекта. Так же ведёт себя и боевой `SetApiUrl`.

### `almost_equal` ложна для совпавших целиком ключей

«Почти» исключает «точно». Реализация F13, перебирающая кандидатов через
`almost_equal` до проверки на равенство, иначе сложила бы настоящие связи в корзину
«почти совпало» и молча занизила бы их число. Глагол и различитель обязаны
совпадать: `GET` и `POST` на одном пути — разные эндпоинты.

### `discriminator` кладётся как написан

Без приведения регистра: дальше он сравнивается с `ref` записи реестра, а
`business.resolve._key` сравнивает `ref` точным равенством. Сложенный здесь регистр
сделал бы пару неразрешимой там, и выглядело бы это как «инструмент не нашёл»,
а не как расхождение соглашений.

### Проверено

```
uv run pytest tests/test_route.py -q  → 31 passed
uv run pytest -q                      → 1242 passed
uv run mypy docpipe                   → Success: no issues found in 48 source files
```

---

## F10 — модели и схема 2.0 ✅

**Изменено:** `docpipe/model.py`, `docpipe/emit.py`, `docpipe/tree.py`, `docpipe/cli.py`,
`docpipe/dotnet/csproj.py`, `docpipe/materialize/build.py`, `schema/doc-tree.schema.json`,
`tests/golden/doc-tree.json`, `docs/manifest.md`, `README.md`, шесть тестов

`Module.csproj` → `Module.project_file`, добавлен обязательный `Module.lang`,
`schema_version` → `"2.0"`, заведены `WebCall` и `RouteEntry`.

Задача сделана **до** F04 вопреки порядку в таблице (зависимостей у неё нет).
Причина: парсер TypeScript обязан отдавать `type_kind` со значениями, которых
`Literal` схемы 1.1 не допускает. Делать модель дважды — сначала временно
расширить, потом переименовать — значило бы два сдвига байтов золотого файла
вместо одного.

### Дифф золотого файла

Ровно то, что обещано планом, и ни одного изменённого узла:

```
-      "csproj": "src/Sample.Common/Sample.Common.csproj",
+      "lang": "cs",
+      "project_file": "src/Sample.Common/Sample.Common.csproj",
-  "schema_version": "1.1"
+  "schema_version": "2.0"
```

Идентификаторы узлов и модулей содержат путь `.csproj` и потому не изменились:
переименовано **поле**, а не значение. Золотое дерево документов
`tests/golden/docs/**` не тронуто ни на байт.

### Отклонение: `type_kind` расширен только синтаксическими видами

План перечислял `component`, `service`, `directive`, `pipe`, `guard`, `interceptor`,
`state`, `interface`, `enum`, `function`, `const`. Первые семь — не виды объявления,
а **виды классификации**: их выдаёт `rules/*.yaml` полем `kind:`, и живут они
в `DocNode.kind`, который и так свободная строка. Положить их в `type_kind` значит
поручить решение классификации парсеру — тому, кто правил не читает. Заодно исчезла
бы разница между «в коде объявлен класс» и «мы решили считать его компонентом»,
а на ней стои́т весь отчёт о нерешённых символах.

Добавлены поэтому две формы, которых у C# действительно нет: `function` и `const`
(в Angular так объявлены функциональные интерцепторы, guard'ы и таблицы роутов).
`interface` и `enum` в `TypeKind` уже были.

### `lang` без значения по умолчанию

Умолчание `"cs"` выглядит удобным и стоило бы одной строки в шести тестах. Но тогда
сборщик модулей фронта, забывший выставить поле, отдал бы манифест, где все модули
объявлены .NET-овскими, — и заметить это было бы нечем: остальные поля у такого
модуля правильные. Обязательное поле делает эту ошибку невозможной структурно.

### `WebCall` носит `RouteKey` целиком

`RouteKey` — обычный `@dataclass(frozen=True)` из `route.py`, и pydantic принимает
его как тип поля: круг через JSON проходит, лишнее поле **внутри ключа** отвергается
(`extra="forbid"` действует на всю глубину). Проверено тестом: опечатка в имени
различителя не должна проходить молча.

### Названо, но не сделано: `module_csproj` во front matter

Ключ генерируемого блока шага 2 врёт ровно так же, как врало поле модели. Его
переименование перегенерирует **все** документы, включая боевые, поэтому в эту
задачу оно не входит: решение принимается в F14, когда появятся web-документы.
Записано в план — иначе следующий реализатор наткнётся на него на боевом дереве.

### Проверено

```
uv run docpipe scan --root tests/fixtures/SampleSolution --out tests/golden/doc-tree.json --no-cache
uv run docpipe schema --out schema/doc-tree.schema.json
git diff --stat tests/golden/  → 8 +++++---, только модули и schema_version
uv run pytest -q               → 1247 passed
uv run mypy docpipe            → Success: no issues found in 48 source files
```

---

## F04 — `web/parser.py`: TypeScript в `FileParseResult` ✅

**Создано:** `docpipe/web/__init__.py`, `docpipe/web/parser.py`,
`docpipe/web/queries/{declarations,members}.scm`, `tests/test_web_parser.py` (29 тестов)
**Изменено:** `pyproject.toml`, `deploy/pyproject.toml`, `deploy/OFFLINE.md`, оба `uv.lock`

`tree-sitter-typescript==0.23.2` с `tree-sitter` 0.26 работает: API 0.25+
(`Query(LANG, src)` + `QueryCursor(q).captures(node)`) — тот же, что у C#.

Вся фикстура разбирается с нулём ошибок, и это проверяется отдельным тестом:
без него все остальные проверки могли бы работать на обломках.

### Ловушка плана подтвердилась ровно как описана

У неэкспортируемого класса `decorator` лежит **внутри** `class_declaration`,
у экспортируемого — **рядом** с ним в `export_statement`. Проверено на фикстуре
до написания кода. Третья форма нашлась при реализации: у члена класса декоратор —
предшествующий **сосед внутри `class_body`**, то есть ни то, ни другое.

### Две ловушки, которых в плане не было

**JSDoc — сосед `export_statement`, а не объявления.** Комментарий стои́т перед
словом `export`, снаружи узла, от которого идёт поиск. Обход предыдущих соседей
`class_declaration` находит JSDoc **только у неэкспортируемого** класса. Разница
выглядела бы как случайность в данных: у половины символов описание есть,
у половины нет, и никакой системы. Лечится одним понятием «якорь» — объявление
либо охватывающий его `export_statement`; тот же якорь отдаёт и декораторы.

**Три клаузы наследования устроены по-разному.**

```
extends_clause        value: Base, type_arguments: <T>   ← двумя ПОЛЯМИ
implements_clause     generic_type Holder<T>, type_identifier Other
extends_type_clause   generic_type First<A>,  type_identifier Second
```

У `extends` класса дженерик — отдельный узел, у остальных — часть типа. Единый
обход по потомкам отдаёт `Base` вместо `Base<T>` только у `extends`. Найдено
пробным разбором: `implements Holder<U>` приехал целиком, а `extends Base<T>` —
обрезанным, и в выводе это выглядело просто как «у одного дженерик, у другого нет».

### Решения, принятые сверх спецификации

**Из декоратора берутся литеральные поля объекта.** `templateUrl` нужен F14
(хэш шаблона входит в `impl_hash`), `standalone` и `selector` — правилам F12,
`@Action(LoadInnerDebts)` — цепочке NGXS в F13. Поле со значением-выражением
не берётся вовсе: «поле есть, но неизвестно» и «поля нет» обязаны различаться.

**Экспортируемость выражена структурой запроса, а не проверкой в Python.**
`(export_statement declaration: (lexical_declaration) @declaration)`. Иначе
`lexical_declaration` ловит каждое `const` внутри каждого тела метода — тысячи
узлов на модуль, и ни один из них не объявление.

**Аксессор `get x()` считается свойством.** Пара «метод чтения / метод записи»
дала бы два раздела документации об одном и том же поле.

### Отложено осознанно: импорты

План перечислял в F04 закрытый список извлекаемого, и импортов в нём нет. Они
нужны только резолву, поэтому заведены в F05 — вместе с моделью для них. В плане
дописано, что F05 правит и `parser.py`: разбор одного файла делает парсер
(результат кэшируется), а второй проход по файлам в `resolve.py` разошёлся бы
с кэшем и удвоил чтение дерева.

### Предел, который стоит знать

Схлопывание пробелов делает `decl_hash` нечувствительным к отступам и переносам,
но не к запятой: `prettier`, разбивая длинный список параметров на строки,
дописывает висящую запятую. Ровно то же свойство у .NET с `dotnet format`.
Чинить нечем, кроме собственного форматтера; переписать документ раз в жизни
файла дешевле.

### Зависимость добавляется в два `pyproject.toml`

`test_runtime_dependencies_match_the_repository` упал сразу: состав поставки
сверяется с составом разработки, и забытая в `deploy/` зависимость — это отказ
на целевой машине закрытого контура. Строка дописана и в таблицу пакетов
`deploy/OFFLINE.md`: грамматика — расширение на C, и на зеркале без готовых
колёс её придётся собирать.

### Проверено

```
uv run pytest tests/test_web_parser.py -q  → 29 passed
uv run pytest -q                           → 1276 passed
uv run mypy docpipe                        → Success: no issues found in 50 source files
```

---

## F05 — `web/resolve.py`: импорты, алиасы, бочки, замыкание ✅

**Создано:** `docpipe/web/resolve.py`, `docpipe/symbols.py`,
`docpipe/web/queries/imports.scm`, `tests/test_web_resolve.py` (25 тестов)
**Изменено:** `docpipe/model.py` (`ImportedName`, `ModuleImport`,
`FileParseResult.imports`), `docpipe/web/parser.py`, `docpipe/dotnet/resolve.py`,
`docpipe/cache.py`

На фикстуре разрешаются все три формы: относительный импорт, алиас с подстановкой
и алиас в каталог через `index.ts` с переэкспортом. Замыкание
`InnerDebtService -> BaseApiService -> CoreService` считается транзитивно
и проходит через алиас и бочку.

### Резолв идёт по файлам, а не по пространствам имён

Главное отличие от .NET, и оно меняет устройство модуля целиком. Там имя ищется
по namespace и usings, поэтому переход «базовый тип → символ» требует арности:
один FQN принадлежит нескольким типам. В TypeScript единица изоляции — файл,
пара «файл + имя» уникальна сама по себе, и арность в переходе не нужна вовсе.

Отсюда же FQN считается **от пути файла**, а не от каталога, хотя `namespace`
у символа остался каталогом (по нему матчится `namespace_regex`). Два файла
одного каталога законно объявляют одноимённый `Helper`; FQN по каталогу склеил
бы их в один символ, и один из двух исчез бы из документации молча.

### Ловушка, которой не было в плане: `tsconfig.json` — это не JSON

И `ng new`, и `nx` генерируют его с комментариями (`/* To learn more about this
file see: … */`), а руками туда дописывают `//` и висящие запятые. `json.loads`
на таком файле бросает исключение. Это значит, что без разбора JSONC не читается
**ни один боевой `tsconfig`**, а вместе с ним не разрешается ни один импорт вида
`@shared/…` — ровно тот отказ, который в плане описан как «часть набора работает,
часть нет».

Комментарии вырезаются сканером, а не регулярным выражением: `//` внутри строки
(`"url": "http://host"`) регулярное выражение приняло бы за начало комментария
и съело бы остаток файла. Висящая запятая снимается там же и только вне строки.

### Вторая: `paths` дочернего конфига замещает родительский целиком

`extends` в TypeScript сливает `compilerOptions` по ключам, и ключ `paths`
**заменяется**, а не дополняется. Слияние выглядит логичнее и даёт набор алиасов,
которого у компилятора нет, — то есть резолв, расходящийся со сборкой. Из-за
этого же в фикстуре (F02) алиасы объявлены только в `tsconfig.base.json`.

Третья, мелкая, но с тем же свойством: побеждает шаблон с самым длинным
префиксом до `*`. При объявленных `@shared` и `@shared/*` выбор «первый
совпавший» дал бы резолв, зависящий от порядка ключей в файле.

### Общий модуль вместо копии: `docpipe/symbols.py`

`symbol_key` и `strip_generics` понадобились обеим сторонам, а `docpipe/web/**`
не имеет права импортировать `docpipe/dotnet/**`. Копия ключа разошлась бы молча:
ключ перестал бы совпадать с идентификатором узла, и символы просто перестали бы
находиться. Функции перенесены в корень пакета и переэкспортируются из
`dotnet/resolve.py` — по этим именам ходят `tree.py` и тесты.

### Импорты пришлось завести в модели

`FileParseResult` не имел места для импортов, а резолву они нужны. Добавлены
`ImportedName` (два поля: как видно здесь и как объявлено в источнике —
`import { Base as Api }` иначе рвёт цепочку) и `ModuleImport` (импорт
и переэкспорт одной моделью: для резолва это одно действие). `CACHE_VERSION`
поднят до `"3"`: схема записи изменилась.

### Проверено

```
uv run pytest tests/test_web_resolve.py -q  → 25 passed
uv run pytest -q                            → 1301 passed
uv run mypy docpipe                         → Success: no issues found in 52 source files
```

---

## F06 — `web/modules.py`: что такое модуль на фронте ✅

**Создано:** `docpipe/web/modules.py`, `tests/test_web_modules.py` (18 тестов)

На фикстуре обе формы дают модули: `angular.json` с проектом `tr-p` (граница
`src`) и nx-проект `widget` (граница `nx-app/apps/widget`). Все 27 файлов `.ts`
привязаны к модулю, ни одного ничьего.

### Ключ модуля — каталог-граница, а не файл объявления

Главное отклонение от буквального переноса .NET. Там файл проекта и модуль
всегда один к одному, поэтому ключом служит путь `.csproj`. На фронте один
многопроектный `angular.json` объявляет несколько модулей — в `PM.Front` их
семь, — и ключ по файлу склеил бы их в один. Причём не только в списке модулей:
ключ модуля входит в **ключ символа**, то есть склеились бы и символы разных
приложений.

Границей берётся `sourceRoot`, при его отсутствии `root`; `id` = `module:<граница>`.
Файл принадлежит модулю с самой длинной подходящей границей — у workspace
с `sourceRoot: "src"` и вложенным nx-проектом файл подходит обоим.

### Три ловушки, каждая про границу

**`root: ""` — это весь репозиторий.** В ML именно так, и по `root` модулем
оказался бы репозиторий целиком, вместе с шестью соседними фронтами и всем
бэкендом. Поэтому `sourceRoot` выигрывает у `root`.

**`sourceRoot` в `project.json` задан относительно корня nx-workspace**, а не
относительно проекта. Склейка с каталогом проекта даёт `apps/widget/apps/widget/src`
— путь правдоподобный и несуществующий; файлы модуля остались бы ничьими
и в манифест не попали бы вовсе. Границей nx-проекта берётся каталог самого
`project.json`.

**Правила применяются по месту, а не глобально.** План говорил «первое найденное
выигрывает»; буквально это означало бы, что `angular.json` в корне отменяет
nx-проект в соседнем каталоге, и многопроектный репозиторий схлопнулся бы
в один модуль. Порядок правил остался, но каждое применяется к своему месту.

### Куда легли данные о стеке

`Module` — общая модель на два языка, и заводить в ней поля под Angular нельзя.
Версии `@angular/core` и `typescript` легли в `target_frameworks`: это ответ
на тот же вопрос, что `net8.0` у .NET, — подо что собран модуль. Имена всех
зависимостей легли в `package_references`, и по ним видно наличие `@ngxs/store`
либо `@ngrx/store`. Пути `proxy.conf*` и `tsconfig` в манифест не идут вовсе —
они нужны разбору и человеку, заполняющему `url_rewrite`, и живут в `WebModule`,
внутренней структуре шага.

`package.json` ищется **ближайший вверх**, как это делает npm: у nx-проекта
внутри workspace своего обычно нет, и стек он наследует от корня.

### Проверено

```
uv run pytest tests/test_web_modules.py -q  → 18 passed
uv run pytest -q                            → 1319 passed
```

---

## F07 — `web/calls.py`: HTTP-вызовы, константы, обращения к реестру ✅

**Создано:** `docpipe/web/calls.py`, `docpipe/web/queries/calls.scm`,
`tests/test_web_calls.py` (26 тестов)

На фикстуре 16 вызовов: 15 восстановлено, 1 нет (URL приходит параметром метода).
Все шесть форм первого аргумента разобраны, приманки `Map.get` и `form.get`
в результат не попали.

### Разбор и конфигурация разведены на два шага

Отклонение от плана, и оно определяет устройство модуля. `extract_calls`
записывает **факты** о вызове и от конфигурации не зависит: результат можно
кэшировать. `build_calls` интерпретирует их с учётом `url_rewrite`
и `registry_calls`. Иначе смена одной строки в `docpipe.yaml` требовала бы
перечитать все исходники, а кэш отдавал бы ключи, собранные по прежней
настройке, — молча.

Из того же деления следует, что `RawCall.url` хранится **вместе с query-строкой**:
различитель обращения к реестру живёт именно там, а нормализация маршрута его
отбрасывает. Отбросить его при разборе — значит склеить все справочники в один
ключ ещё до того, как конфигурация успеет что-то сказать.

### Две ловушки, найденные реализацией

**Таблица литеральных полей должна быть по классу, а не по файлу.** Два сервиса
в одном файле законно объявляют `baseUrl` с разными значениями; общая таблица
подставит в вызов чужую базу — молча и правдоподобно. Константы уровня модуля,
наоборот, общие для файла.

**Конкатенация обходится слева направо.** `a + b + c` — это `(a + b) + c`.
Обход дерева стеком, написанный «как удобно», дал `{}api/ml/structure/getForUpdate/`
вместо `api/ml/structure/getForUpdate/{}`: ключ, который не совпадёт ни с чем,
но выглядит настоящим. Поймано пробным прогоном на фикстуре, а не тестом.

Третья, мелкая: захват `@constant` стоял на `lexical_declaration`, а поля `name`
и `value` есть у `variable_declarator` внутри него. Все три вызова через
`auditUrl` оказались невосстановленными — то есть ровно тот отчёт «27 %
необъяснённых вызовов», ради которого разрешение констант и делалось.

### Числа, которые обязаны сходиться

`len(calls) + len(unresolved)` равно общему числу найденных вызовов — отдельный
тест. `registry_unresolved` — **подмножество** восстановленных: маршрут известен,
различитель нет, и это состояние работы, а не потеря.

### Проверено

```
uv run pytest tests/test_web_calls.py -q  → 26 passed
uv run pytest -q                          → 1345 passed
```

---

## F08 — `web/routes.py`: дерево роутов и якорь страницы ✅

**Создано:** `docpipe/web/routes.py`, `docpipe/web/queries/routes.scm`,
`tests/test_web_routes.py` (15 тестов)
**Изменено:** `docpipe/route.py` (параметр `:id`), `docpipe/web/resolve.py`
(`resolve_exported`)

На фикстуре собраны пять страниц и одна невосстановленная ветка:

```
/                      ShellComponent
/forecast/daily        ForecastComponent     ← loadChildren + короткий loadComponent
/models                ListComponent         ← спред импортированного массива
/models/loader/quiz    QuizComponent         ← loadComponent с import()
/models/{}             DetailComponent       ← параметр :id
? /models              ListComponent         ← сегмент собран выражением
```

### Корень таблицы — тот, на кого никто не ссылается

Правила опознания корня в плане не было, а без него каждая дочерняя таблица
становится ещё и корнем: `daily` появляется и как `/forecast/daily`, и как
`/daily`. Опознавать корень по `provideRouter`/`RouterModule.forRoot` нельзя —
в боевом модуле их нет вовсе, а дерево собрано. Поэтому корень — таблица,
на которую не ссылаются ни спредом, ни `loadChildren`.

Таблица опознаётся по аннотации типа (`: Routes`): «массив объектов с ключом
`path`» поймал бы любой конфиг меню, а таких в модуле больше, чем таблиц роутов.

### Ловушка: в ленивой форме две стрелки, и нужна внутренняя

`() => import('./x').then(m => m.ROUTES)`. Тело внешней стрелки —
`import(…).then(…)`, у которого свойство называется `then`. Первая реализация
брала первое попавшееся свойство при обходе и подставила `then` и в имя таблицы,
и в имя компонента: `/models/loader/quiz` получил компонент `then`, а ветка
`forecast` вообще потеряла префикс и дала `/daily`. Оба симптома выглядели как
разные дефекты, а причина одна.

### `:id` дописан в общую нормализацию

План требовал, чтобы параметр превращался в `{}` через `docpipe/route.py`,
а не своей копией, — но самого шага там не было. Дописан посегментно, после
замены фигурных форм: двоеточие встречается и внутри шаблона ASP.NET
(`{id:guid}`), и отличить формы можно только по началу сегмента. Теперь
`normalize_route("models/:id") == normalize_route("models/{id:guid}")` — то есть
обе стороны действительно сходятся в одном ключе.

### Проверено

```
uv run pytest tests/test_web_routes.py -q  → 15 passed
uv run pytest -q                           → 1360 passed
```

---

## F09 — сторона .NET: проверка склейки маршрутов ✅

**Создано:** `tests/fixtures/WildSolution/src/Wild.Api/Controllers/InnerDebtsController.cs`
**Изменено:** `tests/test_endpoints.py` (3 теста), `tests/test_fixture_wild.py` (инвентарь)

Предсказание плана подтвердилось полностью: **править `dotnet/endpoints.py`
не пришлось**. Все четыре боевые формы собираются существующим кодом:

```
GET  api/ml/innerdebts                 пустой [HttpGet] без [Route]
POST api/ml/innerdebts/insert          путь в аргументе атрибута
GET  api/ml/innerdebts/state/byclient  [HttpGet] + отдельный [Route] на методе
GET  formsx/models/list                абсолютный шаблон ~/
```

Третья форма — преобладающая в АС CF: `[Route]` там 433, из них с токеном
`[controller]` ноль, а пустых `[HttpGet]` 556 против 47 с аргументом. То есть
глагол и путь метода объявлены **разными** атрибутами, и резолвер складывает
три независимые части, ни одна из которых не обязательна.

Маршруты проверяются целиком, а не по сегменту: ошибка склейки даёт
правдоподобный путь, отличающийся одним куском, и посегментная проверка её
пропустит.

### Категория для F13 считается по манифесту

В фикстуру добавлен `HomeController` — конвенциональный, без атрибутов вовсе.
Признак такой категории: узел вида `controller`, у которого эндпоинтов нет
вовсе **либо** есть эндпоинт с пустым маршрутом. Оба случая означают одно —
URL из атрибутов не собирается, и вызовы фронта к такому контроллеру попали бы
в «эндпоинт не найден», то есть выглядели бы дефектом фронта.

Считать это на стороне символов нельзя: `web/link.py` не имеет права
импортировать `docpipe/dotnet/**`, а манифест ему и так на входе. Оба признака
закреплены тестами — по ним F13 и будет считать категорию.

### Проверено

```
uv run pytest tests/test_endpoints.py tests/test_fixture_wild.py -q  → 47 passed
uv run pytest -q                                                     → 1364 passed
```

---

## F11 — `docpipe web scan` ✅

**Создано:** `docpipe/web/tree.py`, `rules/web.yaml` (минимальный),
`tests/test_web_scan_e2e.py` (16 тестов)
**Изменено:** `docpipe/cli.py`, `docpipe/config.py`, `docpipe/model.py`,
`docpipe/tree.py`, `docpipe/web/parser.py`, `docpipe.example.yaml`,
золотой манифест и схема

```
$ uv run docpipe web scan --root tests/fixtures/WebWorkspace --out /tmp/web.json
Модулей: 2, узлов: 27. Записано: /tmp/web.json и /tmp/web.run.json
Вызовов: восстановлено 15, не восстановлено 1; обращений к реестру без различителя 0.
Страниц: 6, из них маршрут не собран у 1.
```

Двойной прогон даёт побайтово одинаковый файл, манифест проходит
`docpipe validate` — то есть общая схема действительно общая.

### Золотой файл сдвинулся второй раз, и это неточность плана

F10 обещала один разовый сдвиг байтов. На деле их два: модель пришлось дописать
ещё и здесь.

1. `ParserVersions`: обе грамматики стали необязательными. Манифест .NET
   не знает про TypeScript, манифест фронта — про C#; версия чужой грамматики
   в нём была бы шумом, а `null` читается однозначно.
2. `DocNode` получил `web_calls` и `routes`. F10 завела модели `WebCall`
   и `RouteEntry`, но не прикрепила их к узлу — а без этого ни связь,
   ни маршруты не доезжают до манифеста, то есть не доезжают до F13 и F14 вовсе.

Предвидеть это в F10 было можно: там модели заводились «по списку из плана»,
без вопроса «кто их прочитает». Записано в план.

### Свой сборщик узлов, а не `tree.build_nodes`

Там модуль ищется по пути `.csproj` (`enrolled[symbol.module]`), а на фронте
ключ модуля — каталог-граница (F06). Общие части (`doc_path_for`,
`signature_hash`) переиспользуются, и ради этого `docpipe/tree.py` перестал
импортировать `docpipe/dotnet/**`: `symbol_key` и `strip_generics` он берёт
из `docpipe/symbols.py`. Граница между языками теперь честная и на уровне
импортов тоже.

### Кэш разведён по файлам

`parse-web.sqlite` рядом с `parse.sqlite`. Запись о версиях в базе одна,
а грамматики у двух шагов разные: общий файл они инвалидировали бы друг другу
на каждом прогоне — и это выглядело бы как «кэш не работает», без всякой
причины в выводе.

Кэшируются только символы. Факты о вызовах извлекаются из **того же дерева**
(`parse_tree` отдаёт дерево и символам, и `calls.py`), а в кэш не идут: второй
формат хранения пришлось бы держать согласованным с первым.

### Ловушка, стоившая бы пустого дерева

`require_public: true`, перенесённый из `rules/dotnet.yaml`, отсеивает **весь**
фронт: у TypeScript модификатора `public` на уровне объявления нет вовсе,
видимость задаётся словом `export`. Прогон при этом отрабатывает успешно
и печатает ноль узлов.

### Минимальный набор правил заведён здесь

Без правил `classify` не возвращает ничего, и проверять на прогоне нечего.
`rules/web.yaml` доводится в F12; здесь он даёт восемь видов на фикстуре
(`component`, `service`, `interceptor`, `state`, `action`, `routes`, `dto`,
`ng-module`).

### Проверено

```
uv run pytest tests/test_web_scan_e2e.py -q  → 16 passed
uv run pytest -q                             → 1380 passed
docpipe web scan дважды                      → cmp: файлы совпадают
docpipe validate /tmp/web.json               → Манифест корректен: 2 модулей, 27 узлов
```

---

## F12 — `rules/web.yaml` и классификация ✅

**Изменено:** `rules/web.yaml`, `docpipe/web/tree.py`, `docpipe/cli.py`,
`tests/fixtures/WebWorkspace/.../list.component.spec.ts`
**Создано:** `tests/test_web_rules.py` (13 тестов)

На фикстуре решение принято про все 30 символов: 27 документируем (девять видов),
3 отсеяны тремя решениями с причинами, **нерешённых ноль**.

```
$ uv run docpipe web scan --root tests/fixtures/WebWorkspace --stats
Решения по 30 символам:
  документируем           27  8 видов
  не документируем         3  3 решения
  --------------------------
  решение не принято       0  решены все символы
```

### Два вида решаются не правилами

`page` и `api-service` зависят от фактов, которых движок правил не видит:
таблица роутов собирается межфайлово, HTTP-вызовы разбираются отдельно,
и в `Symbol` нет ни того, ни другого. Повышение делает `web/tree.py` после
классификации: правило говорит «это компонент», а таблица роутов — «этот
компонент является страницей».

Выразить это предикатом по имени или пути можно было бы только угадыванием
(`**/routes/**`, суффикс `Page`), а угаданная страница ломает якорь
бизнес-документа: он пишется на маршрут, которого у неё нет.

`matched_rules` при повышении не меняется — там записано, что сработало,
а не что получилось. Иначе по отчёту нельзя понять, какое правило настраивать.

На фикстуре: 5 страниц и 2 обычных компонента, 5 сервисов API и 3 обычных
сервиса.

### Ловушка: отсев констант съедает интерцепторы

Отсев проверяется **до** правил классификации. Правило «константа модуля
документируется вместе с сервисом» без выреза убирает заодно функциональный
интерцептор, guard и таблицу роутов — они тоже константы. В АС CF интерцепторы
и guard'ы объявлены именно так, то есть в дереве не осталось бы ни одного,
и выглядело бы это как «правило по интерцепторам не работает».

Лечится единственной формой отрицания в движке — полем `unless`, ради которого
оно и заведено.

### Фикстуру пришлось дополнить

Отсев `*.spec.ts` на фикстуре не срабатывал вовсе: спек-файл состоял из вызовов
`describe`/`it` и не объявлял ни одного символа. Правило было, проверять его
было не на чем. В спек добавлен экспортируемый класс-хелпер — заодно это ровно
та причина, по которой `*.spec.ts` не исключается обходом: тестовые классы
входят в граф наследования.

### `--stats` и свой флаг отказа

`docpipe web scan --stats` печатает тот же отчёт, что и шаг 1, и ничего не пишет.
`--fail-on-undecided` — **свой**, а не общий с шагом 1: общий означал бы, что
настройка фронта роняет CI бэкенда, а на старте нерешённых много.

Команда из плана (`docpipe symbols --root … --state undecided`) не годится:
`symbols` поднимает разбор .NET, и на фронте она посчитала бы ноль символов.
План поправлен.

### Проверено

```
uv run pytest tests/test_web_rules.py -q  → 13 passed
uv run pytest -q                          → 1393 passed
```

---

## F13 — `docpipe web link`: сведение двух манифестов ✅

**Создано:** `docpipe/web/link.py`, `tests/test_web_link.py` (20 тестов),
`tests/fixtures/WildSolution/.../{MlStructureController,ItemsController}.cs`
**Изменено:** `docpipe/cli.py`, `tests/test_fixture_wild.py`

```
$ uv run docpipe web link /tmp/net.json /tmp/web.json
Вызовов фронта: 15, эндпоинтов бэкенда: 10.

      6  связь найдена точно
      3  почти совпало: ключи различаются только числом {}
      6  вызов без эндпоинта
      3  эндпоинт без вызывающего
      1  ОДИН КЛЮЧ У ДВУХ УЗЛОВ БЭКЕНДА
```

Прогон при этом **зелёный**: находка меняет код возврата, только если названа
в `--fail-on`. Включая коллизию маршрутов — единственный настоящий дефект.

### Различитель не участвует в сопоставлении

Решение, которого в плане не было явно, и оно определяет форму отчёта.
`api/items/query` — один и тот же эндпоинт платформы для всех имён списков;
`listInnerName` отвечает на вопрос «зачем звали», а не «куда». Поэтому связь
строится на каждый **вызов** отдельно: два обращения с разными именами списков
дают две связи с одним и тем же эндпоинтом.

Обратное решение (включить различитель в ключ сопоставления) дало бы
«вызов без эндпоинта» на каждом обращении к реестру: у контроллера-то
различителя нет.

### Точное совпадение проверяется раньше «почти»

То же свойство, из-за которого `almost_equal` ложна для совпавших целиком
ключей (F03). Проверять «почти» первым — значит увести настоящие связи
в корзину инвентаря и занизить число настоящих.

### Фикстуре .NET понадобились маршруты

Связь нельзя проверять на выдуманных ключах. В `WildSolution` добавлены два
файла: `MlStructureController` вместе с одноимённым `Legacy`-двойником (точная
связь, пара «почти совпало» с `getForUpdate`, эндпоинт без вызывающего
и коллизия ключа) и `ItemsController` — универсальный эндпоинт списков
платформы, на котором проверяется, что обращения к реестру не склеиваются.

Коллизия в фикстуре настоящая: два контроллера с `[Route("api/ml/structure")]`
и `[HttpGet]` — приложение с такой парой падает на старте либо отвечает
произвольным из двух действий.

### Опечатка в `--fail-on` отвергается

Незнакомое имя категории значило бы, что проверка молча выключена, а прогон
при этом зелёный. Команда отвечает кодом 2 и перечисляет известные категории.

### Проверено

```
uv run pytest tests/test_web_link.py -q  → 20 passed
uv run pytest -q                         → 1415 passed
```

---

## F14 — шаг 2 для web ✅

**Создано:** `templates/{page,component,api-service,state,dto}.md`,
`tests/test_materialize_web.py` (18 тестов)
**Изменено:** `docpipe/web/tree.py`, `docpipe/materialize/build.py`,
`docpipe/tree.py`, `tests/test_materialize_template.py`, золотое дерево документов

`materialize` на web-манифесте создаёт 27 документов, `docs status` показывает
`write` у всех. Правка `.html` переводит документ в устаревшие, правка `.scss` —
нет.

### Шаблон компонента попал в `sources`, а не в новое поле

`templateUrl` разрешается относительно файла компонента, и `.html` дописывается
в `Symbol.sources` рядом с `.ts`. Шаблон и есть часть исходника компонента,
а список источников агент шага 3 и так читает — он печатается в генерируемом
блоке. Новое поле модели тут не нужно, и это четвёртый сдвиг золотого файла,
которого удалось не делать.

Считается это в `web/tree.py`, а не в `materialize/build.py`, как говорил план:
`impl_hash` кладётся в манифест на шаге 1, а шаг 2 его только переписывает
во front matter.

### Скелеты лежат плоско, а не в `templates/web/`

`load_templates` обходит каталог **не рекурсивно**, и это намеренно: рядом лежат
`templates/examples/` с заполненными образцами и `templates/business/`. Рекурсия
затянула бы их в набор скелетов. Имена при этом не сталкиваются: вид сущности —
не свойство языка.

Заодно нашлось, что инвариант «набор правил и комплект скелетов не разъезжаются»
проверялся по **одному** файлу правил. Теперь сравнивается объединение двух
наборов плюс виды-повышения `page` и `api-service`: их не объявишь в правилах,
и без них тест перестал бы ловить забытый скелет ровно у тех двух видов,
которые заведены не правилом.

### Разделы блока печатаются только у фронта

«Маршрут страницы» и «Вызовы к бэкенду» — у узлов модулей с `lang: ts` и только
у них. Симметрии с бэкендом здесь нет и в предметной области: у контроллера
маршрута страницы не бывает, а пустой раздел «Нет.» в каждом документе .NET был
бы шумом ради формы. Различает их `BuildContext.web_modules`, посчитанный
по `Module.lang`.

### Названный пробел: экшены стора

План требовал в блоке страницы ещё и экшены стора. Их нет, и это не забывчивость:
цепочка NGXS «компонент диспатчит → стейт обрабатывает → сервис зовёт»
не собирается **ни одной** задачей плана, а поле `WebCall.via_action`, заведённое
в F10, остаётся пустым. Чтобы его заполнить, нужен отдельный разбор
`dispatch(new X(…))` и связывание по строке типа экшена — работа размером с F07,
и делать её походя внутри F14 значит не проверить её ничем. Записано в план.

### `module_csproj` переименован

Решение, отложенное в F10. На web-документе ключ врал в каждом файле
(`module_csproj: angular.json`). Переименован в `module_project_file`; цена —
разовая перегенерация всех документов, включая золотое дерево. Данные
не теряются: блок `docpipe:` пересобирается на каждом прогоне.

### Проверено

```
uv run docpipe web scan --root tests/fixtures/WebWorkspace --out /tmp/web.json
uv run docpipe materialize /tmp/web.json --root /tmp/webdocs   → 27 документов
uv run docpipe docs status /tmp/web.json --root /tmp/webdocs   → 27 × write, empty
uv run pytest -q                                               → 1433 passed
```

---

## F15 — бизнес-слой: якорь `page` ✅

**Изменено:** `docpipe/business/model.py`, `docpipe/business/resolve.py`,
`docpipe/business/lint.py`, `docpipe/cli.py`, `docpipe/web/tree.py`,
`docs/business-layer.md`
**Создано:** `tests/test_business_web.py` (11 тестов)

`page: "/models/loader/quiz"` разрешается в узел манифеста фронта. Маршрут
нормализуется той же функцией, что и на обеих технических сторонах, поэтому
`/models/:id`, `models/{}` и `/MODELS/:id/` — один якорь.

### Ловушка, которую поймал упавший тест

Первая реализация клала в факты страницы только список реестра. Тест «смена
маршрута меняет `business_hash`» упал: после смены маршрута якорь перестаёт
разрешаться, а неразрешённый якорь входит в хэш «своей идентичностью с пустыми
фактами» — и если фактов не было и раньше, хэш **не меняется**. То есть
исчезнувшая страница выглядела бы как «ничего не произошло».

Починка: в факты кладётся и сам `route`. Он дублирует `ref` якоря, и это
не избыточность — у неразрешённого якоря фактов нет вовсе, и разница видна.
Та же дыра, замечу, есть у якоря `type`: он разрешается с пустыми фактами,
и его исчезновение ловит только линт. Здесь она закрыта.

### Пришлось сделать зависимости на фронте

Путь «страница → список» идёт через сервис: компонент внедряет `ItemsService`,
сервис зовёт `api/items` с именем списка. Зависимостей у web-узлов не было
вовсе — `build_nodes` шага `web` их не считал, и раздел «Зависимости»
в документах фронта был пуст у всех.

Добавлен разбор параметров конструктора. Общую `tree.parameter_types`
переиспользовать нельзя: в C# порядок `Тип имя`, в TypeScript `имя: Тип`,
и общая функция разобрала бы `private` как тип и выдала зависимость
от модификатора.

Обход по зависимостям — **на один шаг**, а не транзитивный: замыкание
притянуло бы в факты страницы всё, до чего дотягивается любой сервис
приложения, и хэш менялся бы от правок, к странице отношения не имеющих.

### Покрытие страниц — информационная проверка

`pages-uncovered` рядом с `uncovered`: печатается числом, кода возврата
не меняет. Линт, красный с первого дня, выключат на второй.

### Второй манифест доезжает через конфигурацию

`business lint --web-manifest`, по умолчанию — `web.out` из `docpipe.yaml`.
Файла нет — страниц ноль, а не отказ: репозиторий без фронта это законное
состояние.

### Проверено

```
uv run pytest tests/test_business_web.py -q  → 11 passed
uv run pytest -q                             → 1444 passed
```

---

## F16 — владение на web-узлах ✅

**Изменено:** `docs/ownership.md`
**Создано:** `tests/test_ownership_web.py` (8 тестов)

Кода править не пришлось: владение считается по узлу, а узел фронта устроен
так же, как узел .NET. Работа задачи оказалась в другом — назвать предикаты,
которые на фронте читаются **иначе**, и те, которые вырождаются.

### Три предиката читаются иначе, и молча

Перенесённые с .NET-привычкой, они дают ноль совпадений, и выглядит это
сломанным инструментом, а не ошибкой в файле:

| Предикат | На .NET | На фронте |
|---|---|---|
| `module_glob` | путь `.csproj` | каталог-граница (`src`, `nx-app/apps/widget`) |
| `namespace_prefix` | `Sbt.Cashflow.ML` | каталог файла (`src/app/inner-debt`) |
| `fqn_prefix` | начало FQN из namespace | путь файла без расширения |

Отдельно стоит `module_glob` против `module`: первый сравнивает **путь**,
второй — имя. На .NET разница видна редко, на фронте она бросается в глаза:
`module_glob: ["widget"]` не совпадёт ни с чем, `module: ["widget"]` совпадёт.

### `inherits` вырождается, и это не чинится

Наследование в Angular — редкость: на фикстуре замыкание непусто у трёх узлов
из двадцати семи, и все три — абстрактный базовый сервис с двумя потомками.
Граница команд на фронте проходит по каталогам и модулям, поэтому основные там
`path_glob` и `module_glob`. Правило по контракту типа не сломается — оно просто
не сработает, и `docs owners --lint` назовёт его мёртвым.

Именно на этом и стоит проверка: тест заводит набор с одним «.NET-овским»
правилом и утверждает, что линт называет его мёртвым.

### Проверено

```
uv run pytest tests/test_ownership_web.py -q  → 8 passed
uv run pytest -q                              → 1452 passed
```

---

## F17 — детерминизм и боевой объём ✅

**Создано:** `tests/test_web_determinism.py` (6 тестов)

### Замер на дереве из 1301 файла

Дерево сгенерировано: 650 компонентов с внешними шаблонами, 650 сервисов,
одна таблица роутов на 650 записей.

```
файлов .ts: 1301
время: 1,4 с; пик памяти: 82 МБ
узлов: 1301, вызовов: 1950, страниц: 650
```

Для сравнения: боевой модуль `Sbt.CMS.Cashflow.ML` — 1258 файлов `.ts`, из них
480 спеки и декларации. То есть замер сделан на объёме чуть больше боевого.

### `--jobs` у `web scan` нет, и это решение, а не пропуск

Критерий плана требовал сверить `--jobs 1` и `--jobs 8`. Флага нет: при 1,4 с
на полном объёме пул процессов стоил бы дороже самой работы. Многопроцессность
у шага 1 появилась потому, что разбор C# кратно дороже.

Если объём вырастет, разбор параллелится без переделки: и `FileParseResult`,
и `RawCall` сериализуемы, а дерево tree-sitter за границу процесса
не передаётся вовсе — оно живёт внутри одного вызова `_parse_files`.

### Детерминизм проверяется на генерированном дереве, а не на фикстуре

На двадцати семи файлах случайный порядок обхода может совпасть
с сортированным, и тест позеленеет, ничего не проверив. Генератор даёт 360
файлов в восьми каталогах; `os.walk` подменяется на перевёрнутый, и сравнивается
**манифест целиком**, а не список идентификаторов: сортировка узлов проверяла бы
только сортировку узлов.

Отдельно проверено, что прогон с кэшем даёт ровно то же, что холодный: кэш,
меняющий вывод, — это тот же недетерминизм, только отложенный до второго
прогона.

### Проверено

```
uv run pytest tests/test_web_determinism.py -q  → 6 passed
uv run pytest -q                                → 1458 passed
```

---

## F18 — документация ✅

**Создано:** `docs/web.md`
**Изменено:** `README.md`, `CLAUDE.md`, `docs/manifest.md`,
`docs/configuration.md`, `docs/frontend-analysis.md`

`docs/web.md` — справочник по выходу шага: три файла, команды, устройство
модуля и узла, пять категорий отчёта связи, шесть шагов нормализации ключа
и таблица «чего шаг не делает». Каждая команда из документа скопирована
и прогнана; `--fail-on duplicate_endpoints` даёт код 1, опечатка в имени
категории — код 2.

### Что дописано в `CLAUDE.md`

Двенадцать ловушек фронта отдельным разделом. Три из них план предсказал
(декоратор-сосед, невыровненные списки захватов, счётчик вызовов без
получателя), остальные нашлись реализацией:

- JSDoc — сосед `export_statement`, а не объявления;
- три клаузы наследования устроены по-разному;
- `tsconfig.json` — это не JSON;
- `paths` дочернего конфига замещает родительский целиком;
- `() => import(…).then(m => m.X)` — это две стрелки;
- `require_public: true` отсеивает весь фронт;
- `sourceRoot` в `project.json` задан относительно корня workspace.

### Аналитика помечена как реализованная

`frontend-analysis.md` писалась до разведки и до кода. В шапке теперь стоит
пометка со ссылкой на план и журнал и названы два главных расхождения: дерево
роутов собирается спредом, а не `loadChildren` (§3.3 писалась под второе),
и конвейер `url-decorator` в боевой сборке выключен вовсе.

### Состояние шага `web`

Все задачи F01–F18 закрыты. Известные пробелы названы в README и в плане:

- **цепочка NGXS не собирается**: `dispatch(new X(…))` не разбирается,
  `WebCall.via_action` всегда пуст, и у страницы нет связи с эндпоинтом через
  стейт. Это отдельная работа размером с F07;
- `this.http.request(method, url)` не поддержан намеренно;
- `--jobs` у `web scan` нет: 1301 файл разбирается за 1,4 с.

### Проверено

```
uv run pytest -q  → 1458 passed
все команды из docs/web.md прогнаны вручную
```

---

## Дефект — `missing` у документа, который лежит на диске ✅

**Изменено:** `docpipe/materialize/plan.py`, `docpipe/materialize/apply.py`,
`docpipe/cli.py`, `tools/why_missing.py`, `tests/test_materialize_plan.py`,
`tests/test_materialize_cli.py`, `docs/materialize.md`, `docs/configuration.md`,
`docs/materialize-implementation-plan.md`, `CLAUDE.md`

Началось с вопроса «почему `materialize` считает документы отсутствующими,
хотя они есть». `missing` ставится в одном месте — `existing is None`,
где `existing = by_path.get(node.doc_path)`, — то есть означает не «файла нет»,
а «`scan_docs` его не вернул». Разница стоит документа: `missing` даёт
`create`, `create` — `write_atomic` поверх существующего файла, без копии
и с бодрым «создано: 1» в отчёте.

### Шесть способов получить `missing` при существующем файле

Все воспроизведены на `tests/fixtures/SampleSolution` до правки:

| Что в дереве | Было | Стало |
|---|---|---|
| UTF-8 BOM в начале документа | `missing`, текст затирался | документ распознаётся |
| front matter без `docpipe.schema` | `missing`, текст затирался | `broken`, отказ |
| `---` не первой строкой (пробел, пустая строка) | `missing` | `broken`, отказ |
| каталог за симлинком | `missing` | `broken`, отказ |
| нет прав на чтение файла | `missing` | `broken`, «файл не читается» |
| `docs_scan_exclude` накрыл дерево документов | `missing` на всём дереве каждый прогон | блокирующая ошибка |

Проверка потери текста: в документ дописывалась строка, добавлялся BOM,
запускался `materialize` — строки в файле не оставалось, в отчёте «создано: 1».

### Почему BOM — самое обидное

`read_document` читает `utf-8-sig` и в докстроке прямо объясняет, зачем;
на это есть тест `test_bom_is_stripped`. Но отсев по первым байтам в `scan_docs`
стоял **до** разбора и сравнивал `read_bytes()[:4]` с `b"---\n"`. Ловушка была
обезврежена на уровень ниже и заново создана уровнем выше, а тест звал
`read_document` напрямую и этого не видел. Отсев нужен (в `.venv` встречаются
`.md` на мегабайты) — но он обязан отвечать на вопрос «наш ли это файл» так же,
как разбор.

### Решение: спрашивать диск, а не перечислять причины

Перечислить способы стать невидимым нельзя — шестой появится вместе
со следующей платформой. Поэтому `shadowed_docs` сверяет узлы без файла
с реальным `is_file()`. Совпало — `refuse`/`broken`, и `content` не собирается
вовсе: `--force` пересоздаёт то, что план собрал, а что лежит в этом файле,
инструменту неизвестно. Файловую систему трогает отдельная функция, как
`check_links`: план остаётся чистым и получает готовый список через
`PlanOptions.shadowed`.

Плюс структурный запрет в самой записи: `create` поверх существующего файла —
ошибка прогона, независимо от того, кто собрал план. Штатно до него не доходит.

### `docs_scan_exclude` против самого дерева

Пара `docs_root` + `modules_dir` держит инвариант «пишем туда, где ищем»
структурно, и этого казалось достаточно. Не достаточно: `docs_scan_exclude:
["docs/ml/**"]` из конфигурации поставки отсекает все документы, как только
`docs_root` окажется под `docs/ml` — а `business_root` там уже лежит.
Воспроизведено: шесть документов на диске, `missing 6` и «создано: 6» на каждом
прогоне. Шаблоны сверяются с реальными `doc_path` манифеста (не с префиксом:
так проверка не зависит от того, как записан шаблон), находка блокирующая.

### `tools/why_missing.py`

Скрипт диагностики, написанный до этой правки, успел разойтись с оригиналом
ровно там, где был баг: он повторял четырёхбайтовый отсев и советовал
«пересохраните без BOM». Теперь зовёт `opens_front_matter` из `docpipe`
и сверяется с фактическим выходом `scan_docs` — иначе случай с симлинком
он объявлял бы принятым, пройдя все фильтры по одному.

### Проверено

```
uv run pytest -q  → 1468 passed  (+10 тестов)
все шесть сценариев прогнаны на SampleSolution до и после правки
uv run python tools/why_missing.py … на дереве с симлинком → называет причину
```

---

## Видимость `file_action` и команда `docs explain` ✅

**Создано:** `docpipe/materialize/explain.py`, `tests/test_docs_explain.py`
**Изменено:** `docpipe/materialize/status.py`, `worklist.py`, `apply.py`,
`docpipe/cli.py`, `schema/doc-worklist.schema.json`, `tests/test_docs_status.py`,
`tests/test_worklist.py`, `tests/test_materialize_cli.py`, `docs/materialize.md`,
`docs/configuration.md`, `README.md`, `CLAUDE.md`
**Удалено:** `tools/why_missing.py`

Два вопроса подряд с боевой настройки — «почему документ `update`, а не
`created`» и «какие именно документы будут обновлены» — оказались одним:
`file_action` не выходил наружу нигде. `docs status` показывает `status`
и решение агента, JSON — то же самое, отчёт `materialize` — только счётчики
и раскладку по каталогам для созданных. Действие с файлом было внутренним
полем плана.

### Что сделано

- `file_action` — колонка в `docs status`, поле в `document_json` (значит,
  и в очереди шага 3), фильтр `--file-action`;
- **`current` перестал молча исчезать** из подробного списка, когда его файл
  переписывается. Смена владельца или домена меняет проекцию: статус остаётся
  `current`, решение агенту — `skip`, а файл открывается на запись. Единственный
  случай, когда действие есть, а строки в отчёте не было;
- «Где обновлено» в отчёте `materialize` — той же раскладкой по каталогам,
  что и «Где создано»: полный список на боевом объёме топит отчёт;
- `docs explain МАНИФЕСТ ПУТЬ` — трассировка одного документа.

### Почему `docs explain`, а не расширение отчёта

Отчёт по дереву группирует по статусу и на три вопроса не отвечает
структурно: какой из пяти фильтров обхода отбросил лежащий на диске файл;
чем именно собранный текст отличается от файла; не задевает ли перезапись
авторские секции. Первый забран из `tools/why_missing.py` — скрипт после
этого удалён, его причины стали частью команды.

Сравнение идёт **по зонам**, а не построчно: у зон разная судьба при прогоне,
поэтому «поменялся генерируемый блок» и «поменялась авторская секция» —
разные новости. Второе означает нарушение главного инварианта шага 2,
печатается отдельной строкой и меняет код возврата: ловить это надо
проверкой, а не глазами в выводе.

### Формат очереди 1.0 → 1.1

Добавление поля версию всё равно двигает: конверт затем и заведён, чтобы
читающий не гадал, тот ли это формат, а строгий читатель ломается и на новом
ключе. `WorklistEntry` с `extra="forbid"` сработал как задумано — тест упал
сразу, как только `document_json` получил поле, и о расхождении стало известно
на тестах, а не у потребителя.

### Ловушка именования

`format_explain` уже занято: так называется вывод `anchors explain` из
бизнес-слоя, и `ruff` поймал переопределение импорта. Здесь функция
`format_explain_document`. Команды при этом две с одним словом в имени
(`anchors explain` и `docs explain`), и это нормально: они про разные объекты.

### Проверено

```
uv run pytest -q  → 1482 passed  (+14 тестов)
docs explain на стенде: update с названной причиной, невидимый документ
с названным фильтром, --diff, путь в обоих видах
```

---

## Секционный `rules.yaml` и поставка шага `web` ✅

**Создано:** `rules/rules.yaml`, `tools/migrate_rules.py`, `tests/test_migrate_rules.py`
**Удалено:** `rules/dotnet.yaml`, `rules/web.yaml` (перенесены в секции)
**Изменено:** `docpipe/classify.py`, `config.py`, `cli.py`, `emit.py`, `web/tree.py`,
`deploy/install.sh`, `deploy/cashflow-docspipe/{docpipe.yaml,rules.yaml,README.md}`,
`docpipe.example.yaml`, десяток тестов, `docs/{configuration,web,materialize,parser-architecture}.md`,
`README.md`, `CLAUDE.md`

Пришло с боевой настройки двумя вопросами: «как настраивать шаг `web`»
и «почему наборов правил два файла, а в проекте лежит один». Ответ на второй
оказался неприятным: указать оба ключа на один файл было **нельзя**, а попытка
кончилась бы пустым деревом фронта без единого сообщения — `require_public: true`
из набора .NET отсеивает весь TypeScript, потому что модификатора `public`
на уровне объявления там нет вовсе.

### Формат

Один файл, секция на шаг:

```yaml
version: "1"          # свойство формата файла
dotnet:               # scan, symbols, validate
  ruleset_version: …
web:                  # web scan
  ruleset_version: …
```

`load_ruleset(path, section)` — секция обязательна и умолчания не имеет.
Обсуждалась мягкая форма «плоский верх = .NET, секция только для web»:
она не требует миграции, но оставляет спецслучай ровно там, где ошибка
молчаливая. Выбрана строгая: плоский файл — отказ с командой переноса.

### Почему перенос текстовый

`yaml.safe_load` + `yaml.dump` дал бы формально верный файл без единого
комментария. В настроенном наборе комментарий объясняет, **почему** решение
принято именно такое, — то есть ровно то, ради чего файл и читают.
`tools/migrate_rules.py` сдвигает строки на два пробела и не делает больше
ничего; `version` поднимается в шапку. Своими же файлами репозитория он
и проверен: `rules/dotnet.yaml` + `rules/web.yaml` → `rules/rules.yaml`.

### Поставка отставала ровно на шаг `web`

- секции `web` в `docpipe.yaml` поставки не было вовсе, поэтому все четыре
  ключа брали умолчания: правила искались в несуществующем `rules/web.yaml`,
  а манифест и отчёт связи писались в корень репозитория продукта — мимо
  `.gitignore` инструмента;
- набора правил фронта в бандле не было ни под каким именем;
- `README.md` бандла про шаг не упоминал.

Всё три закрыты. `url_rewrite` заполнен не именами модулей, а таблицей
префиксов в комментарии: имя модуля считает сам инструмент из `angular.json`,
и выдуманное имя дало бы правило, которое никогда не совпадёт.

Установщик теперь кладёт `tools/migrate_rules.py` (сообщение об ошибке зовёт
его по имени — значит, он обязан доехать до целевой машины) и **сам замечает**
сохранённый плоский набор: `keep_configured` файл не трогает, и без
предупреждения обновление выглядит удачным до первого прогона.

### Проверено

```
uv run pytest -q  → 1496 passed  (+14 тестов)
оба поставочных набора грузятся обеими секциями
install.sh: прогон на пустом дереве, повторный прогон с плоским rules.yaml
```

## Исправление: пути конфигурации искались только от текущего каталога ✅

Найдено при попытке завести первый бизнес-документ на боевой раскладке:
`docpipe business new` падал со «Скелет не найден: templates/business/process.md»
на конфигурации, лежащей в `docs/ml/cashflow-docpipe/docpipe.yaml` с ключом
`templates: "templates"`. Путь написан относительно самой конфигурации — то есть
так, как его может проверить глазами автор, — а искался от текущего каталога.

### Причина не в бизнес-слое

Первая версия диагноза («`business new` ведёт себя не так, как остальные
команды») оказалась неверной, и это стоило захода. Воспроизведение на чистом
`main` на той же конфигурации:

```
docpipe business new … → Скелет не найден: templates/business/process.md
docpipe scan …         → Ошибка конфигурации: No such file or directory: 'rules.yaml'
```

Все ключи, называющие файлы инструмента — `rules`, `web.rules`, `templates`,
`registries`, `ownership`, — разрешались одинаково: `Path(settings.X)`, то есть
от текущего каталога. Ловушка была описана в четырёх местах (`CASHFLOW.md`,
`deploy/README.md`, `docs/materialize.md`, `docs/configuration.md`) как
свойство, которое надо знать, и обходилась в поставке явным путём от корня
репозитория. Обходной путь работает ровно до первой конфигурации, которую
завели не по инструкции поставки. Здесь это и случилось.

### Решение: вторая ступень, а не смена смысла

`config.resolve_input(value, config)` ищет по кандидатам, первый существующий
выигрывает: сначала от текущего каталога, затем от каталога `docpipe.yaml`.

**Порядок обратным быть не может.** Конфигурации в корне репозитория писались
с путями от текущего каталога, и он там совпадает с каталогом конфигурации.
Отдай приоритет второй ступени — на раскладках, где каталоги разные, прогон
не упал бы, а взял другой набор правил или шаблонов, ничего не сказав. Тихо
взятый не тот набор дороже отказа, поэтому старое поведение осталось первым.

Не найдено нигде — возвращается первый кандидат: сообщение об ошибке обязано
называть то, что человек написал в конфигурации, а не последнее из перебранного.
Для этого же список кандидатов вынесен в `candidate_inputs`: `business new`
перечисляет в отказе все пробованные пути, потому что «не найден» с одним путём
заставляет гадать, искался ли второй.

**Цели записи ступени не получили.** `out`, `worklist`, `web.out`,
`web.link_out` — «первый существующий» для них не значит ничего, а угадывать,
куда писать, инструмент не должен. Разделение получилось читаемым и заменило
в `docs/configuration.md` таблицу двух баз отсчёта на таблицу трёх.

### Несимметричный случай: `web.out` читает `business build`

Ключ — цель записи `web scan`, но `business build` его **читает**, и второй
ступени он там тоже не получил: писатель её получить не может в принципе,
а дай её одному читателю — один ключ разошёлся бы на две раскладки, где
`web scan` пишет в один файл, а `business build` читает другой.

Молчать при этом нельзя. «Страниц ноль» — законный исход (репозиторий без
фронта), и по нему не отличить отсутствие фронта от прогона из другого каталога.
Различимый признак ровно один: манифест лежит рядом с конфигурацией. На нём
и печатается предупреждение — при отсутствии фронта вовсе не печатается ничего,
иначе подсказка, выводящаяся всегда, перестала бы что-либо значить.

### Трудность

Три помощника получали уже разобранный `DocpipeConfig` и пути конфигурации
не видели: `_load_ownership_quietly`, `_with_business_links`, `_registries_path`.
Путь пришлось протащить дополнительным параметром через них и четырёх
их вызывающих. Альтернатива — хранить каталог конфигурации полем модели —
отвергнута: модель с `extra="forbid"` валидируется прямо из YAML, и поле
стало бы ключом, который можно задать руками, получив третий источник истины
о том, откуда считать пути.

### Проверено

Воспроизведением до правки и после, на раскладке из отчёта: конфигурация
в `docs/ml/cashflow-docpipe/`, команды из корня. До — «Скелет не найден»
и «No such file or directory: 'rules.yaml'», после — «Создано:
docs/ml/cashflow-docpipe/business/processes/pricing/eod.md» и «Модулей: 2,
узлов: 6». Случай без `--config` по-прежнему отказывает, и это правильно:
относительно чего искать, там неизвестно, — но сообщение теперь называет
обе ступени.

```
uv run pytest -q  → 1510 passed  (+14 тестов)
```

Новые тесты — `tests/test_config_paths.py`: приоритет текущего каталога при
обоих существующих путях, падение на вторую ступень, имя в сообщении при
отсутствии обоих, абсолютный путь одним кандидатом, отсутствие конфигурации,
совпадение обеих ступеней (дубль в отказе выглядел бы ошибкой инструмента),
сквозные `scan` и `web scan` с правилами рядом с конфигурацией, `business new`
на боевой раскладке, перечисление кандидатов в отказе, `--templates` без второй
ступени, `out` не подхватывает одноимённый файл рядом с конфигурацией
и оба исхода предупреждения `business build`.

## F19 — `docpipe web pages`: список страниц и обоснование ✅

**Создано:** `docpipe/web/pages.py`, `tests/test_web_pages.py`
**Изменено:** `docpipe/model.py`, `docpipe/web/routes.py`, `docpipe/web/tree.py`,
`docpipe/cli.py`, `schema/doc-tree.schema.json`, `docs/{web,manifest}.md`,
`docs/frontend-implementation-plan.md`, `README.md`, `CLAUDE.md`

Пришло с боевого прогона вопросом «что для инструмента страница». Ответ —
«компонент, до которого дошла таблица роутов» — проверить было нечем: вид
`page` единственный, который выдаётся **не правилом**, поэтому ни `scan --stats`,
ни `symbols` его не объясняют. Оба показывают срабатывания правил, а правило
здесь сказало `component`. Наружу выходило одно число «страниц N», и сверить
его с кодом можно было только чтением манифеста руками.

### Отчёт читает манифест, а не исходники

Своим разбором команда отвечала бы на другой вопрос — «что получилось бы
сейчас» вместо «что лежит в файле» — и расходилась бы с манифестом на каждой
смене конфигурации. Плата за это: всё, чего в манифесте нет, надо было туда
положить.

### Откуда взялся маршрут — новые поля `RouteEntry`

`source` (файл таблицы) и `table` (имя массива). Без них обоснование
нечестное: дерево собирается межфайлово, и `/models/loader/quiz` объявлен
не в `app.routes.ts`, через который до него доходит обход, а в
`routesPath/models.ts`. Оба поля попали и в ключ сортировки записей: две
записи, совпадающие путём и компонентом, до этого упорядочивал обход.

### Найдено по дороге

**Невосстановленная запись не проходила через `normalize_route`.** Ветка
с выражением в сегменте клала накопленный префикс как есть — с `:id`
и в исходном регистре, в отличие от всех остальных записей. На фикстуре
не видно (префикс `models` нормализован сам по себе), на боевом дереве дало бы
две формы одного пути в одном списке. Починено там же.

**Вызовы страницы приходится вычислять обходом.** `web_calls` лежат на узле
того файла, где вызов записан, то есть на сервисе, — и это правильное решение
(связь строит сервис). Значит «API страницы» — производная величина, и у неё
обязаны быть видны глубина и посредник, иначе список выглядит как факт
манифеста, которым не является. Отсюда `--depth` и строка `<- через X, шаг N`.

**«Вызовов ноль» и «страница пустая» — разные утверждения.** Экшен NGXS
создаётся в теле метода, а не внедряется конструктором, поэтому цепочка
`dispatch → стейт → сервис` обходом не видна вовсе (известное ограничение,
F14). Отчёт, оставивший ноль без объяснения, привёл бы к решению
«документировать нечего» на странице с функционалом. Разведены две заметки:
«признаков функционала не найдено» (ни членов, ни шаблона, ни вызовов —
кандидат в layout) и «вызовов не найдено, но функционал есть».

**Страница, на которую нельзя поставить якорь, не называлась нигде.** Если все
записи роутов узла с `route_unresolved`, вид `page` он получил, а в
`pages_by_route` бизнес-слоя не попал — и `pages-uncovered` о нём молчит.
Теперь это отдельная заметка и отдельный счётчик.

### Что отчёт делать не стал

Наблюдения остаются текстом и `kind` в манифесте **не меняют**. Соблазн был:
`ShellComponent` с пустым маршрутом, нулём членов и без шаблона — очевидная
обёртка с `<router-outlet>`, и её хочется выкинуть из страниц автоматически.
Но угаданная не-страница молча выкидывает экран из документации, а угаданная
страница ломает якорь бизнес-документа. Решение принимает человек — правилами
и конфигурацией; отчёт даёт основание для решения.

### Проверено

```
uv run pytest -q  → 1526 passed  (+16 тестов)
uv run docpipe web pages /tmp/web.json --not-pages
  → 5 страниц, у каждой файл и имя таблицы; 2 компонента без маршрута
```

Тесты — `tests/test_web_pages.py`: состав страниц фикстуры, файл и таблица
маршрута, `matched_rules` при повышении, вызовы через зависимость с указанием
шага, `--depth 0` против `--depth 1`, обратная сторона `--not-pages`, четыре
заметки, счётчики по всему дереву при суженном списке, детерминизм трёх
форматов, `\n` в CSV, сквозной прогон CLI и подсказка про `RouterModule.forRoot`
при пустом списке.

---

## P01 — вызов знает член, в котором записан ✅

**Создано:** `docpipe/web/members.py`, `tests/test_web_call_member.py`
**Изменено:** `docpipe/model.py`, `docpipe/web/tree.py`, фикстура `WebWorkspace`

Разбора не понадобилось: у `WebCall` есть файл и строка, у члена символа —
`line` и `end_line`. Владельцем становится **самый узкий** накрывающий
диапазон; перебор в порядке объявления отдал бы вызов из локальной стрелки
первому попавшемуся накрывающему методу.

Две мелочи, каждая из которых сделала бы поле бесполезным на части случаев:
границы сравниваются нестрого (у поля-стрелки `line == end_line`), а ключ
индекса — **первый** источник символа: шаблон `.html` дописывается в `sources`
шагом `web`, и его строки к членам отношения не имеют.

Фикстура дополнена четырьмя формами: вызов в методе, вызов в локальной стрелке
внутри метода, поле-стрелка, фабрика уровня модуля. У последней `member`
пустой — это состояние «записан вне члена», а не неизвестность.

```
uv run pytest -q  → 1534 passed  (+8 тестов)
числа вызовов фикстуры: 16 -> 19
```

## P02 — граф вызовов: какие методы зависимостей зовёт узел ✅

**Создано:** `docpipe/web/usage.py`, `docpipe/web/queries/usage.scm`,
`tests/test_web_usage.py`
**Изменено:** `docpipe/model.py`, `docpipe/web/tree.py`, фикстура

`Usage(target, member, via)` — ребро графа вызовов. Именно подмена этого ребра
внедрением давала на боевом модуле `ForecastComponent` 51 эндпоинт и 43
эндпоинта из 61 «ровно на четырёх страницах».

**Разрешается только однозначное.** Обмен осознанный: ложное ребро втянет
в страницу чужой сервис и породит неверный раздел документа, пропуск лишь
оставит раздел короче. Поэтому берутся `this.x.m()` и `x.m()`, а `this.a.b.m()`
пропускается — тип `b` известен только после разбора типа `a`.

Тип получателя собирается **из своего класса**: пары «имя → тип» из конструктора
и из полей `inject(…)`, попавших в диапазон символа. Таблица на файл подставила
бы чужой тип там, где два класса объявляют одноимённое поле. Имя типа
разрешается таблицей импортов файла, а не глобальным индексом простых имён.

Три счётчика вместо одного: `usages`, `usages_external` (`HttpClient`, `Store`,
`Router` — узла документации у них нет), `usages_unresolved`. Одно число
не сказало бы, сколько графа потеряно.

Разбор сигнатуры конструктора переехал в `usage.py` и стал общим
с `_dependencies`: две копии разошлись бы на первой же форме параметра.

```
uv run pytest -q  → 1546 passed  (+12 тестов)
```

## P03 — цепочка NGXS ✅

**Создано:** `docpipe/web/store.py`, `docpipe/web/queries/store.scm`,
`tests/test_web_store.py`, фикстура `audit.state.ts`
**Изменено:** `docpipe/model.py`, `docpipe/web/tree.py`, `docpipe/web/calls.py`

Названный пробел F14 закрыт. Звено, которого «не хватало», лежало в манифесте
с самого начала: у члена стейта записан атрибут `Action` с именем класса
экшена. Связывание идёт резолвом этого имени через таблицу импортов — надёжнее
строки `type`, которую разбор полей не берёт.

**Цепочка стала рёбрами того же графа, что и P02.** Страница обращается к члену
стейта, помеченному `@Action`; стейт обращается к методу сервиса. Отдельный вид
связи потребовал бы второго обхода в каждой задаче, которая ходит по графу.

### Отклонение от плана: `via_action` — не про путь

План требовал пометить `via_action` у вызова `InnerDebtService.byClient`. Так
делать нельзя: вызов записан в сервисе, и тот же метод зовут и обработчик,
и компонент напрямую, — пометка утверждала бы, что до него **всегда** доходят
через стор. Тип экшена уехал на ребро (`Usage.action`), а `WebCall.via_action`
остался за вызовом, записанным внутри обработчика. План поправлен.

Заодно из `build_calls` убран мёртвый параметр `via_action`: он ключевался
по строке без файла и не заполнялся ничем с самого F10.

### Формы, каждая из которых давала бы ноль

- `dispatch([new A(), new B()])` — массив разворачивается; «первый аргумент
  как объект» дал бы ноль ровно там, где пишут пакетную загрузку экрана;
- один экшен обрабатывают **два стейта** — законно в NGXS (данные в одном,
  журнал в другом), и «первый попавшийся обработчик» терял бы половину;
- `@Select(S.items)` и `store.selectSnapshot(S.items)` — одно и то же ребро;
- экшен без обработчика — состояние работы (`actions_without_handler`),
  а не ошибка: обработчик может жить в подписке или эффекте вне стейта.

```
uv run pytest -q  → 1557 passed  (+11 тестов)
на фикстуре: dispatches 2, actions_without_handler 1, selects 2
```

## P04 — точный список эндпоинтов страницы ✅

**Изменено:** `docpipe/web/pages.py`, `tests/test_web_pages.py`

Обход переехал с зависимостей на граф вызовов и стал идти **по парам «узел,
член»**: ребро `uses` говорит не «страница знает сервис», а «член страницы
зовёт вот этот член сервиса», и вызовы берутся только из этого члена. На
фикстуре сразу видно, ради чего: у `DetailComponent` два эндпоинта вместо пяти,
которые объявляет `ModelService`.

**Старая величина не выброшена, а печатается рядом** — «2 зовёт, 5 достижимо
по внедрению». Разница не дефект, а мера того, сколько чужого приезжало
в документ страницы; без второго числа нельзя понять, стало ли лучше.

Собственные вызовы страницы — шаг ноль. Компонент, зовущий `http` сам,
в Angular редкость, но он есть, и потерять его нельзя.

**Смысл нуля изменился, и заметка сменилась вместе с ним.** До графа вызовов
«вызовов не найдено» означало «обход не видит цепочку»; теперь — «страница
не ходит за данными сама», то есть данные приходят через `@Input` или
из платформенного грида. Оставить прежний текст значило бы объяснять
несуществующую причину. Вызовы из шаблона по-прежнему невидимы, и отчёт
говорит это словами.

```
uv run pytest -q  → 1560 passed
на фикстуре: 7 эндпоинтов зовут страницы, 10 было бы по внедрению
```

## P05 — доработки отчёта: контейнеры, CSV, `SetApiUrl` ✅

**Изменено:** `docpipe/web/pages.py`, `docpipe/web/usage.py`, `tests/test_web_pages.py`,
`tests/test_web_usage.py`

**Дочерние страницы считаются по сегментам маршрута.** Это факт из таблицы
роутов, а не эвристика: на боевом модуле `/forecast/{}` имеет семь дочерних
при 45 членах и своих вызовах, а `…/structuring` — три дочерние, два члена
и ни одного вызова. «Мало членов — значит контейнер» их не разводит, число
разводит. Сравнение по сегментам обязательно: подстрока сделала бы
`/forecast` родителем `/forecaster`.

**CSV получил путь вызова, число дочерних и обе величины эндпоинтов.** По этому
файлу ведут разметку страниц руками, и «через кого» нужнее всего именно там.

### `SetApiUrl` в зависимостях шести страниц

Воспроизвести на фикстуре не удалось — узел с таким именем есть только
в боевом наборе правил. Сделано три вещи, и они закрывают вопрос с разных
сторон:

1. после P04 участник попадает в список, **только если у него зовут метод**;
   декоратор-функция так попасть не может;
2. отчёт теперь называет, какие члены зовут у каждого участника, — то есть
   на вопрос «почему он здесь» отвечает сам отчёт, а не догадка;
3. разбор сигнатуры конструктора перестал резать по двоеточию внутри скобок
   и строк. `@Inject('http://token') private api: ApiService` давал тип
   `//token…` — выдуманную зависимость с правдоподобным именем. Форма не редкая:
   так задают токены внедрения и адреса, и на боевом репозитории это самый
   вероятный источник призрака.

Окончательная проверка — прогоном на АС CF (P12).

```
uv run pytest -q  → 1566 passed
```

## P06 — `pages.yaml`: ручной состав страниц ✅

**Создано:** `docpipe/web/overrides.py`, `pages.example.yaml`,
`tests/test_web_overrides.py`
**Изменено:** `docpipe/config.py`, `docpipe/web/tree.py`, `docpipe/cli.py`,
`docpipe/web/pages.py`, `docpipe/business/resolve.py`

**Ручная страница — запись маршрута из другого источника, а не второй
механизм.** Добавление кладёт `RouteEntry` с `source: pages.yaml`,
`table: manual`, и дальше работает всё существующее: повышение вида, документ,
якорь бизнес-слоя, покрытие. Отдельная ветка кода для «ручных» страниц
разошлась бы с автоматической на первом же различии.

Снятие **отменяет повышение, а не удаляет узел**: класс остаётся компонентом
и документируется как компонент. Маршрут при этом сохраняется — это факт кода,
и он же оказался единственным признаком, по которому отчёт отличает «снято
руками» от «маршрута нет вовсе»: пара «маршрут есть, вида страницы нет» другим
способом не получается.

### Что пришлось уточнить по ходу

**`add-redundant` срабатывает на совпадении маршрута, а не на «уже страница».**
Первая версия считала лишним любое правило для узла, который и так страница, —
и запрещала законный случай: у экрана есть своя точка входа и вторая,
собираемая в рантайме.

**Снятый узел обязан исчезнуть из покрытия бизнес-слоя.** `pages_by_route`
строился по всем узлам с маршрутами независимо от вида; снятый экран продолжал
бы требовать бизнес-документ. Теперь индекс берёт только `kind == "page"`.

**Неоднозначное снятие — отказ.** Компонент, стоящий на двух маршрутах
(в боевом модуле это `StructuringCreatePage`: `create` и `edit` — один класс),
снимается целиком, потому что вид у него один. Правило по маршруту, которое
задело бы и второй маршрут, роняет прогон и называет, что нашлось.

**Протухшее правило печатается всегда, код возврата меняет только флаг**
(`--fail-on-stale-overrides`). Линт, красный с первого дня, выключат на второй.

```
uv run pytest -q  → 1582 passed  (+16 тестов)
```

## P07 — принадлежность узла странице ✅

**Создано:** `docpipe/web/absorb.py`, `tests/test_web_absorption.py`
**Изменено:** `docpipe/model.py`, `docpipe/web/tree.py`, золотое дерево, схема

`absorbed_by` проставляется узлу, до которого дотягивается **ровно одна**
страница. Порога нет: «общий» — это «появился второй потребитель». На фикстуре
это сразу видно: `ModelService` уходит внутрь `DetailComponent`, `ItemsService`
— внутрь `QuizComponent`, а `AuditService`, который зовут две страницы,
остаётся самостоятельным документом.

Достижимость считается по графу вызовов и **транзитивно, без ограничения
глубины**. Иначе принадлежность зависела бы от длины цепочки, а не от того,
кто ею пользуется: утилита, которую зовёт сервис страницы, принадлежит
той же странице.

Цепочка NGXS здесь окупилась вторично: `DebtState` достижим от `ListComponent`
только через диспатч (`Store` — внешний тип, ребра к нему нет), и без P03
стейт остался бы самостоятельным документом при единственном потребителе.

Страница страницей не поглощается никогда: вложить экран в экран значит
потерять его якорь.

```
uv run pytest -q  → 1591 passed  (+9 тестов)
на фикстуре поглощено 5 узлов из 30
```

## P08 — состояние `page_covered` ✅

**Изменено:** `docpipe/stats.py`, `docpipe/explain.py`, `docpipe/cli.py`,
`tests/test_stats.py`, `tests/test_explain.py`

Поглощённый узел — принятое решение, а не пробел: своего файла у него нет,
но описан он будет разделом той страницы, которая одна до него дотягивается.
Прецедент и приём те же, что у `interface_covered`: отдельная категория, чтобы
не портить `undecided` — единственное число, которое обязано идти к нулю.

Считается по манифесту, а не по правилам: поглощение — свойство графа вызовов,
и правило о нём ничего не знает. Поэтому `decide` принимает карту поглощений
готовой, а обе точки её сборки (`collect_stats` и `select`) берут её одной
и той же функцией — иначе `--stats` и выборка символов разошлись бы в числах.

### `symbols` научился второму языку

Состояние `page_covered` иначе нельзя было увидеть вовсе: поглощение бывает
только на фронте, а `symbols` умел один язык. Появился `--lang cs|ts`, и он же
выбирает секцию правил — набор .NET, прочитанный для фронта, отсеял бы весь
TypeScript молча (`require_public`).

```
uv run pytest -q  → 1595 passed  (+4 теста)
docpipe symbols --root … --lang ts --state page_covered
  → 5 символов, у каждого названа страница
```

## P09 — агрегатный документ ✅

**Создано:** `tests/test_materialize_page_aggregate.py`
**Изменено:** `templates/page.md`, `docpipe/materialize/build.py`,
`docpipe/materialize/plan.py`, `docpipe/web/absorb.py`, `docpipe/web/pages.py`

Документ страницы стал единицей документации. У поглощённого узла своего файла
больше нет — план его пропускает, — а в документе страницы появились разделы
«Дочерние страницы», «Состояние», «Что зовёт страница», «Данные» и «Состав
документа». Автору добавлены две секции: «Состояние» и «Логика».

**Общий узел попадает в документ ссылкой, а не текстом.** То же правило,
по которому бизнес-слой ссылается на технический: копия разошлась бы
с оригиналом на первой правке.

**Стейт называется по `name` из декоратора**, а не по имени класса:
`innerDebt`, а не `DebtState`. Переименование класса смысла не меняет,
а `name` — контракт, по которому стейт селектят.

### Хэши агрегата

`impl_hash` страницы включает хэши поглощённых узлов, `signature_hash` —
их состав. Первое ловит правку тела сервиса, живущего внутри страницы: своего
документа у него больше нет, и заметить изменение было бы негде. Второе ловит
переезд: узел ушёл из страницы или пришёл в неё, и документ устарел, даже если
ни строки в коде не изменилось.

### Обход у отчёта и у документа один

`web/pages.called` стала публичной и зовётся из `materialize/build.py`. Вторая
реализация того же обхода разошлась бы с первой, и документ показывал бы
не то, что отчёт, — при том, что оба отвечают на один вопрос.

```
uv run pytest -q  → 1607 passed  (+12 тестов)
на фикстуре: 25 документов вместо 30, пять узлов описаны внутри страниц
```

## P10 — переезд и сироты после поглощения ✅

**Создано:** `tests/test_page_orphans.py`
**Изменено:** `docpipe/materialize/plan.py`, `docpipe/cli.py`

Включение агрегата отбирает у поглощённых узлов их документы, а файлы остаются
— с уже написанным текстом, и на боевом репозитории таких будут сотни.
Инструмент их **не удаляет и не трогает**: они получают статус `orphan`
и причину, называющую страницу, внутри которой узел теперь описан.

**Две причины сиротства разведены.** «Узла с таким id в манифесте нет» — класс
исчез или перестал документироваться. «Описывается внутри страницы X» — текст
переехал, и человеку надо решить, перенести его туда или отложить. Одна
формулировка на оба случая отправила бы половину сирот в неправильную работу.

**`docs adopt` отказывается и говорит куда.** Слить авторский текст в раздел
страницы автоматически нельзя: место ему выбирает человек, а не совпадение
имён. Но отказ обязан называть страницу и разделы, иначе он выглядит
как «непонятно что».

Идемпотентность проверена: повторный прогон не создаёт ни новых сирот,
ни новых файлов.

```
uv run pytest -q  → 1611 passed  (+4 теста)
переход «до поглощения -> после» воспроизведён на фикстуре целиком
```

## P11 — бизнес-слой: факты страницы ✅

**Изменено:** `docpipe/business/resolve.py`, `tests/test_business_web.py`

В факты якоря `page` добавлены **эндпоинты** (HTTP-контракт) и **состояние**
(имена стейтов). Считаются они тем же обходом графа вызовов, что и документ
страницы: вторая реализация разошлась бы с первой, и `business_hash` менялся бы
от того, чего в документе не видно.

**Имя стейта берётся из декоратора, а не из имени класса.** `innerDebt`,
а не `DebtState` — по той же причине, по которой якорь пишется на маршрут:
переименование класса смысла не меняет, а `name` — контракт, по которому стейт
селектят. Проверено обеими сторонами: переименование класса хэш не меняет,
смена `name: 'innerDebt'` — меняет.

Факты по нескольким узлам одного маршрута сливаются, а не берутся у первого:
один путь может обслуживаться несколькими компонентами, и «первый» описал бы
половину экрана.

```
uv run pytest -q  → 1614 passed  (+3 теста)
```

## P12 — документация и сводка по P01–P12 ✅

**Создано:** `docs/pages.md`
**Изменено:** `README.md`, `CLAUDE.md`, `CASHFLOW.md`, `docs/web.md`,
`docs/manifest.md`, `docs/configuration.md`

`docs/pages.md` — справочник: что такое страница, две величины эндпоинтов
и почему их две, `pages.yaml`, состав документа-агрегата, состояние
`page_covered`, порядок настройки на новом репозитории. Каждая команда
из него прогнана.

### Что стало на фикстуре

| Величина | До P01–P12 | После |
|---|---|---|
| документов | 30 | 25 |
| узлов внутри страниц | — | 5 (`page_covered`) |
| эндпоинтов у страниц | 10 (по внедрению) | 7 (по вызовам) |
| цепочка NGXS | не видна | 2 диспатча, 2 выборки, 1 оборванная |
| решение не принято | 0 | 0 |

Разница «7 против 10» на фикстуре мала — она и не может быть большой там, где
у сервисов по два метода. Настоящая проверка — на АС CF, где было 51 против
ожидаемых единиц.

### Чего в этом плане не сделано и почему

**Прогон на боевом репозитории не выполнен.** У этой среды нет доступа
к `Sbt.CMS.Cashflow.ML`; все числа боевого модуля в плане и в журнале взяты
из отчёта пользователя (`pages.csv` от 16.08.2026) и из разведки F01. Что
именно проверить первым делом и какие числа записать — в `CASHFLOW.md`,
раздел «Страницы: что проверить после P01–P12». Это единственный критерий
приёмки P12, который закрыт не здесь.

**Вызовы из шаблона по-прежнему невидимы** — грамматика Angular-шаблонов
в план не входила намеренно. Ограничение печатается в отчёте словами, а не
обнаруживается потом по расхождению цифр.

### Поставка догнала P06

`pages.yaml` приезжает в бандле пустым и при обновлении **сохраняется**:
в нём решения человека о том, что считать страницей. `web.pages` в
`docpipe.yaml` бандла на него указывает, `install.sh` кладёт его через
`keep_configured`, README бандла описывает порядок. Это ровно та ловушка,
на которой поставка уже стояла один раз: ключ, указывающий на файл, которого
в поставке нет, выглядит настройкой и ею не является. Оба свойства закреплены
тестами.

```
uv run ruff check . && uv run ruff format --check . && uv run mypy docpipe && uv run pytest -q
  → All checks passed, 1616 passed
```

## P13 — вторая ветка дерева документации ✅

**Создано:** `tests/test_web_docs_tree.py`
**Изменено:** `docpipe/config.py`, `docpipe/web/tree.py`,
`docpipe/materialize/plan.py`, `docpipe/cli.py`, `docpipe.example.yaml`,
поставка, `docs/{configuration,pages,materialize}.md`

Задача пришла после P12: документы фронта лежали вперемешку с документами
бэкенда в одном `modules_dir`. Ключ `web.modules_dir` разводит их на две ветки
внутри `docs_root`:

```
docs/modules/...   бэкенд, единица документации — класс
docs/front/...     фронт,  единица документации — страница
```

Пустое значение — умолчание и совместимость: конфигурации, написанные до ключа,
дают те же пути.

### Почему каталог, а не путь целиком

Тот же довод, что и у `modules_dir`, и он же тут проверен тестом: обход
документов идёт от `docs_root` и накрывает обе ветки разом, поэтому инвариант
«`materialize` пишет туда, где ищет `docs status`» держится структурно.
Отдельным путём его можно развести, и тогда каждый документ фронта навсегда
`missing` и переписывается на каждом прогоне.

### Что сломалось бы без второй половины работы

**Сверка префикса знала только один корень.** `layout_drift` сравнивает пути
манифеста с `docs_root + modules_dir`; манифест фронта со своей веткой не прошёл
бы её никогда — прогон падал бы на каждом запуске. Префикс выбирается
по манифесту (`expected_root`, `is_web`).

**Сообщение об отказе называло не тот ключ.** Отправлять чинить `modules_dir`
там, где человек правил `web.modules_dir`, — отказ дороже отсутствия отказа.
Имя ключа выбирается по тому же признаку и только когда ветки действительно
разведены: пустой `web.modules_dir` даёт ту же ветку, и называть его значило бы
отправить чинить настройку, которой нет.

**Очередь шага 3 несёт префикс отдельным полем.** Взятый из `modules_root`,
он сообщил бы внешнему исполнителю путь, которого нет ни у одного документа
очереди.

### Поставка

`web.modules_dir: "front-docs"` в бандле, README называет ключ и предупреждает:
сохранённая конфигурация приезжает как `.new`, и ключ придётся дописать руками
— иначе фронт продолжит писаться в ветку бэкенда.

```
uv run pytest -q  → 1627 passed  (+11 тестов)
```

## P14 — обрыв цепочки и глубина по умолчанию ✅

**Изменено:** `docpipe/web/pages.py`, `docpipe/business/resolve.py`,
`docpipe/materialize/build.py`, `tests/test_web_pages.py`, `docs/pages.md`

Обе правки продиктованы боевым прогоном на АС CF (17.08.2026), а не фикстурой.

### Обрыв цепочки — след ненастроенных правил

`web scan --stats` на боевом модуле: **1249 символов из 2152 без решения**.
Символ без решения узлом не становится, а обход графа вызовов ходит по узлам —
значит цепочка через него обрывается, и список эндпоинтов страницы оказывается
короче правды. Молча: до этой правки страница с одним эндпоинтом при 51
достижимом выглядела как «экран, который почти не ходит за данными».

`called` возвращает `Walk` с полем `stops`; страница получила `chain_stops`,
отчёт — счётчик, заметку и объяснение, что чинится это настройкой правил,
а не глубиной обхода.

### Глубина по умолчанию: 2 -> 3

Замер тем же прогоном: на глубине 2 страницы зовут 42 эндпоинта, на глубине 4
— 49, и почти вся разница набирается третьим шагом. Три — это и есть полная
цепочка боевого фронта: «страница → стейт (обработчик экшена) → сервис →
сервис». Четвёртый шаг добавляет единицы и уводит документ страницы в чужую
ответственность.

```
uv run pytest -q  → 1630 passed  (+3 теста)
```

## P15 — два источника типа получателя: поля и наследование ✅

**Изменено:** `docpipe/web/usage.py`, `docpipe/web/tree.py`, фикстура,
`tests/test_web_usage.py`

Замер на АС CF (17.08.2026): рёбер найдено **502**, получателей внешних **1527**,
неразрешённых — **3490**. То есть 63 % обращений к членам не давали ребра, и это
вторая дыра в графе страниц, независимая от ненастроенных правил.

Две формы, каждая из которых однозначна ничуть не меньше параметра
конструктора:

1. **поле с аннотацией типа** (`private svc!: ModelService`) — так объявляют
   то, что присваивается в `ngOnInit`, приходит через `@Input()` или
   выставляется фабрикой;
2. **связывание из базового класса** — `this.http` объявлено в базе, а зовут
   его в наследнике. На боевом модуле сервисы поголовно наследуют общий
   базовый класс с внедрёнными зависимостями.

Своё сильнее унаследованного: имя, переопределённое в наследнике, значит его
тип. Поля по-прежнему берутся по диапазону символа — два класса в одном файле
законно объявляют одноимённое поле с разными типами.

Локальные переменные и цепочки через чужое поле остаются неразрешёнными
намеренно: ложное ребро втянет в страницу чужой сервис, а пропуск лишь оставит
раздел короче.

```
uv run pytest -q  → 1633 passed  (+3 теста)
на фикстуре: рёбер 10 -> 12
```

## P16 — раздел: единица документации без маршрута ✅

**Создано:** `templates/feature.md`, `tests/test_web_features.py`
**Изменено:** `docpipe/web/{overrides,tree,absorb,pages}.py`,
`docpipe/materialize/build.py`, `docpipe/business/{model,resolve,lint}.py`,
`pages.example.yaml`, поставка, `docs/pages.md`

Задача пришла с боевого репозитория: каталоги `inner-debt`, `leasing`,
`invest-portfolio` — законченные функциональные единицы со своим `state/`
и `api/`, которые открывают из меню с нескольких экранов.

**Ни один из существующих механизмов их не берёт, и это следствие правил,
а не пробел.** Страницей раздел быть не может: якорь страницы — маршрут,
а URL здесь не меняется, и выдуманный маршрут сломал бы якорь
бизнес-документа. Поглотиться страницей тоже не может: его зовут и `loader`,
и `structuring`, то есть по правилу «ровно одна страница» он общий — и правило
право, спорить с ним нечем.

Значит, раздел **объявляет человек**: `features:` в `pages.yaml`, граница —
каталог. Узел синтетический, символа у него нет: раздел не класс, а решение
о том, что вот эти классы — одна вещь.

### Что выяснилось по ходу

**Объявленное сильнее выведенного.** Порядок поглощения фиксирован: сначала
разделы, потом страницы. `InnerDebtService` без раздела уходил внутрь
`ListComponent`; инструмент не переигрывает решение, записанное явно.

**Раздел «зовёт своих».** Вызовы сервиса, к которому внутри раздела никто
не обращается (его зовут снаружи), пропали бы из «Данных» — при том, что
своего документа у сервиса больше нет. Узлу дописываются синтетические рёбра
на собственные члены с вызовами. Копировать сами вызовы на узел раздела
нельзя: тот же вызов приходит в таблицу дважды — своим и через ребро.

**Состав документа — не участники обхода.** Внутри агрегата живут узлы,
которых никто не зовёт: экшены, модели, DTO. Список «что описано в этом
документе» строится по `absorbed_by`; по обходу он молча терял бы половину.
То же и с разделом «Состояние»: стейт раздела никем снаружи не зовётся,
состоянием быть не перестаёт.

**Якорь — имя, а не каталог.** Каталог сменится при первом переносе, класса
у раздела нет вовсе. Имя человек объявил сам и им же называет раздел в меню.
Покрытие разделов линт считает наравне со страницами и кода возврата не меняет.

**Вместо «Дочерних страниц» — «Откуда открывается».** Маршрута у раздела нет,
и это единственный способ ответить, откуда сюда попадают: страницы, которые
дотягиваются до его членов по графу вызовов.

```
uv run pytest -q  → 1648 passed  (+15 тестов)
на фикстуре: раздел `inner-debt` забрал 8 узлов, документов 31 -> 23
```

## G00 — ревизия того, что уже написано ✅

**Создано:** `docs/module-review.md`
**Изменено:** `docs/graph-implementation-plan.md` (раздел «Что решено
по существующим модулям» и пометка под предварительной таблицей G00)

Первая фича истории «разведка и граф связей» — и единственная, которая
не пишет кода. Про каждый из 64 модулей `docpipe/**` записаны категория,
потребитель и признак смерти; полный прогон тестов после ревизии идёт
без единой правки (1648 passed), потому что ревизия — это решение,
а не рефакторинг.

### Что выяснилось

**Механизм «нет потребителя → под вопросом» сработал и нашёл то, чего
в предварительной таблице не было.** `docpipe/dotnet/sln.py` (`parse_sln`,
76 строк, `.slnx` и BOM — обе ловушки стоили захода) не зовёт ни один модуль
`docpipe`: его зовут только тесты. `discovery` кладёт найденные решения
в `sln_files`, и это поле не читает никто. Модуль работает, покрыт тестами
и не нужен — ровно тот случай, ради которого п. 2 сформулирован механизмом,
а не оценкой на глаз. Условие решения записано: закрытие G04 (нужна ли
группировка проектов по решению, чтобы сужать `binds` по хосту).

**Предварительная позиция по `ownership` была неверна, и опровержение нашлось
в самом плане.** «Владение — понятие командное, у личного инструмента потребителя
не видно» — но форма `card` (G11) перечисляет владельца среди полей ответа.
На вопрос «что это за узел» ответ «и вот к кому идти» осмыслен и на личном
инструменте. Плюс два действующих потребителя: `business lint` и `docs owners`.

**Механизм приёмки уже существует в двух экземплярах.** План говорит «выделить
хэши и приёмку из `materialize`» — но `business/status.py` содержит собственные
`accepted_state` и статусы поверх `business_hash`. Значит, задача не «вынести»,
а «свести две реализации в одну»: перенос рядом с существующей второй копией
даст третью в G17. Выделяемое названо по именам функций, как требует п. 5:
`Segment`/`ParsedDocument`, `parse_document`/`assemble`/`read_document`/
`is_section_empty`, `write_atomic`, `Accepted`/`accepted_state` и сравнение
принятых хэшей из `decide`. Признак верного выделения один: после него обе
приёмки зовут одну функцию.

**Разбор мельче модуля понадобился пяти файлам, а не двум.** План требовал этого
от `materialize` и `business`; на деле общий вердикт заведомо неверен ещё
у `web/overrides.py` — ручное объявление страницы (`add`/`remove`) это тот же
путь, что реестр R03 для бэка (человек объявляет корень, которого обход
не вывел), а разделы без маршрута (`features:`) заморожены. Один файл,
две противоположные категории.

**Две проверки линта дублируют будущий `graph health`.** `uncovered`
и `registry-unlinked` считают ровно то, что G08-п.1 обязан печатать как
«корней без узла кода». Два счётчика одной величины расходятся, поэтому
у обеих проверок записано условие вывода — при закрытии G08.

### Отклонение от плана

Пятая категория плана — «выводится» — осталась пустой, и это осознанно.
Ни один модуль не признан мёртвым **сейчас**: у `sln.py` условие решения
привязано к G04, у двух проверок линта — к G08. Признать модуль ненужным
до того, как появилось событие, значит подменить механизм ощущением, —
ровно та ловушка, от которой предостерегает вторая ловушка G00
(«признать ненужным то, что просто не доделано»).

```
uv run pytest -q  → 1648 passed  (кода не менялось)
64 модуля: 41 развивается, 17 поддерживается, 1 под вопросом,
5 разобраны мельче модуля (12 частей)
```

## R01 — разведочные скрипты ✅

**Создано:** `tools/recon.py` (1600 строк), `tests/test_recon.py` (16 тестов)
**Изменено:** `CLAUDE.md` (строка про `docs/module-review.md`)

Детерминированный сбор фактов о репозитории, который видишь впервые: пять
вопросов и шестой блок про то, чему в отчёте нельзя верить. Ноль LLM, ноль
настройки, ноль знания о том, что это за система.

**Скрипт на Python, а не на bash** — в отличие от прецедента
(`tools/recon-frontend.sh`). Причина не в удобстве: критерий приёмки требует
двух форм вывода, машинной и человеческой, из одного прохода, а `git grep`
в цикле по 22 тысячам файлов не укладывается ни в какой бюджет. Ограничение
среды при этом соблюдено буквально: только стандартная библиотека и `git`,
и это проверяется тестом обходом AST, а не обещанием в шапке.

### Замеры

```
abp, 22 274 файла:  1,83 с, 142 МБ, два прогона байт в байт
semantic-kernel:    1,7 с      squidex: 0,8 с     wexflow: 0,6 с
бюджет плана: 2 минуты на 17 тыс. файлов — уложились в 65 раз
```

### Что выяснилось

**Первый же блок отчёта врал про собственный репозиторий.** Языковая таблица
abp показывала 3,5 млн строк, из которых 2,4 млн (68 %) приходились на «прочее».
«Прочее» оказалось картинками: `\n` встречается в PNG, и счётчик строк честно
их считал. Двоичные файлы теперь не читаются вовсе — это и правда (C# на abp
не 14 %, а 42 % строк), и пятикратное ускорение прогона: 9,16 с → 1,83 с.

**Средней длины строки недостаточно, чтобы отличить бандл от кода.**
`redoc.standalone.js` в squidex: 870 КБ на 1819 строк — 478 байт на строку,
порог в 500 не переходит. При этом самая длинная строка в нём — 621 848
символов, и он занимал первое место в блоке реестров с 1099 «идентификаторами».
Проверяется максимум длины строки, а не среднее; для файлов мельче 50 КБ
максимум не считается вовсе — там разбить текст дороже, чем выиграть.

**Реестр опознаётся двумя независимыми признаками, и одного мало.** План
описывает признак так: идентификаторы файла данных пересекаются со строковыми
литералами кода, при том что объявлений с такими именами нет. На wexflow этот
признак даёт **ноль**: имя задачи в XML (`<Task name="ApproveRecord">`)
совпадает с именем класса и не встречается строкой ни разу. Детектор,
требующий литерала, объявил бы, что реестров нет, — на репозитории с 756
файлами workflow. Поэтому признаков два, и они разведены по видам:
«реестр» (в коде таких имён не объявлено — трудный случай, ради которого
всё затевалось) и «реестр по именам типов» (записи названы классами, связь
восстанавливается сопоставлением имён, адаптер дешевле на порядок). Второй
вид сразу печатает файл объявления — то есть готовое ребро «запись → класс»,
которого у движка разбора нет (это его измеренная граница).

**Реестром часто является каталог, а не файл.** У wexflow один workflow —
один XML, и пофайловый счёт даёт либо 756 строк отчёта, либо ноль:
у отдельного файла идентификаторов слишком мало для любого порога. Считается
по паре «каталог + группа», где группа — это «элемент + атрибут» для XML,
путь ключа для JSON, имя ключа для YAML. Пара нужна не для счёта, а для
автора адаптера (R04): ему нужно знать, из какого атрибута какого элемента
брать записи.

**Копии одного реестра вытесняли из отчёта всё остальное.** wexflow держит
четыре копии каталога workflow (`samples/net`, `samples/netcore/{windows,
linux,macos}`), и первые четыре абзаца блока были одинаковыми. Копии
сворачиваются по пересечению множеств значений (80 %), а не по равенству:
копии расходятся на пару записей — 104, 103 и 98 значений в одной группе.

**Ключ JSON, который сам является маршрутом, нельзя брать в имя группы.**
В swagger `paths./api/v1/projects/{projectId}/imports.post.parameters[].name` —
это отдельная группа на каждую ручку API, то есть сорок одинаковых абзацев.
Такой сегмент заменяется на `*`.

**Окно археологии считается от даты HEAD, а не от `datetime.now()`.** Иначе
один и тот же репозиторий даёт разные ответы во вторник и в среду, и два
прогона нельзя сравнить. Это дешёвая замена, и она снимает единственный
источник недетерминизма, который был в замысле блока.

**Отсев по сегментам пути, а не глобом.** Та же ловушка `**`, что записана
в правилах репозитория: `**/obj/**` не ловит `obj/x.cs` в корне. На разведке
она стоила бы блока «что читать первым», заполненного генератом.

### Что осталось за границей намеренно

Импорты ловятся построчно, семантики нет: «мёртвые зоны» — кандидаты
с явной пометкой, а не список неиспользуемого. Каждый шаг глубже ломает сразу
двухминутный бюджет и правило «только git и python», и ломает тихо —
на маленьком репозитории всё по-прежнему быстро. Это записано в блоке 6
отчёта, а не только здесь: инструмент обязан сам называть, чему в нём
нельзя верить.

```
uv run pytest -q  → 1664 passed  (+16 тестов)
```

## R03 — нормализованный реестр архитектурных элементов ✅

**Создано:** `docpipe/keys.py`, `docpipe/arch/{__init__,model,load,status}.py`,
`arch-registry.example.yaml`, `schema/arch-registry.schema.json`,
`docs/arch-registry.md`, `tests/test_arch_registry.py` (26 тестов)
**Изменено:** `docpipe/cli.py` (команды `arch validate` и `arch status`,
модель `arch` у `schema`), `docpipe/config.py` (ключ `arch`),
`docs/configuration.md`, `CLAUDE.md`

Формат — контракт между разведкой и графом: слева догадки и ручная работа,
справа детерминированная сборка. Делается **до** R02 и до G03, иначе черновики
скилла и потребители реестра разойдутся по форме.

### Что решено по ходу

**Граница Р10 сделана структурной, а не устной.** Провенанс `skill_proposed`
реестром **не принимается**: `arch validate` отвергает такую запись и говорит,
что делать. Черновик скилла проверяется тем же кодом с флагом `--draft`.
Правило «предложение модели становится входом графа, только пройдя через
человека» перестало быть договорённостью и стало свойством загрузчика; ранний
признак того, что подтверждение выпало из цикла (риск Р-11), теперь виден
в данных — записи со `skill_proposed` просто не грузятся.

**Дыра Р-12 закрыта, а не смягчена.** План говорит: хэш источника ловит только
случай «источник менялся», а удалённый или переименованный файл им
не ловится. Существование источника проверяется **до** хэша и независимо
от него, поэтому `source_missing` виден и у записи без снимка вовсе. Стоило
это одной перестановки условий.

**Отчёт печатает хэш, готовый к вставке.** Человек, увидевший «снимок отстал»,
иначе идёт считать `sha256` руками — ровно та мелкая работа, из-за которой
регулярную проверку перестают делать (Р-11: человек — самый дефицитный ресурс
плана). Строка на файл, а не на запись: у реестра из сорока записей источник
один, и сорок одинаковых хэшей — не подсказка.

**Схема к имени таблицы не дописывается.** `USER_TASKS` и `dbo.USER_TASKS`
остаются разными ключами. Дописать умолчание `dbo` заманчиво: оно склеило бы
записи, которые «очевидно» об одном. На репозитории с несколькими схемами это
склейка таблиц разных схем в один узел — уверенно неправильный ответ вместо
отсутствующего, а это худший вид ошибки по Р6.

**Нормализация вынесена в `docpipe/keys.py` до первого потребителя.** План
требует одного правила на весь проект дважды (R03 п. 7 и G02 п. 5), и написать
его на месте — соблазн, который стоил бы расхождения ключей между реестром
и графом. Маршрутную часть модуль не переписывает, а зовёт `route.normalize_route`:
третьей нормализации маршрута в проекте не появилось.

**«ё» и регистр кириллицы свёрнуты уже здесь**, хотя нужны они G10. Причина
записана в плане: `LOWER()` и `NOCASE` в SQLite без ICU сворачивают только
ASCII, и русский поиск умер бы молча — вместе с единственным глоссарием
предметных слов. Нормализация обязана быть одна и на индексации, и на запросе,
поэтому её место здесь, а не в поиске.

**Проверка Р11 сделана на чужом репозитории и до того, как написан первый
адаптер.** `arch-registry.example.yaml` заполнен по открытому Wexflow —
движку workflow с клиентами на десяти языках, — и заполнен из вывода R01:
блок «чем система заякорена» дал запись `Task/@name`, блок «как языки говорят
между собой» — базовый адрес REST, общий для C++, Go, Java, PHP, Python, Ruby
и Rust. Прогон `arch status --root` на реальном чекауте отработал и назвал
пять записей без хэша с готовыми строками. Ни одного упоминания целевой
системы в примере нет, и это проверяется тестом, а не глазами.

### Что осталось на R04

Пункт 5 приёмки — перебазирование `docpipe/registry/` на этот формат —
делается в R04. Причина в порядке: перебазировать значит превратить `registry/`
в адаптер, а механизм адаптеров и есть предмет R04. В R03 сделано то, что
делает перебазирование возможным и однозначным: формат, ключи, загрузка
и проверка.

```
uv run pytest -q  → 1690 passed  (+26 тестов)
docpipe arch validate arch-registry.example.yaml → в порядке, 6 записей
```

## R04 — адаптеры реестров ✅

**Создано:** `docpipe/arch/adapters/{__init__,base,declared,code}.py`,
`docpipe/arch/{dump,collect}.py`, `tests/test_arch_adapters.py` (24 теста)
**Изменено:** `docpipe/arch/{model,status,__init__}.py`, `docpipe/cli.py`
(команды `arch records` и `arch snapshot`), `docpipe/config.py`
(`arch_adapters`), `docpipe.example.yaml`, `docs/arch-registry.md`,
`docs/configuration.md`, `docs/graph-implementation-plan.md`

Два адаптера: декларативные реестры по описанию (`registries` — это
перебазированный `docpipe/registry/`) и реестр, записанный кодом
(`python_code`, разбор через AST без импорта).

### Что выяснилось на фикстурах боевых конструкций

**Версия обязана входить в ключ, и это выяснилось на первом же прогоне.**
Один и тот же процесс лежит в репозитории в двух версиях сразу, `Id` у них
общий. Ключ без версии склеивал их: вторая запись отбрасывалась как дубль —
молча, потому что дубль и есть штатная ситуация. Инструмент при этом
уверенно отвечал бы про не ту версию процесса. Форма `Id@Version` совпала
с якорем бизнес-слоя не случайно: это одно утверждение с двух сторон.

**У одного якоря бывает несколько реализаций.** На паре «список + EventType»
в фикстуре сидят два обработчика — аудит и запуск процесса, — и срабатывают
оба. Поле `impl` из строки стало списком: строка потеряла бы второй молча,
а молча потерянный корень — это ветка графа, которой в ответе просто нет.
При слиянии сохраняются только атрибуты, совпавшие у всех реализаций:
записать одно из двух значений `assembly` значит соврать про вторую.
В файле `impl` пишется и строкой, и списком — заставлять человека писать
список ради единственного значения не за что.

**У встроенного реестра (`format: inline`) источник — файл его описания.**
Записи лежат прямо в `registries.yaml`, пути к файлу данных у них нет,
а запись без источника не принимается на загрузке — и правильно делает.
Источником объявлено само описание: там факт и объявлен, и его правка
обязана делать снимок устаревшим.

**Дубль снимка и дубль внутри адаптера — разные находки.** Первая редакция
считала их одной категорией, и отчёт на фикстуре сказал «запись есть
и в снимке, и у адаптера» при пустом снимке. Первое означает «человек
и машина описали одно и то же» (выигрывает человек, Р10), второе —
«источник объявляет запись дважды или ключу не хватает различителя»,
и чинят их в разных местах.

**Состояние `live` из отчёта убрано.** Оно означало «провенанс `adapter`,
значит читается заново, стареть нечем» — и было неправдой: всё, что лежит
в файле, есть снимок, кем бы он ни был снят. Проверка молча пропускала
ровно те записи, которые легче всего снять снимком. Провенанс говорит,
кто запись добыл, а не читается ли она заново.

### Снимок с адаптера — команда, а не упражнение

`docpipe arch snapshot --out arch-registry.yaml` превращает живое чтение
в файл, который можно прочитать глазами и закоммитить, — с хэшами
источников, поэтому `arch status` начинает ловить его устаревание сразу.
Требование приёмки «снимок и адаптер совпадают запись в запись» проверяется
на этом же пути: вывод адаптера пишется, читается обратно загрузчиком
и сравнивается — расхождение означало бы, что формат существует в двух
версиях и вторая уже разошлась.

### Отклонение от плана: перевод потребителей — в G03

Пункт 5 приёмки R03 («бизнес-слой и `anchors` читают записи отсюда»)
распадается на два шага. Формат перебазирован здесь: `registry/` зовётся
адаптером и выдаёт записи R03, второй реализации формата не появилось.
Перевод потребителей на чтение отсюда сделан **не будет до G03** — там,
где общий реестр корней и появляется; то же требование записано в G03 п. 5.
Сделать это раньше значит построить мост дважды: сначала под словарь
бизнес-якорей, потом под словарь графа. План поправлен, а не обойдён.

```
uv run pytest -q  → 1714 passed  (+24 теста)
на фикстуре реестров: 17 записей (15 точек входа, 2 узла данных),
четыре находки чтения — битый XML и оборванная ссылка не роняют прогон
```

## R02 — скилл разведки ✅

**Создано:** `.claude/skills/recon/SKILL.md`, `tests/test_recon_skill.py` (5 тестов)
**Изменено:** `docpipe/arch/load.py` (обоснование обязательно у предложения),
`tools/recon.py` (семьи языков, импорты не швы, фильтр литералов без структуры),
`tests/test_recon.py` (+3 теста), `tests/test_arch_registry.py`, `CLAUDE.md`

Скилл упаковывает разведку: гоняет R01, читает вывод, проверяет кандидатов
глазами и оставляет черновик `arch-registry.draft.yaml`, который человек
переносит руками.

### Граница Р10 держится загрузчиком, а не инструкцией

Критерий «предложение без обоснования в черновик не попадает» сделан
проверкой, а не абзацем: запись с провенансом `skill_proposed` и пустым
`note` отвергается **даже в режиме черновика**. Инструкцию можно
не выполнить, а загрузчик не уговоришь; обоснование — единственное, что
отличает предложение модели от выдумки, и стоит оно одной строки.

Пример черновика внутри самого скилла проверяется тестом: он грузится
с `--draft` и отвергается без него. Пример в инструкции, который не грузится,
хуже отсутствия примера — агент воспроизведёт его буквально, получит отказ
и решит, что сломан инструмент.

### Прогон на открытом репозитории нашёл три дыры в разведке

Скилл прогнан по своей же инструкции на `ever-traduora` (NestJS + Angular,
508 файлов, ни одной черты целевой системы). Блок швов оказался бесполезен,
и по трём разным причинам:

**`.ts` и `.js` — не разные языки.** Они живут в одном рантайме и делят
модули, поэтому общий литерал у них — переиспользование, а не сообщение
через строку. Первые места блока занимали `root`, `name`, `value`, `react`.
Введены семьи языков: шов требует двух **семей**, а не двух расширений.

**Шов внутри одного языка терять нельзя.** Прямое следствие первой правки:
у traduora фронт и бэк оба на TypeScript, и `/api/v1/projects/{}/terms` —
настоящий шов, который правило «две семьи» выбросило бы. Поэтому второе
условие: форма маршрута плюс два файла достаточно и внутри одной семьи.
Это тот самый шов, который на целевом репозитории сводит фронт с .NET.

**Спецификатор импорта — не маршрут.** `@angular/core` и `@nestjs/common`
содержат косую черту и заняли первые пять мест как «маршруты». Строка,
по которой модуль импортирует модуль, — зависимость; импорты уже собраны
в блоке 3, и теперь они же вычитаются из кандидатов в швы.

Четвёртая правка помельче: голое короткое слово в нижнем регистре
(`name`, `term`, `value`) якорем не бывает — у якоря есть разделитель,
верхний регистр или длина.

После правок первые кандидаты traduora — реальные маршруты API
(`/api/v1/projects/${…}/terms` в восьми файлах), а у wexflow —
`http://localhost:8000/api/v1`, общий для клиентов на девяти языках.

### «Реестров нет» — успешный исход, и он получен

На traduora блок 4 дал семь кандидатов, и ни один реестром не является:
четыре — параметры swagger, один — фикстуры форматтеров, один — манифесты
k8s, один — модель на TypeScript. Точки входа объявлены в коде
(`@Controller('api/v1/projects/:projectId/terms')`), и правильный ответ —
сказать это, а не назначить реестром первый попавшийся `.json`.

Черновик по traduora получился из пяти записей: три слоя и два шва — и он
проходит `arch validate --draft`, а без `--draft` отвергается по каждой
записи. Ровно то поведение, ради которого разведена граница.

### Отсутствие сети проверено сильнее, чем требовал план

Плановая проверка — `strace` за прогон. `strace` в среде нет, и вместо него
сделана проверка строже: прогон в сетевом неймспейсе без единого интерфейса
(`unshare -rn python3 tools/recon.py`). Код возврата 0, вывод тот же.
`strace` показал бы, каких вызовов не было; неймспейс доказывает, что сеть
скрипту не нужна вовсе.

```
uv run pytest -q  → 1723 passed  (+9 тестов)
unshare -rn python3 tools/recon.py  → код 0, вывод совпадает с обычным прогоном
```

## G01 п. 1 — перемер протокола на движке 0.6.0 ✅ (блокирующий пункт)

**Создано:** дополнение «Перемер на версии 0.6.0» в `docs/findings-codebase-memory.md`
**Изменено:** `docs/graph-implementation-plan.md` (форма чтения в G01, механика
G04, риск Р-8, шапка документа)

Пункт стоит первым в этапе и блокирует остальные: все числа findings сняты
на v0.10.8, а в контур прошла только 0.6.0. Разработка на другой версии —
это разработка против другого движка.

**Итог: движок фильтр проходит, условие переключения не сработало.**

### Числа

```
                узлов    рёбер   CALLS   время   память
eshoponweb      1 997    3 436     263   0,07 с    45 МБ
wexflow        11 430   28 919   7 389   1,06 с   486 МБ
squidex        30 499   97 096  29 863   1,92 с   564 МБ
abp           156 592  246 629  30 186   4,70 с  1,24 ГБ
```

Против v0.10.8: узлов 49–90 %, рёбер 26–61 %, а рёбер **вызова** на abp —
77 % (30 186 против 39 072). Время в десять раз меньше, память в пять.
Полный цикл движка на целевом масштабе — пять секунд вместо сорока шести,
и это заранее отвечает на вопрос G09 об инкрементальности.

### Перемер едва не закончился неверным выводом

Первые полчаса он говорил, что **рёбра из 0.6.0 не читаются вовсе**:
`MATCH (a)-[:CALLS]->(b)` возвращает ноль строк. Ошибки при этом нет —
есть пустой результат, неотличимый от «таких рёбер в графе нет». Дальше
проверялись обходные пути: `search_graph` с `include_connected` отдаёт
соседей короткими именами без направления, и было измерено, что имя соседа
уникально в 41–51 % случаев, а встречная проверка поднимает разрешимость
до 65–86 %. Это уже похоже на условие переключения на запасной вариант.

Настоящая причина оказалась в синтаксисе: **у обоих концов ребра обязана
быть метка**. `MATCH (a:Method)-[:CALLS]->(b:Method)` работает и отдаёт
полные имена с обеих сторон; сумма по парам меток совпадает со счётчиком
схемы точно (263 = 263 на eshoponweb). Граф читается целиком.

Три соседние ловушки той же природы — тихий неверный ответ вместо ошибки:
неизвестное имя свойства даёт пустую строку (`n.file` пусто, `n.file_path`
работает); агрегатов и `labels()` нет; `trace_path` сопоставляет по короткому
имени и молча берёт один узел, а полное имя не принимает вовсе.

### DI-случай разрешается иначе, и это переписывает G04

Главный вопрос перемера. v0.10.8 останавливала вызов через интерфейс
на интерфейсе, и G04 писалась как «довершить ребро по `binds`».

0.6.0 доводит его **до реализации сразу, по имени**:
`AuthenticateEndpoint.HandleAsync → IdentityTokenClaimService.GetTokenAsync`
при поле типа `ITokenClaimsService`. Довершать нечего.

Обратная сторона — случай с декоратором: ребро
`Cached….GetCatalogItems → CatalogViewModelService.GetCatalogItems`,
которое у v0.10.8 было, у 0.6.0 **потеряно**; у внутренней реализации ноль
входящих рёбер. Где кандидатов на имя несколько, движок выбирает одного
и про остальных молчит — то есть уверенно отвечает не обязательно верно.

G04 переписана: вместо «довершения» — сверка выбора движка нашими `binds`,
дополнение рёбрами к остальным зарегистрированным реализациям с пониженной
уверенностью и отдельная категория отчёта на расхождение. Заодно выяснилось,
что `USAGE`-рёбра от регистрации, которые findings называли «сырьём для
связывания», в 0.6.0 отсутствуют: сырьё есть только у нашего `dotnet/di.py`.

### Детерминизм измерен, виновник назван

Два прогона с чистого кэша на одном входе. Полный дамп eshoponweb
расходится на **пяти строках из 5 433**: узлы вида `Module` собирают
`qualified_name` без расширения, поэтому `app.component.css`
и `app.component.scss` спорят за одно имя и побеждает разный файл.

Проекция моста (объявления кода плюс `CALLS`/`INHERITS`) совпадает **байт
в байт** на всех трёх проверенных репозиториях: eshoponweb (1 006 строк),
squidex (48 248), abp (67 545). Находка не блокирующая — она целиком
за пределами того, что идёт в наш индекс; правило отсева записано в G01.

Чтение всего графа abp через Cypher — 1,9 секунды на 52 запроса по парам
меток. Бюджет сборки этим не расходуется.

### Риск Р-8 снят

Память: 1,24 ГБ на abp против 6,9 ГБ у v0.10.8 при потолке среды 16 ГБ.
Замер на целевом репозитории остаётся обязательным, но перспектива выхода
за потолок исчезла.

```
чек-сумма бинаря совпала с ожидаемой (3a3b6491…fb39e6c7)
codebase-memory-mcp --version → 0.6.0
```

## G01 — мост к движку разбора и наш индекс ✅

**Создано:** `docpipe/graph/{__init__,engine,model,store,build}.py`,
`tests/test_graph_index.py` (18 тестов), `tests/test_graph_bridge.py`
(12 контрактных тестов)
**Изменено:** `docpipe/cli.py` (`graph build`, `graph info`),
`docpipe/config.py` (секция `graph`), `docpipe.example.yaml`,
`docs/configuration.md`, `CLAUDE.md`

Два артефакта и одна граница: мост знает о движке всё, остальные фичи — ничего.

### Замеры полного цикла

```
фикстура SampleSolution   24 узла,     5 рёбер
фикстура WildSolution     37 узлов,    0 рёбер
фикстура WebWorkspace      0 узлов     (весь фронт не проецируется — правило)
squidex               13 094 узла, 22 684 ребра,  4,6 с
abp                   31 890 узлов, 35 044 ребра, 10,0 с, пик 1,28 ГБ
```

Бюджет плана — пять минут на целевом масштабе; abp проходит за десять секунд.
Замер на боевом репозитории остаётся невыполненным пунктом приёмки (доступа
нет), и это записано как долг, а не закрыто похожим числом.

**Наш индекс меньше графа движка вчетверо, и это правильно.** У движка на abp
156 592 узла, у нас 31 890: мы проецируем объявления (типы и члены), а его
`Variable`, `File`, `Folder`, `Section` в нашей модели выражены иначе или
не выражены вовсе. Целевые числа плана (100–200 тыс. узлов) считались
по движку — наш индекс лежит в другом масштабе, и это стоит помнить,
когда придёт время сверять с бюджетом диска.

### Что решено по ходу

**Ключ узла — файл плюс хвост полного имени.** Полное имя движка собрано
как «путь файла точками + тип + член», то есть начинается с пути; хвост
после пути — это `Тип.Член`. Получается `src/Web/Controllers/ManageController.cs#ManageController.AddErrors`
— читаемо, не зависит ни от номеров строк, ни от того, где лежит чекаут.
Имя проекта движок выводит из абсолютного пути, и оно срезается: иначе два
чекаута одного репозитория дали бы два разных индекса.

**Перегрузки в ключ не разводятся, и это не забыто, а посчитано.** У движка
у всех перегрузок одно полное имя; здесь совпадение ключей идёт в отчёт
отдельной категорией, а разведение по сигнатуре — задача G02, где параметры
берутся из манифеста.

**Фронт не проецируется вовсе — ни узлы, ни рёбра.** Правило модели графа
(«источник рёбер задан по языкам») реализовано в проекции, а не оставлено
на потом: на squidex это 5 677 узлов и 6 793 ребра, которые иначе легли бы
в индекс вторым счётом наших же вызовов. На фикстуре `WebWorkspace` индекс
пуст целиком, и это проверяется тестом.

**Поколение индекса выводится из содержимого.** Хэш логического содержимого
и есть поколение: два прогона на одном входе дают одно поколение, времени
в индексе нет вовсе. Читатель, сверяющий поколение (MCP-сервер в G12),
получает свойство «индекс тот же» бесплатно и без часов.

**«У разборщика 83 ребра, у нас 5» — это объяснённое число, а не потеря.**
Структурные рёбра движка (`DEFINES`, `CONTAINS_*`) в нашей модели выражены
иначе; их количество печатается отдельной строкой отчёта. Без неё разница
в два порядка выглядит как дыра в мосте.

### Ловушки, найденные при реализации

**Тест на правило Р13 сам себя чуть не обманул.** Проверка «словарь движка
не выходит за мост» подстрочным поиском нашла `CALLS` в нашей же константе
`NOTE_NO_CALLS` (`web/pages.py`). Поиск переведён на границы слова: тест,
который ловит не то, что называет, вреднее отсутствующего.

**Ответ об индексации в 0.6.0 не содержит ни `not_indexed_files`,
ни `parse_partial`.** Протокол v0.10.8 обещал и то и другое с причиной
на каждый файл — в 0.6.0 ответ состоит из имени проекта, статуса и двух
чисел. Значит, покрытие придётся считать самим: сравнением нашего файлового
множества с файлами, встреченными среди узлов. Пока такого счёта нет,
«разбор ничего не нашёл в файле» неотличимо от «в файле нечего находить».
Записано в шапке моста как дыра и в задачу G08.

**`#if` внутри выражения теряет тип и у движка тоже.** Фикстура
`ConditionalModule.cs`, стоившая нам захода на шаге 1, не даёт ни одного
узла и у него. Это зафиксировано контрактным тестом: значит, сопоставление
манифеста с графом на ней не разойдётся, а число потерянного обязан считать
отчёт о неполноте.

### Приёмка

| Пункт | Состояние |
|---|---|
| 1. Перемер на 0.6.0 | ✅ отдельной записью выше |
| 2. Полный цикл, хэш совпадает между прогонами | ✅ тест и прогон на фикстуре |
| 3. Детерминизм движка измерен | ✅ байт в байт на трёх репозиториях |
| 4. Чек-сумма до запуска | ✅ отказ, не предупреждение |
| 5. Контрактные тесты моста | ✅ 12 тестов, пропускаются без бинаря |
| 6. `grep` не находит словаря движка за мостом | ✅ два теста |
| 7. Замер на целевом репозитории | ⬜ доступа нет; замер на abp записан |
| 8. Прогон без сети | ✅ полный цикл в сетевом неймспейсе, код 0 |
| 9. Отказ движка — внятная ошибка | ✅ тесты на отсутствие бинаря и чек-сумму |
| 10. Запись атомарна, поколение читается | ✅ временный файл и переименование |
| 11. `doc-tree.json` не изменился | ✅ структурная проверка импортов |

```
uv run pytest -q  → 1753 passed  (+30 тестов)
unshare -rn docpipe graph build → код 0, поколение то же
```

## G02 — идентичность узла и сопоставление с манифестом ✅

**Создано:** `docpipe/graph/{identity,match}.py`, `tests/test_graph_identity.py`
(20 тестов)
**Изменено:** `docpipe/graph/{build,__init__}.py`, `docpipe/cli.py`
(`graph build --manifest`), `docs/graph-implementation-plan.md`

Ключ типа продолжает правило манифеста — модуль, FQN, арность. Ключ члена
добавляет имя, арность дженерика и **отпечаток списка параметров**:
перегрузки различаются, а порядковый номер перегрузки ключом не является.

### Разбор сигнатуры: три случая, на которых ломается наивный `split`

Сигнатура в манифесте — это текст объявления целиком
(`public async Task<ActionResult<decimal>> GetAsync(Guid id, CancellationToken ct)`),
и вытащить из него список типов параметров сложнее, чем кажется:

- **`Dictionary<string, int> map` — один параметр, а не два.** Запятая внутри
  дженерика разбивается наивным `split(",")`, и отпечаток не совпадает
  ни с чем;
- **у метода с кортежным результатом первая группа скобок принадлежит типу
  результата.** `public (int, int) GetPair(string key)` при разборе по первой
  группе даёт параметры `int, int` вместо `string`. Берётся **последняя**
  сбалансированная группа;
- **имя параметра и значение по умолчанию в ключ не входят**: переименование
  параметра перегрузки не создаёт, а ключ менять не должно.

Вид члена входит в отпечаток: поле и метод без параметров с одним именем
не спорят за ключ.

### Перегрузки различает манифест, а граф — нет, и это записано в план

Приёмка требует, чтобы перегрузки одного имени различались ключом. Ключ такой
реализован и работает — **на стороне манифеста**. У разбора 0.6.0 одно полное
имя на все перегрузки, и развести их нечем: информации нет в источнике.

Поэтому узел графа у перегруженного имени один, число объявлений манифеста
печатается атрибутом `declarations`, а ключ члена такому узлу **не
проставляется вовсе**. Выбор одного из нескольких молча не делается — это то
же правило Р6, только применённое к самим себе.

### Ключ графа не подменяется ключом манифеста

Соблазн был: сопоставили — переименовали узел в манифестный ключ. Тогда
половина узлов индекса адресуется одним способом, а половина другим,
и первый же запрос это перепутает. Связь с манифестом живёт **полями**
узла (`symbol_key`, `member_key`, `doc_node`, `module`, `fqn`), пространство
ключей остаётся одно.

### Отчёт сопоставления: важно второе число, а не третье

На `eshoponweb` (259 файлов, 253 символа, 18 документируемых):

```
сопоставлено с манифестом:                      100
есть в манифесте — нет в графе:                   0   ← вот это число
есть в графе — нет в манифесте:                 607
узлов графа на несколько объявлений манифеста:    4
```

Третье число велико **по построению**: манифест — дерево документации,
а не полный список объявлений; остальные символы отсеяны правилами
классификации или ещё не решены. План этого не оговаривал, и в него дописано:
читать надо второе число — узел, про который принято решение «документируем»,
обязан существовать в графе. Здесь их разошлось ноль из ста.

### Проверка на коллизиях FQN — на настоящей фикстуре

`WildSolution` собрана из конструкций, пойманных в реальных репозиториях:
два `Constants` в одном модуле, интерфейсы разной арности, вложенные типы,
типы вне пространства имён. Тест строит по ней манифест и проверяет, что
ни один ключ типа и ни один ключ члена не совпадает у разных объявлений.

```
uv run pytest -q  → 1773 passed  (+20 тестов)
```

## G03 — точки входа как объекты графа ✅

**Создано:** `docpipe/graph/entrypoints.py`, `tests/fixtures/arch/sample-arch.yaml`,
`tests/test_graph_entrypoints.py` (13 тестов)
**Изменено:** `docpipe/arch/adapters/declared.py` (разобранные части ключа —
полями), `docpipe/graph/{build,__init__}.py`, `docpipe/cli.py`
(`graph entrypoints`, `graph build` читает реестр), `docs/graph-implementation-plan.md`

Корень графа — первоклассная сущность: ключ у него тот, что знает вызывающий,
а бизнес-название необязательно.

### Два источника, и это видно в данных

Из **реестра** приходит то, чего в коде нет структурно (workflow, джобы,
обработчики событий), из **манифеста** — то, что видно в коде (HTTP-эндпоинты).
Источник записан у каждого корня полем, и отсутствие декларативных источников
произносится вслух: иначе «реестра нет» и «точек входа нет» выглядят одинаково.

Прогон на канонической фикстуре с реестром рядом:

```
корней из реестра: 3, из манифеста: 2
связано по полному имени: 4, узел кода не найден: 1
сопоставлено с манифестом: 18, документированных узлов без графа: 0
```

Корень без узла кода (джоб, класса которого в репозитории нет) попадает
в отчёт и прогон не роняет.

### Связывание корня с кодом — три состояния, а не два

«Связан точно» (по полному имени) и «связан по короткому имени типа» — разные
утверждения, и различать их обязательно. Несколько типов с одним коротким
именем не повод выбрать первого: ребро идёт ко всем с уверенностью 1/N,
и число видно в отчёте. Третье состояние — «узел кода не найден» — не дефект.

Ребро называется `dispatches`, а не `calls`, и это не педантизм: реестр
называет класс, а запускает его универсальный раннер. Вызывающего в коде
нет вовсе, и записать связь как вызов значило бы утверждать то, чего нет.

### Разобранные части ключа кладутся полями, а не разбираются обратно

Адаптер реестров теперь пишет в запись `ref`, `version`, `parent`,
`registry_kind` и `registry_id` отдельными полями. Причина та же, по которой
ключ графа не подменяется ключом манифеста: разбирать ключ обратно — это
вторая реализация правила его сборки, и она разойдётся с первой на первом же
реестре, где ключ собран иначе.

### Пункт 5 приёмки: риск снят тестом, а не переписыванием

Требование «`anchors` и бизнес-слой читают корни из общего реестра, а не
из своей копии» выполнено по существу: источник записей общий (один
`registry/reader.py`), а отображения у сторон **разные и должны быть
разными** — `anchors` отвечает «какой документ описывает точку входа»,
граф «какой узел кода из неё достигается».

Риск, о котором говорит пункт, — расхождение источников. Он снят тестом,
сравнивающим множества якорей у обеих сторон: разойдутся — тест упадёт.
Полного равенства нет намеренно, и это записано в плане: `anchors` держит
два якоря на паре «список + EventType» при двух обработчиках, граф сводит
их в один корень с двумя реализациями; шаги workflow у `anchors` — дети,
в графе — корни. Переписывание разрешения якорей на записи R03 отложено
до G17, которой оно понадобится: перезапись работающего слоя без потребителя
— это риск без выигрыша.

```
uv run pytest -q  → 1786 passed  (+13 тестов)
docpipe graph entrypoints → 5 корней, из них связано с кодом 4
```

## G04 — связывание: DI и диспетчеризация ✅ (частично, см. п. 6)

**Создано:** `docpipe/graph/binding.py`, `tests/test_graph_binding.py` (18 тестов)
**Изменено:** `docpipe/model.py` (`Manifest.di_registrations`,
`Manifest.dispatch_handlers`, `DispatchDeclaration`), `docpipe/emit.py`
(регистрации и объявления диспетчеризации остаются в манифесте),
`docpipe/merge.py` (слияние регистраций в скоуп-режиме),
`docpipe/discovery.py` (`map_files_to_modules` переехал сюда из `emit`),
`docpipe/config.py` (`dispatch_interfaces`), `docpipe/graph/{build,__init__}.py`,
`tests/golden/doc-tree.json`, `docs/{graph-implementation-plan,configuration}.md`,
`docpipe.example.yaml`

### Регистрации переехали в манифест, и это не уступка графу

`dotnet/di.py` извлекал их всегда, но результат жил внутри сборки узлов
и выбрасывался. Теперь регистрации остаются в манифесте — как эндпоинты
контроллеров, которые лежат там ровно по той же причине: это **извлечённый
факт о коде**, добытый тем же проходом. Альтернатива — второй разбор всех
файлов репозитория ради того же факта, то есть удвоенный проход.

Побочно всплыл честный дефект теста: `test_deleted_file_disappears_from_the_manifest`
проверял отсутствие **строки** `ValuationWorkflow` в манифесте, а после
переезда регистраций строка осталась — `Program.cs` по-прежнему содержит
`AddTransient<ValuationWorkflow>()`. Это правда, а не остаток кэша:
регистрация на удалённый тип — законный факт файла и отдельная находка
связывания. Тест переписан на проверку узла.

### Обе ветки связывания нужны, и это выяснилось на фикстурах

Перемер показал, что 0.6.0 доводит вызов через интерфейс до реализации сам.
На канонической фикстуре он этого **не сделал**: ребро остановилось
на `IPricingService.PriceAsync`, и сработало довершение по регистрации.
На `eshoponweb` — наоборот: 44 цели уже конкретны и подтверждены нашими
регистрациями. Значит, поведение источника зависит от кода, а не только
от версии, и обе ветки — довершение и сверка — обязаны быть.

### Две ловушки, каждая давала уверенное неверное ребро

**Вызов внутри типа через интерфейс не проходит.** `this.GetBrands()` —
собственный метод, а не обращение к сервису. Без проверки внутренний вызов
`GetCatalogItems → GetBrands` порождал ребро в одноимённый метод декоратора:
на `eshoponweb` таких было четыре, и все четыре — ровно того вида ошибки,
ради которого сверка и делается. После проверки расхождений ноль,
подтверждений сорок четыре.

**`partial class` — один тип и несколько узлов графа.** Ключ узла содержит
файл, а объявление живёт в нескольких файлах: у `PricingService` в фикстуре
два узла. Без сведения их в один логический тип (по `symbol_key` манифеста)
регистрация выглядела как две конкурирующие реализации, уверенность падала
вдвое на ровном месте, а члены искались не в том узле.

**Проект хранится путём файла проекта, а не `id` модуля.** Символ манифеста
несёт путь (`Symbol.module`), ссылки между проектами записаны путями,
а `id` отличается префиксом `module:`. Ключ по `id` выглядел правильным,
не совпадал ни с чем, и сужение по хосту молча не срабатывало никогда —
пока не сверил числа: «не сужено: 2» при двух проектах, которые прекрасно
знают друг друга.

### Пункт 6 выполнен наполовину, и вторая половина упирается в источник

Объявление диспетчеризации («этот тип обслуживает такой запрос») извлекается
и лежит в манифесте: список интерфейсов задаётся ключом `dispatch_interfaces`
(зашивать `IRequestHandler` в код нельзя — на другом репозитории он называется
иначе), тип запроса берётся **первым** аргументом дженерика. На `eshoponweb`
найдены оба обработчика MediatR.

Место отправки взять неоткуда: **разбор 0.6.0 не записывает создание объекта
как обращение**. Измерено: у обоих объявленных обработчиков единственное
обращение к типу запроса приходит от их же метода `Handle` — это его параметр.
Первая редакция строила по ним рёбра, и получались петли «обработчик
диспетчеризует сам себя»; теперь такие обращения отбрасываются, а число
«обработчиков без места отправки» печатается. Гап измерен, а не забыт;
закрывается он разбором тел (запасной вариант) либо источником, который
записывает конструирование объектов.

### Числа на открытом репозитории

```
eshoponweb: узлов 732, рёбер 344
  DI-регистраций 31: разобрано 18, на себя 11, тип не найден 2
  довершено по регистрации 2, сужено по хосту 10, хост неизвестен 36
  целей подтверждено регистрацией 44, разошлось 0
  объявленных обработчиков 2, рёбер диспетчеризации 0, без места отправки 2
```

```
uv run pytest -q  → 1803 passed  (+18 тестов)
```

## G05 и G05b — узлы данных из объявлений и из реестра ✅ (граница записана)

**Создано:** `docpipe/graph/data.py`, `tests/test_graph_data.py` (13 тестов)
**Изменено:** `docpipe/graph/{build,__init__}.py`,
`docs/graph-implementation-plan.md`

Узел данных — логическое имя, извлечённое из кода и декларации: доступа
к боевой базе нет и не планируется.

### Что сделано и где проходит граница

Реализованы источники, живущие **в объявлениях**: атрибут таблицы на сущности,
`DbSet<T>` и обобщённый репозиторий в сигнатуре члена. Источники, живущие
**в телах** (`ToTable("FOO")`, `CreateTable` в миграциях, SQL-литералы, имена
процедур), не реализованы — у каждого имя лежит в литерале внутри вызова,
а тела здесь не разбираются. Дыра измерена: доля имён «по соглашению»
и число обращений, не разрешённых вовсе, печатаются.

На `eshoponweb`: 5 узлов данных, 20 обращений, **все 20 — по соглашению**
(атрибутов таблицы в этом репозитории нет вовсе). Число само по себе
и есть ответ на вопрос «сколько здесь выведено, а не прочитано».

### Три решения, каждое против правдоподобной ошибки

**Ребро идёт от типа, а не от члена.** Поле репозитория объявлено на типе,
и какой метод к нему обращается, из объявления не следует. Приписать
обращение конструктору — ровно ошибка «внедрил ≠ зовёт», стоившая захода
на фронте; доводит обращение до члена достижимость, а не догадка.

**Носители перечислены списком.** `Task<Contract>` сущностью не делает
ничего: без ограничения списком (`DbSet`, `IRepository`, `IReadRepository`)
узлом данных стал бы каждый тип из возвращаемого значения.

**Схема не дописывается.** `CONTRACTS` и `dbo.CONTRACTS` остаются разными
узлами. Склейка на репозитории с несколькими схемами дала бы уверенно
неверный узел вместо отсутствующего, а это худший вид ошибки.

### G05b: реестр приносит то, чего в коде нет вовсе

Из реестра приходят **объявленное** имя таблицы, человеческое название
и названия полей — на репозитории с русским глоссарием это единственное
место, где предметные слова вообще есть. `FieldLookup/@ListSource` даёт ребро
`references` между узлами данных: связь читается из декларации, а не выводится
из графа кода.

Точка входа «список + EventType» получает ребро `touches` к своей таблице
**без прохода через код**: связь объявлена, и проходить за ней через вызовы
нечем — литерала в C# не существует.

Слияние по нормализованному ключу проверено тестом в обе стороны: одинаковое
имя из двух источников даёт один узел с человеческим названием из реестра;
`CONTRACTS` и `[dbo].[CONTRACTS]` остаются разными, потому что схема — часть
имени, а не украшение.

```
uv run pytest -q  → 1816 passed  (+13 тестов)
```

## G07 и G08 — достижимость, веерность и отчёты ✅ = ВЕХА 2

**Создано:** `docpipe/graph/{reach,report}.py`, `tests/test_graph_reach.py`
(16 тестов)
**Изменено:** `docpipe/graph/{model,store,build,__init__}.py`, `docpipe/cli.py`
(`graph report`, `health`, `reaches`, `affects`, `path`), `CLAUDE.md`

Одно вычисление, три ответа: что достигает точка входа, какие точки входа
достигают узел, насколько узел общий.

### Конденсация SCC, а не тысяча обходов

Корней на целевом репозитории тысячи, рёбер сотни тысяч: тысяча независимых
обходов на чистом Python не влезает ни в какой бюджет, и наивная реализация
прошла бы приёмку на фикстуре. Считается конденсация компонент сильной
связности (итеративный Тарьян — рекурсивный упал бы на глубине реальной
цепочки вызовов), затем маска «какие корни меня достигают» собирается одним
проходом в топологическом порядке.

Замер полного цикла на `abp` (движок + наш слой + сопоставление + связывание
+ данные + достижимость):

```
32 136 узлов, 35 416 рёбер, 244 корня
10,7 с, пик 1,27 ГБ, индекс 54 МБ
бюджеты плана: 5 минут, 16 ГБ, 500 МБ — с запасом
```

### Три решения о том, что считать путём исполнения

**`binds` путём не является.** Это «кто мог бы позвать», а не «зовёт»:
включить его значило бы объявить достижимым весь код, куда ведёт хоть одна
регистрация. Наследование — тоже не путь исполнения. Остаются вызовы,
диспетчеризация, швы и обращения к данным.

**Член достигнут — достигнут и его тип.** Обращения к данным объявлены
на типе (поле репозитория объявлено там), поэтому в графе достижимости есть
ребро «член → его тип». Огрубление в сторону «достижимо больше», и оно
названо прямо в отчёте.

**Тип раскрывается в члены только у цели диспетчеризации.** Реестр говорит
«этот класс и есть джоб», и без раскрытия такой корень не достигает ничего:
на фикстуре грид-сервис показывал 0 членов при трёх методах. Сделать
раскрытие общим правилом нельзя — тогда любой достигнутый тип тянет за собой
все свои методы, и достижимость перестаёт что-либо сужать.

### Ловушка, стоившая обрыва пути

Обход пути возвращал цепочку, обрывающуюся на предпоследнем узле. Причина —
тень имени: переменная цикла называлась `target`, как и параметр функции,
и после цикла обход сравнивал цель с последним ключом смежности. Ошибка
не в алгоритме, и тестом на «путь дошёл до конца» она ловится сразу,
а глазами — нет.

### Веха 2: таблица точек входа печатается одной командой

`docpipe graph report` — детерминированный markdown: для каждой точки входа
вид, источник, сколько кода достигается, какие таблицы, где обрывается
анализ. Ниже — корни без узла кода (состояние работы, не дефект), общие
компоненты и категории неполноты. Времени в отчёте нет: его кладут в ревью
и сравнивают между прогонами.

Честная сдача реализована в `graph affects`: узел выше порога веерности
не получает список процессов, он получает признание, что анализ не сужает,
и само число. Инструмент, который всегда что-то отвечает, теряет доверие
целиком.

`graph health` печатает категории неполноты — те же числа, что собираются
по ходу сборки: DI-регистрации по видам, довершения, сужение по хосту,
сопоставление с манифестом, имена таблиц по соглашению, отсев файлового
множества.

### Долг вехи

Веха 2 требует таблицу и на открытом репозитории, и на АС CF. Открытый
закрыт (`eshoponweb`, `abp`, каноническая фикстура с реестром); прогон
на АС CF невозможен без доступа и остаётся невыполненным пунктом — как
и замер G01 п. 7.

```
uv run pytest -q  → 1832 passed  (+16 тестов)
```

## G09 — пересборка ✅ (инкрементальность закрыта числом)

**Изменено:** `docpipe/graph/build.py` (прогресс по этапам), `docpipe/cli.py`,
`docs/graph-implementation-plan.md`

Фича состоит из одного решения и одной строки вывода.

**Решение: инкрементальность не строится.** Полный цикл на `abp` — 10,7 с
при бюджете в пять минут; пересборка после PR в полсотни файлов — тот же
прогон целиком, то есть те же одиннадцать секунд против бюджета в две минуты.
Строить инвалидацию по изменённым файлам не нужно, и это хорошо не только
экономией: у полной пересборки нет самой злой ловушки этого класса — одна
изменённая DI-регистрация переставляет рёбра в другом конце системы,
и инвалидация обязана была бы это ловить.

**Прогресс по этапам виден.** Пользователь, ждущий минуты без единого
сообщения, решит, что инструмент завис, и прервёт прогон, — а прерванный
прогон выглядит как сломанный инструмент. Этапы называются по делу:
проверка разборщика, индексация, чтение графа с числами, перевод в наши
термины, сопоставление, связывание, точки входа, данные, достижимость.

Пункт 4 приёмки (равенство инкрементального и полного результата) снят
вместе с инкрементальностью: сравнивать нечего.

## G10 — поиск и разрешение имён ✅

**Создано:** `docpipe/graph/search.py`, `tests/test_graph_search.py` (13 тестов)
**Изменено:** `docpipe/graph/{store,build,__init__}.py`, `docpipe/cli.py`
(`graph resolve`)

Без этой фичи API бесполезен: попытка найти связь двух сущностей провалилась бы
потому, что одна «не нашлась», и отличить это от «связи нет» было бы нечем.

**Ни эмбеддингов, ни морфологии, ни синонимов.** Семантику приносит вызывающий
— на том конце MCP языковая модель, которая переформулирует и переведёт.
Работа индекса — быстрый нечёткий матч и честный список кандидатов
**с указанием, чем совпало**: точное совпадение, начало, подстрока, триграммы.
Вызывающий, знающий, что матч был по триграммам в английском имени,
переформулирует осмысленно; вызывающий с голым списком может только гадать.

**Регистр кириллицы и «ё» сворачиваются на нашей стороне** — той же
`normalize_text`, что нормализует ключи. Это не перестраховка: `LOWER()`
и `NOCASE` в SQLite без ICU сворачивают только ASCII, «Пользовательские»
не совпало бы с «пользовательские» никогда, а тесты на английских именах
этого не показывают. Проверяется двумя тестами — на регистр и на «ё».

**Пространство поиска перечислено положительно**: имена узлов и ключи, полные
имена, маршруты, ссылки реестра, **русские названия полей списков**,
заголовки документов, домены и тексты XML-doc. Названия полей попали туда
не для полноты: на репозитории, где код английский, а предметная область
русская, это единственное место, где предметные слова вообще есть.

**Пустого ответа не бывает.** Если не совпало ничего — печатается, где
искали, что есть в индексе по видам и несколько точек входа для примера.
Пустота неотличима от факта и не даёт зацепки для второй попытки.

**Одна находка на узел.** Тип, найденный и по имени, и по заголовку документа,
— это одна находка, а не две; поле, по которому совпало, остаётся в ответе.

```
uv run pytest -q  → 1845 passed  (+13 тестов)
resolve на 4 000 узлов — единицы миллисекунд при требовании ≤ 100 мс
```

## G11 и G12 — формы вопроса и MCP-сервер ✅ = ВЕХА 3

**Создано:** `docpipe/graph/{api,mcp}.py`, `tests/test_graph_api.py` (13 тестов),
`tests/test_graph_mcp.py` (9 тестов)
**Изменено:** `docpipe/cli.py` (`graph serve`)

Семь форм вопроса и доставка их агенту. Универсальный обход графа наружу
не выпускается — им немедленно построят лавину.

### Что отличает ответ от отписки

**Каждый ответ несёт признак неполноты, относящийся к нему самому.** Не общую
сводку: сводка на вопрос «чему здесь нельзя верить» не отвечает. `reaches`
показывает категории про корни, довершения и данные; `card` — про
сопоставление и перегрузки.

**Усечение восстановимо.** «Показано 20 из 300» без способа увидеть остальные
заставляет звать инструмент заново с другими параметрами, то есть угадывать;
маркер несёт команду.

**`affects` принимает пути файлов, а не только ключи.** Вывод
`git diff --name-only` — основной сценарий в PR, и требовать от вызывающего
перевода путей в ключи значит требовать знания, которого у него нет.

**`path` называет, какая сторона не разрешилась.** «Связь не найдена» при
неразрешённой стороне запрещено: это два разных ответа, и второй отправляет
искать не там.

**Обход графа делает только `path`** — проверяется тестом, который разбирает
AST и требует, чтобы обход звался ровно из одной функции. Если запрос
требует обхода, нарушена архитектура, а не «медленно работает».

**`overview` и `why` работают без индекса** — это разведка и git. Они первое,
что инструмент вообще может сказать о незнакомом репозитории.

### MCP без единой зависимости

Протокол реализован руками: три метода JSON-RPC поверх stdio. Это решение,
а не лень — ограничение среды записано в плане трижды, и сервер, которому
нужен `pip install`, в закрытом контуре не запустится ни разу.

**Набор инструментов совпадает с набором форм один в один**, и это проверяется
тестом через AST: расхождение — это два описания одного, и второе отстанет.

**Поколение индекса сверяется на каждом запросе.** Сборка подменяет файл
атомарно; сервер, держащий в памяти прежний индекс, отвечал бы по устаревшему
без единого признака. Поколение выводится из содержимого, поэтому
«сменилось» означает именно «пересобрали с другим результатом».

**Инструкция «как здесь работать» отдаётся в `initialize`**: с чего начинать,
чему верить без перепроверки (рёбра вызова внутри языка, связи из реестра),
чему с оглядкой (связи по имени типа, имена таблиц по соглашению) и когда
идти грепом. Без неё агент зовёт не то и заключает, что инструмент бесполезен.

Отсутствие индекса — внятная ошибка с командой сборки; неизвестное имя
инструмента проверяется **до** индекса, иначе ответ «индекса нет» отправляет
чинить не то.

```
uv run pytest -q  → 1867 passed  (+22 теста)
echo '{"jsonrpc":"2.0","id":1,"method":"tools/list"}' | docpipe graph serve → семь инструментов
```

## G15 — шов Angular ↔ .NET в общем графе ✅ = ВЕХА 4

**Создано:** `docpipe/graph/web.py`, `tests/test_graph_web.py` (8 тестов)
**Изменено:** `docpipe/graph/{build,model,reach}.py`, `docpipe/cli.py`
(`graph build --web-manifest`)

Вопрос вехи — «как связана страница с сервисом .NET» — отвечается одним
запросом, и шов виден звеном пути:

```
entry:page:models
  --dispatches(entrypoint:web)--> ListComponent
  --owns(владение)-------------> ListComponent.save
  --calls(web:usage)-----------> DebtState.save
  --calls(web:usage)-----------> InnerDebtService.insert
  --crosses(web:route)---------> entry:http_endpoint:post api/ml/innerdebts/insert
  --dispatches(entrypoint:manifest)--> InnerDebtsController.InsertAsync
```

На фикстурах (`WebWorkspace` + `WildSolution`): 5 страниц, 17 рёбер цепочки,
6 швов, 12 вызовов фронта без эндпоинта — последнее число и есть мера
ненастроенности, а не молчание.

### Три решения

**Якорь страницы — маршрут, а не имя класса.** Переименование компонента
якорь не меняет, а URL — это то, что знает пользователь. Страница, у которой
маршрут не собрался, точкой входа не становится: якоря нет, и выдумывать его
нельзя. Число таких страниц печатается.

**Цепочка лежит рёбрами, а не схлопнута.** «Компонент → экшен → стейт →
сервис → вызов» — это четыре ребра, и вопрос «почему страница зовёт этот
эндпоинт» отвечается путём. Схлопнув цепочку в прямое ребро
«страница → эндпоинт», инструмент отвечал бы «потому что» — и ничего больше.
Тип экшена живёт **на ребре**: тот же метод сервиса зовут и обработчик
экшена, и компонент напрямую.

**Шов сводится по ключу маршрута, тому же, что у эндпоинта .NET.**
Различитель («один маршрут на много смыслов») входит в ключ вызова
и в атрибуты ребра, но **не** участвует в сопоставлении — правило перенесено
из `web link` без изменений.

### Ребро `owns` названо своим именем

Связь «тип содержит член», по которой идёт достижимость, раньше показывалась
в пути как `calls`. Это неправда: исполнением она не является. Теперь у неё
свой вид, и в индексе её нет — она существует только в ответе о пути, где
иначе пришлось бы называть вызовом то, что вызовом не является.

```
uv run pytest -q  → 1875 passed  (+8 тестов)
```

## G14 — разбор Angular-шаблонов ✅

**Создано:** `docpipe/web/templates.py`, `tests/test_web_templates.py` (9 тестов)
**Изменено:** `docpipe/web/tree.py`, `docpipe/cli.py` (числа по шаблонам
в `web scan`), фикстура `WebWorkspace` (вызовы в разметке),
`docs/graph-implementation-plan.md`

Закрыта известная дыра: `(click)="save()"` и `service.list() | async` живут
в `.html`, и разбор `.ts` их не видел вовсе.

**Грамматики шаблонов у нас нет, и притворяться, что есть, не нужно.**
Выражения достаются из мест, где они по синтаксису Angular и лежат:
интерполяции `{{ … }}` и значения биндингов `(click)=`, `[value]=`, `*ngIf=`.
Ложные срабатывания снимаются не хитростью разбора, а **сверкой с
объявленными именами**: имя, которого нет ни среди членов компонента,
ни среди его зависимостей, ребром не становится. Локальная переменная
`*ngFor="let model of models"` этой сверкой отсекается сама.

**Свои члены считаются, но рёбрами не становятся.** Это выяснилось падением
двух тестов, и они были правы: `Usage` по модели — обращение к члену
**другого** узла, а самоссылка ломает и модель, и список зависимостей
страницы (в нём появился бы сам компонент). Достижимость такого члена
от страницы при этом не теряется — её даёт связь «тип содержит член»
в графе. Число печатается: оно отвечает на вопрос «сколько методов зовут
только из разметки».

**Внешние и инлайновые шаблоны обрабатываются одинаково**, а отсутствие
файла шаблона компонент не роняет: шаблон могли удалить или переименовать,
компонент от этого никуда не делся.

На фикстуре: 7 шаблонов, из разметки зовут чужие узлы 1 раз (сервис,
которого в `.ts` не видно) и свои члены 4 раза.

```
uv run pytest -q  → 1884 passed  (+9 тестов)
```

## G18 — оценочный набор ✅ (частично: целевой репозиторий недоступен)

**Создано:** `docpipe/graph/evaluate.py`, `evals/fixtures.yaml`,
`evals/eshoponweb.yaml`, `tests/test_graph_eval.py` (8 тестов)
**Изменено:** `docpipe/cli.py` (`graph eval`)

Набор меряет **связку** «сторонний разбор + наш слой» как одно целое:
пользователю всё равно, чьё ребро соврало.

### Два набора на открытых репозиториях

**`evals/fixtures.yaml`** — фикстуры репозитория (Angular + .NET + реестр):
восемь вопросов, включая главный вопрос Вехи 4 («что достигает страница»),
путь со швом, вопрос по приблизительному имени, влияние правки по списку
файлов и **два вопроса по-русски** — по человеческому названию точки входа
и по названию таблицы. Полнота 1.0, точность 1.0.

**`evals/eshoponweb.yaml`** — открытый репозиторий с другим стеком: .NET
без Angular, MediatR, Razor Pages, ни одной черты целевой системы. Пять
вопросов, включая «происхождение имени таблицы» — ответ обязан показывать,
что имя выведено по соглашению, иначе выведенное неотличимо от прочитанного.
Полнота 1.0, точность 1.0.

### Как устроен счёт

Ожидание — это список того, что в ответе **обязано быть** (`expect`),
и того, чего в нём **быть не должно** (`refute`). Второе не формальность:
ответ, в который затесался чужой сервис, выглядит полнее правильного,
и без запретов такая ошибка не ловится вовсе.

Полнота и точность считаются по ожиданиям, а не по вопросам: вопрос,
у которого нашлось три ожидания из четырёх, отвечен наполовину, и округлять
это до «не отвечен» значит терять различие.

Два правила формата держатся тестами: вопрос без ожидания не принимается
(он ничего не мерит, а в отчёте выглядит как измерение), и у каждого вопроса
обязано быть объяснение, зачем он задан — вопрос без объяснения через месяц
неотличим от случайного, и первый его провал спишут на «набор устарел».

### Что осталось невыполненным и почему

- **набор на целевом репозитории** (п. 1) — доступа нет;
- **набор из десяти влитых PR** (п. 4) — требует репозитория, где `affects`
  вообще работает: у самого `docpipe` индекс не строится, потому что его
  код на Python, а манифест собирается по .NET и TypeScript. Прогон
  на открытом репозитории потребовал бы истории его PR и ревью, которых
  у нас нет.

Оба пункта записаны долгом, а не закрыты похожим числом.

```
uv run pytest -q  → 1892 passed  (+8 тестов)
docpipe graph eval evals/fixtures.yaml   → 8/8, полнота 1.0, точность 1.0
docpipe graph eval evals/eshoponweb.yaml → 5/5, полнота 1.0, точность 1.0
```

## G06 — грид-сервисы как шов .NET ↔ .NET ✅

**Создано:** `docpipe/graph/grid.py`, `tests/test_graph_grid.py` (5 тестов)
**Изменено:** `docpipe/graph/build.py`, `docs/graph-implementation-plan.md`

Фича считалась требующей разбора тел — и не потребовала.

**Почему.** Вызов `GetServiceProxy<IFoo>().Bar()` разбор видит как обращение
к члену **интерфейса**: интерфейс лежит в репозитории, и до него цепочка
доходит. Останавливается он на связи «интерфейс → зарегистрированная
в кластере реализация», а её в коде не существует структурно — она объявлена
реестром. Значит, разбирать тело незачем: достаточно взять готовое ребро
к члену интерфейса и продолжить его по записи реестра.

**Контракт ищется двумя способами, и оба нужны.** По имени сервиса —
в реестре сервис часто назван именем интерфейса (`IBlockFilesService`);
по наследованию реализации — когда имя произвольно (`CalcDxExcel` при классе
`DXExcelCalcService`), контракт виден по тому, что класс наследует. Ни один
способ по отдельности не покрывает оба случая.

Ребро получается `crosses`, а не `calls`: путь «член → грид-сервис → член»
виден как шов, а не как прямой вызов. Грид-сервис без узла кода и грид-сервис
без контракта — два разных числа отчёта, а не одно молчание.

```
uv run pytest -q  → 1897 passed  (+5 тестов)
```

## G13 п. 5 — перекрёстная проверка с разбором ✅ (остальное блокировано доступом)

**Изменено:** `docpipe/graph/{engine,build,web,store}.py`,
`tests/test_graph_bridge.py` (+2 теста), `docs/graph-implementation-plan.md`

Настройка семи фронтов целевой системы требует доступа к ней; механизм,
которым её проверяют, сделан и проверен на открытом репозитории.

### Четыре числа рядом

Сборка индекса печатает: маршрутов фронта у нас, у разбора, только у нас,
только у разбора. На `squidex` (настоящий .NET + Angular, 4779 файлов) —
**2 против 18**. Это и есть рабочий список: наш разбор фронта восстановил
3 вызова из 81, потому что там другая форма обращения к API, и она
не настроена. Перекрёстная проверка сказала об этом сразу, а не через
месяц по расхождению отчётов.

### Мусорная категория подтвердилась

План предсказывал: узлы маршрутов собираются по литералам, и литерал
регулярного выражения из минифицированного JS выглядит как путь. Так и есть
— `/&/g`, `/---/g`, `/%20/g`. Отсев записан правилом в мосте, срабатывания
считаются.

**Первая редакция правила сама съедала настоящие маршруты.** Наивное
`^/.*/[a-z]*$` матчит и `/api/items`: жадная точка съедает `/api`, остаток
принимается за флаги. Числа перекрёстной проверки это и показали — маршрутов
у разбора было 9 вместо 18. Правило переписано: тело без косой черты, флаги
только настоящие (`gimsuy`).

### Падение на реальном репозитории: совпавшие ключи узлов

`squidex` уронил запись индекса с `UNIQUE constraint failed`. Две причины,
обе законные: **один маршрут у двух компонентов** (макет и содержимое)
и **два члена с одним именем** (поле и геттер). Узлы фронта теперь
собираются в словарь по ключу, число совпадений печатается (на `squidex` —
12), а перед записью стоит последняя защита: столкновение **между**
источниками отбрасывается и тоже считается. Падение с `UNIQUE constraint
failed` объясняет ровно ничего, а число объясняет.

```
squidex: 19 788 узлов, 23 857 рёбер, 342 корня, 87 страниц, 794 ребра цепочки
uv run pytest -q  → 1899 passed  (+2 теста)
```

## G17 (часть) — покрытие точек входа бизнес-документами ✅

**Создано:** `docpipe/graph/coverage.py`, `tests/test_graph_coverage.py` (6 тестов)
**Изменено:** `docpipe/cli.py` (`graph coverage`)

Отчёт печатает два числа, и они про разное:

- **точка входа без документа** — состояние работы. Печатается всегда, кода
  возврата не меняет, пока порог не задан явно: линт, красный с первого дня,
  выключают на второй;
- **якорь без точки входа** — уже находка: через месяц он неотличим
  от опечатки. Кроме якорей с `verify: false` — это граница зоны
  ответственности, процесс может начинаться в чужой команде, и требовать
  доказательства чужого триггера значит получить вечно красный отчёт.

**Стрелка одна: техника ссылается на бизнес.** Документ объявляет якорь —
вид и `ref`, — а связь строится сопоставлением с записями реестра. Обратная
стрелка вернула бы схему, которую уже признали неподдерживаемой: рефакторинг
ломал бы бизнес-документ.

**Словари видов расходятся намеренно, и мост между ними один.** Аналитик
пишет `table` и `kafka`, реестр объявляет `list` и `kafka_topic`;
сопоставление идёт через `business.resolve.REGISTRY_KIND` — тот же мост,
что и у бизнес-слоя, а не вторая его копия. Пара, которой в нём нет, просто
никогда не разрешится, и выглядеть это будет как «инструмент не нашёл».

### Что из G17 осталось невыполненным

- **связь со спецификацией OpenSpec** (пп. 3, 5) — требует кооперации другой
  команды и идентификатора на стороне спецификации;
- **выделение механизма принятого хэша** (решение G00) — нужно, чтобы G17
  не зависел от `materialize` как модуля. Механизм есть и работает
  в двух экземплярах (шаг 2 и бизнес-слой); сведение их в один остаётся
  задачей, и она записана в `module-review.md` с именами функций;
- **метрика размера ручной части** (п. 2) — считается по каталогу, но
  осмысленна только на репозитории, где каталог заполняют.

```
uv run pytest -q  → 1905 passed  (+6 тестов)
```

## G04 п. 6 и G05: то, что выглядело блокированным, закрыто целевым запросом

**Создано:** `docpipe/dotnet/facts.py`, `docpipe/dotnet/queries/facts.scm`,
`tests/test_dotnet_facts.py` (9 тестов)
**Изменено:** `docpipe/model.py` (`Construction`, `LiteralCall`,
`DispatchSend`, `TableLiteral`), `docpipe/dotnet/parser.py`, `docpipe/emit.py`,
`docpipe/graph/{binding,data,build}.py`, `tests/test_graph_binding.py`,
`tests/golden/doc-tree.json`, план

Оба пункта были записаны как упирающиеся в источник: сторонний разбор
не считает создание объекта обращением, а тела мы не разбираем. Оба закрыты
**своим целевым запросом по дереву** — тем же приёмом, которым уже извлекались
регистрации контейнера (`services.Add*`). Разбором тел это не является: тела
в граф не идут и не хранятся, извлекаются два названных факта под два
названных вопроса.

### Где отправляют запрос

`new GetMyOrders(...)` плюс член, в теле которого это записано. На открытом
репозитории: `OrderController.MyOrders → GetMyOrdersHandler.Handle` с типом
запроса на ребре, четыре ребра на два объявленных обработчика (два
из контроллеров, два из тестов), обработчиков без места отправки — ноль.

Первая редакция брала место отправки у стороннего разбора и получала только
петли «обработчик диспетчеризует сам себя»: единственное обращение к типу
запроса приходило от метода `Handle`, где этот тип — параметр.

### Где имя таблицы записано литералом

`ToTable("Catalog")`, `CreateTable("AspNetUsers")`. Сущность, которой имя
принадлежит, ищется в двух формах, и **вторая на реальном коде оказалась
основной**: `Entity<X>().ToTable(…)` не встретилась на открытом репозитории
ни разу, а `Configure(EntityTypeBuilder<X> builder)` — у каждой конфигурации.

Порядок силы: литерал сильнее атрибута, атрибут сильнее соглашения — не
по старшинству, а по факту: `ToTable("Catalog")` переопределяет оба,
и исполняется именно он.

Вызов **без** литерала (имя из константы) записывается с пустым именем
и считается отдельно: выбросив такие, мы получили бы «неразрешённых нет»
вместо числа. На `eshoponweb`: 51 имя прочитано литералом, 12 выведено
по соглашению, 28 не разрешено.

### Побочная находка: лишнее поле в модели разбора

У `FileParseResult` обнаружилось поле `dispatch_handlers`, попавшее туда
глобальной заменой при добавлении того же поля в манифест: одинаковая строка
встречалась в обоих классах. Поле никто не заполнял и не читал — мёртвый
вес, который прошёл бы через каждый следующий рефакторинг. Убрано.

```
uv run pytest -q  → 1915 passed  (+10 тестов)
eshoponweb: узлов данных 18 (было 5), рёбер диспетчеризации 4 (было 0)
```

## G05c — разбор SQL: механизм готов, разведка целевого репозитория осталась

**Создано:** `docpipe/sql.py`, `tests/fixtures/WildSolution/db/*.sql`,
`tests/test_graph_sql.py` (12 тестов)
**Изменено:** `docpipe/discovery.py` (`.sql` в обходе), `docpipe/model.py`
(`SqlUsage`, `SqlObject`), `docpipe/dotnet/facts.py` (`SQL_METHODS`),
`docpipe/emit.py`, `docpipe/graph/{data,build,reach}.py`, план

Разделение оказалось не тем, каким записывалось: «есть ли на АС CF исходники
процедур» — вопрос к доступу и остаётся открытым, а «что делать, если они
есть» — работа, которую можно сделать и проверить где угодно.

**SQL читается из двух мест.** Исходники (`.sql` в обходе) и литералы в коде
(`FromSqlRaw`, `ExecuteSqlRaw`, Dapper) — тем же целевым запросом, которым
берутся имена таблиц.

**Разбор намеренно грубый.** Регулярные выражения по ключевым словам,
а не грамматика диалекта: нужен ответ «какие имена участвуют», а не «что
этот запрос делает». Полная грамматика T-SQL стоит отдельного проекта
и ломается на первом расширении диалекта, а имена лежат сразу за ключевыми
словами и одинаково в любом диалекте.

**Три правила, каждое иначе ломается молча:**

- **динамический SQL — отдельная категория.** Это не «не разобрали литерал»:
  имени нет вовсе до момента исполнения, и складывать их в одно число значит
  обещать, что второе когда-нибудь дочинят;
- **временные таблицы узлами не становятся.** Иначе граф зарастает `#tmp`,
  а веерность теряет смысл: у временной таблицы одной процедуры нет ничего
  общего с одноимённой в другой. Число отброшенных печатается;
- **процедура, вызванная и не найденная, остаётся узлом** с пометкой «тело
  не найдено». На репозитории, где процедуры живут только в базе, таких будет
  сто процентов — и это нормальный исход.

**`reads` и `writes` добавлены в рёбра достижимости.** Без них процедура
достигнута, а её таблицы — нет, то есть ответ на главный вопрос («какие
таблицы трогает процесс») терялся ровно там, где появлялся источник.

```
uv run pytest -q  → 1927 passed  (+12 тестов)
на фикстуре: 2 процедуры с телом, 1 вызвана без тела, 1 динамическая
```

## G16 пп. 2–3 — Python в общем графе ✅ (шов остаётся за разведкой)

**Изменено:** `tests/test_graph_bridge.py` (+2 контрактных теста), план

Проверка, а не реализация: узлы и рёбра Python приходили тем же мостом
с самого начала — правило «источник рёбер задан по языкам» исключает
из проекции только TypeScript, и это было записано, но не измерено.

**Измерено на настоящем питоновском репозитории — на самом `docpipe`:**
2 980 узлов и 5 053 ребра вызова через мост, ни строки разбора Python
в коде не появилось.

**Реестр в виде исполняемого кода даёт корни.** Прогон на маленьком
репозитории с `SERVICES = {"forecast": ForecastHandler}`: две точки входа,
обе связаны с классами, модуль при этом не импортируется — разбирается
статически.

Открытым остаётся шов с .NET (пп. 1 и 4): он строится, только если разведка
нашла ключ-литерал, а разведка требует целевого репозитория. Механика шва
при этом уже есть — та же, что у фронта и у кластера.

```
uv run pytest -q  → 1929 passed  (+2 теста)
```

## G18 п. 4 — набор из влитых правок ✅, и что он нашёл

**Изменено:** `docpipe/graph/evaluate.py`, `docpipe/graph/api.py`,
`docpipe/graph/match.py`, `docpipe/graph/build.py`, `docpipe/graph/model.py`,
`docpipe/graph/store.py`, `docpipe/dotnet/di.py`, `docpipe/dotnet/parser.py`,
`docpipe/emit.py`, `docpipe/cache.py`, `docpipe/config.py`, `docpipe/cli.py`,
`docs/configuration.md`, план, тесты (+13)

Пункт был записан как упирающийся в доступ: «что реально затрагивалось
в ревью» знает только ревью. Но проверяемая часть этого утверждения есть
в любом репозитории с историей: **правка меняла файл точки входа — значит,
`affects` обязан эту точку входа назвать.** Пропуск здесь — ложное отрицание,
самый опасный вид ошибки, потому что он тихий. Всё, что названо сверх, —
предсказание вниз по графу; оно считается отдельно и истиной не объявляется.

`docpipe graph pr-check --root … --count 10 --scan 200`. Замер на squidex:
пять правок из десяти меняли файл точки входа, **188 обязательных находок,
пропущено 0**, предсказание вниз по графу — от 7 до 136 корней на правку.

**Три ошибки самой проверки, каждая давала бы зелёный отчёт ни о чём.**

- **Истина по полю `file` узла корня пуста всегда.** У корня из манифеста
  там путь документа (`docs/modules/…/x.md`), а не файл кода. Первая редакция
  насчитала 4 обязательных находки вместо 188 и показала «пропусков нет» —
  ровно потому, что проверять было нечего. Истина собирается по узлу кода,
  с которым корень связан ребром.
- **Усечение списка — свойство представления.** `affects` отдаёт страницу
  в 20 записей; проверка, читающая её как полный ответ, мерила бы размер
  страницы. Предел вынесен параметром.
- **Правка, не тронувшая код, проверкой не является.** Подряд идущие слияния
  часто оказываются правками документации: десять таких дают десять строк
  «предсказано 0, обязано 0». Отбор по расширению файла, `--scan` против
  `--count`.

**Ноль точек входа теперь объяснён (Р7).** Три правки дали ноль, и он был
верен: они меняли `Startup.cs` и `Config/**`. Композиционный корень —
обратная сторона общего компонента: веерность ноль, а влияние на всё, что
здесь собирается. Голый ноль читается как «ничего не задето» — самый
безопасный из трёх смыслов при том, что верен чаще самый опасный. Список
файлов сборки контейнера лежит в паспорте индекса (`composition_roots`),
и он же покрывает `Program.cs` на top-level statements, где узлов нет вовсе
и «не найдено» формально верно.

**Найдено попутно: манифест и разбор можно снять с разных корней.** На
eshoponweb каталог содержит вложенный `eShopOnWeb/`, и `graph build --root`
от внешнего каталога дал пути на один сегмент длиннее манифестных. Совпасть
не может ничего; прогон при этом проходил целиком, «сопоставлено 0» стояло
строкой среди прочих чисел, а `affects` по `git diff` не находил ни файла.
Теперь это отказ с диагнозом: сообщение называет ведущий сегмент, пару
путей и то, какой корень исправить.

**Найдено попутно: стандартной формы регистрации может не быть вовсе.**
На squidex 275 `AddSingletonAs`, 37 `AddTransientAs`, 2 `AddScopedAs` —
самодельные обёртки. Отчёт показывал 67 регистраций (стандартная форма там
тоже есть), то есть ноль был незаметен. Ключ `di_methods` перечисляет
обёртки; интерфейс у них называет **продолжение цепочки** `.As<I>()`, и
цепочка растёт наружу, поэтому идти надо вверх по родителям. Условие входа
— «сервис равен реализации», а не «реализация не названа»: один тип-аргумент
даёт пару `(X, X)`. Итог на squidex: 67 → 421 регистрация, 30 → 303
разрешённых, рёбер 23 929 → 29 385.

**Кэш разбора соврал на собственной правке.** Числа не менялись, потому что
файлы приезжали из кэша: его ключ собран из версий грамматики, а менялся наш
код. `CACHE_VERSION` → 4, а настройки, влияющие на разбор, теперь входят
в ключ отдельным полем — иначе смена `di_methods` ничего бы не пересобрала.

```
uv run pytest -q  → 1945 passed  (+16 тестов)
squidex: 421 регистрация, 303 разрешено, 49 файлов сборки контейнера
pr-check: 10 правок, 188 обязательных находок, пропущено 0
```

## G17 п. 5 — механизм приёмки выделен: `docpipe/documents/` ✅

**Изменено:** новый пакет `docpipe/documents/` (`model.py`, `zones.py`,
`write.py`), `docpipe/materialize/{plan,build,apply,template,explain,__init__}.py`,
`docpipe/business/{status,build}.py`, `docpipe/cli.py`,
`tests/test_documents_state.py` (новый), `docs/module-review.md`, план

Пункт был записан в блокированные вместе с остальным G17, а зависел только
от нас: G17 стоит на механизме принятого хэша и **не имеет права зависеть
от `materialize` как модуля** — параллельная история в фундамент графа
не встраивается.

**Что оказалось общим — не то, что ожидалось.** Ревизия G00 называла
выделяемое по именам функций, и главным пунктом шёл разбор зон. Но копии
его не было: `business/status.py` уже импортировал `read_document`
и `is_section_empty` из `materialize`. То есть проблемой была не дубликация,
а **зависимость бизнес-слоя от шага 2**.

Настоящие копии нашлись мельче и опаснее — по одной строке каждая:

- форма блока `{"accepted": …, "review": None}` литералом в двух слоях.
  В ней записано правило «приёмка снимает отметку о пересмотре»: слой,
  забывший его, оставит документ с признаком пересмотра навсегда;
- чтение принятого блока (`isinstance(raw, dict)`) — в двух местах, и в обоих
  на нём стои́т различение «сверяли и приняли» против «не сверяли»;
- умолчание `{"accepted": None, "review": None}` при сборке front matter —
  одной и той же строкой в `materialize/build.py` и `business/build.py`;
- список зарезервированных ключей `("docpipe", "docpipe_state")` в фильтре
  чужих ключей обоих слоёв.

**Полезная нагрузка блока намеренно осталась у слоёв.** `signature_hash` +
`impl_hash` + члены у шага 2, `business_hash` + разрешённые факты
у бизнес-слоя. Общий знаменатель здесь пришлось бы придумать, а придуманный
развалился бы на третьем потребителе — том самом, ради которого выделение
и делалось.

**Держится тестом, а не соглашением.** `grep` по `docpipe/**` за пределами
пакета не находит ни ключа состояния, ни литерала `"review": None` — тот же
приём, что охраняет границу моста к разбору (Р13). Все четыре копии появились
по одной строчке за раз, и без теста пятая появится так же.

Сквозной прогон после переноса: шесть документов материализованы, один
наполнен и принят, `docs status` показывает `current 1` и `empty 5`,
блок `docpipe_state.accepted` записан с `review: null`.

```
uv run pytest -q  → 1952 passed  (+7 тестов)
```
