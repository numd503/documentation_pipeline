// Цель алиаса `exceljs` из tsconfig. Файл существует НАМЕРЕННО: резолв обязан
// пометить такую цель внешней по пути, а не по отсутствию файла — иначе проверка
// проходила бы просто потому, что резолвить нечего.
export declare class Workbook {
  addWorksheet(name: string): unknown;
}
