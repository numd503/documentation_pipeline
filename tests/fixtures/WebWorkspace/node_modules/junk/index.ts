// Файл под node_modules: обязан быть исключён ОБХОДОМ, а не правилом.
// В .gitignore этот каталог добавлять нельзя — иначе проверка исключения
// продолжит проходить, ничего не проверяя.
export class JunkFromNodeModules {}
