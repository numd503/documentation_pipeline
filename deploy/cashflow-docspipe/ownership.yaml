# Владение: какой команде принадлежит документ.
#
# Файл заготовка: команд нет, правил нет. В таком виде он работает — у всех
# документов `team: null`, — но ничего не раздаёт. Заполняется по мере того,
# как становится понятно, кто за что отвечает.
#
# Пустой файл здесь лучше выдуманного: правило с несуществующим `module_glob`
# молча не срабатывает, и «ничьих узлов 4820» не отличить от «правило написано
# с опечаткой». Полный образец со всеми слоями — `ownership.example.yaml`
# в репозитории разработки.
#
# Считается на шаге 2, а не в манифесте. Состав команд меняется чаще кода;
# попав в `doc-tree.json`, владение сделало бы манифест зависящим от
# орг-структуры и сломало бы свойство «манифест — чистая функция кода и правил
# классификации».
#
# Владелец определяется по УЗЛУ, а не по модулю. Соблазн «команда = проект»
# разбивается о шэренный .csproj — ровно ту ситуацию, ради которой владение
# и заводится.
#
# Синтаксис условий тот же, что у rules.yaml: один движок, одна диагностика
# опечаток. Правила ТОЛЬКО положительные: более специфичное выражается более
# высоким приоритетом, а не исключением из общего.

version: "1"
ownership_version: "2026-07-30.1"

# Команда, которой нет здесь, не может быть названа в правиле — и `--team`
# с неизвестным именем тоже отказ, а не пустая выборка.
teams: []
  # - id: pricing
  #   title: Ценообразование
  # - id: risk
  #   title: Рыночные риски
  # - id: platform
  #   title: Платформа

# Порядок работы: начать с одного слоя приоритета 10 по `module_glob`, прогнать
# `docpipe docs owners $OUT --config $BUNDLE/docpipe.yaml --lint`
# и посмотреть, сколько узлов осталось ничьими и в каких каталогах они лежат.
# Слои 50 и выше добавлять только там, где линт покажет реальное смешение.
rules: []
  # ----------------------------------------------------------------------------------
  # 10 — проект целиком
  # ----------------------------------------------------------------------------------
  # Слои идут с шагом приоритета: 10 — проект, 50 — каталог, 80 — namespace,
  # 100 — точечно. Промежутки оставлены намеренно, чтобы вставка нового уровня
  # не требовала перенумерации всего набора.
  # - id: pricing.modules
  #   team: pricing
  #   priority: 10
  #   when:
  #     module_glob: ["src/*.Pricing.*/**"]
  #
  # ----------------------------------------------------------------------------------
  # 50 — шэренный проект режется по каталогам с КОДОМ
  # ----------------------------------------------------------------------------------
  # Ради этого слоя всё и заводится: `path_glob` — единственный способ разделить
  # проект, в котором лежит код нескольких команд.
  #
  # Условие истинно, если совпал ХОТЯ БЫ ОДИН источник. У partial-класса файлы
  # бывают в каталогах разных команд; правило «все источники» оставило бы такой
  # тип ничьим, и это выглядело бы как дефект инструмента. Такие типы попадают
  # в предупреждения `docs owners --lint`.
  # - id: pricing.shared-folders
  #   team: pricing
  #   priority: 50
  #   when:
  #     path_glob: ["src/*.Shared/Pricing/**"]

# Восемь предикатов, намеренно мало — та же причина, что в правилах
# классификации: большой набор означает, что настройщик каждый раз выбирает
# из десяти способов написать одно и то же.
#
#   path_glob         пути источников, любой из них    главный инструмент
#   module            имя модуля                       короткая запись
#   module_glob       путь .csproj                     имена проектов не уникальны
#   domain            домен из docpipe.yaml
#   kind              вид сущности
#   namespace_prefix  начало namespace
#   namespace_regex   namespace целиком, re.fullmatch
#   fqn_prefix        начало FQN                       точечно
