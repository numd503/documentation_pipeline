"""Запуск пакетом: `python -m docpipe`.

Нужен как запасной путь там, где нельзя собрать сам проект: если сборочный
бэкенд недоступен, окружение ставится как `uv sync --no-install-project`
(только зависимости), и консольной команды `docpipe` в нём не появляется.
Импорт пакета из текущего каталога при этом работает.
"""

from docpipe.cli import app

if __name__ == "__main__":
    app()
