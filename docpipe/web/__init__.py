"""Шаг `web`: разбор фронтенда на Angular.

Пакет **не импортирует** `docpipe/dotnet/**` и не импортируется им. Граница
проверяется тестом обходом AST, а не импортом: импорт подтвердил бы только то,
что модуль загружается, а не то, что зависимости нет.

Общее у двух языков — модели (`docpipe/model.py`), нормализация маршрута
(`docpipe/route.py`), обход (`docpipe/discovery.py`) и всё, что идёт после
манифеста: материализация, статусы, бизнес-слой.
"""
