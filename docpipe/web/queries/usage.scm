; Обращения к членам зависимостей: `this.models.byId(id)`.
;
; Запрос отдельный от `calls.scm`, хотя формы пересекаются: тот служит
; восстановлению URL и меняется вместе с формами записи адреса, этот —
; графу вызовов. Общий файл связал бы две задачи, которые расходятся.

(call_expression
  function: (member_expression)) @call

; Поля с внедрением через функцию: `private readonly api = inject(ModelService)`.
; В боевом модуле их девять против сотен конструкторов, но обходятся дёшево:
; имя и тип берутся из одного узла.
(public_field_definition) @field
